---
phase: 01-data-integrity
plan: 02
type: execute
wave: 1
depends_on: [01-01]
files_modified:
  - primeaudit/lib/screens/audit_execution_screen.dart
autonomous: true
requirements: [DINT-01, DINT-02, DINT-03]
tags: [flutter, retry, error-handling, snackbar, audit-execution]

must_haves:
  truths:
    - "O catch silencioso em `audit_execution_screen.dart:228` não existe mais — catch captura `e`, loga e propaga para a UI"
    - "Quando `upsertAnswer` lança, um SnackBar com texto `Não foi possível salvar` e action `Tentar novamente` aparece"
    - "Tocar em `Tentar novamente` chama `_saveAnswer` de novo com o mesmo `itemId`/`response`/`observation`"
    - "Saves com falha ficam registrados em `_failedSaves` até terem sucesso ou o usuário dismiss"
    - "`_finalize()` é bloqueado por um AlertDialog quando `_failedSaves.isNotEmpty`"
    - "Retry automático com backoff exponencial (1s, 2s, 4s, 8s, stop) roda em background, cancela se widget unmount"
    - "UI otimista preservada: `_answers[itemId]` ainda é setado antes do save (D-03 — sem spinner)"
  artifacts:
    - path: "primeaudit/lib/screens/audit_execution_screen.dart"
      provides: "Fix completo: _PendingSave alias, _failedSaves, _retrying, _showSaveError, _scheduleRetry, _saveAnswer revisado, _finalize com guard D-06"
      contains: "import 'pending_save.dart'"
  key_links:
    - from: "primeaudit/lib/screens/audit_execution_screen.dart"
      to: "primeaudit/lib/screens/pending_save.dart"
      via: "relative import"
      pattern: "import 'pending_save.dart'"
    - from: "_saveAnswer catch block"
      to: "_showSaveError + _scheduleRetry"
      via: "direct call on exception"
      pattern: "_showSaveError\\(|_scheduleRetry\\("
    - from: "_finalize guard"
      to: "_failedSaves.isNotEmpty"
      via: "early-return with AlertDialog"
      pattern: "_failedSaves\\.isNotEmpty"
---

<objective>
Corrigir o bug raiz da Fase 1: o `catch (_)` silencioso em `audit_execution_screen.dart:228`. Implementar fila de retry em memória, snackbar de erro com action "Tentar novamente", retry automático com backoff exponencial, e guarda de finalização D-06 — tudo dentro do arquivo `audit_execution_screen.dart`, usando os padrões já estabelecidos no projeto (Map por `itemId`, `setState`, `ScaffoldMessenger`, `AppColors.error`, `mounted` guard).

Purpose: Satisfazer DINT-01 (mensagem de erro visível), DINT-02 (estado pendente resolvido por D-03 = otimismo + indicador de falha pós-erro), DINT-03 (retry manual), e D-06 (bloquear finalização com falhas). Todas as decisões D-01..D-07 de `01-CONTEXT.md` são implementadas exatamente como fixadas.

Output:
- `audit_execution_screen.dart` modificado com:
  1. Import de `dart:math` (para `pow`)
  2. Import relativo de `pending_save.dart`
  3. Type alias local `_PendingSave = PendingSave` (convenção do projeto: usar prefixo `_` dentro da tela)
  4. Novos campos de estado `_failedSaves` (Map) e `_retrying` (Set)
  5. Novo método `_showSaveError`
  6. Novo método `_scheduleRetry` com backoff exponencial
  7. `_saveAnswer` reescrito (substitui linhas 219-231)
  8. Guarda D-06 inserida no topo de `_finalize` (linha 235 — antes do `showDialog` existente)
- Nenhum novo arquivo além do já criado no Plan 01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-integrity/01-CONTEXT.md
@.planning/phases/01-data-integrity/01-RESEARCH.md
@.planning/phases/01-data-integrity/01-PATTERNS.md
@.planning/phases/01-data-integrity/01-VALIDATION.md
@.planning/phases/01-data-integrity/01-01-SUMMARY.md

<interfaces>
<!-- Tipos e assinaturas que o executor precisa, extraídos diretamente do código -->

Do Plan 01 (já em disco):
```dart
// primeaudit/lib/screens/pending_save.dart
class PendingSave {
  final String itemId;
  final String response;
  final String? observation;
  final int attemptCount;
  const PendingSave({
    required this.itemId,
    required this.response,
    this.observation,
    this.attemptCount = 0,
  });
  PendingSave copyWithAttempt();
}
```

Do service alvo (já existente, não alterar):
```dart
// primeaudit/lib/services/audit_answer_service.dart:23-38
Future<void> upsertAnswer({
  required String auditId,
  required String templateItemId,
  required String response,
  String? observation,
}) async;
// Propaga exceções: PostgrestException, ClientException, SocketException, TimeoutException.
```

Da tela (estado atual, linhas 19-33):
```dart
class _AuditExecutionScreenState extends State<AuditExecutionScreen> {
  final _templateService = AuditTemplateService();
  final _answerService = AuditAnswerService();
  final _auditService = AuditService();
  List<TemplateSection> _sections = [];
  List<TemplateItem> _allItems = [];
  final Map<String, String> _answers = {};
  final Map<String, String> _observations = {};
  bool _loading = true;
  bool _finalizing = false;
  String? _error;
  // ...
}
```

Da tela (código-bug atual, linhas 219-231):
```dart
Future<void> _saveAnswer(String itemId, String response,
    {String? observation}) async {
  try {
    await _answerService.upsertAnswer(
      auditId: widget.audit.id,
      templateItemId: itemId,
      response: response,
      observation: observation ?? _observations[itemId],
    );
  } catch (_) {
    // Falha silenciosa — pode tentar novamente ao finalizar  ← LINHA 228, O BUG
  }
}
```

Da tela (_finalize atual, linhas 235-297) — a guarda D-06 entra ANTES do `showDialog<bool>` existente.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar imports, campos de estado e alias de tipo na State class</name>
  <files>primeaudit/lib/screens/audit_execution_screen.dart</files>
  <read_first>
    - primeaudit/lib/screens/audit_execution_screen.dart (linhas 1-40: imports e declaração de campos; linhas 19-33 especificamente)
    - primeaudit/lib/screens/pending_save.dart (classe `PendingSave` criada no Plan 01)
    - .planning/phases/01-data-integrity/01-PATTERNS.md (seção "Padrão de imports" e "Padrão de campos de estado por itemId")
  </read_first>
  <behavior>
    - Após esta task, `flutter analyze` não deve relatar imports não usados (os novos imports ficam não-utilizados até as Tasks 2-4 os consumirem — ACEITÁVEL, será fechado até o final da Wave 1). Para evitar warnings intermediários, aplicar Tasks 1-4 em sequência sem commit parcial entre elas.
    - `grep "class _AuditExecutionScreenState" audit_execution_screen.dart` ainda acha a classe.
    - `grep "_failedSaves"` encontra a declaração.
  </behavior>
  <action>
Passo A — No topo de `primeaudit/lib/screens/audit_execution_screen.dart`, adicionar DOIS novos imports após o último `import` existente (linha 8). O bloco de imports resultante deve ficar EXATAMENTE assim:

```dart
import 'dart:math';

import 'package:flutter/material.dart';
import '../core/app_colors.dart';
import '../core/app_theme.dart';
import '../models/audit.dart';
import '../models/audit_template.dart';
import '../services/audit_answer_service.dart';
import '../services/audit_service.dart';
import '../services/audit_template_service.dart';
import 'pending_save.dart';
```

Regras:
- `dart:math` vai na primeira linha (Dart style: dart: primeiro, package: depois, relative por último).
- `pending_save.dart` entra como último import relative.
- Ordem Alfabética dentro de cada grupo é mantida (já está).

Passo B — Adicionar um type alias LOCAL logo após o último import e ANTES da declaração `class AuditExecutionScreen`:

```dart
// Alias interno: usamos `_PendingSave` na tela (convenção de classe privada
// do projeto para tipos usados apenas dentro deste arquivo), mas a classe
// real é pública para permitir teste unitário direto em test/pending_save_test.dart.
typedef _PendingSave = PendingSave;
```

Passo C — Dentro de `_AuditExecutionScreenState` (após a linha `final Map<String, String> _observations = {};` — atual linha 29), adicionar DOIS novos campos EXATAMENTE:

```dart
  // Fila de retry: itemId → dados do save com falha
  final Map<String, _PendingSave> _failedSaves = {};

  // Controle de retry em andamento por item (evita loops duplos)
  final Set<String> _retrying = {};
```

Passo D — Adicionar a constante de configuração do retry como `static const` na State class, logo antes dos campos de instância (ou logo após as outras declarações de constantes se existirem):

```dart
  static const int _maxAutoRetryAttempts = 4;
```

Nenhuma outra mudança nesta task. Não tocar em `_saveAnswer`, `_finalize`, build methods.
  </action>
  <verify>
    <automated>cd primeaudit && flutter analyze lib/screens/audit_execution_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^import 'dart:math';" primeaudit/lib/screens/audit_execution_screen.dart` retorna `1`.
    - `grep -c "^import 'pending_save.dart';" primeaudit/lib/screens/audit_execution_screen.dart` retorna `1`.
    - `grep -c "typedef _PendingSave = PendingSave;" primeaudit/lib/screens/audit_execution_screen.dart` retorna `1`.
    - `grep -c "final Map<String, _PendingSave> _failedSaves = {};" primeaudit/lib/screens/audit_execution_screen.dart` retorna `1`.
    - `grep -c "final Set<String> _retrying = {};" primeaudit/lib/screens/audit_execution_screen.dart` retorna `1`.
    - `grep -c "static const int _maxAutoRetryAttempts = 4;" primeaudit/lib/screens/audit_execution_screen.dart` retorna `1`.
    - `flutter analyze lib/screens/audit_execution_screen.dart` pode mostrar warnings de "unused import" / "unused field" (aceitável — limpados nas tasks 2-4) mas NÃO deve mostrar erro.
  </acceptance_criteria>
  <done>Imports, alias e campos declarados. Classe ainda compila (warnings de uso são aceitáveis nesta task intermediária). Nenhum comportamento alterado ainda.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar _showSaveError e _scheduleRetry, reescrever _saveAnswer</name>
  <files>primeaudit/lib/screens/audit_execution_screen.dart</files>
  <read_first>
    - primeaudit/lib/screens/audit_execution_screen.dart (estado após Task 1; linhas 165-173 e 208-215 como analog de SnackBar; linhas 219-231 do `_saveAnswer` bugado)
    - primeaudit/lib/core/app_colors.dart (confirmar existência de `AppColors.error`)
    - .planning/phases/01-data-integrity/01-RESEARCH.md (seções "Pattern 1", "Pattern 2", "Pattern 4" — código de referência literal)
    - .planning/phases/01-data-integrity/01-PATTERNS.md (seção "Padrão de SnackBar com action button" — código verbatim)
  </read_first>
  <behavior>
    - Quando `upsertAnswer` lança, `_saveAnswer` captura, loga via `debugPrint`, adiciona a entrada a `_failedSaves`, mostra snackbar com texto EXATO `Não foi possível salvar` e action `Tentar novamente`, e dispara `_scheduleRetry`.
    - Tocar no action button do snackbar chama `_saveAnswer(itemId, response, observation: observation)` com os valores capturados via closure (Strings imutáveis — seguro).
    - `_scheduleRetry` não roda dois loops para o mesmo `itemId` (guard via `_retrying`).
    - Após tentativa de retry bem-sucedida, `_failedSaves[itemId]` é removido.
    - Após `_maxAutoRetryAttempts` falhas, loop para (manual retry via snackbar permanece disponível).
    - Se widget unmount durante `await Future.delayed` ou `await upsertAnswer`, o loop sai via `break` sem `setState` em disposed widget.
  </behavior>
  <action>
Passo A — Substituir COMPLETAMENTE o método `_saveAnswer` (atual linhas 219-231) pelo código abaixo. Use este diff mental:

REMOVER:
```dart
Future<void> _saveAnswer(String itemId, String response,
    {String? observation}) async {
  try {
    await _answerService.upsertAnswer(
      auditId: widget.audit.id,
      templateItemId: itemId,
      response: response,
      observation: observation ?? _observations[itemId],
    );
  } catch (_) {
    // Falha silenciosa — pode tentar novamente ao finalizar
  }
}
```

INSERIR:
```dart
  Future<void> _saveAnswer(
    String itemId,
    String response, {
    String? observation,
  }) async {
    // Resolve observação: parâmetro explícito vence, senão o map, senão null.
    final obs = observation ?? _observations[itemId];
    try {
      await _answerService.upsertAnswer(
        auditId: widget.audit.id,
        templateItemId: itemId,
        response: response,
        observation: obs,
      );
      // Sucesso: se esse item estava na fila de falhas, remove.
      if (_failedSaves.containsKey(itemId) && mounted) {
        setState(() => _failedSaves.remove(itemId));
      }
    } catch (e) {
      // D-07: não mascarar erros — log de dev + UI feedback + retry queue.
      debugPrint('[_saveAnswer] itemId=$itemId erro: $e');
      if (!mounted) return;
      setState(() {
        _failedSaves[itemId] = _PendingSave(
          itemId: itemId,
          response: response,
          observation: obs,
        );
      });
      _showSaveError(itemId, response, obs);
      _scheduleRetry(itemId);
    }
  }
```

Passo B — Adicionar DOIS novos métodos privados imediatamente APÓS o novo `_saveAnswer` (antes da linha `// ── Finalizar ──` que marca o início do `_finalize`).

Método `_showSaveError` (D-01, D-02, D-04):
```dart
  // D-01 + D-02 + D-04: notificação única via snackbar com action de retry manual.
  void _showSaveError(String itemId, String response, String? observation) {
    if (!mounted) return;
    // Captura o messenger ANTES de qualquer async gap (evita uso de context
    // inválido após a closure do action ser invocada).
    final messenger = ScaffoldMessenger.of(context);
    messenger.clearSnackBars(); // evita acúmulo de snackbars em falhas sucessivas
    messenger.showSnackBar(
      SnackBar(
        content: const Text('Não foi possível salvar'),
        backgroundColor: AppColors.error,
        behavior: SnackBarBehavior.floating,
        duration: const Duration(seconds: 6),
        action: SnackBarAction(
          label: 'Tentar novamente',
          textColor: Colors.white,
          onPressed: () {
            // itemId/response/observation são Strings imutáveis — captura segura.
            _saveAnswer(itemId, response, observation: observation);
          },
        ),
      ),
    );
  }
```

Método `_scheduleRetry` (D-05 — backoff exponencial 1s/2s/4s/8s, então para):
```dart
  // D-05: retry automático com backoff exponencial.
  // Delays: tentativa 0 = 1s, 1 = 2s, 2 = 4s, 3 = 8s. Após 4 falhas auto,
  // para — retry manual via snackbar continua disponível e bloqueio de
  // finalização (D-06) sinaliza ao usuário que algo ainda está pendente.
  Future<void> _scheduleRetry(String itemId) async {
    if (_retrying.contains(itemId)) return; // já existe loop para este item
    _retrying.add(itemId);
    try {
      while (_failedSaves.containsKey(itemId)) {
        final pending = _failedSaves[itemId]!;
        if (pending.attemptCount >= _maxAutoRetryAttempts) break;

        final delaySeconds = pow(2, pending.attemptCount).toInt(); // 1, 2, 4, 8
        await Future.delayed(Duration(seconds: delaySeconds));

        // Guard mounted (convenção do projeto — CONVENTIONS.md):
        if (!mounted || !_failedSaves.containsKey(itemId)) break;

        try {
          await _answerService.upsertAnswer(
            auditId: widget.audit.id,
            templateItemId: itemId,
            response: pending.response,
            observation: pending.observation,
          );
          if (mounted) {
            setState(() => _failedSaves.remove(itemId));
          }
          break; // sucesso — sai do loop
        } catch (_) {
          if (mounted) {
            setState(() {
              _failedSaves[itemId] = pending.copyWithAttempt();
            });
          }
        }
      }
    } finally {
      _retrying.remove(itemId);
    }
  }
```

Regras:
- NÃO adicionar um indicador visual (spinner, borda) — D-03 proíbe.
- NÃO capturar `context` dentro da closure de `SnackBarAction` — capturar `ScaffoldMessenger` antes do show é o padrão correto (RESEARCH.md Anti-Patterns).
- NÃO trocar `catch (e)` por `on PostgrestException catch` — RESEARCH.md exige catch amplo porque `ClientException`/`SocketException`/`TimeoutException` também são esperadas.
- NÃO persistir `_failedSaves` entre navegações — deferred em CONTEXT.md.
  </action>
  <verify>
    <automated>cd primeaudit && flutter analyze lib/screens/audit_execution_screen.dart &amp;&amp; flutter test test/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "catch (_) {" primeaudit/lib/screens/audit_execution_screen.dart` retorna `0` para o bloco de `_saveAnswer`. (Pode existir em `_scheduleRetry` internamente — OK; o catch ORIGINAL na linha 228 deve estar removido).
    - `grep -c "// Falha silenciosa" primeaudit/lib/screens/audit_execution_screen.dart` retorna `0` (comentário do bug original foi removido).
    - `grep -c "debugPrint('\\[_saveAnswer\\] itemId=" primeaudit/lib/screens/audit_execution_screen.dart` retorna `1`.
    - `grep -c "'Não foi possível salvar'" primeaudit/lib/screens/audit_execution_screen.dart` retorna `1` (texto EXATO de D-02, sem variações).
    - `grep -c "'Tentar novamente'" primeaudit/lib/screens/audit_execution_screen.dart` retorna pelo menos `1`.
    - `grep -c "void _showSaveError(" primeaudit/lib/screens/audit_execution_screen.dart` retorna `1`.
    - `grep -c "Future<void> _scheduleRetry(" primeaudit/lib/screens/audit_execution_screen.dart` retorna `1`.
    - `grep -c "pow(2, pending.attemptCount).toInt()" primeaudit/lib/screens/audit_execution_screen.dart` retorna `1`.
    - `grep -c "_failedSaves\\[itemId\\] = _PendingSave(" primeaudit/lib/screens/audit_execution_screen.dart` retorna `1`.
    - `grep -c "messenger.clearSnackBars()" primeaudit/lib/screens/audit_execution_screen.dart` retorna `1`.
    - `cd primeaudit && flutter analyze lib/screens/audit_execution_screen.dart` imprime `No issues found!`.
    - `cd primeaudit && flutter test test/` exit 0 (suíte continua verde; `_saveAnswer` ainda não coberto por teste real até Plan 03).
  </acceptance_criteria>
  <done>`_saveAnswer` já NÃO falha silenciosamente. Snackbar aparece em falha, retry manual funciona via action button, retry automático roda em background com backoff. Fila de falhas `_failedSaves` é fonte da verdade consumida pelo guard D-06 (próxima task).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Inserir guarda D-06 no _finalize bloqueando se _failedSaves não vazio</name>
  <files>primeaudit/lib/screens/audit_execution_screen.dart</files>
  <read_first>
    - primeaudit/lib/screens/audit_execution_screen.dart (estado após Tasks 1-2; método `_finalize` atual linhas 235-297)
    - .planning/phases/01-data-integrity/01-CONTEXT.md (D-06 texto exato da mensagem de bloqueio)
    - .planning/phases/01-data-integrity/01-RESEARCH.md (Pattern 3: Finalization Guard — código literal)
    - .planning/phases/01-data-integrity/01-PATTERNS.md (seção "Padrão de `_finalize` — guarda D-06 inserida no início")
  </read_first>
  <behavior>
    - Quando `_finalize()` é chamado e `_failedSaves.isEmpty` → comportamento ORIGINAL preservado (confirm dialog + finalizeAudit).
    - Quando `_finalize()` é chamado e `_failedSaves.isNotEmpty` → AlertDialog de bloqueio aparece com título `Respostas não salvas`, contagem correta, botão único `Entendido`, e método retorna SEM chamar `finalizeAudit`.
    - Plural/singular correto na mensagem: 1 falha = "1 resposta não foi salva"; 2+ = "N respostas não foram salvas".
  </behavior>
  <action>
Passo único — Modificar `_finalize()` inserindo a guarda D-06 como as PRIMEIRAS linhas do método, ANTES do `final confirm = await showDialog<bool>(...)` existente na linha 236.

A assinatura atual é:
```dart
Future<void> _finalize() async {
  final confirm = await showDialog<bool>(
    context: context,
    // ...
```

Substituir pela versão com guard no início:
```dart
  Future<void> _finalize() async {
    // ── Guarda D-06: bloqueia finalização se há respostas com falha ───────
    if (_failedSaves.isNotEmpty) {
      final count = _failedSaves.length;
      final respostas = count > 1 ? 'respostas' : 'resposta';
      final verbo = count > 1 ? 'foram salvas' : 'foi salva';
      await showDialog<void>(
        context: context,
        builder: (ctx) => AlertDialog(
          title: const Text('Respostas não salvas'),
          content: Text(
            '$count $respostas não $verbo. '
            'Resolva as falhas antes de finalizar.',
          ),
          actions: [
            TextButton(
              onPressed: () => Navigator.pop(ctx),
              child: const Text('Entendido'),
            ),
          ],
        ),
      );
      return; // NÃO prossegue para o dialog de confirmação de finalização
    }

    final confirm = await showDialog<bool>(
      context: context,
      // ... (mantido igual, a partir daqui nada muda)
```

TODO o resto do `_finalize` (linhas 236-297 atuais) fica INALTERADO. Apenas o trecho acima é inserido no topo da função.

Regras:
- NÃO adicionar o check `_failedSaves.isNotEmpty` ao getter `_canFinalize` ou ao estado disabled do botão — RESEARCH.md Anti-Patterns explícito: "Adding `_failedSaves` check to `_canFinalize` getter" → confunde usuário. O bloqueio é via dialog no `_finalize`, não via disabled.
- NÃO tocar em `_discardAndExit`, `_cancelAudit`, ou qualquer outro método — D-07 restringe escopo a `_saveAnswer` + `_finalize`.
- Texto EXATO do título: `Respostas não salvas` (sem "as" inicial, sem "há").
- Texto EXATO do botão: `Entendido` (CONTEXT.md specifics).
- Formato EXATO da mensagem: `"N respostas não foram salvas. Resolva as falhas antes de finalizar."` (com ponto final, espaço, plural condicional).
  </action>
  <verify>
    <automated>cd primeaudit && flutter analyze lib/screens/audit_execution_screen.dart &amp;&amp; flutter test test/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "if (_failedSaves.isNotEmpty) {" primeaudit/lib/screens/audit_execution_screen.dart` retorna pelo menos `1`.
    - `grep -c "'Respostas não salvas'" primeaudit/lib/screens/audit_execution_screen.dart` retorna `1`.
    - `grep -c "'Resolva as falhas antes de finalizar.'" primeaudit/lib/screens/audit_execution_screen.dart` retorna `1` (substring da mensagem completa).
    - `grep -c "'Entendido'" primeaudit/lib/screens/audit_execution_screen.dart` retorna `1`.
    - `grep -c "showDialog<void>" primeaudit/lib/screens/audit_execution_screen.dart` retorna pelo menos `1` (o dialog de bloqueio D-06).
    - `grep -c "_canFinalize" primeaudit/lib/screens/audit_execution_screen.dart` — valor deve ser IGUAL ao valor pré-task (sem incremento). Confirma que `_canFinalize` NÃO foi alterado.
    - Confirma via `grep -n "Future<void> _finalize" primeaudit/lib/screens/audit_execution_screen.dart` que a função existe e o `if (_failedSaves.isNotEmpty)` aparece ANTES do primeiro `showDialog<bool>` dentro dela.
    - `cd primeaudit && flutter analyze lib/screens/audit_execution_screen.dart` imprime `No issues found!`.
    - `cd primeaudit && flutter test test/` exit 0.
  </acceptance_criteria>
  <done>Guarda D-06 ativa: `_finalize` mostra dialog de bloqueio com contagem correta quando há falhas, e só procede para o dialog de confirmação original quando `_failedSaves` está vazio. Fix completo da Wave 1 pronto para ser coberto por testes no Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Este plano NÃO introduz novos boundaries. A única camada nova é UI-internal (fila em memória, snackbar, dialog). O caminho `upsertAnswer → Supabase` pré-existe e não muda.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | I (Information Disclosure) | `debugPrint('[_saveAnswer] itemId=$itemId erro: $e')` | accept | `debugPrint` só é visível em logs de desenvolvimento; em release builds do Flutter é silenciado (Flutter framework behavior). O `e.toString()` pode conter detalhes da exceção PostgrestException — aceitável para ambiente de dev; risco baixo. Se futuramente migrar para logger estruturado, sanitizar aqui. |
| T-01-02-02 | D (DoS) | Retry loop com backoff exponencial | mitigate | `_maxAutoRetryAttempts = 4` limita auto-retry a 4 tentativas (delays 1s+2s+4s+8s = 15s total). `_retrying` Set previne loops concorrentes para o mesmo `itemId`. Sem loop infinito. |
| T-01-02-03 | — | Novos fluxos de dados / auth / rede | accept | Nenhum — RESEARCH.md Security Domain: "This phase adds no new data access paths, no new authentication flows, and no new network endpoints." |
</threat_model>

<verification>
- `cd primeaudit && flutter analyze lib/screens/audit_execution_screen.dart` → `No issues found!`.
- `cd primeaudit && flutter test test/` → exit 0 (suíte Plan 01 + scaffolds skipped).
- `grep -c "// Falha silenciosa" primeaudit/lib/screens/audit_execution_screen.dart` → `0` (bug comentário removido).
- Inspeção visual das mudanças via `git diff primeaudit/lib/screens/audit_execution_screen.dart` mostra APENAS:
  1. Adições no bloco de imports (2 linhas)
  2. `typedef` no topo de arquivo (3-4 linhas)
  3. Constante `_maxAutoRetryAttempts` + 2 campos no `_AuditExecutionScreenState`
  4. `_saveAnswer` reescrito (substitui linhas 219-231)
  5. Métodos `_showSaveError` e `_scheduleRetry` novos
  6. Guard D-06 no topo de `_finalize`
  7. NENHUMA outra mudança — nem em `_discardAndExit`, `_cancelAudit`, `build`, `_ItemCard`, etc.
</verification>

<success_criteria>
- DINT-01: código atual tem `catch (e)` que chama `_showSaveError` exibindo snackbar `Não foi possível salvar`. VERIFICÁVEL: grep + leitura do arquivo.
- DINT-02: a interpretação autoritativa (Pitfall 4 em RESEARCH.md) é satisfeita — UI otimista preservada (`_answers[itemId]` setado em `_onAnswer`, sem mudança), indicador implícito via snackbar/dialog no caso de falha.
- DINT-03: `SnackBarAction` com label `Tentar novamente` chama `_saveAnswer(itemId, response, observation: observation)`.
- D-06: `_finalize` bloqueado por AlertDialog quando `_failedSaves.isNotEmpty`, não chama `finalizeAudit`.
- D-01 a D-07: todas as 7 decisões de CONTEXT.md estão implementadas conforme especificado.
- Nenhum side effect fora do escopo: outros catch blocks (`_discardAndExit`, `_cancelAudit`) intocados (D-07).
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-integrity/01-02-SUMMARY.md` describing:
- Diff resumido de `audit_execution_screen.dart` (linhas adicionadas/removidas por região)
- Trecho final do novo `_saveAnswer` (para o Plan 03 usar como fonte da verdade do comportamento a testar)
- Trecho final da guarda D-06 em `_finalize`
- Resultado de `flutter analyze` e `flutter test`
- Qualquer desvio (se `messenger.clearSnackBars()` precisou ser trocado por `hideCurrentSnackBar`, por exemplo — RESEARCH.md Open Question #1)
</output>
