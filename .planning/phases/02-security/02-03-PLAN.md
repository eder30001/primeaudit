---
phase: 02-security
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - primeaudit/lib/core/cnpj_validator.dart
  - primeaudit/test/core/cnpj_validator_test.dart
  - primeaudit/lib/screens/register_screen.dart
  - primeaudit/lib/screens/admin/company_form.dart
autonomous: true
requirements:
  - SEC-04
user_setup: []

must_haves:
  truths:
    - "Função pura isValidCnpj(String) retorna true apenas se os dois dígitos verificadores conferem"
    - "Função validateCnpj(String?) retorna null para entrada vazia/nula (campo opcional)"
    - "Função validateCnpj retorna mensagem de erro quando CNPJ tem 14 dígitos mas dígitos verificadores falham"
    - "Função validateCnpj retorna mensagem de erro quando CNPJ tem comprimento diferente de 14 dígitos"
    - "Função rejeita CNPJs de dígitos iguais (ex: 00000000000000, 11111111111111)"
    - "TextFormField de CNPJ em register_screen.dart tem validator: validateCnpj"
    - "TextFormField de CNPJ em company_form.dart tem validator: validateCnpj"
    - "flutter test passa com os testes unitários novos"
  artifacts:
    - path: "primeaudit/lib/core/cnpj_validator.dart"
      provides: "Pure Dart CNPJ checksum validation (SEC-04)"
      contains: "bool isValidCnpj"
      exports: ["isValidCnpj", "validateCnpj"]
    - path: "primeaudit/test/core/cnpj_validator_test.dart"
      provides: "Unit tests covering valid CNPJs, invalid checksums, length errors, same-digit rejection, empty input"
      contains: "isValidCnpj"
    - path: "primeaudit/lib/screens/register_screen.dart"
      provides: "CNPJ TextFormField with validator: validateCnpj wired"
      contains: "validator: validateCnpj"
    - path: "primeaudit/lib/screens/admin/company_form.dart"
      provides: "CNPJ _buildField call passing validator: validateCnpj"
      contains: "validator: validateCnpj"
  key_links:
    - from: "register_screen.dart CNPJ TextFormField"
      to: "cnpj_validator.dart validateCnpj function"
      via: "direct import + validator parameter"
      pattern: "import '.*cnpj_validator\\.dart'"
    - from: "company_form.dart _buildField for CNPJ"
      to: "cnpj_validator.dart validateCnpj function"
      via: "direct import + validator parameter"
      pattern: "validator: validateCnpj"
---

<objective>
Implementar validação de checksum de CNPJ (SEC-04) no client: criar `lib/core/cnpj_validator.dart` como utilitário puro Dart, cobrir com testes unitários, e plugar em `register_screen.dart` e `company_form.dart` via `validator:` do `TextFormField`.

Purpose: Hoje ambos os campos CNPJ aceitam qualquer string de 14 dígitos — `register_screen._searchCompany()` só checa comprimento, e `company_form._buildField` nem tem `validator`. A validação deve rejeitar CNPJs com dígitos verificadores inválidos antes de qualquer chamada ao banco (D-01, D-03).

Output: 1 arquivo novo (`cnpj_validator.dart`), 1 arquivo de teste novo (`cnpj_validator_test.dart`), 2 arquivos modificados (2 linhas em register_screen.dart, 1 linha em company_form.dart — mais imports).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-security/02-CONTEXT.md
@.planning/phases/02-security/02-RESEARCH.md
@.planning/phases/02-security/02-VALIDATION.md
@.planning/phases/02-security/02-PATTERNS.md

@primeaudit/lib/core/app_roles.dart
@primeaudit/lib/screens/register_screen.dart
@primeaudit/lib/screens/admin/company_form.dart
@primeaudit/test/pending_save_test.dart

<interfaces>
<!-- Pattern analog from primeaudit/lib/core/app_roles.dart (canonical core/ utility style) -->
<!-- Pure Dart, no Flutter imports, triple-slash doc comments, no instantiation -->

<!-- Target contract for cnpj_validator.dart (D-02: "função pura" — top-level functions, not class) -->
```dart
/// Returns true if [cnpj] passes the official Brazilian checksum validation.
/// Accepts formatted (dots/slashes/dashes/whitespace) or raw 14-digit strings.
bool isValidCnpj(String cnpj);

/// TextFormField.validator-compatible wrapper.
/// Returns null for valid/empty input (field is optional); error string otherwise.
String? validateCnpj(String? value);
```

<!-- CNPJ algorithm weights (Receita Federal standard, verified in research) -->
w1 = [5, 4, 3, 2, 9, 8, 7, 6, 5, 4, 3, 2]
w2 = [6, 5, 4, 3, 2, 9, 8, 7, 6, 5, 4, 3, 2]

<!-- Valid test CNPJs (standard examples used in Brazilian CNPJ validation libraries) -->
- 11.222.333/0001-81  (valid)
- 11444777000161      (valid, raw)
- 60.746.948/0001-12  (valid — Banco Bradesco public CNPJ)

<!-- Invalid CNPJs -->
- 00000000000000 (repeated digits — reject)
- 11111111111111 (repeated digits — reject)
- 11.222.333/0001-80 (last digit wrong — reject)
- 11.222.333/0001-91 (second-last digit wrong — reject)
- 123 (too short)
- 12345678901234 (14 digits but random — reject)

<!-- Current CNPJ field in register_screen.dart (lines 305-334) -->
```dart
TextFormField(
  controller: _cnpjController,
  keyboardType: TextInputType.number,
  textInputAction: TextInputAction.done,
  onChanged: _searchCompany,
  onFieldSubmitted: (_) => _register(),
  decoration: _inputDecoration(
    label: 'CNPJ da empresa',
    hint: '00.000.000/0000-00',
    icon: Icons.business_outlined,
  ).copyWith(suffixIcon: ...),
)
// NOTE: no validator: parameter today — must add `validator: validateCnpj`
```

<!-- Current CNPJ field in company_form.dart (lines 138-143) via _buildField helper -->
```dart
_buildField(
  controller: _cnpjController,
  label: 'CNPJ',
  icon: Icons.badge_outlined,
  keyboardType: TextInputType.number,
  // NOTE: no validator passed — must add `validator: validateCnpj`
),
```
`_buildField` already accepts optional `String? Function(String?)? validator` (line 197-198).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create cnpj_validator.dart with isValidCnpj and validateCnpj + unit tests</name>
  <files>primeaudit/lib/core/cnpj_validator.dart, primeaudit/test/core/cnpj_validator_test.dart</files>
  <read_first>
    - primeaudit/lib/core/app_roles.dart (pattern analog: pure Dart `lib/core/` utility, doc comments, no Flutter/Supabase imports)
    - primeaudit/test/pending_save_test.dart (pattern analog for unit test structure, `flutter_test` usage)
    - .planning/phases/02-security/02-CONTEXT.md (D-02, D-03 — função pura, no server-side validation)
    - .planning/phases/02-security/02-RESEARCH.md (Pattern 5 — CNPJ algorithm reference; Pitfall 4 — optional field)
  </read_first>
  <behavior>
    isValidCnpj:
    - Test 1: returns true for "11.222.333/0001-81" (formatted valid CNPJ)
    - Test 2: returns true for "11222333000181" (raw valid CNPJ, same digits as Test 1)
    - Test 3: returns true for "60.746.948/0001-12" (another known valid CNPJ)
    - Test 4: returns false for "11.222.333/0001-80" (first check digit wrong)
    - Test 5: returns false for "11.222.333/0001-91" (second check digit wrong)
    - Test 6: returns false for "00000000000000" (all zeros — same-digit rejection)
    - Test 7: returns false for "11111111111111" (all ones — same-digit rejection)
    - Test 8: returns false for "123" (too short)
    - Test 9: returns false for "12345678901234567890" (too long)
    - Test 10: returns false for "1234567890123a" (non-digit char in 14-char position)

    validateCnpj:
    - Test 11: returns null for null input (optional field passes)
    - Test 12: returns null for empty string "" (optional field passes)
    - Test 13: returns null for whitespace-only "   " (treat as empty)
    - Test 14: returns null for valid CNPJ "11.222.333/0001-81"
    - Test 15: returns "CNPJ deve ter 14 dígitos" for "123" (wrong length, non-empty)
    - Test 16: returns "CNPJ inválido — dígitos verificadores incorretos" for "11.222.333/0001-80" (14 digits but bad checksum)
  </behavior>
  <action>
Passo 1 — Criar `primeaudit/lib/core/cnpj_validator.dart` com conteúdo exato:

```dart
/// Validação de CNPJ (Cadastro Nacional da Pessoa Jurídica).
///
/// Implementa o algoritmo oficial da Receita Federal (dígitos verificadores),
/// não apenas verificação de comprimento. Puro Dart — sem dependências Flutter
/// ou Supabase. Pode ser usado tanto pelo app (TextFormField.validator) quanto
/// por testes unitários.
///
/// Exposto como duas funções top-level (sem wrapper de classe — D-02: função pura):
///   - `isValidCnpj(String)` — true/false, decisão pura
///   - `validateCnpj(String?)` — wrapper compatível com TextFormField.validator
library;

/// Retorna true se [cnpj] passa na validação de dígitos verificadores.
///
/// Aceita entrada formatada (com pontos, barras, hífens, espaços) ou crua
/// (14 dígitos contínuos). Rejeita:
///   - comprimento diferente de 14 dígitos (após limpeza)
///   - caracteres não-dígito em posições de dígito
///   - CNPJs com todos os 14 dígitos iguais (ex: 00000000000000)
///   - CNPJs onde os dois dígitos verificadores (posições 12 e 13) não conferem
bool isValidCnpj(String cnpj) {
  final digits = cnpj.replaceAll(RegExp(r'[.\-/\s]'), '');
  if (digits.length != 14) return false;
  // Aceita apenas dígitos em todas as 14 posições
  if (!RegExp(r'^\d{14}$').hasMatch(digits)) return false;
  // Rejeita sequências de um único dígito repetido (11111111111111 etc.)
  if (RegExp(r'^(\d)\1{13}$').hasMatch(digits)) return false;

  int calc(String s, List<int> weights) {
    var sum = 0;
    for (var i = 0; i < weights.length; i++) {
      sum += int.parse(s[i]) * weights[i];
    }
    final rem = sum % 11;
    return rem < 2 ? 0 : 11 - rem;
  }

  const w1 = [5, 4, 3, 2, 9, 8, 7, 6, 5, 4, 3, 2];
  const w2 = [6, 5, 4, 3, 2, 9, 8, 7, 6, 5, 4, 3, 2];

  final d1 = calc(digits, w1);
  final d2 = calc(digits, w2);

  return d1 == int.parse(digits[12]) && d2 == int.parse(digits[13]);
}

/// Validator compatível com `TextFormField.validator:`.
///
/// Retorna:
///   - null para entrada vazia/nula (campo opcional — D-03 / Pitfall 4)
///   - "CNPJ deve ter 14 dígitos" para entrada não-vazia com comprimento incorreto
///   - "CNPJ inválido — dígitos verificadores incorretos" para 14 dígitos com checksum inválido
///   - null para CNPJ válido
String? validateCnpj(String? value) {
  if (value == null || value.trim().isEmpty) return null;
  final digits = value.replaceAll(RegExp(r'[.\-/\s]'), '');
  if (digits.length != 14) return 'CNPJ deve ter 14 dígitos';
  if (!isValidCnpj(value)) return 'CNPJ inválido — dígitos verificadores incorretos';
  return null;
}
```

Passo 2 — Criar diretório `primeaudit/test/core/` se não existir e então criar `primeaudit/test/core/cnpj_validator_test.dart` com conteúdo exato:

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:primeaudit/core/cnpj_validator.dart';

void main() {
  group('isValidCnpj', () {
    test('accepts a known valid formatted CNPJ', () {
      expect(isValidCnpj('11.222.333/0001-81'), isTrue);
    });

    test('accepts the raw (unformatted) form of the same valid CNPJ', () {
      expect(isValidCnpj('11222333000181'), isTrue);
    });

    test('accepts another known valid CNPJ (60.746.948/0001-12)', () {
      expect(isValidCnpj('60.746.948/0001-12'), isTrue);
    });

    test('rejects a CNPJ with wrong first check digit', () {
      expect(isValidCnpj('11.222.333/0001-80'), isFalse);
    });

    test('rejects a CNPJ with wrong second check digit', () {
      expect(isValidCnpj('11.222.333/0001-91'), isFalse);
    });

    test('rejects all-zeros (same-digit sequence)', () {
      expect(isValidCnpj('00000000000000'), isFalse);
    });

    test('rejects all-ones (same-digit sequence)', () {
      expect(isValidCnpj('11111111111111'), isFalse);
    });

    test('rejects too-short input', () {
      expect(isValidCnpj('123'), isFalse);
    });

    test('rejects too-long input', () {
      expect(isValidCnpj('12345678901234567890'), isFalse);
    });

    test('rejects input containing non-digit chars in 14-char positions', () {
      expect(isValidCnpj('1234567890123a'), isFalse);
    });
  });

  group('validateCnpj', () {
    test('returns null for null input (optional field)', () {
      expect(validateCnpj(null), isNull);
    });

    test('returns null for empty string (optional field)', () {
      expect(validateCnpj(''), isNull);
    });

    test('returns null for whitespace-only input (optional field)', () {
      expect(validateCnpj('   '), isNull);
    });

    test('returns null for a valid CNPJ', () {
      expect(validateCnpj('11.222.333/0001-81'), isNull);
    });

    test('returns length error for non-empty input shorter than 14 digits', () {
      expect(validateCnpj('123'), equals('CNPJ deve ter 14 dígitos'));
    });

    test('returns checksum error for 14-digit input with invalid check digits', () {
      expect(
        validateCnpj('11.222.333/0001-80'),
        equals('CNPJ inválido — dígitos verificadores incorretos'),
      );
    });
  });
}
```

Verificar:
- O import no teste deve ser `package:primeaudit/core/cnpj_validator.dart` (nome do package em pubspec.yaml).
- Não adicionar nenhum import além de `flutter_test` e o arquivo testado.
- O library directive `library;` no cnpj_validator.dart é opcional mas explicita que é um arquivo library — seguro manter.

Passo 3 — Executar `cd primeaudit && flutter test test/core/cnpj_validator_test.dart` e verificar que todos os 16 testes passam.
  </action>
  <verify>
    <automated>cd primeaudit && flutter test test/core/cnpj_validator_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - File `primeaudit/lib/core/cnpj_validator.dart` exists
    - File `primeaudit/test/core/cnpj_validator_test.dart` exists
    - `cnpj_validator.dart` contains exact string `bool isValidCnpj(String cnpj)` (top-level function, not a class method)
    - `cnpj_validator.dart` contains exact string `String? validateCnpj(String? value)` (top-level function)
    - `cnpj_validator.dart` contains weight list `[5, 4, 3, 2, 9, 8, 7, 6, 5, 4, 3, 2]`
    - `cnpj_validator.dart` contains weight list `[6, 5, 4, 3, 2, 9, 8, 7, 6, 5, 4, 3, 2]`
    - `cnpj_validator.dart` has NO `import 'package:flutter/...'` (pure Dart per D-02)
    - `cnpj_validator.dart` has NO `import 'package:supabase_flutter/...'` (pure Dart per D-02)
    - `cnpj_validator_test.dart` has `import 'package:primeaudit/core/cnpj_validator.dart';`
    - `cnpj_validator_test.dart` has at least 10 `test(` calls under `group('isValidCnpj'`
    - `cnpj_validator_test.dart` has at least 6 `test(` calls under `group('validateCnpj'`
    - Command `cd primeaudit && flutter test test/core/cnpj_validator_test.dart` exits 0 with all tests passing
    - Command `cd primeaudit && flutter test` exits 0 (existing suite still green — no regressions in pending_save_test or audit_execution_save_error_test)
  </acceptance_criteria>
  <done>
    cnpj_validator.dart existe com assinaturas exatas `isValidCnpj(String): bool` e `validateCnpj(String?): String?`, teste unitário existe com 16 casos, `flutter test test/core/cnpj_validator_test.dart` passa verde.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire validateCnpj to register_screen.dart and company_form.dart CNPJ fields</name>
  <files>primeaudit/lib/screens/register_screen.dart, primeaudit/lib/screens/admin/company_form.dart</files>
  <read_first>
    - primeaudit/lib/screens/register_screen.dart (especialmente linhas 1-20 para estilo de import; linhas 305-334 — CNPJ TextFormField atual)
    - primeaudit/lib/screens/admin/company_form.dart (linhas 1-20 — imports; linhas 138-143 — CNPJ _buildField; linha 192+ — assinatura _buildField)
    - primeaudit/lib/core/cnpj_validator.dart (criado em Task 1 — contrato a importar)
    - .planning/phases/02-security/02-CONTEXT.md (D-01 — validar nos dois pontos)
    - .planning/phases/02-security/02-RESEARCH.md (Code Examples section — wiring snippets; Pitfall 5 — _buildField já aceita validator)
  </read_first>
  <action>
Duas alterações mínimas, uma em cada arquivo.

**Alteração 1 — `primeaudit/lib/screens/register_screen.dart`:**

a) Adicionar o import no topo do arquivo, junto aos outros imports do `package:primeaudit/...`. Inserir logo após o último import existente de `'../core/...'` (ou equivalente):

```dart
import '../core/cnpj_validator.dart';
```

b) No `TextFormField` do CNPJ (começando em ~linha 305 com `controller: _cnpjController,`), adicionar a linha `validator: validateCnpj,` entre `onFieldSubmitted:` e `decoration:`. Resultado final do widget:

```dart
TextFormField(
  controller: _cnpjController,
  keyboardType: TextInputType.number,
  textInputAction: TextInputAction.done,
  onChanged: _searchCompany,
  onFieldSubmitted: (_) => _register(),
  validator: validateCnpj,
  decoration: _inputDecoration(
    label: 'CNPJ da empresa',
    hint: '00.000.000/0000-00',
    icon: Icons.business_outlined,
  ).copyWith(
    // ... suffixIcon bloco existente inalterado
  ),
),
```

NÃO alterar nenhuma outra parte do arquivo. NÃO remover `onChanged: _searchCompany` (continua sendo necessário para busca automática). O `validator:` só é invocado quando `_formKey.currentState!.validate()` é chamado (dentro de `_register()`) — não conflita com `onChanged`.

**Alteração 2 — `primeaudit/lib/screens/admin/company_form.dart`:**

a) Adicionar o import no topo do arquivo, junto aos demais imports do package:

```dart
import '../../core/cnpj_validator.dart';
```

(O caminho relativo correto a partir de `lib/screens/admin/company_form.dart` é `../../core/cnpj_validator.dart` — dois níveis acima para sair de `admin/` e `screens/`.)

b) Na chamada de `_buildField` para o campo CNPJ (linhas 138-143 do arquivo atual), adicionar o parâmetro `validator: validateCnpj,`. Resultado final:

```dart
_buildField(
  controller: _cnpjController,
  label: 'CNPJ',
  icon: Icons.badge_outlined,
  keyboardType: TextInputType.number,
  validator: validateCnpj,
),
```

NÃO alterar a assinatura de `_buildField` (já aceita `String? Function(String?)? validator` opcional na linha ~197-198). NÃO adicionar validator nos outros campos (nome, email, telefone, endereço) — só o CNPJ neste plano.

**Verificação mecânica:**
1. `flutter analyze` deve passar sem novos warnings/errors em register_screen.dart e company_form.dart.
2. `flutter test` deve passar — nenhum teste existente usa esses validators, mas a compilação deve ficar limpa.
3. Nenhuma mudança de comportamento para usuários que não preenchem CNPJ (campo opcional — validateCnpj retorna null para vazio).
  </action>
  <verify>
    <automated>cd primeaudit && grep -q "import '../core/cnpj_validator.dart';" lib/screens/register_screen.dart && grep -q "validator: validateCnpj," lib/screens/register_screen.dart && grep -q "import '../../core/cnpj_validator.dart';" lib/screens/admin/company_form.dart && grep -q "validator: validateCnpj," lib/screens/admin/company_form.dart && flutter analyze lib/screens/register_screen.dart lib/screens/admin/company_form.dart lib/core/cnpj_validator.dart && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - `primeaudit/lib/screens/register_screen.dart` contains exact string `import '../core/cnpj_validator.dart';`
    - `primeaudit/lib/screens/register_screen.dart` contains exact string `validator: validateCnpj,` (with comma, inside the CNPJ TextFormField)
    - `grep -c "validator: validateCnpj," primeaudit/lib/screens/register_screen.dart` returns exactly `1` (validator added only to CNPJ field, not to name/email/password fields which already have their own validators)
    - `primeaudit/lib/screens/register_screen.dart` still contains `onChanged: _searchCompany,` (not removed — coexists with validator)
    - `primeaudit/lib/screens/admin/company_form.dart` contains exact string `import '../../core/cnpj_validator.dart';`
    - `primeaudit/lib/screens/admin/company_form.dart` contains exact string `validator: validateCnpj,` inside the CNPJ `_buildField` call
    - `grep -c "validator: validateCnpj," primeaudit/lib/screens/admin/company_form.dart` returns exactly `1`
    - Command `cd primeaudit && flutter analyze` exits 0 (no errors) — allows pre-existing warnings but zero new ones introduced by these 2 files
    - Command `cd primeaudit && flutter test` exits 0 (full suite green, including pending_save_test, audit_execution_save_error_test, and cnpj_validator_test)
    - No modifications to any other file (git diff shows only these 2 files plus the ones from Task 1)
  </acceptance_criteria>
  <done>
    Ambos os campos CNPJ em `register_screen.dart` e `company_form.dart` têm `validator: validateCnpj`; `flutter analyze` limpo nos 3 arquivos; `flutter test` suite full verde.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> CNPJ TextFormField | Usuário digita CNPJ no cadastro ou em company_form — validator é a primeira barreira |
| Validator -> PostgREST | Apenas CNPJs que passam pelo validator chegam ao banco (antes disso, Form.validate() rejeita submit) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Tampering | CNPJ aceito com dígitos verificadores inválidos | mitigate | `validateCnpj` roda algoritmo oficial da Receita Federal no `TextFormField.validator` — rejeita antes de `_register()` / `_save()` chamarem o PostgREST (SEC-04, D-03) |
| T-02-10 | Tampering | CNPJ bypass via DevTools alterando onChanged | transfer | Validação client-side é suficiente para UX; D-03 explicitamente rejeita validação server-side (os dois pontos de entrada estão cobertos; anon key é public by design conforme decisão inicial). Bypass resulta apenas em row com CNPJ inválido — não há consequência de segurança (CNPJ não é usado em checks de autorização) |
| T-02-11 | Denial of Service | CNPJ absurdamente longo causando travamento | mitigate | Early return em `digits.length != 14` — O(1); algoritmo subsequente roda em 26 iterações no máximo |
</threat_model>

<verification>
- `cnpj_validator.dart` existe com `isValidCnpj` e `validateCnpj` como top-level functions puras
- 16 testes unitários cobrindo CNPJs válidos conhecidos, checksums inválidos, comprimento errado, mesmo-dígito, e casos opcionais
- `flutter test test/core/cnpj_validator_test.dart` verde
- `flutter test` completo verde (sem regressões em Phase 1)
- Ambos os arquivos de tela importam `cnpj_validator.dart` e passam `validator: validateCnpj` nos campos CNPJ
- `flutter analyze` sem novos warnings nos 3 arquivos
</verification>

<success_criteria>
- Phase 1 tests continue green (pending_save_test, audit_execution_save_error_test)
- Phase 2 SEC-04 tests green (16 new test cases)
- Compilação limpa (`flutter analyze` nos 3 arquivos)
- 2 campos CNPJ plugados ao validator
</success_criteria>

<output>
After completion, create `.planning/phases/02-security/02-03-SUMMARY.md`
</output>
