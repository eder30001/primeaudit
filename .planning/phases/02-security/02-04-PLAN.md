---
phase: 02-security
plan: 04
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
  - "02-03"
files_modified:
  - primeaudit/SECURITY-AUDIT.md
autonomous: false
requirements:
  - SEC-01
  - SEC-02
  - SEC-03
user_setup:
  - service: supabase
    why: "Schema push requer CLI autenticada e acesso ao projeto remoto; verificações manuais RLS requerem Supabase SQL editor"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard -> Account -> Access Tokens (necessário para ambiente não-TTY)"
    dashboard_config:
      - task: "Confirmar que o projeto correto está linkado via `supabase link`"
        location: "CLI local + Supabase Dashboard -> Project Settings -> Reference ID"

must_haves:
  truths:
    - "supabase db push foi executado com sucesso — ambas as migrations (20260418_fix_active_guard.sql e 20260418_rls_profiles_companies_perimeters.sql) estão aplicadas no banco remoto"
    - "Todas as 9 verificações manuais listadas em SECURITY-AUDIT.md foram executadas e documentadas (passed/failed)"
    - "Se qualquer verificação falhar, o usuário é notificado e gaps são criados para gap closure"
    - "Status final da fase fica documentado no SECURITY-AUDIT.md (resultados preenchidos)"
  artifacts:
    - path: "primeaudit/SECURITY-AUDIT.md"
      provides: "Documentação preenchida com resultados das 9 verificações manuais"
      contains: "✅ passed"
  key_links:
    - from: "supabase db push output"
      to: "tabela de verificações em SECURITY-AUDIT.md"
      via: "execução da CLI + transcrição do resultado no documento"
      pattern: "Applying migration 20260418"
---

<objective>
Portão bloqueante da Phase 2: aplicar as migrations no Supabase remoto e executar as 9 verificações manuais de RLS documentadas em `SECURITY-AUDIT.md` (SEC-01, SEC-02, SEC-03). Sem este plano, as mudanças dos Plans 01 e 02 existem apenas como arquivos locais — o banco continua com as policies quebradas.

Purpose:
- `supabase db push` é obrigatório porque build e `flutter test` passam sem ele (tipos vêm da config, não do banco), criando falso-positivo de verificação.
- Verificação manual das policies RLS não pode ser automatizada por `flutter test` — exige SQL editor com sessões de múltiplos usuários (auditor, adm, inativo).
- Resultado preenchido em `SECURITY-AUDIT.md` é o artefato que satisfaz SEC-01.

Output: Migrations aplicadas no remoto + `SECURITY-AUDIT.md` atualizado com 9 resultados (✅ passed ou ❌ failed) + relatório ao usuário sobre quaisquer failures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-security/02-CONTEXT.md
@.planning/phases/02-security/02-RESEARCH.md
@.planning/phases/02-security/02-VALIDATION.md

@primeaudit/supabase/migrations/20260418_fix_active_guard.sql
@primeaudit/supabase/migrations/20260418_rls_profiles_companies_perimeters.sql
@primeaudit/SECURITY-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Run supabase db push to apply both Phase 2 migrations to remote database</name>
  <files>(none — CLI-only side effect on remote Supabase project)</files>
  <read_first>
    - primeaudit/supabase/migrations/20260418_fix_active_guard.sql (verificar que o arquivo existe e tem o conteúdo esperado)
    - primeaudit/supabase/migrations/20260418_rls_profiles_companies_perimeters.sql (idem)
    - .planning/config.json (confirmar mode=yolo e settings relacionados)
  </read_first>
  <action>
Executar `supabase db push` a partir da raiz `primeaudit/` para sincronizar as duas novas migrations com o Supabase remoto.

Passo 1 — Verificar o estado da CLI localmente:
```bash
cd primeaudit && supabase --version
```
Se `supabase` não estiver instalado, abortar a task e reportar que o usuário precisa instalar o Supabase CLI (`npm install -g supabase` ou via scoop/brew) — não tentar instalar globalmente sem confirmação.

Passo 2 — Verificar autenticação. Se `SUPABASE_ACCESS_TOKEN` estiver no ambiente, `supabase db push` roda em modo não-TTY. Caso contrário:
```bash
cd primeaudit && supabase projects list
```
Se retornar "You are not logged in", parar e reportar ao usuário: exigir `supabase login` manualmente (TTY requerido) OU setar `SUPABASE_ACCESS_TOKEN` (visto em Account -> Access Tokens no dashboard).

Passo 3 — Verificar link com projeto remoto:
```bash
cd primeaudit && supabase status 2>&1 | head -10
```
Se não linkado, pedir ao usuário o project ref e rodar `supabase link --project-ref <ref>`. Não inventar o ref.

Passo 4 — Dry-run opcional para inspecionar o plano:
```bash
cd primeaudit && supabase db diff --schema public 2>&1 | head -40
```

Passo 5 — Push real:
```bash
cd primeaudit && supabase db push
```

Capturar o output completo. Se a CLI reportar conflito de migration (ex: migration com timestamp mais recente já no remoto), parar e escalar — não usar `--include-all` ou `--force` sem confirmação.

Passo 6 — Após sucesso, validar que as duas migrations aparecem como aplicadas:
```bash
cd primeaudit && supabase migration list 2>&1
```
Deve listar `20260418_fix_active_guard` e `20260418_rls_profiles_companies_perimeters` com status Applied (ou marca equivalente indicando remote-local sync).

**Fallback se `supabase db push` exigir interatividade que não pode ser suprimida:**
- Gerar um bloco de instruções para o usuário executar manualmente no dashboard Supabase (SQL Editor):
  1. Copiar o conteúdo integral de `20260418_fix_active_guard.sql` e executar.
  2. Copiar o conteúdo integral de `20260418_rls_profiles_companies_perimeters.sql` e executar.
  3. Aguardar `NOTIFY pgrst, 'reload schema'` (última linha) confirmar.
- Marcar esta task como requerendo intervenção humana (`autonomous: false` já está no frontmatter).
  </action>
  <verify>
    <automated>cd primeaudit && supabase migration list 2>&1 | grep -E "20260418_fix_active_guard|20260418_rls_profiles_companies_perimeters" | wc -l | awk '{ if ($1 >= 2) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `supabase migration list` output (saved to plan summary) contains both `20260418_fix_active_guard` and `20260418_rls_profiles_companies_perimeters` as applied/remote-present
    - No error lines in push output (no "error", "failed", "conflict" unless explicitly resolved by fallback path)
    - If fallback path used: SUMMARY.md records exactly which SQL blocks were run manually and the observed server responses
    - No changes to any local file during push (migrations were already created in Plans 01/02; this task only applies them remotely)
    - Se a task usar o fallback manual, marcar no SUMMARY e permitir que Task 2 prossiga apenas após confirmação do usuário
  </acceptance_criteria>
  <done>
    Ambas as migrations estão aplicadas no banco remoto (confirmado via `supabase migration list` ou via execução manual no SQL editor com output capturado). Schema reload disparado (`NOTIFY pgrst`).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [CHECKPOINT] Execute 9 manual RLS verifications and fill SECURITY-AUDIT.md</name>
  <files>primeaudit/SECURITY-AUDIT.md</files>
  <read_first>
    - primeaudit/SECURITY-AUDIT.md (a tabela "Manual Verification Plan" lista as 9 verificações em status ⬜ pending)
    - primeaudit/supabase/migrations/20260418_fix_active_guard.sql (referência SEC-03)
    - primeaudit/supabase/migrations/20260418_rls_profiles_companies_perimeters.sql (referência SEC-01 + SEC-02)
    - .planning/phases/02-security/02-VALIDATION.md (tabela "Manual-Only Verifications")
  </read_first>
  <what-built>
    - Plan 01 aplicou guarda `active = true` em `get_my_role()` e `get_my_company_id()` (SEC-03)
    - Plan 02 criou policies RLS corretas para profiles, companies, perimeters, audit_types, audit_templates, template_items (SEC-01, SEC-02) e gerou `primeaudit/SECURITY-AUDIT.md` com plano de 9 verificações manuais em status ⬜ pending
    - Plan 03 adicionou validação de checksum CNPJ nos 2 formulários (SEC-04)
    - Plan 04 Task 1 aplicou ambas as migrations no Supabase remoto
  </what-built>
  <action>
Executar as 9 verificações listadas na seção **"Manual Verification Plan"** de `primeaudit/SECURITY-AUDIT.md`, usando o SQL Editor do Supabase Dashboard e/ou sessões autenticadas via app. Ao final, editar o arquivo `primeaudit/SECURITY-AUDIT.md` substituindo cada `⬜ pending` pelo resultado observado.

**Pré-requisitos no dashboard Supabase:**
Precisará de pelo menos 4 usuários de teste com roles e active-status distintos:
  1. Usuário A: `role = 'superuser'`, `active = true`
  2. Usuário B: `role = 'adm'`, `active = true`, `company_id = <X>`
  3. Usuário C: `role = 'auditor'`, `active = true`, `company_id = <X>`
  4. Usuário D: `role = 'auditor'`, `active = false` (via SQL editor autenticado como service_role: `UPDATE profiles SET active = false WHERE id = '<D>';`)

**Roteiro de execução — preencher cada uma das 9 linhas do SECURITY-AUDIT.md com ✅ passed, ❌ failed, ou anotação descritiva:**

1. **[SEC-03]** Autenticado como Usuário A (ativo), no SQL Editor executar `SELECT get_my_role();` → Esperado: retorna a role (ex: 'superuser'). Se retornar NULL → ❌.

2. **[SEC-03]** Autenticado como Usuário D (inativo), `SELECT get_my_role();` → Esperado: retorna NULL. Se retornar 'auditor' → ❌ (guarda active não funcionou).

3. **[SEC-03]** Como Usuário D (inativo), `SELECT COUNT(*) FROM audits;` → Esperado: 0. Se > 0 → ❌.

4. **[SEC-03]** Como Usuário D (inativo), `SELECT COUNT(*) FROM audit_answers;` → Esperado: 0. Se > 0 → ❌.

5. **[SEC-02]** Como Usuário C (auditor ativo): `UPDATE profiles SET role = 'superuser' WHERE id = auth.uid();` → Esperado: 0 rows updated OU erro PostgREST 42501. Se alterou → ❌.

6. **[SEC-02]** Como Usuário B (adm ativo): `UPDATE profiles SET role = 'superuser' WHERE id = '<id do Usuário C da mesma empresa>';` → Esperado: 0 rows updated (a policy `adm_profiles_update` WITH CHECK exige `role = (SELECT p.role FROM profiles p WHERE p.id = profiles.id)`). Se 1 row updated → ❌ crítico.

7. **[SEC-02]** Mesma sessão B: `UPDATE profiles SET full_name = 'Teste Adm' WHERE id = '<id do Usuário C>';` → Esperado: 1 row updated. Se 0 rows → ❌ (policy tight demais).

8. **[SEC-01]** No dashboard Supabase, Table Editor → cada tabela → aba Policies: verificar que todas as 8 tabelas (`audits`, `audit_answers`, `profiles`, `companies`, `perimeters`, `audit_types`, `audit_templates`, `template_items`) mostram "RLS enabled". Se alguma mostra RLS desabilitado → ❌.

9. **[SEC-01]** No dashboard Supabase, Table Editor: procurar por `template_sections`. Se existe, documentar: nome + tem policies? Se não existe, documentar "tabela não existe". Item 9 é documentação apenas — não falha.

**Preenchimento do arquivo:**
Editar `primeaudit/SECURITY-AUDIT.md`. Para cada uma das 9 linhas da tabela de verificações, substituir `⬜ pending` por exatamente uma das strings abaixo:
- `✅ passed` — comportamento observado == esperado
- `❌ failed — <descrição de 1 linha>` — comportamento diferente do esperado
- `✅ documented — <resultado>` — apenas para item 9 (documentação)

Após preencher, adicionar uma linha no changelog do arquivo:
```markdown
| 2026-04-XX | Plan 04: 9 verificações manuais executadas (<N> passed, <M> failed) |
```

**Lógica de aprovação (decisão do usuário no resume-signal):**
- Se itens 1-8 todos ✅ → phase aprovada, signalizar "approved"
- Se qualquer item 1-8 ❌ → não aprovar ainda; signalizar "gaps: <numbers>" e orchestrator criará gap plans via `/gsd-plan-phase --gaps`

**Diagnóstico sugerido para failures mais prováveis:**
- Item 2 (guarda active não aplicada) → Plan 01 não foi pushed, ou função não foi sobrescrita; re-executar Task 1 do Plan 04
- Item 6 (subquery WITH CHECK não bloqueia) → alias `p.` pode não funcionar conforme esperado; alternativa é trigger BEFORE UPDATE em `profiles` com check de `NEW.role = OLD.role` quando `get_my_role() = 'adm'`
  </action>
  <how-to-verify>
Ler `primeaudit/SECURITY-AUDIT.md` após preenchimento: a tabela "Manual Verification Plan" não deve conter nenhuma ocorrência de `⬜ pending`. Cada linha deve ter `✅` ou `❌` ou `documented`.

Se todos 1-8 mostram `✅ passed`: phase aprovada.
Se algum mostra `❌`: reportar lista para gap closure antes de aprovar.
  </how-to-verify>
  <verify>
    <automated>! grep -q "⬜ pending" primeaudit/SECURITY-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `primeaudit/SECURITY-AUDIT.md` não contém mais nenhuma ocorrência de `⬜ pending` (confirmável via `! grep -q "⬜ pending" primeaudit/SECURITY-AUDIT.md`)
    - Cada uma das 9 linhas de verificação contém uma das strings: `✅ passed`, `❌ failed`, ou `✅ documented` (para item 9)
    - Se houver pelo menos 1 `❌`: SUMMARY.md deste plano lista explicitamente os números dos itens com falha e inclui recomendação de executar `/gsd-plan-phase --gaps`
    - Se todos itens 1-8 são `✅`: o usuário respondeu "approved" no resume-signal e isso fica registrado no SUMMARY.md
    - Changelog de SECURITY-AUDIT.md tem nova linha documentando a execução das 9 verificações com contagem de passed/failed
  </acceptance_criteria>
  <resume-signal>
Type "approved" when all 9 verifications are documented in SECURITY-AUDIT.md AND items 1-8 all show ✅ passed.

If any of items 1-8 show ❌, type "gaps: <comma-separated item numbers>" and the phase will enter gap closure mode.

Examples:
- "approved" → Phase 2 complete
- "gaps: 2, 6" → items 2 and 6 failed; create follow-up plans to remediate
  </resume-signal>
  <done>
    SECURITY-AUDIT.md preenchido com 9 resultados (zero `⬜ pending` remanescentes). Usuário deu sinal explícito: "approved" (todos 1-8 passed) ou "gaps: N,M,..." (lista específica de failures para gap closure).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine -> Supabase project | `supabase db push` requer acesso privilegiado (access token); falha silenciosa de push é risco se verificação manual for pulada |
| SQL editor session -> RLS | Tests de verificação assumem que as sessões estão corretamente autenticadas como os roles de teste |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Tampering | Migration parcialmente aplicada (Plan 01 push ok, Plan 02 push falhou) | mitigate | `supabase migration list` verificado após push; acceptance_criteria de Task 1 exige ambas listadas |
| T-02-13 | Repudiation | Verificações manuais marcadas como ✅ sem realmente executar | accept | Checkpoint humano-verify — responsabilidade do desenvolvedor; não há como auto-verificar automaticamente |
| T-02-14 | Information Disclosure | Tokens Supabase vazados em output de CLI | mitigate | `supabase db push` não imprime tokens por padrão; não logar `SUPABASE_ACCESS_TOKEN` em output/summary |
</threat_model>

<verification>
- `supabase migration list` mostra ambas as migrations de 2026-04-18 aplicadas
- `SECURITY-AUDIT.md` não contém mais `⬜ pending` nas 9 linhas (todas ✅ ou ❌ ou "documented")
- Se houver ❌, o usuário decide se aprova com gaps documentados ou se entra em gap closure
</verification>

<success_criteria>
- Banco remoto sincronizado com os arquivos locais de migration
- 9 verificações manuais documentadas em SECURITY-AUDIT.md
- Usuário dá sinal explícito de aprovação ou solicita gap closure
</success_criteria>

<output>
After completion, create `.planning/phases/02-security/02-04-SUMMARY.md` com:
- Output de `supabase migration list` (antes e depois)
- Tabela de 9 verificações com resultados
- Lista de gaps (se houver)
- Recomendação próxima ação (aprovar / `/gsd-plan-phase --gaps`)
</output>
