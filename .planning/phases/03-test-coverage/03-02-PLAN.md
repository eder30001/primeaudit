---
phase: 03-test-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - primeaudit/test/models/app_role_test.dart
autonomous: true
requirements:
  - QUAL-02
must_haves:
  truths:
    - "Todos os 5 roles (superuser, dev, adm, auditor, anonymous) têm asserções explícitas para cada helper estático de AppRole"
    - "canAccessAdmin é verdadeiro para superuser, dev, adm e falso para auditor, anonymous"
    - "canAccessDev é verdadeiro para superuser, dev e falso para adm, auditor, anonymous"
    - "isSuperOrDev é verdadeiro para superuser, dev e falso para adm, auditor, anonymous"
    - "label retorna os rótulos portugueses corretos para os 5 roles e devolve o próprio role quando desconhecido"
    - "Nenhum teste faz referência ao método inexistente canEdit"
    - "flutter test test/models/app_role_test.dart passa com código 0"
  artifacts:
    - path: "primeaudit/test/models/app_role_test.dart"
      provides: "Unit tests for AppRole static helpers covering all 5 defined roles + unknown role fallback for label()"
      min_lines: 70
      contains: "AppRole.canAccessAdmin"
  key_links:
    - from: "primeaudit/test/models/app_role_test.dart"
      to: "primeaudit/lib/core/app_roles.dart"
      via: "import direto e chamadas estáticas"
      pattern: "AppRole\\.(canAccessAdmin|canAccessDev|isSuperOrDev|label)"
---

<objective>
Cobrir os helpers estáticos de `AppRole` (`canAccessAdmin`, `canAccessDev`, `isSuperOrDev`, `label`) com testes unitários cobrindo os 5 roles definidos (`superuser`, `dev`, `adm`, `auditor`, `anonymous`), atendendo QUAL-02.

Purpose: `AppRole` é a única fonte de verdade para decisões de autorização na camada cliente (a verdadeira segurança vive na RLS do Supabase, já coberta pela Phase 2). Sem testes, uma edição acidental em `app_roles.dart` pode expor áreas administrativas a auditors sem sinal do CI. Cobrir explicitamente todos os roles garante regressão imediata.

Output: novo arquivo `primeaudit/test/models/app_role_test.dart` com testes determinísticos, sem mocks, sem Supabase.

Nota sobre canEdit: A ROADMAP (success criterion #3) menciona `canEdit` como um método a ser testado. Esse método NÃO existe em `app_roles.dart` (verificado em 03-RESEARCH.md Pitfall 4 e 03-PATTERNS.md). O plano cobre os métodos que existem (`canAccessAdmin`, `canAccessDev`, `isSuperOrDev`, `label`) e deliberadamente NÃO cria um método inexistente — criar uma API pública só para cumprir literalmente um critério seria um anti-padrão. Esse critério é satisfeito cobrindo os helpers reais.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-test-coverage/03-RESEARCH.md
@.planning/phases/03-test-coverage/03-PATTERNS.md
@.planning/phases/03-test-coverage/03-VALIDATION.md
@primeaudit/lib/core/app_roles.dart
@primeaudit/test/core/cnpj_validator_test.dart

<interfaces>
<!-- Extraído de primeaudit/lib/core/app_roles.dart — executor não deve explorar -->

```dart
class AppRole {
  static const String superuser = 'superuser';
  static const String dev = 'dev';
  static const String adm = 'adm';
  static const String auditor = 'auditor';
  static const String anonymous = 'anonymous';

  static const List<String> all = [superuser, dev, adm, auditor, anonymous];

  // Rótulos:
  //   superuser -> 'Super Usuário'
  //   dev       -> 'Desenvolvedor'
  //   adm       -> 'Administrador'
  //   auditor   -> 'Auditor'
  //   anonymous -> 'Anônimo'
  //   <outro>   -> retorna a própria string recebida
  static String label(String role) { ... }

  static bool canAccessAdmin(String role) =>
      role == superuser || role == dev || role == adm;

  static bool canAccessDev(String role) => role == superuser || role == dev;

  static bool isSuperOrDev(String role) => role == superuser || role == dev;
}
```

Não existe: `canEdit`, `canAccessAuditor`, `isAdm`, `hasRole`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar test/models/app_role_test.dart com cobertura dos 4 helpers × 5 roles</name>
  <files>primeaudit/test/models/app_role_test.dart</files>
  <read_first>
    - primeaudit/lib/core/app_roles.dart (confirmar assinaturas e rótulos portugueses exatos)
    - primeaudit/test/core/cnpj_validator_test.dart (padrão de referência de estrutura `group()`/`test()`)
  </read_first>
  <behavior>
    - canAccessAdmin: TRUE para superuser/dev/adm; FALSE para auditor/anonymous
    - canAccessDev: TRUE para superuser/dev; FALSE para adm/auditor/anonymous
    - isSuperOrDev: TRUE para superuser/dev; FALSE para adm/auditor/anonymous
    - label: retorna rótulo português para os 5 roles conhecidos; retorna o próprio string para role desconhecido
    - Nenhum teste referencia AppRole.canEdit (método inexistente)
  </behavior>
  <action>
    Criar o arquivo `primeaudit/test/models/app_role_test.dart` com EXATAMENTE este conteúdo:

    ```dart
    // Unit tests for AppRole static helpers (QUAL-02).
    // Covers all 5 defined roles against canAccessAdmin, canAccessDev,
    // isSuperOrDev, and label. Does NOT test `canEdit` — that method
    // does not exist in app_roles.dart (see 03-RESEARCH.md Pitfall 4).

    import 'package:flutter_test/flutter_test.dart';
    import 'package:primeaudit/core/app_roles.dart';

    void main() {
      group('AppRole.canAccessAdmin', () {
        test('true for superuser', () {
          expect(AppRole.canAccessAdmin(AppRole.superuser), isTrue);
        });
        test('true for dev', () {
          expect(AppRole.canAccessAdmin(AppRole.dev), isTrue);
        });
        test('true for adm', () {
          expect(AppRole.canAccessAdmin(AppRole.adm), isTrue);
        });
        test('false for auditor', () {
          expect(AppRole.canAccessAdmin(AppRole.auditor), isFalse);
        });
        test('false for anonymous', () {
          expect(AppRole.canAccessAdmin(AppRole.anonymous), isFalse);
        });
        test('false for unknown role string', () {
          expect(AppRole.canAccessAdmin('chaos_goblin'), isFalse);
        });
      });

      group('AppRole.canAccessDev', () {
        test('true for superuser', () {
          expect(AppRole.canAccessDev(AppRole.superuser), isTrue);
        });
        test('true for dev', () {
          expect(AppRole.canAccessDev(AppRole.dev), isTrue);
        });
        test('false for adm', () {
          expect(AppRole.canAccessDev(AppRole.adm), isFalse);
        });
        test('false for auditor', () {
          expect(AppRole.canAccessDev(AppRole.auditor), isFalse);
        });
        test('false for anonymous', () {
          expect(AppRole.canAccessDev(AppRole.anonymous), isFalse);
        });
      });

      group('AppRole.isSuperOrDev', () {
        test('true for superuser', () {
          expect(AppRole.isSuperOrDev(AppRole.superuser), isTrue);
        });
        test('true for dev', () {
          expect(AppRole.isSuperOrDev(AppRole.dev), isTrue);
        });
        test('false for adm', () {
          expect(AppRole.isSuperOrDev(AppRole.adm), isFalse);
        });
        test('false for auditor', () {
          expect(AppRole.isSuperOrDev(AppRole.auditor), isFalse);
        });
        test('false for anonymous', () {
          expect(AppRole.isSuperOrDev(AppRole.anonymous), isFalse);
        });
      });

      group('AppRole.label', () {
        test('superuser -> Super Usuário', () {
          expect(AppRole.label(AppRole.superuser), equals('Super Usuário'));
        });
        test('dev -> Desenvolvedor', () {
          expect(AppRole.label(AppRole.dev), equals('Desenvolvedor'));
        });
        test('adm -> Administrador', () {
          expect(AppRole.label(AppRole.adm), equals('Administrador'));
        });
        test('auditor -> Auditor', () {
          expect(AppRole.label(AppRole.auditor), equals('Auditor'));
        });
        test('anonymous -> Anônimo', () {
          expect(AppRole.label(AppRole.anonymous), equals('Anônimo'));
        });
        test('unknown role returns role string itself', () {
          expect(AppRole.label('chaos_goblin'), equals('chaos_goblin'));
        });
      });

      group('AppRole.all', () {
        test('contains exactly the 5 defined roles in expected order', () {
          expect(
            AppRole.all,
            equals([
              AppRole.superuser,
              AppRole.dev,
              AppRole.adm,
              AppRole.auditor,
              AppRole.anonymous,
            ]),
          );
        });
      });
    }
    ```

    NÃO adicionar import de `flutter/material.dart`. NÃO criar um teste para `canEdit`. NÃO usar `setUp`/`tearDown`. NÃO usar `testWidgets`.
  </action>
  <verify>
    <automated>cd primeaudit && flutter test test/models/app_role_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `primeaudit/test/models/app_role_test.dart` existe
    - `cd primeaudit && flutter test test/models/app_role_test.dart` sai com código 0
    - Output mostra "All tests passed!" e pelo menos 23 testes executados (6+5+5+6+1)
    - grep `AppRole.canAccessAdmin` em `primeaudit/test/models/app_role_test.dart` retorna >= 6 matches
    - grep `AppRole.canAccessDev` em `primeaudit/test/models/app_role_test.dart` retorna >= 5 matches
    - grep `AppRole.isSuperOrDev` em `primeaudit/test/models/app_role_test.dart` retorna >= 5 matches
    - grep `AppRole.label` em `primeaudit/test/models/app_role_test.dart` retorna >= 6 matches
    - grep `canEdit` em `primeaudit/test/models/app_role_test.dart` retorna 0 matches
    - grep `Super Usuário|Desenvolvedor|Administrador|Auditor|Anônimo` em `primeaudit/test/models/app_role_test.dart` confirma os 5 rótulos portugueses
    - `cd primeaudit && flutter test` (suite completa) sai com código 0
  </acceptance_criteria>
  <done>
    Arquivo de teste existe com cobertura dos 4 helpers estáticos para todos os 5 roles conhecidos mais caso de role desconhecido. Todos os testes passam.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → production code | Testes importam `AppRole` direto; não há entrada de usuário; nenhuma rede; nenhuma PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | AppRole API surface | accept | Testes são assertivos (congelam comportamento); qualquer mudança acidental em `app_roles.dart` quebra o CI, reduzindo risco de tampering silencioso. Nenhuma escrita em dados reais. |
| T-03-02-02 | Elevation of Privilege | Cobertura insuficiente de roles | mitigate | Cada um dos 5 roles é testado explicitamente em cada helper; ausência de cobertura seria o principal vetor de regressão silenciosa de privilégio. |
| T-03-02-03 | Information Disclosure | Fixtures do teste | accept | Nenhum dado real, nenhum PII, nenhuma credencial usada. Apenas constantes de string já públicas em `app_roles.dart`. |
</threat_model>

<verification>
- `cd primeaudit && flutter test test/models/app_role_test.dart` passa com 23+ testes verdes
- `cd primeaudit && flutter test` (suite completa) permanece verde
- `cd primeaudit && flutter analyze` não introduz novos warnings
</verification>

<success_criteria>
- Plan 02 concluído quando:
  - [ ] `test/models/app_role_test.dart` existe com testes para os 4 helpers estáticos
  - [ ] Cada um dos 5 roles tem assert explícito em cada helper aplicável
  - [ ] Caso de role desconhecido coberto em `label` e `canAccessAdmin`
  - [ ] Nenhum teste referencia `canEdit` (método inexistente)
  - [ ] `flutter test` verde sem regressões
</success_criteria>

<output>
After completion, create `.planning/phases/03-test-coverage/03-02-SUMMARY.md`
</output>
</content>
</invoke>