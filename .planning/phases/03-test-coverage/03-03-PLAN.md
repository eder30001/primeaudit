---
phase: 03-test-coverage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - primeaudit/test/models/audit_test.dart
  - primeaudit/test/models/audit_answer_test.dart
  - primeaudit/test/models/audit_template_test.dart
autonomous: true
requirements:
  - QUAL-03
must_haves:
  truths:
    - "Audit.fromMap parseia corretamente campos escalares e joins aninhados (audit_types, companies, auditor, audit_templates, perimeters) cobrindo presença e ausência das chaves de join"
    - "Audit.fromMap mapeia todas as strings válidas de status para o enum AuditStatus correspondente, incluindo fallback para rascunho"
    - "AuditAnswer.fromMap parseia todos os campos obrigatórios, trata observation ausente como null e populado quando presente"
    - "TemplateItem.fromMap parseia campos obrigatórios e aplica defaults (response_type=ok_nok, required=true, weight=1, order_index=0, options=[]) quando ausentes"
    - "AuditTemplate.fromMap parseia campos escalares, aplica default active=true, popula typeName/typeIcon a partir do join audit_types e marca isGlobal quando company_id é null"
    - "Todos os 3 arquivos de teste passam em flutter test"
  artifacts:
    - path: "primeaudit/test/models/audit_test.dart"
      provides: "Unit tests for Audit.fromMap covering scalars, nested joins (audit_types, companies, auditor, audit_templates, perimeters), status enum mapping, and missing-join fallbacks"
      min_lines: 90
      contains: "Audit.fromMap"
    - path: "primeaudit/test/models/audit_answer_test.dart"
      provides: "Unit tests for AuditAnswer.fromMap covering required fields and optional observation"
      min_lines: 35
      contains: "AuditAnswer.fromMap"
    - path: "primeaudit/test/models/audit_template_test.dart"
      provides: "Unit tests for TemplateItem.fromMap + AuditTemplate.fromMap covering scalars, defaults, nested audit_types join, and isGlobal"
      min_lines: 90
      contains: "TemplateItem.fromMap"
  key_links:
    - from: "primeaudit/test/models/audit_test.dart"
      to: "primeaudit/lib/models/audit.dart"
      via: "import direto + chamada a Audit.fromMap com mapas literais"
      pattern: "Audit\\.fromMap"
    - from: "primeaudit/test/models/audit_answer_test.dart"
      to: "primeaudit/lib/models/audit_answer.dart"
      via: "import direto + chamada a AuditAnswer.fromMap"
      pattern: "AuditAnswer\\.fromMap"
    - from: "primeaudit/test/models/audit_template_test.dart"
      to: "primeaudit/lib/models/audit_template.dart"
      via: "import direto + chamadas a TemplateItem.fromMap e AuditTemplate.fromMap"
      pattern: "(TemplateItem|AuditTemplate)\\.fromMap"
---

<objective>
Cobrir `fromMap()` para os 3 models com maior superfície de parsing: `Audit`, `AuditAnswer`, `AuditTemplate` (inclui `TemplateItem`). Cada teste usa um mapa literal que replica a estrutura real devolvida pelo Supabase (incluindo joins aninhados onde aplicável). Atende parte de QUAL-03.

Purpose: `Audit.fromMap` é o ponto de maior risco em parsing — lê 5 joins aninhados diferentes (`audit_types`, `audit_templates`, `companies`, `perimeters`, `auditor`) com fallbacks distintos por campo. `AuditTemplate.fromMap` lê o join `audit_types` e tem a propriedade derivada `isGlobal`. `TemplateItem.fromMap` tem 4 defaults explícitos que precisam ser travados por teste. `AuditAnswer.fromMap` é o mais simples mas crítico para Data Integrity (Phase 1). Cobri-los juntos consolida QUAL-03 nos 3 arquivos de maior volume de parsing.

Output: 3 novos arquivos em `primeaudit/test/models/`, cada um puro Dart, sem mocks, sem Supabase.

Nota sobre QUAL-03: QUAL-03 cobre "todos os models" — este plano cobre Audit, AuditAnswer, AuditTemplate+TemplateItem; o Plan 04 cobre Company, AppUser, Perimeter. A divisão em 2 plans paraleliza a execução e mantém cada plan dentro do budget de contexto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-test-coverage/03-RESEARCH.md
@.planning/phases/03-test-coverage/03-PATTERNS.md
@.planning/phases/03-test-coverage/03-VALIDATION.md
@primeaudit/lib/models/audit.dart
@primeaudit/lib/models/audit_answer.dart
@primeaudit/lib/models/audit_template.dart
@primeaudit/test/pending_save_test.dart

<interfaces>
<!-- Extraído dos arquivos de modelo — executor não deve explorar -->

From primeaudit/lib/models/audit.dart:
```dart
enum AuditStatus { rascunho, emAndamento, concluida, atrasada, cancelada }

// Audit.fromMap lê:
//   id, title, audit_type_id, template_id, company_id, auditor_id      (strings)
//   perimeter_id, deadline, conformity_percent                          (opcionais)
//   created_at                                                          (DateTime.parse)
//   audit_types.name  (default '')   audit_types.icon (default '📋')    audit_types.color (default '#2196F3')
//   audit_templates.name (default '')
//   companies.name (default '')      companies.requires_perimeter (default false)
//   perimeters.name (default null)
//   auditor.full_name (default '')
//   status: 'em_andamento'|'concluida'|'atrasada'|'cancelada'|<outro>|null → rascunho
```

From primeaudit/lib/models/audit_answer.dart:
```dart
// AuditAnswer.fromMap lê:
//   id, audit_id, template_item_id, response   (strings)
//   observation                                 (String? pode ser null)
//   answered_at                                 (DateTime.parse)
// Não tem joins aninhados.
```

From primeaudit/lib/models/audit_template.dart:
```dart
// TemplateItem.fromMap defaults:
//   response_type ?? 'ok_nok'
//   required ?? true
//   weight ?? 1
//   order_index ?? 0
//   options cast para List<String> ou []
// section_id e guidance são opcionais (String?)

// AuditTemplate.fromMap:
//   id, type_id, name   (strings obrigatórias)
//   company_id (String? — null significa global)
//   description (String?)
//   active ?? true
//   typeName  = map['audit_types']?['name']   (null se join ausente)
//   typeIcon  = map['audit_types']?['icon']
//   isGlobal (getter) = companyId == null
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar test/models/audit_test.dart cobrindo Audit.fromMap com joins aninhados e mapeamento de status</name>
  <files>primeaudit/test/models/audit_test.dart</files>
  <read_first>
    - primeaudit/lib/models/audit.dart (confirmar chaves de join exatas e defaults de fallback)
    - primeaudit/test/pending_save_test.dart (padrão de referência de estrutura)
  </read_first>
  <behavior>
    - Parse de campos escalares (id, title, auditTypeId, templateId, companyId, auditorId)
    - Parse do join audit_types (name, icon, color)
    - Parse do join audit_templates (name em templateName)
    - Parse do join companies (name em companyName, requires_perimeter em companyRequiresPerimeter)
    - Parse do join auditor (full_name em auditorName)
    - Parse do join perimeters quando presente e null quando ausente
    - deadline opcional parseado como DateTime ou null
    - conformityPercent opcional parseado como double ou null
    - Fallbacks corretos quando joins estão ausentes: auditTypeName='', auditTypeIcon='📋', auditTypeColor='#2196F3', templateName='', companyName='', companyRequiresPerimeter=false, auditorName=''
    - Mapeamento de status para os 5 valores + fallback para rascunho quando desconhecido/null
  </behavior>
  <action>
    Criar o arquivo `primeaudit/test/models/audit_test.dart` com EXATAMENTE este conteúdo:

    ```dart
    // Unit tests for Audit.fromMap (QUAL-03).
    // Audit is the highest-surface parsing target — 5 nested join keys
    // plus status enum mapping and numeric/date optional fields.

    import 'package:flutter_test/flutter_test.dart';
    import 'package:primeaudit/models/audit.dart';

    Map<String, dynamic> _fullAuditMap() => <String, dynamic>{
      'id': 'a1',
      'title': 'Auditoria Teste',
      'audit_type_id': 'at1',
      'template_id': 't1',
      'company_id': 'c1',
      'auditor_id': 'u1',
      'perimeter_id': 'per1',
      'status': 'em_andamento',
      'created_at': '2024-01-01T00:00:00.000Z',
      'deadline': '2024-06-01T00:00:00.000Z',
      'conformity_percent': 87.5,
      'audit_types': {'name': 'Safety', 'icon': '📋', 'color': '#2196F3'},
      'audit_templates': {'name': 'Template A'},
      'companies': {'name': 'Acme', 'requires_perimeter': true},
      'perimeters': {'name': 'Area A'},
      'auditor': {'full_name': 'Ana'},
    };

    void main() {
      group('Audit.fromMap — scalar fields', () {
        test('parses required ids and title', () {
          final a = Audit.fromMap(_fullAuditMap());
          expect(a.id, equals('a1'));
          expect(a.title, equals('Auditoria Teste'));
          expect(a.auditTypeId, equals('at1'));
          expect(a.templateId, equals('t1'));
          expect(a.companyId, equals('c1'));
          expect(a.auditorId, equals('u1'));
        });

        test('parses createdAt as DateTime', () {
          final a = Audit.fromMap(_fullAuditMap());
          expect(a.createdAt, equals(DateTime.parse('2024-01-01T00:00:00.000Z')));
        });

        test('parses deadline as DateTime when present', () {
          final a = Audit.fromMap(_fullAuditMap());
          expect(a.deadline, equals(DateTime.parse('2024-06-01T00:00:00.000Z')));
        });

        test('deadline is null when absent', () {
          final m = _fullAuditMap()..['deadline'] = null;
          expect(Audit.fromMap(m).deadline, isNull);
        });

        test('conformityPercent is double when present', () {
          final a = Audit.fromMap(_fullAuditMap());
          expect(a.conformityPercent, closeTo(87.5, 0.01));
        });

        test('conformityPercent is null when absent', () {
          final m = _fullAuditMap()..['conformity_percent'] = null;
          expect(Audit.fromMap(m).conformityPercent, isNull);
        });
      });

      group('Audit.fromMap — nested audit_types join', () {
        test('parses name, icon, color', () {
          final a = Audit.fromMap(_fullAuditMap());
          expect(a.auditTypeName, equals('Safety'));
          expect(a.auditTypeIcon, equals('📋'));
          expect(a.auditTypeColor, equals('#2196F3'));
        });

        test('falls back to defaults when audit_types is null', () {
          final m = _fullAuditMap()..['audit_types'] = null;
          final a = Audit.fromMap(m);
          expect(a.auditTypeName, equals(''));
          expect(a.auditTypeIcon, equals('📋'));
          expect(a.auditTypeColor, equals('#2196F3'));
        });
      });

      group('Audit.fromMap — nested audit_templates join', () {
        test('parses templateName', () {
          expect(Audit.fromMap(_fullAuditMap()).templateName, equals('Template A'));
        });

        test('templateName falls back to empty string when join absent', () {
          final m = _fullAuditMap()..['audit_templates'] = null;
          expect(Audit.fromMap(m).templateName, equals(''));
        });
      });

      group('Audit.fromMap — nested companies join', () {
        test('parses companyName and companyRequiresPerimeter', () {
          final a = Audit.fromMap(_fullAuditMap());
          expect(a.companyName, equals('Acme'));
          expect(a.companyRequiresPerimeter, isTrue);
        });

        test('falls back to defaults when companies is null', () {
          final m = _fullAuditMap()..['companies'] = null;
          final a = Audit.fromMap(m);
          expect(a.companyName, equals(''));
          expect(a.companyRequiresPerimeter, isFalse);
        });
      });

      group('Audit.fromMap — nested auditor join', () {
        test('parses auditorName', () {
          expect(Audit.fromMap(_fullAuditMap()).auditorName, equals('Ana'));
        });

        test('auditorName falls back to empty string when auditor is null', () {
          final m = _fullAuditMap()..['auditor'] = null;
          expect(Audit.fromMap(m).auditorName, equals(''));
        });
      });

      group('Audit.fromMap — nested perimeters join', () {
        test('parses perimeterName when present', () {
          expect(Audit.fromMap(_fullAuditMap()).perimeterName, equals('Area A'));
        });

        test('perimeterName is null when perimeters is null', () {
          final m = _fullAuditMap()..['perimeters'] = null;
          expect(Audit.fromMap(m).perimeterName, isNull);
        });
      });

      group('Audit.fromMap — status enum mapping', () {
        test('em_andamento string -> AuditStatus.emAndamento', () {
          final m = _fullAuditMap()..['status'] = 'em_andamento';
          expect(Audit.fromMap(m).status, equals(AuditStatus.emAndamento));
        });

        test('concluida string -> AuditStatus.concluida', () {
          final m = _fullAuditMap()..['status'] = 'concluida';
          expect(Audit.fromMap(m).status, equals(AuditStatus.concluida));
        });

        test('atrasada string -> AuditStatus.atrasada', () {
          final m = _fullAuditMap()..['status'] = 'atrasada';
          expect(Audit.fromMap(m).status, equals(AuditStatus.atrasada));
        });

        test('cancelada string -> AuditStatus.cancelada', () {
          final m = _fullAuditMap()..['status'] = 'cancelada';
          expect(Audit.fromMap(m).status, equals(AuditStatus.cancelada));
        });

        test('unknown status string -> AuditStatus.rascunho (fallback)', () {
          final m = _fullAuditMap()..['status'] = 'imaginary_status';
          expect(Audit.fromMap(m).status, equals(AuditStatus.rascunho));
        });

        test('null status -> AuditStatus.rascunho (fallback)', () {
          final m = _fullAuditMap()..['status'] = null;
          expect(Audit.fromMap(m).status, equals(AuditStatus.rascunho));
        });
      });
    }
    ```
  </action>
  <verify>
    <automated>cd primeaudit && flutter test test/models/audit_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `primeaudit/test/models/audit_test.dart` existe
    - `cd primeaudit && flutter test test/models/audit_test.dart` sai com código 0
    - Output mostra "All tests passed!" e pelo menos 20 testes executados
    - grep `Audit.fromMap` em `primeaudit/test/models/audit_test.dart` retorna >= 20 matches
    - grep `AuditStatus.emAndamento|AuditStatus.concluida|AuditStatus.atrasada|AuditStatus.cancelada|AuditStatus.rascunho` em `primeaudit/test/models/audit_test.dart` cobre os 5 valores do enum
    - grep `audit_types|audit_templates|companies|perimeters|auditor` em `primeaudit/test/models/audit_test.dart` cobre os 5 joins
  </acceptance_criteria>
  <done>
    Arquivo de teste existe e passa, cobrindo escalares, 5 joins aninhados (com casos de ausência), campos opcionais e os 5 valores de status + fallback.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar test/models/audit_answer_test.dart cobrindo AuditAnswer.fromMap (campos obrigatórios + observation opcional)</name>
  <files>primeaudit/test/models/audit_answer_test.dart</files>
  <read_first>
    - primeaudit/lib/models/audit_answer.dart (confirmar chaves e tipos)
    - primeaudit/test/pending_save_test.dart (padrão de referência)
  </read_first>
  <behavior>
    - id, auditId, templateItemId, response são todos parseados corretamente
    - answeredAt é parseado como DateTime (ISO 8601)
    - observation é null quando ausente no mapa
    - observation é populado corretamente quando presente
  </behavior>
  <action>
    Criar o arquivo `primeaudit/test/models/audit_answer_test.dart` com EXATAMENTE este conteúdo:

    ```dart
    // Unit tests for AuditAnswer.fromMap (QUAL-03).
    // AuditAnswer is the flattest of the models — no nested joins.

    import 'package:flutter_test/flutter_test.dart';
    import 'package:primeaudit/models/audit_answer.dart';

    Map<String, dynamic> _baseMap() => <String, dynamic>{
      'id': 'aa1',
      'audit_id': 'a1',
      'template_item_id': 'ti1',
      'response': 'ok',
      'observation': null,
      'answered_at': '2024-01-01T10:00:00.000Z',
    };

    void main() {
      group('AuditAnswer.fromMap', () {
        test('parses required scalar fields', () {
          final aa = AuditAnswer.fromMap(_baseMap());
          expect(aa.id, equals('aa1'));
          expect(aa.auditId, equals('a1'));
          expect(aa.templateItemId, equals('ti1'));
          expect(aa.response, equals('ok'));
        });

        test('parses answeredAt as DateTime', () {
          final aa = AuditAnswer.fromMap(_baseMap());
          expect(
            aa.answeredAt,
            equals(DateTime.parse('2024-01-01T10:00:00.000Z')),
          );
        });

        test('observation is null when map value is null', () {
          final aa = AuditAnswer.fromMap(_baseMap());
          expect(aa.observation, isNull);
        });

        test('observation is populated when present', () {
          final m = _baseMap()..['observation'] = 'Observação do auditor';
          expect(AuditAnswer.fromMap(m).observation, equals('Observação do auditor'));
        });

        test('response may be any string (numeric payload for scale_1_5)', () {
          final m = _baseMap()..['response'] = '5';
          expect(AuditAnswer.fromMap(m).response, equals('5'));
        });
      });
    }
    ```
  </action>
  <verify>
    <automated>cd primeaudit && flutter test test/models/audit_answer_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `primeaudit/test/models/audit_answer_test.dart` existe
    - `cd primeaudit && flutter test test/models/audit_answer_test.dart` sai com código 0
    - Output mostra "All tests passed!" com pelo menos 5 testes
    - grep `AuditAnswer.fromMap` em `primeaudit/test/models/audit_answer_test.dart` retorna >= 5 matches
    - grep `observation` em `primeaudit/test/models/audit_answer_test.dart` retorna >= 2 matches (caso null + caso populado)
  </acceptance_criteria>
  <done>
    Arquivo de teste existe e passa, cobrindo campos obrigatórios e observation opcional em ambos os estados.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Criar test/models/audit_template_test.dart cobrindo TemplateItem.fromMap e AuditTemplate.fromMap</name>
  <files>primeaudit/test/models/audit_template_test.dart</files>
  <read_first>
    - primeaudit/lib/models/audit_template.dart (confirmar defaults exatos e chaves de join)
    - primeaudit/test/pending_save_test.dart (padrão de referência)
  </read_first>
  <behavior>
    - TemplateItem.fromMap: parseia campos obrigatórios e aplica defaults (response_type=ok_nok, required=true, weight=1, order_index=0, options=[])
    - TemplateItem.fromMap: section_id e guidance permanecem null quando ausentes
    - TemplateItem.fromMap: options são convertidas corretamente de List<dynamic> para List<String>
    - AuditTemplate.fromMap: parseia campos obrigatórios
    - AuditTemplate.fromMap: active default é true
    - AuditTemplate.fromMap: typeName/typeIcon são populados a partir de audit_types
    - AuditTemplate.fromMap: typeName/typeIcon são null quando join ausente
    - AuditTemplate.isGlobal é true quando company_id é null, false quando definido
  </behavior>
  <action>
    Criar o arquivo `primeaudit/test/models/audit_template_test.dart` com EXATAMENTE este conteúdo:

    ```dart
    // Unit tests for TemplateItem.fromMap and AuditTemplate.fromMap (QUAL-03).

    import 'package:flutter_test/flutter_test.dart';
    import 'package:primeaudit/models/audit_template.dart';

    Map<String, dynamic> _itemMap() => <String, dynamic>{
      'id': 'ti1',
      'template_id': 't1',
      'section_id': null,
      'question': 'Item conforme?',
      'guidance': null,
      'response_type': 'ok_nok',
      'required': true,
      'weight': 3,
      'order_index': 0,
      'options': <dynamic>[],
    };

    Map<String, dynamic> _templateMap() => <String, dynamic>{
      'id': 't1',
      'type_id': 'at1',
      'company_id': null,
      'name': 'Template A',
      'description': null,
      'active': true,
      'audit_types': {'name': 'Safety', 'icon': '📋'},
    };

    void main() {
      group('TemplateItem.fromMap — required fields', () {
        test('parses id, templateId, question, responseType, weight', () {
          final item = TemplateItem.fromMap(_itemMap());
          expect(item.id, equals('ti1'));
          expect(item.templateId, equals('t1'));
          expect(item.question, equals('Item conforme?'));
          expect(item.responseType, equals('ok_nok'));
          expect(item.weight, equals(3));
          expect(item.orderIndex, equals(0));
          expect(item.required, isTrue);
        });

        test('sectionId and guidance are null when absent', () {
          final item = TemplateItem.fromMap(_itemMap());
          expect(item.sectionId, isNull);
          expect(item.guidance, isNull);
        });
      });

      group('TemplateItem.fromMap — defaults', () {
        test('response_type defaults to ok_nok when key absent', () {
          final m = _itemMap()..remove('response_type');
          expect(TemplateItem.fromMap(m).responseType, equals('ok_nok'));
        });

        test('required defaults to true when key absent', () {
          final m = _itemMap()..remove('required');
          expect(TemplateItem.fromMap(m).required, isTrue);
        });

        test('weight defaults to 1 when key absent', () {
          final m = _itemMap()..remove('weight');
          expect(TemplateItem.fromMap(m).weight, equals(1));
        });

        test('order_index defaults to 0 when key absent', () {
          final m = _itemMap()..remove('order_index');
          expect(TemplateItem.fromMap(m).orderIndex, equals(0));
        });

        test('options defaults to [] when key absent', () {
          final m = _itemMap()..remove('options');
          expect(TemplateItem.fromMap(m).options, isEmpty);
        });
      });

      group('TemplateItem.fromMap — options list', () {
        test('casts List<dynamic> of strings to List<String>', () {
          final m = _itemMap()..['options'] = <dynamic>['a', 'b', 'c'];
          final item = TemplateItem.fromMap(m);
          expect(item.options, equals(<String>['a', 'b', 'c']));
        });
      });

      group('AuditTemplate.fromMap — required fields', () {
        test('parses id, typeId, name, active', () {
          final t = AuditTemplate.fromMap(_templateMap());
          expect(t.id, equals('t1'));
          expect(t.typeId, equals('at1'));
          expect(t.name, equals('Template A'));
          expect(t.active, isTrue);
        });

        test('description is null when absent', () {
          expect(AuditTemplate.fromMap(_templateMap()).description, isNull);
        });

        test('active defaults to true when key absent', () {
          final m = _templateMap()..remove('active');
          expect(AuditTemplate.fromMap(m).active, isTrue);
        });
      });

      group('AuditTemplate.fromMap — audit_types join', () {
        test('populates typeName and typeIcon from nested audit_types map', () {
          final t = AuditTemplate.fromMap(_templateMap());
          expect(t.typeName, equals('Safety'));
          expect(t.typeIcon, equals('📋'));
        });

        test('typeName and typeIcon are null when audit_types is null', () {
          final m = _templateMap()..['audit_types'] = null;
          final t = AuditTemplate.fromMap(m);
          expect(t.typeName, isNull);
          expect(t.typeIcon, isNull);
        });
      });

      group('AuditTemplate.isGlobal', () {
        test('isGlobal is true when company_id is null', () {
          expect(AuditTemplate.fromMap(_templateMap()).isGlobal, isTrue);
        });

        test('isGlobal is false when company_id is set', () {
          final m = _templateMap()..['company_id'] = 'c1';
          expect(AuditTemplate.fromMap(m).isGlobal, isFalse);
        });
      });
    }
    ```
  </action>
  <verify>
    <automated>cd primeaudit && flutter test test/models/audit_template_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `primeaudit/test/models/audit_template_test.dart` existe
    - `cd primeaudit && flutter test test/models/audit_template_test.dart` sai com código 0
    - Output mostra "All tests passed!" com pelo menos 15 testes
    - grep `TemplateItem.fromMap` em `primeaudit/test/models/audit_template_test.dart` retorna >= 7 matches
    - grep `AuditTemplate.fromMap` em `primeaudit/test/models/audit_template_test.dart` retorna >= 6 matches
    - grep `isGlobal` em `primeaudit/test/models/audit_template_test.dart` retorna >= 2 matches (true + false)
    - `cd primeaudit && flutter test` (suite completa) sai com código 0
  </acceptance_criteria>
  <done>
    Arquivo de teste existe e passa, cobrindo todos os defaults de TemplateItem, parse de AuditTemplate com join audit_types e ambos os estados de isGlobal.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → production code | Testes importam os models direto; mapas literais substituem o Supabase — nenhum tráfego de rede |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | Contrato de fromMap | mitigate | Testes travam nomes de chaves (`audit_types`, `companies`, etc.). Uma alteração acidental no SQL que mude nomes de colunas quebrará o CI. |
| T-03-03-02 | Information Disclosure | Fixture data | accept | Todos os ids e nomes são fictícios (`'a1'`, `'Acme'`, `'Ana'`); sem PII real. |
| T-03-03-03 | Denial of Service | Suite de testes | accept | ~40 testes unitários puros adicionados; runtime esperado < 1s. |
| T-03-03-04 | Repudiation | AuditAnswer.fromMap | mitigate | Testes garantem que `answered_at` é parseado; perda acidental desse campo (audit trail) seria detectada imediatamente. |
</threat_model>

<verification>
- `cd primeaudit && flutter test test/models/audit_test.dart` passa com 20+ testes verdes
- `cd primeaudit && flutter test test/models/audit_answer_test.dart` passa com 5+ testes verdes
- `cd primeaudit && flutter test test/models/audit_template_test.dart` passa com 15+ testes verdes
- `cd primeaudit && flutter test` (suite completa) permanece verde
- `cd primeaudit && flutter analyze` não introduz novos warnings
</verification>

<success_criteria>
- Plan 03 concluído quando:
  - [ ] `test/models/audit_test.dart` cobre escalares, 5 joins aninhados, fallbacks e mapeamento de status
  - [ ] `test/models/audit_answer_test.dart` cobre campos obrigatórios e observation opcional
  - [ ] `test/models/audit_template_test.dart` cobre TemplateItem.fromMap (com defaults) e AuditTemplate.fromMap (com audit_types join + isGlobal)
  - [ ] `flutter test` verde sem regressões
</success_criteria>

<output>
After completion, create `.planning/phases/03-test-coverage/03-03-SUMMARY.md`
</output>
</content>
</invoke>