---
phase: 03-test-coverage
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - primeaudit/test/models/company_test.dart
  - primeaudit/test/models/app_user_test.dart
  - primeaudit/test/models/perimeter_test.dart
autonomous: true
requirements:
  - QUAL-03
  - QUAL-04
must_haves:
  truths:
    - "Company.fromMap parseia campos obrigatórios e trata todos os campos opcionais (cnpj, email, phone, address) como null quando ausentes"
    - "Company.fromMap aplica default active=true e requires_perimeter=false quando ausentes"
    - "AppUser.fromMap parseia campos escalares e popula companyName a partir do join companies(name), com null quando join ausente"
    - "AppUser.fromMap aplica default active=true quando ausente"
    - "Perimeter.fromMap parseia campos obrigatórios e aplica default active=true quando ausente"
    - "Perimeter.buildTree retorna lista vazia para entrada vazia"
    - "Perimeter.buildTree cria 1 raiz sem filhos para hierarquia de 1 nível"
    - "Perimeter.buildTree aninha corretamente filhos em hierarquia de 2 níveis"
    - "Perimeter.buildTree aninha corretamente neto em hierarquia de 3 níveis"
    - "Perimeter.buildTree lida corretamente com múltiplas raízes"
    - "Testes de Perimeter.buildTree constroem instâncias frescas por caso (evita contaminação cruzada via mutação de children)"
  artifacts:
    - path: "primeaudit/test/models/company_test.dart"
      provides: "Unit tests for Company.fromMap covering scalars, defaults, and optional fields"
      min_lines: 50
      contains: "Company.fromMap"
    - path: "primeaudit/test/models/app_user_test.dart"
      provides: "Unit tests for AppUser.fromMap covering scalars, companies join, and default active"
      min_lines: 50
      contains: "AppUser.fromMap"
    - path: "primeaudit/test/models/perimeter_test.dart"
      provides: "Unit tests for Perimeter.fromMap AND Perimeter.buildTree covering 0, 1, 2, 3-level hierarchies plus multiple roots"
      min_lines: 90
      contains: "Perimeter.buildTree"
  key_links:
    - from: "primeaudit/test/models/perimeter_test.dart"
      to: "primeaudit/lib/models/perimeter.dart"
      via: "import direto + construção de instâncias frescas de Perimeter + chamadas a buildTree"
      pattern: "Perimeter\\.buildTree"
    - from: "primeaudit/test/models/app_user_test.dart"
      to: "primeaudit/lib/models/app_user.dart"
      via: "import direto + chamada a AppUser.fromMap com join companies aninhado"
      pattern: "AppUser\\.fromMap"
    - from: "primeaudit/test/models/company_test.dart"
      to: "primeaudit/lib/models/company.dart"
      via: "import direto + chamada a Company.fromMap"
      pattern: "Company\\.fromMap"
---

<objective>
Fechar a cobertura de QUAL-03 para os 3 models restantes (`Company`, `AppUser`, `Perimeter`) e implementar QUAL-04 cobrindo `Perimeter.buildTree` com hierarquias de 0, 1, 2 e 3 níveis.

Purpose: `AppUser` é o equivalente de `UserProfile` mencionado na ROADMAP (que não existe sob esse nome — vide 03-RESEARCH.md Pitfall 5). `Perimeter` é o único model com uma função estática não-trivial (`buildTree`) e um campo mutável (`children`) — cobrir ambos no mesmo arquivo evita competição de ownership entre planos paralelos e permite que os testes de buildTree reusem o helper de construção. `Company` é um model simples mas central (sem perímetros obrigatórios, `requires_perimeter` controla regras de UX em toda a app).

Output: 3 novos arquivos em `primeaudit/test/models/`, puros Dart, sem mocks, sem Supabase.

Nota sobre fixtures frescas: 03-RESEARCH.md Pitfall 3 confirma que `Perimeter.children` é um `List<Perimeter>` mutável e `buildTree` faz `p.children = []` para todos os nós em seguida `.add(p)` para filhos. Se duas chamadas de teste compartilharem a mesma instância de `Perimeter`, o segundo teste vê children da primeira chamada. O plano mitiga isso construindo instâncias frescas via função helper `_p(...)` dentro de cada `test()`.

Nota sobre UserProfile: A ROADMAP lista `UserProfile` entre os models a cobrir. Esse nome não existe no código — o model correto é `AppUser` (que mapeia a tabela `profiles`). Testar `AppUser.fromMap` satisfaz o critério (vide 03-RESEARCH.md Pitfall 5).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-test-coverage/03-RESEARCH.md
@.planning/phases/03-test-coverage/03-PATTERNS.md
@.planning/phases/03-test-coverage/03-VALIDATION.md
@primeaudit/lib/models/company.dart
@primeaudit/lib/models/app_user.dart
@primeaudit/lib/models/perimeter.dart
@primeaudit/test/pending_save_test.dart

<interfaces>
<!-- Extraído dos arquivos de modelo — executor não deve explorar -->

From primeaudit/lib/models/company.dart:
```dart
// Company.fromMap:
//   id, name, created_at          (obrigatórios)
//   cnpj, email, phone, address   (String? — null quando ausente)
//   active ?? true
//   requires_perimeter ?? false
//   createdAt = DateTime.parse(map['created_at'])
```

From primeaudit/lib/models/app_user.dart:
```dart
// AppUser.fromMap:
//   id, full_name, email, role, created_at   (obrigatórios)
//   company_id   (String? — null para superuser/dev sem empresa)
//   companies.name -> companyName  (null se join ausente)
//   active ?? true
```

From primeaudit/lib/models/perimeter.dart:
```dart
// Perimeter.fromMap:
//   id, company_id, name, created_at  (obrigatórios)
//   parent_id, description             (String? — null quando ausente)
//   active ?? true

// Perimeter.buildTree(List<Perimeter> flat):
//   - Itera flat 2 vezes: primeiro p.children = []; depois liga por parent_id
//   - Retorna List<Perimeter> onde apenas nós com parent_id == null estão no topo
//   - MUTA a lista de entrada: children de cada Perimeter é reescrito
//   - Usa lookup por id via map; se parent_id referencia id ausente, o filho é ignorado silenciosamente

// Construtor Perimeter:
//   Perimeter({required id, required companyId, parentId, required name,
//              description, required active, required createdAt,
//              children = const []})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar test/models/company_test.dart cobrindo Company.fromMap com defaults e campos opcionais</name>
  <files>primeaudit/test/models/company_test.dart</files>
  <read_first>
    - primeaudit/lib/models/company.dart (confirmar chaves, defaults e tipos)
    - primeaudit/test/pending_save_test.dart (padrão de referência)
  </read_first>
  <behavior>
    - id, name, createdAt parseados corretamente
    - Todos os campos opcionais (cnpj, email, phone, address) são null quando ausentes
    - active default é true quando chave ausente
    - requires_perimeter default é false quando chave ausente
    - requires_perimeter populado como true quando presente
    - Campos opcionais populados quando presentes
  </behavior>
  <action>
    Criar o arquivo `primeaudit/test/models/company_test.dart` com EXATAMENTE este conteúdo:

    ```dart
    // Unit tests for Company.fromMap (QUAL-03).

    import 'package:flutter_test/flutter_test.dart';
    import 'package:primeaudit/models/company.dart';

    Map<String, dynamic> _baseMap() => <String, dynamic>{
      'id': 'c1',
      'name': 'Acme',
      'cnpj': null,
      'email': null,
      'phone': null,
      'address': null,
      'active': true,
      'requires_perimeter': false,
      'created_at': '2024-01-01T00:00:00.000Z',
    };

    void main() {
      group('Company.fromMap — required fields', () {
        test('parses id, name, createdAt', () {
          final c = Company.fromMap(_baseMap());
          expect(c.id, equals('c1'));
          expect(c.name, equals('Acme'));
          expect(c.createdAt, equals(DateTime.parse('2024-01-01T00:00:00.000Z')));
        });
      });

      group('Company.fromMap — optional fields null when absent', () {
        test('cnpj, email, phone, address are null', () {
          final c = Company.fromMap(_baseMap());
          expect(c.cnpj, isNull);
          expect(c.email, isNull);
          expect(c.phone, isNull);
          expect(c.address, isNull);
        });
      });

      group('Company.fromMap — optional fields populated when present', () {
        test('cnpj is populated', () {
          final m = _baseMap()..['cnpj'] = '11.222.333/0001-81';
          expect(Company.fromMap(m).cnpj, equals('11.222.333/0001-81'));
        });

        test('email is populated', () {
          final m = _baseMap()..['email'] = 'contato@acme.com';
          expect(Company.fromMap(m).email, equals('contato@acme.com'));
        });

        test('phone is populated', () {
          final m = _baseMap()..['phone'] = '+55 11 99999-0000';
          expect(Company.fromMap(m).phone, equals('+55 11 99999-0000'));
        });

        test('address is populated', () {
          final m = _baseMap()..['address'] = 'Rua X, 123';
          expect(Company.fromMap(m).address, equals('Rua X, 123'));
        });
      });

      group('Company.fromMap — defaults', () {
        test('active defaults to true when key absent', () {
          final m = _baseMap()..remove('active');
          expect(Company.fromMap(m).active, isTrue);
        });

        test('requires_perimeter defaults to false when key absent', () {
          final m = _baseMap()..remove('requires_perimeter');
          expect(Company.fromMap(m).requiresPerimeter, isFalse);
        });

        test('requiresPerimeter is true when explicitly set', () {
          final m = _baseMap()..['requires_perimeter'] = true;
          expect(Company.fromMap(m).requiresPerimeter, isTrue);
        });

        test('active is false when explicitly set', () {
          final m = _baseMap()..['active'] = false;
          expect(Company.fromMap(m).active, isFalse);
        });
      });
    }
    ```
  </action>
  <verify>
    <automated>cd primeaudit && flutter test test/models/company_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `primeaudit/test/models/company_test.dart` existe
    - `cd primeaudit && flutter test test/models/company_test.dart` sai com código 0
    - Output mostra "All tests passed!" com pelo menos 10 testes
    - grep `Company.fromMap` em `primeaudit/test/models/company_test.dart` retorna >= 10 matches
    - grep `requiresPerimeter|requires_perimeter` em `primeaudit/test/models/company_test.dart` cobre defaults + estado explícito
  </acceptance_criteria>
  <done>
    Arquivo de teste existe e passa, cobrindo campos obrigatórios, 4 opcionais em ambos os estados e os 2 defaults (active, requires_perimeter).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar test/models/app_user_test.dart cobrindo AppUser.fromMap incluindo join companies</name>
  <files>primeaudit/test/models/app_user_test.dart</files>
  <read_first>
    - primeaudit/lib/models/app_user.dart (confirmar chaves e estrutura do join companies)
    - primeaudit/test/pending_save_test.dart (padrão de referência)
  </read_first>
  <behavior>
    - Campos escalares (id, fullName, email, role) parseados
    - companyId populado quando presente, null quando ausente
    - companyName populado a partir do join companies(name)
    - companyName é null quando join companies é null
    - active default é true quando ausente
    - createdAt parseado como DateTime
  </behavior>
  <action>
    Criar o arquivo `primeaudit/test/models/app_user_test.dart` com EXATAMENTE este conteúdo:

    ```dart
    // Unit tests for AppUser.fromMap (QUAL-03).
    // AppUser is the model equivalent of "UserProfile" referenced in the
    // roadmap; see 03-RESEARCH.md Pitfall 5 — the actual class name is AppUser
    // and it maps the `profiles` table.

    import 'package:flutter_test/flutter_test.dart';
    import 'package:primeaudit/models/app_user.dart';

    Map<String, dynamic> _baseMap() => <String, dynamic>{
      'id': 'u1',
      'full_name': 'Ana Silva',
      'email': 'ana@example.com',
      'role': 'adm',
      'company_id': 'c1',
      'active': true,
      'created_at': '2024-01-01T00:00:00.000Z',
      'companies': {'name': 'Acme'},
    };

    void main() {
      group('AppUser.fromMap — required scalar fields', () {
        test('parses id, fullName, email, role', () {
          final u = AppUser.fromMap(_baseMap());
          expect(u.id, equals('u1'));
          expect(u.fullName, equals('Ana Silva'));
          expect(u.email, equals('ana@example.com'));
          expect(u.role, equals('adm'));
        });

        test('parses createdAt as DateTime', () {
          final u = AppUser.fromMap(_baseMap());
          expect(u.createdAt, equals(DateTime.parse('2024-01-01T00:00:00.000Z')));
        });
      });

      group('AppUser.fromMap — companyId', () {
        test('companyId is populated when present', () {
          expect(AppUser.fromMap(_baseMap()).companyId, equals('c1'));
        });

        test('companyId is null when explicitly null', () {
          final m = _baseMap()..['company_id'] = null;
          expect(AppUser.fromMap(m).companyId, isNull);
        });
      });

      group('AppUser.fromMap — companies join', () {
        test('companyName is populated from companies.name join', () {
          expect(AppUser.fromMap(_baseMap()).companyName, equals('Acme'));
        });

        test('companyName is null when companies join is null', () {
          final m = _baseMap()..['companies'] = null;
          expect(AppUser.fromMap(m).companyName, isNull);
        });
      });

      group('AppUser.fromMap — active default', () {
        test('active defaults to true when key absent', () {
          final m = _baseMap()..remove('active');
          expect(AppUser.fromMap(m).active, isTrue);
        });

        test('active is false when explicitly set (deactivated user)', () {
          final m = _baseMap()..['active'] = false;
          expect(AppUser.fromMap(m).active, isFalse);
        });
      });

      group('AppUser.fromMap — derived getters', () {
        test('canAccessAdmin reflects role (adm -> true)', () {
          expect(AppUser.fromMap(_baseMap()).canAccessAdmin, isTrue);
        });

        test('canAccessAdmin is false for auditor role', () {
          final m = _baseMap()..['role'] = 'auditor';
          expect(AppUser.fromMap(m).canAccessAdmin, isFalse);
        });

        test('isSuperOrDev is true for dev role', () {
          final m = _baseMap()..['role'] = 'dev';
          expect(AppUser.fromMap(m).isSuperOrDev, isTrue);
        });

        test('isSuperOrDev is false for adm role', () {
          expect(AppUser.fromMap(_baseMap()).isSuperOrDev, isFalse);
        });
      });
    }
    ```
  </action>
  <verify>
    <automated>cd primeaudit && flutter test test/models/app_user_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `primeaudit/test/models/app_user_test.dart` existe
    - `cd primeaudit && flutter test test/models/app_user_test.dart` sai com código 0
    - Output mostra "All tests passed!" com pelo menos 11 testes
    - grep `AppUser.fromMap` em `primeaudit/test/models/app_user_test.dart` retorna >= 11 matches
    - grep `companyName` em `primeaudit/test/models/app_user_test.dart` retorna >= 2 matches (caso populado + caso null)
    - grep `canAccessAdmin|isSuperOrDev` em `primeaudit/test/models/app_user_test.dart` confirma cobertura dos getters derivados
  </acceptance_criteria>
  <done>
    Arquivo de teste existe e passa, cobrindo escalares, join companies em ambos os estados, active default e getters derivados baseados em AppRole.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Criar test/models/perimeter_test.dart cobrindo Perimeter.fromMap + Perimeter.buildTree (QUAL-03 + QUAL-04)</name>
  <files>primeaudit/test/models/perimeter_test.dart</files>
  <read_first>
    - primeaudit/lib/models/perimeter.dart (confirmar assinatura do construtor e comportamento de buildTree)
    - primeaudit/test/pending_save_test.dart (padrão de referência)
  </read_first>
  <behavior>
    - Perimeter.fromMap: parseia id, companyId, name, createdAt; parentId/description null quando ausentes; active default = true
    - Perimeter.buildTree: entrada vazia retorna lista vazia
    - Perimeter.buildTree: 1 raiz sem filhos retorna 1 elemento com children vazio
    - Perimeter.buildTree: múltiplas raízes coexistem (2 raízes sem filhos -> 2 elementos)
    - Perimeter.buildTree: hierarquia de 2 níveis (pai + filho) -> 1 raiz com 1 filho
    - Perimeter.buildTree: hierarquia de 3 níveis (pai + filho + neto) -> 1 raiz com 1 filho com 1 neto
    - Perimeter.buildTree: múltiplos filhos compartilhando o mesmo pai -> 1 raiz com N filhos
    - Perimeter.buildTree: parent_id apontando para id inexistente -> filho é descartado silenciosamente (orfão não aparece em roots nem em children de ninguém)
    - Cada teste de buildTree constrói instâncias frescas de Perimeter (sem compartilhar children mutável entre testes)
  </behavior>
  <action>
    Criar o arquivo `primeaudit/test/models/perimeter_test.dart` com EXATAMENTE este conteúdo:

    ```dart
    // Unit tests for Perimeter.fromMap (QUAL-03) and Perimeter.buildTree (QUAL-04).
    // buildTree mutates `children` in place, so each test must construct fresh
    // Perimeter instances via _p() — never share instances across tests.

    import 'package:flutter_test/flutter_test.dart';
    import 'package:primeaudit/models/perimeter.dart';

    Perimeter _p(String id, {String? parentId}) => Perimeter(
      id: id,
      companyId: 'co1',
      parentId: parentId,
      name: 'Perimeter $id',
      active: true,
      createdAt: DateTime(2024, 1, 1),
    );

    Map<String, dynamic> _mapBase() => <String, dynamic>{
      'id': 'p1',
      'company_id': 'c1',
      'parent_id': null,
      'name': 'Area A',
      'description': null,
      'active': true,
      'created_at': '2024-01-01T00:00:00.000Z',
    };

    void main() {
      group('Perimeter.fromMap — required fields', () {
        test('parses id, companyId, name, createdAt', () {
          final p = Perimeter.fromMap(_mapBase());
          expect(p.id, equals('p1'));
          expect(p.companyId, equals('c1'));
          expect(p.name, equals('Area A'));
          expect(p.createdAt, equals(DateTime.parse('2024-01-01T00:00:00.000Z')));
        });

        test('parentId and description are null when absent', () {
          final p = Perimeter.fromMap(_mapBase());
          expect(p.parentId, isNull);
          expect(p.description, isNull);
        });

        test('parentId is populated when present', () {
          final m = _mapBase()..['parent_id'] = 'parent1';
          expect(Perimeter.fromMap(m).parentId, equals('parent1'));
        });

        test('description is populated when present', () {
          final m = _mapBase()..['description'] = 'Setor industrial';
          expect(Perimeter.fromMap(m).description, equals('Setor industrial'));
        });
      });

      group('Perimeter.fromMap — active default', () {
        test('active defaults to true when key absent', () {
          final m = _mapBase()..remove('active');
          expect(Perimeter.fromMap(m).active, isTrue);
        });

        test('active is false when explicitly set', () {
          final m = _mapBase()..['active'] = false;
          expect(Perimeter.fromMap(m).active, isFalse);
        });
      });

      group('Perimeter.buildTree — 0 levels', () {
        test('empty list returns empty roots', () {
          expect(Perimeter.buildTree(<Perimeter>[]), isEmpty);
        });
      });

      group('Perimeter.buildTree — 1 level (roots only)', () {
        test('single root has no children', () {
          final roots = Perimeter.buildTree([_p('root')]);
          expect(roots.length, equals(1));
          expect(roots.first.id, equals('root'));
          expect(roots.first.children, isEmpty);
        });

        test('two unrelated roots coexist (both with empty children)', () {
          final roots = Perimeter.buildTree([_p('r1'), _p('r2')]);
          expect(roots.length, equals(2));
          expect(roots[0].id, equals('r1'));
          expect(roots[0].children, isEmpty);
          expect(roots[1].id, equals('r2'));
          expect(roots[1].children, isEmpty);
        });
      });

      group('Perimeter.buildTree — 2 levels (parent + child)', () {
        test('single parent with single child', () {
          final roots = Perimeter.buildTree([
            _p('parent'),
            _p('child', parentId: 'parent'),
          ]);
          expect(roots.length, equals(1));
          expect(roots.first.id, equals('parent'));
          expect(roots.first.children.length, equals(1));
          expect(roots.first.children.first.id, equals('child'));
        });

        test('single parent with multiple children', () {
          final roots = Perimeter.buildTree([
            _p('parent'),
            _p('child_a', parentId: 'parent'),
            _p('child_b', parentId: 'parent'),
            _p('child_c', parentId: 'parent'),
          ]);
          expect(roots.length, equals(1));
          expect(roots.first.children.length, equals(3));
          final childIds = roots.first.children.map((c) => c.id).toList();
          expect(childIds, containsAll(<String>['child_a', 'child_b', 'child_c']));
        });
      });

      group('Perimeter.buildTree — 3 levels (grandparent + parent + grandchild)', () {
        test('nested grandchild inside child inside root', () {
          final roots = Perimeter.buildTree([
            _p('root'),
            _p('child', parentId: 'root'),
            _p('grandchild', parentId: 'child'),
          ]);
          expect(roots.length, equals(1));
          expect(roots.first.children.length, equals(1));
          expect(roots.first.children.first.id, equals('child'));
          expect(roots.first.children.first.children.length, equals(1));
          expect(
            roots.first.children.first.children.first.id,
            equals('grandchild'),
          );
        });

        test('mixed 3-level forest: 2 roots, each with 1 child, one grandchild', () {
          final roots = Perimeter.buildTree([
            _p('r1'),
            _p('r2'),
            _p('c1', parentId: 'r1'),
            _p('c2', parentId: 'r2'),
            _p('g1', parentId: 'c1'),
          ]);
          expect(roots.length, equals(2));
          final r1 = roots.firstWhere((p) => p.id == 'r1');
          final r2 = roots.firstWhere((p) => p.id == 'r2');
          expect(r1.children.length, equals(1));
          expect(r1.children.first.id, equals('c1'));
          expect(r1.children.first.children.length, equals(1));
          expect(r1.children.first.children.first.id, equals('g1'));
          expect(r2.children.length, equals(1));
          expect(r2.children.first.id, equals('c2'));
          expect(r2.children.first.children, isEmpty);
        });
      });

      group('Perimeter.buildTree — orphan handling', () {
        test('child referencing a non-existent parentId is silently dropped', () {
          final orphan = _p('orphan', parentId: 'ghost');
          final root = _p('root');
          final roots = Perimeter.buildTree([root, orphan]);
          expect(roots.length, equals(1));
          expect(roots.first.id, equals('root'));
          expect(roots.first.children, isEmpty);
          // orphan is neither a root (has parentId != null) nor nested anywhere
        });
      });
    }
    ```
  </action>
  <verify>
    <automated>cd primeaudit && flutter test test/models/perimeter_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `primeaudit/test/models/perimeter_test.dart` existe
    - `cd primeaudit && flutter test test/models/perimeter_test.dart` sai com código 0
    - Output mostra "All tests passed!" com pelo menos 14 testes
    - grep `Perimeter.fromMap` em `primeaudit/test/models/perimeter_test.dart` retorna >= 6 matches
    - grep `Perimeter.buildTree` em `primeaudit/test/models/perimeter_test.dart` retorna >= 8 matches
    - grep `grandchild` em `primeaudit/test/models/perimeter_test.dart` confirma cobertura de 3 níveis
    - grep `orphan|ghost` em `primeaudit/test/models/perimeter_test.dart` confirma cobertura de parentId inexistente
    - `cd primeaudit && flutter test` (suite completa) sai com código 0
  </acceptance_criteria>
  <done>
    Arquivo de teste existe e passa, cobrindo Perimeter.fromMap (escalares, opcionais, default active) e Perimeter.buildTree em hierarquias de 0, 1, 2, 3 níveis + múltiplas raízes + caso orfão. Cada teste usa instâncias frescas via helper _p().
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → production code | Testes importam os 3 models; nenhuma rede; nenhuma PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | Perimeter.buildTree mutation semantics | mitigate | Testes constroem instâncias frescas por caso (helper `_p()`); alterações acidentais no algoritmo de iteração dupla em `buildTree` quebram múltiplos testes simultaneamente. |
| T-03-04-02 | Tampering | Contrato de fromMap nos 3 models | mitigate | Testes travam nomes de chaves SQL (`company_id`, `parent_id`, `companies.name`). Renomear colunas sem atualizar `fromMap` é pegado pelo CI. |
| T-03-04-03 | Information Disclosure | AppUser fixtures | accept | Emails e nomes são fictícios (`ana@example.com`, `Ana Silva`). |
| T-03-04-04 | Denial of Service | Árvore de perímetros muito grande | accept | Testes usam no máximo 5 nós; complexidade real (O(n)) é verificada por implementação, não por teste. |
</threat_model>

<verification>
- `cd primeaudit && flutter test test/models/company_test.dart` passa com 10+ testes verdes
- `cd primeaudit && flutter test test/models/app_user_test.dart` passa com 11+ testes verdes
- `cd primeaudit && flutter test test/models/perimeter_test.dart` passa com 14+ testes verdes
- `cd primeaudit && flutter test` (suite completa) permanece verde
- `cd primeaudit && flutter analyze` não introduz novos warnings
</verification>

<success_criteria>
- Plan 04 concluído quando:
  - [ ] `test/models/company_test.dart` cobre escalares, 4 opcionais e 2 defaults
  - [ ] `test/models/app_user_test.dart` cobre escalares, join companies, active default e getters derivados
  - [ ] `test/models/perimeter_test.dart` cobre Perimeter.fromMap e buildTree em hierarquias de 0, 1, 2, 3 níveis + caso orfão
  - [ ] Todos os testes constroem fixtures frescas por caso (sem contaminação cruzada em Perimeter.children)
  - [ ] `flutter test` verde sem regressões
</success_criteria>

<output>
After completion, create `.planning/phases/03-test-coverage/03-04-SUMMARY.md`
</output>
</content>
</invoke>