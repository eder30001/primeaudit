---
phase: 7
slug: dashboard
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-24
---

# Phase 7 — UI Design Contract

> Visual and interaction contract for Phase 7: Dashboard.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — Flutter Material 3 + AppTheme/AppColors tokens |
| Preset | not applicable |
| Component library | Flutter Material widgets (built-in) |
| Icon library | Material Icons (`Icons.*`) — bundled with Flutter |
| Font | System default (Roboto on Android, SF Pro on iOS) via Flutter Material |

Source: CLAUDE.md stack, `primeaudit/lib/core/app_colors.dart`, `primeaudit/lib/core/app_theme.dart`.

No shadcn gate applies — this is a Flutter project, not React/Next.js/Vite.

---

## Spacing Scale

Declared values (multiples of 4, from the standard set {4, 8, 16, 24, 32, 48, 64}):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gap inside `_summaryCard` (SizedBox between icon container and text column) |
| sm | 8px | SizedBox between role-label text and first card row; SizedBox between card rows (gutter between the two KPI rows); SizedBox between icon container and label column inside each card |
| md | 16px | Card internal padding (`EdgeInsets.all(16)`); dashboard outer horizontal/vertical padding (`EdgeInsets.all(16)`) |
| lg | 24px | SizedBox below greeting section before first card row; SizedBox below card rows before chart section |

Exceptions:
- Icon touch container: 40x40px (not a spacing token — fixed container size for KPI card icon).
- Icon container border-radius: 8px (remapped from 10px; closest standard-friendly value — visual rounding for icon container).
- Card container border-radius: 12px (shared with chart empty-state container — border-radius is not a spacing token).
- Chart height: `data.length * 48.0 + 40` px (dynamic, calculated from bar count).
- Chart empty-state container height: 120px minimum.
- Chart label reserved width: 120px (`reservedSize` for template name labels).

Source: Derived from `home_screen.dart` `_summaryCard()` and `_buildDashboard()` existing measurements, plus PATTERNS.md chart widget specification. Non-standard values (12px, 20px, 28px) remapped per checker fix: 12→8, 20→16, 28→24.

---

## Typography

Exactly 4 font sizes, exactly 2 font weights:

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Display | 22px | bold (700) | 1.2 | Greeting heading — "Olá, {name}!" |
| KPI value | 20px | bold (700) | 1.0 | KPI card number (Total, Pendentes, Atrasadas, Ações abertas / Empresas) |
| Heading | 15px | bold (700) | 1.2 | Chart section title — "Conformidade por template" |
| Body | 13px | regular (400) | 1.5 | Role subtitle under greeting; KPI card labels; chart axis labels; chart value labels; chart empty-state message |

Notes:
- Semibold (600) weight is removed. The chart section heading uses bold (700) instead.
- The 11px, 10px, and 9px sizes from the original draft are merged into the 13px Body role. Chart axis and value labels use `TextStyle(fontSize: 13, color: textSecondary)` — Flutter scales these down visually within the chart's reserved space if needed, but the declared token remains 13px.
- Only two weights are in use: bold (700) and regular (400).

Source: Checker fix — consolidated from 7 sizes to 4 and from 3 weights to 2.

---

## Color

| Role | Light Mode Value | Dark Mode Value | Usage |
|------|-----------------|-----------------|-------|
| Dominant (60%) | `#F5F7FA` | `#121212` | Page background (`AppTheme.of(context).background`) |
| Secondary (30%) | `#FFFFFF` | `#1E1E1E` | Card surfaces, chart container (`AppTheme.of(context).surface`) |
| Text primary | `#1A1A2E` | `#E8EAED` | KPI values, greeting, section titles (`AppTheme.of(context).textPrimary`) |
| Text secondary | `#6B7280` | `#9AA0A6` | Role subtitle, KPI labels, chart labels, chart empty-state text (`AppTheme.of(context).textSecondary`) |
| Divider | `#E5E7EB` | `#3C4043` | Card border, chart empty-state container border (`AppTheme.of(context).divider`) |
| Accent (icon color) | `#2196F3` | `#2196F3` | "Total" KPI card icon tint (`AppColors.accent`) |
| Primary (bars + refresh) | `#1E3A5F` | `#1E3A5F` | Chart bar fill; `RefreshIndicator` spinner (`AppColors.primary`) |
| Error (icon color) | `#E53935` | `#E53935` | "Atrasadas" KPI card icon tint (`AppColors.error`) |
| Warning (icon color) | `Colors.orange` | `Colors.orange` | "Pendentes" KPI card icon tint |
| Teal (icon color) | `Colors.teal` | `Colors.teal` | "Ações abertas" KPI card icon tint (for non-superuser roles) |
| Purple (icon color) | `Colors.purple` | `Colors.purple` | "Empresas" KPI card icon tint (for superuser/dev only) |
| Icon container bg | icon color at 12% opacity | same | Icon container background within each KPI card (`color.withValues(alpha: 0.12)`) |

Accent reserved for: "Total" KPI card icon and icon container background only. `AppColors.primary` (`#1E3A5F`) is reserved for the chart bars and the `RefreshIndicator` color. All other interactive surfaces (AppBar, drawer header) already use `AppColors.primary` — do not change.

Source: `app_colors.dart`, `app_theme.dart`, existing `_summaryCard()` in `home_screen.dart`.

---

## Component Inventory

> Flutter — no shadcn components. All components are Flutter Material widgets or custom private widgets already in `home_screen.dart`.

| Component | Widget | Source | Notes |
|-----------|--------|--------|-------|
| Dashboard page wrapper | `RefreshIndicator` > `SingleChildScrollView` | Flutter SDK | `onRefresh: _loadDashboard`; requires `AlwaysScrollableScrollPhysics()` |
| KPI card | `_summaryCard(icon, label, value, color)` | Existing — `home_screen.dart` line 384 | No changes to widget signature or layout |
| KPI card icon container | `Container` 40x40, radius 8 | Existing (radius remapped 10→8) | `color.withValues(alpha: 0.12)` background |
| Loading state (KPI) | Inline `'...'` string in `value` param | Local state `_dashboardLoading` | When `_dashboardLoading == true`, value shows `'...'` |
| Error state (dashboard) | Inline `Text` inside `_buildDashboard()` body | Local state `_dashboardError` | Shown when `_dashboardError != null`; replaces chart area only |
| Chart section title | `Text` with heading style | Existing pattern | "Conformidade por template" |
| Conformity bar chart | `BarChart` (fl_chart 1.2.0) | `fl_chart ^1.2.0` (to be added) | Horizontal bars via `rotationQuarterTurns: 1`; sorted descending |
| Chart empty state | `Container` h=120, centered `Text` | Custom in `_buildConformityChart()` | When `_chartData.isEmpty` |
| Full-screen loader | `CircularProgressIndicator(color: AppColors.primary)` | Existing | Only during initial `_loading` (profile + dashboard first load) |

---

## Interaction Contracts

### Pull-to-Refresh (DASH-02)

- Trigger: user pulls down on the `SingleChildScrollView` while at top of scroll.
- Widget: `RefreshIndicator` wrapping `SingleChildScrollView`.
- Callback: `onRefresh: _loadDashboard` (returns `Future<void>`).
- `RefreshIndicator` color: `AppColors.primary` (`#1E3A5F`).
- Physics: `SingleChildScrollView` must have `physics: const AlwaysScrollableScrollPhysics()`.
- During refresh: KPI card values show `'...'` (via `_dashboardLoading` flag). Chart remains visible with stale data until reload completes.
- On error: `_dashboardError` is set; chart area shows inline error message with problem + retry suggestion.

### KPI Card Loading State

- While `_dashboardLoading == true`: each card's `value` parameter receives `'...'` (ellipsis, not a spinner — avoids layout reflow from icon changes).
- While `_loading == true` (initial full-screen load): entire body is replaced by `CircularProgressIndicator` — same as existing behavior.

### Role-Based Card Visibility

| Role | Row 1 | Row 2 Left | Row 2 Right |
|------|-------|------------|-------------|
| auditor | Total (own audits) + Pendentes (own) | Atrasadas (own) | Ações abertas (always 0 until Phase 8) |
| adm | Total (company-wide) + Pendentes | Atrasadas | Ações abertas |
| superuser / dev | Total (company-scoped or all if no active company) + Pendentes | Atrasadas | Empresas (total companies count) |

Decision source: D-05, D-06, D-07 from `07-CONTEXT.md`. Use `AppRole.isSuperOrDev(_role)` (not `canAccessAdmin`) for the companies card.

### Chart Interaction

- Static display only — no tap, no drill-down, no period filter (deferred per `07-CONTEXT.md`).
- Bars are sorted descending by average conformity (best template first).
- Data scope: all `concluida` audits with non-null `conformityPercent`, no date filter.
- Empty guard: when `_chartData.isEmpty`, chart widget is replaced by empty-state container (never crash on empty `barGroups`).

---

## Copywriting Contract

| Element | Copy (Portuguese) |
|---------|------------------|
| Greeting (with name) | `Olá, {firstName}!` — source: existing `home_screen.dart` line 268 |
| Greeting (fallback) | `Bem-vindo!` — source: existing `home_screen.dart` line 270 |
| Role subtitle — superuser | `Acesso total ao sistema` |
| Role subtitle — dev | `Ambiente de desenvolvimento` |
| Role subtitle — adm | `Administrador da empresa` |
| Role subtitle — auditor | `Auditor` |
| Role subtitle — anonymous | `Acesso restrito` |
| KPI label — card 1 | `Total` |
| KPI label — card 2 | `Pendentes` |
| KPI label — card 3 | `Atrasadas` |
| KPI label — card 4 (non-superuser) | `Ações abertas` |
| KPI label — card 4 (superuser/dev) | `Empresas` |
| KPI loading value | `...` (three dots, not em dash) |
| Chart section title | `Conformidade por template` |
| Chart empty state | `Nenhuma auditoria concluída para exibir` |
| Dashboard error state | `Erro ao carregar dashboard. Puxe a tela para baixo para tentar novamente.` |
| Pull-to-refresh action | No label — standard `RefreshIndicator` gesture, no text needed |

Destructive actions: none in this phase. No confirmation dialogs required.

Source: Existing copy preserved from `home_screen.dart`; new copy follows Portuguese PT-BR convention already established in codebase. Error state copy updated per checker fix to include recovery instruction.

---

## State Contract

| State Field | Type | Initial Value | Triggers Re-render |
|-------------|------|---------------|-------------------|
| `_totalAudits` | `int` | `0` | yes |
| `_pendingAudits` | `int` | `0` | yes |
| `_overdueAudits` | `int` | `0` | yes |
| `_openActions` | `int` | `0` | yes |
| `_companiesCount` | `int` | `0` | yes — superuser/dev only |
| `_chartData` | `List<_TemplateConformity>` | `[]` | yes |
| `_dashboardLoading` | `bool` | `false` | yes — controls KPI `'...'` values |
| `_dashboardError` | `String?` | `null` | yes — controls error inline display |

KPI card values render as `_dashboardLoading ? '...' : '$_fieldName'`.

---

## Layout Structure

```
Scaffold
  AppBar (unchanged — AppColors.primary bg, white fg, "PrimeAudit" title)
  Drawer (unchanged)
  body:
    _loading → CircularProgressIndicator (full-screen, centered)
    !_loading → _buildDashboard()

_buildDashboard():
  RefreshIndicator (onRefresh: _loadDashboard, color: AppColors.primary)
    SingleChildScrollView (physics: AlwaysScrollableScrollPhysics, padding: EdgeInsets.all(16))
      Column (crossAxisAlignment: start)
        Text greeting (22px bold, textPrimary)
        SizedBox(height: 4)
        Text role-label (13px regular, textSecondary)
        SizedBox(height: 24)
        Row [card Total | card Pendentes] (Expanded each, SizedBox(width: 8) between)
        SizedBox(height: 8)
        Row [card Atrasadas | card Ações abertas OR Empresas] (same layout)
        SizedBox(height: 24)
        Text "Conformidade por template" (15px bold, textPrimary)
        SizedBox(height: 8)
        _buildConformityChart(_chartData)
          → if empty: Container(h:120) centered Text (13px, textSecondary)
          → if data:  SizedBox(h: data.length*48+40) > BarChart(...)

_summaryCard (unchanged widget signature):
  Container (padding: EdgeInsets.all(16), surface color, radius:12, divider border)
    Row
      Container 40x40 radius:8 (icon color at 12% opacity bg)
        Icon (20px, icon color)
      SizedBox(width: 8)
      Column (crossAxisAlignment: start)
        Text value (20px bold, textPrimary) — shows '...' during loading
        Text label (13px regular, textSecondary, ellipsis overflow)
```

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| Flutter SDK built-ins | `RefreshIndicator`, `SingleChildScrollView`, `BarChart` (via fl_chart) | not applicable — native Flutter |
| pub.dev official | `fl_chart ^1.2.0` | pub.dev verified — MIT license, 7k+ likes, Dart SDK >=3.6 compatible with project's >=3.11.4; source reviewed in RESEARCH.md |

No third-party shadcn-style registries. No registry vetting gate required for pub.dev packages in Flutter projects.

---

## Pre-Population Sources

| Source | Decisions Pre-Populated |
|--------|------------------------|
| `07-CONTEXT.md` | D-01 through D-07 (all KPI definitions and role scoping), deferred list (no date filter, no drill-down, no realtime) |
| `07-RESEARCH.md` | fl_chart 1.2.0 API (BarChartData, rotationQuarterTurns, AlwaysScrollableScrollPhysics, DashboardService pattern) |
| `07-PATTERNS.md` | Exact widget structure for _buildConformityChart, _loadDashboard, role card matrix |
| `home_screen.dart` | Existing spacing measurements, typography sizes, color assignments, copywriting |
| `app_colors.dart` + `app_theme.dart` | All color tokens with exact hex values and dark mode variants |
| `REQUIREMENTS.md` | DASH-01, DASH-02, DASH-03 requirement descriptions confirmed |
| CLAUDE.md | Stack constraints (no BLoC/Riverpod, no new screens, Flutter + Material 3) |
| Checker revision | Typography consolidated (7→4 sizes, 3→2 weights); spacing remapped (12→8, 20→16, 28→24); error copy updated with recovery instruction |

User questions asked during this session: none — all design contract questions answered by upstream artifacts.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---

*Phase: 07-dashboard*
*UI-SPEC created: 2026-04-24*
*UI-SPEC revised: 2026-04-24 (checker fix — typography, spacing, copywriting)*
*Valid until: 2026-05-24 (fl_chart 1.2.0 stable; internal code stable; 30 days)*
