---
phase: 08-corrective-actions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - primeaudit/supabase/migrations/20260425_create_corrective_actions.sql
  - primeaudit/lib/models/corrective_action.dart
  - primeaudit/lib/services/corrective_action_service.dart
  - primeaudit/test/models/corrective_action_test.dart
  - primeaudit/test/services/corrective_action_service_test.dart
autonomous: true
requirements:
  - ACT-01
  - ACT-02
  - ACT-03
  - ACT-04

must_haves:
  truths:
    - "A tabela corrective_actions existe no banco com RLS habilitado e scoping por company_id"
    - "CorrectiveAction.fromMap() parseia todos os campos incluindo joins de profiles e audits"
    - "CorrectiveActionStatus.fromDb() mapeia todos os 6 valores de DB para enum"
    - "CorrectiveActionService.isNonConforming() retorna correto para todos os tipos de resposta"
    - "CorrectiveActionService.canTransitionTo() aplica RBAC corretamente para admin, responsavel e auditor"
    - "Tests unitarios passam sem Supabase client"
  artifacts:
    - path: "primeaudit/supabase/migrations/20260425_create_corrective_actions.sql"
      provides: "Tabela corrective_actions com FKs, CHECK constraint, indexes e RLS policies"
      contains: "CREATE TABLE IF NOT EXISTS corrective_actions"
    - path: "primeaudit/lib/models/corrective_action.dart"
      provides: "CorrectiveAction model + CorrectiveActionStatus enum"
      exports: ["CorrectiveAction", "CorrectiveActionStatus"]
    - path: "primeaudit/lib/services/corrective_action_service.dart"
      provides: "CorrectiveActionService com CRUD + static isNonConforming + static canTransitionTo"
      exports: ["CorrectiveActionService"]
    - path: "primeaudit/test/models/corrective_action_test.dart"
      provides: "Unit tests para fromMap, status enum, isOverdue, isFinal"
      contains: "void main()"
    - path: "primeaudit/test/services/corrective_action_service_test.dart"
      provides: "Unit tests para isNonConforming e canTransitionTo (static, sem Supabase)"
      contains: "void main()"
  key_links:
    - from: "corrective_action_service.dart"
      to: "corrective_action.dart"
      via: "import + CorrectiveAction.fromMap"
      pattern: "import.*corrective_action.dart"
    - from: "corrective_action_test.dart"
      to: "corrective_action.dart"
      via: "import package:primeaudit/models/corrective_action.dart"
      pattern: "import.*package:primeaudit/models/corrective_action.dart"
    - from: "corrective_action_service_test.dart"
      to: "corrective_action_service.dart"
      via: "static method calls CorrectiveActionService.isNonConforming + canTransitionTo"
      pattern: "CorrectiveActionService\\.isNonConforming|CorrectiveActionService\\.canTransitionTo"
---

<objective>
Entregar a fundação completa da Phase 8: migration SQL idempotente criando a tabela corrective_actions com RLS, model CorrectiveAction com enum de 6 estados, service CorrectiveActionService com CRUD e funções estáticas testáveis, e os stubs de test que garantem cobertura das regras de negócio core (non-conformidade e RBAC de transição).

Purpose: Tudo nas waves seguintes depende da tabela existir no banco e dos contratos de model/service estarem definidos. O schema push é BLOQUEANTE — nenhum código que usa Supabase pode ser verificado sem ele.

Output: Migration aplicada ao banco, corrective_action.dart, corrective_action_service.dart, test/models/corrective_action_test.dart, test/services/corrective_action_service_test.dart — tudo verde em `flutter test`.
</objective>

<execution_context>
@C:\Users\eder3\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\eder3\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-corrective-actions/08-CONTEXT.md
@.planning/phases/08-corrective-actions/08-RESEARCH.md
@.planning/phases/08-corrective-actions/08-PATTERNS.md
@.planning/phases/08-corrective-actions/08-VALIDATION.md

<interfaces>
<!-- Padrões de referência necessários para esta wave. Ler antes de implementar. -->

De primeaudit/supabase/migrations/20260406_create_audits.sql (padrão idempotente):
```sql
CREATE TABLE IF NOT EXISTS <tabela> (id UUID DEFAULT gen_random_uuid() PRIMARY KEY);
ALTER TABLE <tabela> ADD COLUMN IF NOT EXISTS <coluna> <tipo>;
ALTER TABLE <tabela> DROP CONSTRAINT IF EXISTS <nome>; ALTER TABLE <tabela> ADD CONSTRAINT <nome> ...;
CREATE INDEX IF NOT EXISTS <nome> ON <tabela> (<coluna>);
ALTER TABLE <tabela> ENABLE ROW LEVEL SECURITY;
DROP POLICY IF EXISTS "<nome>" ON <tabela>; CREATE POLICY "<nome>" ON <tabela> USING (...) WITH CHECK (...);
NOTIFY pgrst, 'reload schema';
```

De primeaudit/lib/models/audit.dart (padrão de model):
```dart
enum AuditStatus { ... ; String get label; Color get color; IconData get icon; static AuditStatus _statusFromString(String? s); }
class Audit { final String id; ...; const Audit({required this.id, ...}); factory Audit.fromMap(Map<String,dynamic> map); bool get isOverdue; }
```

De primeaudit/lib/services/audit_answer_service.dart (padrão de service):
```dart
class AuditAnswerService {
  final _client = Supabase.instance.client;
  // No try/catch inside — callers handle exceptions
  Future<List<T>> getX(...) async { final data = await _client.from('...').select(...); return (data as List).map((e) => T.fromMap(e)).toList(); }
  static double calculateConformity(...) { ... }  // static for testability
}
```

De primeaudit/test/services/dashboard_service_test.dart (padrão de test sem Supabase):
```dart
// NÃO instancia o service — testa apenas funções estáticas/top-level
import 'package:flutter_test/flutter_test.dart';
import 'package:primeaudit/services/corrective_action_service.dart';
void main() { group('...', () { test('...', () { expect(X.staticMethod(...), isTrue); }); }); }
```

De primeaudit/lib/core/app_roles.dart:
```dart
class AppRole {
  static const String superuser = 'superuser';
  static const String dev = 'dev';
  static const String adm = 'adm';
  static const String auditor = 'auditor';
  static bool canAccessAdmin(String? role) => role == adm || role == superuser || role == dev;
  static bool isSuperOrDev(String? role) => role == superuser || role == dev;
}
```

Status chip colors por 08-UI-SPEC.md:
```dart
// aberta:      Colors.orange.shade100 / Colors.orange.shade800
// em_andamento: Color(0xFFE3F2FD) / Color(0xFF1565C0)
// em_avaliacao: Colors.purple.shade50 / Colors.purple.shade800
// aprovada:    Color(0xFFE8F5E9) / Color(0xFF2E7D32)
// rejeitada:   Color(0xFFFFEBEE) / AppColors.error
// cancelada:   Colors.grey.shade100 / Colors.grey.shade700
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration SQL — tabela corrective_actions idempotente com RLS</name>
  <files>primeaudit/supabase/migrations/20260425_create_corrective_actions.sql</files>

  <read_first>
    - primeaudit/supabase/migrations/20260406_create_audits.sql — padrão idempotente exato a seguir (CREATE TABLE IF NOT EXISTS, ADD COLUMN IF NOT EXISTS, DROP CONSTRAINT IF EXISTS + ADD CONSTRAINT, CREATE INDEX IF NOT EXISTS, DROP POLICY IF EXISTS + CREATE POLICY, NOTIFY pgrst)
    - primeaudit/supabase/migrations/20260418_rls_profiles_companies_perimeters.sql — padrão de RLS por role
    - .planning/phases/08-corrective-actions/08-RESEARCH.md §Code Examples §Migration pattern
    - .planning/phases/08-corrective-actions/08-CONTEXT.md §decisions (campos obrigatórios: id, audit_id, template_item_id, title, description, responsible_user_id, due_date, status, company_id, created_by, created_at, updated_at)
  </read_first>

  <action>
Criar o arquivo `primeaudit/supabase/migrations/20260425_create_corrective_actions.sql` com o seguinte conteúdo exato:

```sql
-- =============================================================================
-- Migracao: tabela corrective_actions e RLS
-- Data: 2026-04-25
-- Idempotente: pode ser executado multiplas vezes sem erro.
-- =============================================================================

-- 1. Tabela base (somente id na criacao — colunas adicionadas com ADD COLUMN IF NOT EXISTS)
CREATE TABLE IF NOT EXISTS corrective_actions (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY
);

-- 2. Colunas (idempotentes)
ALTER TABLE corrective_actions ADD COLUMN IF NOT EXISTS audit_id            UUID NOT NULL;
ALTER TABLE corrective_actions ADD COLUMN IF NOT EXISTS template_item_id    UUID NOT NULL;
ALTER TABLE corrective_actions ADD COLUMN IF NOT EXISTS title               TEXT NOT NULL;
ALTER TABLE corrective_actions ADD COLUMN IF NOT EXISTS description         TEXT;
ALTER TABLE corrective_actions ADD COLUMN IF NOT EXISTS responsible_user_id UUID NOT NULL;
ALTER TABLE corrective_actions ADD COLUMN IF NOT EXISTS due_date            DATE NOT NULL;
ALTER TABLE corrective_actions ADD COLUMN IF NOT EXISTS status              TEXT NOT NULL DEFAULT 'aberta';
ALTER TABLE corrective_actions ADD COLUMN IF NOT EXISTS company_id          UUID NOT NULL;
ALTER TABLE corrective_actions ADD COLUMN IF NOT EXISTS created_by          UUID NOT NULL;
ALTER TABLE corrective_actions ADD COLUMN IF NOT EXISTS created_at          TIMESTAMPTZ NOT NULL DEFAULT NOW();
ALTER TABLE corrective_actions ADD COLUMN IF NOT EXISTS updated_at          TIMESTAMPTZ NOT NULL DEFAULT NOW();

-- 3. Foreign keys (drop + add para idempotencia — padrao de 20260406_create_audits.sql)
ALTER TABLE corrective_actions DROP CONSTRAINT IF EXISTS corrective_actions_audit_id_fkey;
ALTER TABLE corrective_actions ADD CONSTRAINT corrective_actions_audit_id_fkey
  FOREIGN KEY (audit_id) REFERENCES audits(id) ON DELETE CASCADE;

ALTER TABLE corrective_actions DROP CONSTRAINT IF EXISTS corrective_actions_template_item_id_fkey;
ALTER TABLE corrective_actions ADD CONSTRAINT corrective_actions_template_item_id_fkey
  FOREIGN KEY (template_item_id) REFERENCES template_items(id) ON DELETE RESTRICT;

ALTER TABLE corrective_actions DROP CONSTRAINT IF EXISTS corrective_actions_responsible_user_id_fkey;
ALTER TABLE corrective_actions ADD CONSTRAINT corrective_actions_responsible_user_id_fkey
  FOREIGN KEY (responsible_user_id) REFERENCES profiles(id) ON DELETE RESTRICT;

ALTER TABLE corrective_actions DROP CONSTRAINT IF EXISTS corrective_actions_company_id_fkey;
ALTER TABLE corrective_actions ADD CONSTRAINT corrective_actions_company_id_fkey
  FOREIGN KEY (company_id) REFERENCES companies(id) ON DELETE RESTRICT;

ALTER TABLE corrective_actions DROP CONSTRAINT IF EXISTS corrective_actions_created_by_fkey;
ALTER TABLE corrective_actions ADD CONSTRAINT corrective_actions_created_by_fkey
  FOREIGN KEY (created_by) REFERENCES profiles(id) ON DELETE RESTRICT;

-- 4. CHECK constraint de status (drop + add para idempotencia)
ALTER TABLE corrective_actions DROP CONSTRAINT IF EXISTS corrective_actions_status_check;
ALTER TABLE corrective_actions ADD CONSTRAINT corrective_actions_status_check
  CHECK (status IN ('aberta','em_andamento','em_avaliacao','aprovada','rejeitada','cancelada'));

-- 5. Indexes
CREATE INDEX IF NOT EXISTS idx_corrective_actions_company_id     ON corrective_actions (company_id);
CREATE INDEX IF NOT EXISTS idx_corrective_actions_status         ON corrective_actions (status);
CREATE INDEX IF NOT EXISTS idx_corrective_actions_responsible    ON corrective_actions (responsible_user_id);
CREATE INDEX IF NOT EXISTS idx_corrective_actions_audit_id       ON corrective_actions (audit_id);
CREATE INDEX IF NOT EXISTS idx_corrective_actions_created_at     ON corrective_actions (created_at DESC);

-- 6. RLS
ALTER TABLE corrective_actions ENABLE ROW LEVEL SECURITY;

-- superuser e dev: acesso total
DROP POLICY IF EXISTS "superuser_dev_corrective_actions_full" ON corrective_actions;
CREATE POLICY "superuser_dev_corrective_actions_full" ON corrective_actions
  USING (get_my_role() IN ('superuser', 'dev'))
  WITH CHECK (get_my_role() IN ('superuser', 'dev'));

-- adm: CRUD na propria empresa
DROP POLICY IF EXISTS "adm_corrective_actions_company" ON corrective_actions;
CREATE POLICY "adm_corrective_actions_company" ON corrective_actions
  USING (get_my_role() = 'adm' AND company_id = get_my_company_id())
  WITH CHECK (get_my_role() = 'adm' AND company_id = get_my_company_id());

-- auditor: SELECT na propria empresa
DROP POLICY IF EXISTS "auditor_corrective_actions_select" ON corrective_actions;
CREATE POLICY "auditor_corrective_actions_select" ON corrective_actions
  FOR SELECT
  USING (get_my_role() = 'auditor' AND company_id = get_my_company_id());

-- auditor: INSERT na propria empresa (apenas proprias acoes)
DROP POLICY IF EXISTS "auditor_corrective_actions_insert" ON corrective_actions;
CREATE POLICY "auditor_corrective_actions_insert" ON corrective_actions
  FOR INSERT
  WITH CHECK (
    get_my_role() = 'auditor'
    AND company_id = get_my_company_id()
    AND created_by = auth.uid()
  );

-- auditor: UPDATE na propria empresa (transicoes de status)
DROP POLICY IF EXISTS "auditor_corrective_actions_update" ON corrective_actions;
CREATE POLICY "auditor_corrective_actions_update" ON corrective_actions
  FOR UPDATE
  USING (get_my_role() = 'auditor' AND company_id = get_my_company_id())
  WITH CHECK (get_my_role() = 'auditor' AND company_id = get_my_company_id());

-- 7. Recarregar schema do PostgREST (sempre ultima linha)
NOTIFY pgrst, 'reload schema';
```

IMPORTANTE: `template_item_id` referencia `template_items(id)` — confirmar nome da tabela lendo as migrations existentes antes de escrever. Se a tabela se chamar diferente (ex: `audit_template_items`), ajustar o FK.
  </action>

  <verify>
    <automated>grep -c "CREATE TABLE IF NOT EXISTS corrective_actions" "primeaudit/supabase/migrations/20260425_create_corrective_actions.sql" && grep -c "NOTIFY pgrst" "primeaudit/supabase/migrations/20260425_create_corrective_actions.sql" && grep -c "DROP POLICY IF EXISTS" "primeaudit/supabase/migrations/20260425_create_corrective_actions.sql"</automated>
  </verify>

  <acceptance_criteria>
    - `grep "CREATE TABLE IF NOT EXISTS corrective_actions"` retorna match
    - `grep "ADD COLUMN IF NOT EXISTS"` retorna 11 linhas (uma por coluna alem de id)
    - `grep "DROP CONSTRAINT IF EXISTS"` retorna 5 matches (4 FKs + 1 CHECK)
    - `grep "DROP POLICY IF EXISTS"` retorna 5 matches (superuser_dev, adm, auditor select, auditor insert, auditor update)
    - `grep "NOTIFY pgrst"` retorna 1 match na ultima linha semanticamente significativa
    - Arquivo existe em `primeaudit/supabase/migrations/20260425_create_corrective_actions.sql`
  </acceptance_criteria>

  <done>Migration SQL completa, idempotente, com RLS por role, pronta para `supabase db push`</done>
</task>

<task type="auto">
  <name>Task 2 [BLOCKING]: supabase db push — aplicar migration ao banco</name>
  <files></files>

  <read_first>
    - primeaudit/supabase/migrations/20260425_create_corrective_actions.sql — confirmar que Task 1 criou o arquivo antes de executar
  </read_first>

  <action>
Executar o push da migration para o banco Supabase. Este task é BLOQUEANTE — as tasks de Wave 2+ não podem ser verificadas contra o banco sem esta migration aplicada.

Rodar a partir do diretório do projeto Flutter:

```bash
cd primeaudit && supabase db push
```

Se o comando pedir confirmação interativa, responder 'y'. Se retornar erro de autenticação, verificar se `SUPABASE_ACCESS_TOKEN` está configurado ou executar `supabase login` primeiro.

Se `supabase` CLI não estiver no PATH, tentar: `npx supabase db push`

Em caso de erro de FK (ex: `template_items` não existe com esse nome), ler as migrations existentes para descobrir o nome correto da tabela e corrigir o arquivo de migration antes de reexecutar.

Sinal de sucesso: output contém "Finished supabase db push" ou similar sem erros de constraint.
  </action>

  <verify>
    <automated>cd primeaudit && supabase db push 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - Comando `supabase db push` termina sem linha de erro `ERROR:` ou `FATAL:`
    - A saída menciona o arquivo `20260425_create_corrective_actions.sql` como aplicado
    - Após o push, `DashboardService.getOpenActionsCount` em HomeScreen pode consultar a tabela sem o try/catch retornar 0
  </acceptance_criteria>

  <done>Migration aplicada ao banco — tabela corrective_actions existe com RLS ativo</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CorrectiveAction model + CorrectiveActionStatus enum + unit tests</name>
  <files>
    primeaudit/lib/models/corrective_action.dart
    primeaudit/test/models/corrective_action_test.dart
  </files>

  <read_first>
    - primeaudit/lib/models/audit.dart — padrão exato de enum (label, color, icon getters; static fromString; AuditStatus values) e de model (const constructor com required/nullable, factory fromMap com nested join access via ?['key'], computed getter isOverdue)
    - primeaudit/lib/models/audit_template.dart — padrão adicional de enum enhanced
    - primeaudit/test/models/audit_test.dart — padrão de test: _fullAuditMap() factory, group/test structure, expect(fromMap(map).field, equals(...))
    - .planning/phases/08-corrective-actions/08-RESEARCH.md §Pattern 2 — Model with fromMap factory
    - .planning/phases/08-corrective-actions/08-PATTERNS.md §corrective_action.dart
    - .planning/phases/08-corrective-actions/08-UI-SPEC.md §CAPA Status Color Map
  </read_first>

  <behavior>
    - Test: CorrectiveAction.fromMap(_fullActionMap()) parseia id='ca1', auditId='a1', templateItemId='ti1', title='Acao teste'
    - Test: fromMap parseia dueDate como DateTime (DATE string '2027-01-01' -> DateTime(2027,1,1))
    - Test: fromMap parseia responsibleName de map['profiles']['full_name'] == 'Ana'
    - Test: fromMap parseia responsibleName como null quando profiles join ausente
    - Test: fromMap parseia linkedAuditTitle de map['audits']['title'] == 'Auditoria X'
    - Test: fromMap parseia description como null quando campo ausente no map
    - Test: CorrectiveActionStatus.fromDb('aberta') retorna CorrectiveActionStatus.aberta
    - Test: CorrectiveActionStatus.fromDb('em_andamento') retorna CorrectiveActionStatus.emAndamento
    - Test: CorrectiveActionStatus.fromDb('em_avaliacao') retorna CorrectiveActionStatus.emAvaliacao
    - Test: CorrectiveActionStatus.fromDb('aprovada') retorna CorrectiveActionStatus.aprovada
    - Test: CorrectiveActionStatus.fromDb('rejeitada') retorna CorrectiveActionStatus.rejeitada
    - Test: CorrectiveActionStatus.fromDb('cancelada') retorna CorrectiveActionStatus.cancelada
    - Test: CorrectiveActionStatus.fromDb(null) retorna CorrectiveActionStatus.aberta (fallback)
    - Test: CorrectiveActionStatus.fromDb('desconhecido') retorna CorrectiveActionStatus.aberta (fallback)
    - Test: CorrectiveActionStatus.aprovada.isFinal == true
    - Test: CorrectiveActionStatus.rejeitada.isFinal == true
    - Test: CorrectiveActionStatus.cancelada.isFinal == true
    - Test: CorrectiveActionStatus.aberta.isFinal == false
    - Test: CorrectiveActionStatus.emAndamento.isFinal == false
    - Test: CorrectiveActionStatus.emAvaliacao.isFinal == false
    - Test: isOverdue == true quando dueDate no passado E status não-final
    - Test: isOverdue == false quando status é aprovada (final), mesmo dueDate passado
    - Test: isOverdue == false quando dueDate é futuro E status aberta
  </behavior>

  <action>
PASSO 1 — Escrever os testes primeiro (RED):

Criar `primeaudit/test/models/corrective_action_test.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:primeaudit/models/corrective_action.dart';

Map<String, dynamic> _fullActionMap() => {
  'id': 'ca1',
  'audit_id': 'a1',
  'template_item_id': 'ti1',
  'title': 'Acao teste',
  'description': 'Descricao',
  'responsible_user_id': 'u1',
  'due_date': '2027-01-01',
  'status': 'aberta',
  'company_id': 'c1',
  'created_by': 'u2',
  'created_at': '2026-04-25T00:00:00.000Z',
  'updated_at': '2026-04-25T00:00:00.000Z',
  'profiles': {'full_name': 'Ana'},
  'audits': {'title': 'Auditoria X'},
};

void main() {
  // fromMap — scalar fields
  group('CorrectiveAction.fromMap — campos escalares', () {
    test('parseia id, auditId, templateItemId, title', () {
      final a = CorrectiveAction.fromMap(_fullActionMap());
      expect(a.id, equals('ca1'));
      expect(a.auditId, equals('a1'));
      expect(a.templateItemId, equals('ti1'));
      expect(a.title, equals('Acao teste'));
    });

    test('parseia dueDate como DateTime a partir de DATE string', () {
      final a = CorrectiveAction.fromMap(_fullActionMap());
      expect(a.dueDate, equals(DateTime(2027, 1, 1)));
    });

    test('description e null quando campo ausente', () {
      final map = Map<String, dynamic>.from(_fullActionMap())..remove('description');
      final a = CorrectiveAction.fromMap(map);
      expect(a.description, isNull);
    });
  });

  // fromMap — nested joins
  group('CorrectiveAction.fromMap — joins', () {
    test('parseia responsibleName de profiles join', () {
      final a = CorrectiveAction.fromMap(_fullActionMap());
      expect(a.responsibleName, equals('Ana'));
    });

    test('responsibleName e null quando profiles join ausente', () {
      final map = Map<String, dynamic>.from(_fullActionMap())..remove('profiles');
      final a = CorrectiveAction.fromMap(map);
      expect(a.responsibleName, isNull);
    });

    test('parseia linkedAuditTitle de audits join', () {
      final a = CorrectiveAction.fromMap(_fullActionMap());
      expect(a.linkedAuditTitle, equals('Auditoria X'));
    });
  });

  // Status enum fromDb
  group('CorrectiveActionStatus.fromDb — todos os 6 valores', () {
    test("'aberta' -> aberta", () => expect(CorrectiveActionStatus.fromDb('aberta'), equals(CorrectiveActionStatus.aberta)));
    test("'em_andamento' -> emAndamento", () => expect(CorrectiveActionStatus.fromDb('em_andamento'), equals(CorrectiveActionStatus.emAndamento)));
    test("'em_avaliacao' -> emAvaliacao", () => expect(CorrectiveActionStatus.fromDb('em_avaliacao'), equals(CorrectiveActionStatus.emAvaliacao)));
    test("'aprovada' -> aprovada", () => expect(CorrectiveActionStatus.fromDb('aprovada'), equals(CorrectiveActionStatus.aprovada)));
    test("'rejeitada' -> rejeitada", () => expect(CorrectiveActionStatus.fromDb('rejeitada'), equals(CorrectiveActionStatus.rejeitada)));
    test("'cancelada' -> cancelada", () => expect(CorrectiveActionStatus.fromDb('cancelada'), equals(CorrectiveActionStatus.cancelada)));
    test('null -> aberta (fallback)', () => expect(CorrectiveActionStatus.fromDb(null), equals(CorrectiveActionStatus.aberta)));
    test('desconhecido -> aberta (fallback)', () => expect(CorrectiveActionStatus.fromDb('desconhecido'), equals(CorrectiveActionStatus.aberta)));
  });

  // isFinal
  group('CorrectiveActionStatus.isFinal', () {
    test('aprovada e final', () => expect(CorrectiveActionStatus.aprovada.isFinal, isTrue));
    test('rejeitada e final', () => expect(CorrectiveActionStatus.rejeitada.isFinal, isTrue));
    test('cancelada e final', () => expect(CorrectiveActionStatus.cancelada.isFinal, isTrue));
    test('aberta NAO e final', () => expect(CorrectiveActionStatus.aberta.isFinal, isFalse));
    test('emAndamento NAO e final', () => expect(CorrectiveActionStatus.emAndamento.isFinal, isFalse));
    test('emAvaliacao NAO e final', () => expect(CorrectiveActionStatus.emAvaliacao.isFinal, isFalse));
  });

  // isOverdue
  group('CorrectiveAction.isOverdue', () {
    test('true quando dueDate passado e status nao-final', () {
      final map = Map<String, dynamic>.from(_fullActionMap())
        ..['due_date'] = '2020-01-01'
        ..['status'] = 'aberta';
      expect(CorrectiveAction.fromMap(map).isOverdue, isTrue);
    });

    test('false quando status final (aprovada), mesmo dueDate passado', () {
      final map = Map<String, dynamic>.from(_fullActionMap())
        ..['due_date'] = '2020-01-01'
        ..['status'] = 'aprovada';
      expect(CorrectiveAction.fromMap(map).isOverdue, isFalse);
    });

    test('false quando dueDate futuro', () {
      final map = Map<String, dynamic>.from(_fullActionMap())
        ..['due_date'] = '2099-12-31'
        ..['status'] = 'aberta';
      expect(CorrectiveAction.fromMap(map).isOverdue, isFalse);
    });
  });
}
```

Confirmar que os testes falham (RED) com `cd primeaudit && flutter test test/models/corrective_action_test.dart -x` (esperado: erro de import).

PASSO 2 — Implementar o model (GREEN):

Criar `primeaudit/lib/models/corrective_action.dart`:

```dart
import 'package:flutter/material.dart';
import '../core/app_colors.dart';

enum CorrectiveActionStatus {
  aberta,
  emAndamento,
  emAvaliacao,
  aprovada,
  rejeitada,
  cancelada;

  String get dbValue {
    switch (this) {
      case aberta:       return 'aberta';
      case emAndamento:  return 'em_andamento';
      case emAvaliacao:  return 'em_avaliacao';
      case aprovada:     return 'aprovada';
      case rejeitada:    return 'rejeitada';
      case cancelada:    return 'cancelada';
    }
  }

  String get label {
    switch (this) {
      case aberta:       return 'Aberta';
      case emAndamento:  return 'Em andamento';
      case emAvaliacao:  return 'Em avaliacao';
      case aprovada:     return 'Aprovada';
      case rejeitada:    return 'Rejeitada';
      case cancelada:    return 'Cancelada';
    }
  }

  Color get chipBackground {
    switch (this) {
      case aberta:       return Colors.orange.shade100;
      case emAndamento:  return const Color(0xFFE3F2FD);
      case emAvaliacao:  return Colors.purple.shade50;
      case aprovada:     return const Color(0xFFE8F5E9);
      case rejeitada:    return const Color(0xFFFFEBEE);
      case cancelada:    return Colors.grey.shade100;
    }
  }

  Color get chipText {
    switch (this) {
      case aberta:       return Colors.orange.shade800;
      case emAndamento:  return const Color(0xFF1565C0);
      case emAvaliacao:  return Colors.purple.shade800;
      case aprovada:     return const Color(0xFF2E7D32);
      case rejeitada:    return AppColors.error;
      case cancelada:    return Colors.grey.shade700;
    }
  }

  IconData get icon {
    switch (this) {
      case aberta:       return Icons.radio_button_unchecked_rounded;
      case emAndamento:  return Icons.pending_rounded;
      case emAvaliacao:  return Icons.rate_review_rounded;
      case aprovada:     return Icons.check_circle_rounded;
      case rejeitada:    return Icons.cancel_rounded;
      case cancelada:    return Icons.block_rounded;
    }
  }

  bool get isFinal =>
      this == aprovada || this == rejeitada || this == cancelada;

  static CorrectiveActionStatus fromDb(String? value) {
    switch (value) {
      case 'em_andamento':  return CorrectiveActionStatus.emAndamento;
      case 'em_avaliacao':  return CorrectiveActionStatus.emAvaliacao;
      case 'aprovada':      return CorrectiveActionStatus.aprovada;
      case 'rejeitada':     return CorrectiveActionStatus.rejeitada;
      case 'cancelada':     return CorrectiveActionStatus.cancelada;
      default:              return CorrectiveActionStatus.aberta;
    }
  }
}

class CorrectiveAction {
  final String id;
  final String auditId;
  final String templateItemId;
  final String title;
  final String? description;
  final String responsibleUserId;
  final String? responsibleName;     // via join profiles!responsible_user_id(full_name)
  final DateTime dueDate;
  final CorrectiveActionStatus status;
  final String companyId;
  final String createdBy;
  final DateTime createdAt;
  final DateTime updatedAt;
  final String? linkedAuditTitle;    // via join audits(title)

  const CorrectiveAction({
    required this.id,
    required this.auditId,
    required this.templateItemId,
    required this.title,
    this.description,
    required this.responsibleUserId,
    this.responsibleName,
    required this.dueDate,
    required this.status,
    required this.companyId,
    required this.createdBy,
    required this.createdAt,
    required this.updatedAt,
    this.linkedAuditTitle,
  });

  factory CorrectiveAction.fromMap(Map<String, dynamic> map) {
    return CorrectiveAction(
      id: map['id'] as String,
      auditId: map['audit_id'] as String,
      templateItemId: map['template_item_id'] as String,
      title: map['title'] as String,
      description: map['description'] as String?,
      responsibleUserId: map['responsible_user_id'] as String,
      responsibleName: (map['profiles'] as Map<String, dynamic>?)?['full_name'] as String?,
      dueDate: DateTime.parse(map['due_date'] as String),
      status: CorrectiveActionStatus.fromDb(map['status'] as String?),
      companyId: map['company_id'] as String,
      createdBy: map['created_by'] as String,
      createdAt: DateTime.parse(map['created_at'] as String),
      updatedAt: DateTime.parse(map['updated_at'] as String),
      linkedAuditTitle: (map['audits'] as Map<String, dynamic>?)?['title'] as String?,
    );
  }

  bool get isOverdue =>
      dueDate.isBefore(DateTime.now()) && !status.isFinal;
}
```

PASSO 3 — Confirmar GREEN: `cd primeaudit && flutter test test/models/corrective_action_test.dart -x`
  </action>

  <verify>
    <automated>cd primeaudit && flutter test test/models/corrective_action_test.dart -x</automated>
  </verify>

  <acceptance_criteria>
    - `flutter test test/models/corrective_action_test.dart -x` termina com exit code 0
    - `grep "class CorrectiveAction" primeaudit/lib/models/corrective_action.dart` retorna match
    - `grep "enum CorrectiveActionStatus" primeaudit/lib/models/corrective_action.dart` retorna match
    - `grep "static CorrectiveActionStatus fromDb" primeaudit/lib/models/corrective_action.dart` retorna match
    - `grep "bool get isFinal" primeaudit/lib/models/corrective_action.dart` retorna match
    - `grep "bool get isOverdue" primeaudit/lib/models/corrective_action.dart` retorna match
    - Todos os 20+ casos de teste passam (fromMap, fromDb x8, isFinal x6, isOverdue x3)
  </acceptance_criteria>

  <done>CorrectiveAction model completo com enum de 6 estados, fromMap com joins, isFinal, isOverdue — todos os testes verdes</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: CorrectiveActionService — CRUD + static isNonConforming + static canTransitionTo + unit tests</name>
  <files>
    primeaudit/lib/services/corrective_action_service.dart
    primeaudit/test/services/corrective_action_service_test.dart
  </files>

  <read_first>
    - primeaudit/lib/services/audit_answer_service.dart — padrão exato: _client field, Future<T> sem try/catch, static calculateConformity para testability
    - primeaudit/lib/services/dashboard_service.dart — padrão de count query com inFilter
    - primeaudit/lib/core/app_roles.dart — AppRole.canAccessAdmin(), AppRole.isSuperOrDev(), AppRole.auditor constant
    - primeaudit/test/services/dashboard_service_test.dart — padrão de test sem instanciar service (apenas funções static)
    - .planning/phases/08-corrective-actions/08-RESEARCH.md §Pattern 1 (service) + §Pattern 3 (isNonConforming) + §Pattern 7 (RBAC canTransitionTo)
    - .planning/phases/08-corrective-actions/08-PATTERNS.md §corrective_action_service.dart
  </read_first>

  <behavior>
    isNonConforming:
    - Test: isNonConforming('ok_nok', 'ok') == false
    - Test: isNonConforming('ok_nok', 'nok') == true
    - Test: isNonConforming('yes_no', 'yes') == false
    - Test: isNonConforming('yes_no', 'no') == true
    - Test: isNonConforming('scale_1_5', '1') == true
    - Test: isNonConforming('scale_1_5', '2') == true
    - Test: isNonConforming('scale_1_5', '3') == false
    - Test: isNonConforming('scale_1_5', '5') == false
    - Test: isNonConforming('percentage', '49') == true
    - Test: isNonConforming('percentage', '50') == false
    - Test: isNonConforming('percentage', '100') == false
    - Test: isNonConforming('text', 'qualquer coisa') == true (sempre quando respondido)
    - Test: isNonConforming('selection', 'opcao_a') == true (sempre quando respondido)
    - Test: isNonConforming('ok_nok', null) == false (sem resposta = sem icone)
    - Test: isNonConforming('ok_nok', '') == false (vazio = sem icone)

    canTransitionTo (admin):
    - Test: adm pode transicionar para qualquer status
    - Test: superuser pode transicionar para qualquer status
    - Test: dev pode transicionar para qualquer status

    canTransitionTo (responsavel):
    - Test: responsavel pode mover aberta -> em_andamento
    - Test: responsavel pode mover em_andamento -> em_avaliacao
    - Test: responsavel pode re-abrir rejeitada -> em_andamento
    - Test: responsavel NAO pode cancelar (apenas admin)
    - Test: responsavel NAO pode aprovar (apenas auditor/admin)

    canTransitionTo (auditor nao-responsavel):
    - Test: auditor pode mover em_avaliacao -> aprovada
    - Test: auditor pode mover em_avaliacao -> rejeitada
    - Test: auditor NAO pode mover aberta -> em_andamento (precisa ser responsavel)
    - Test: auditor NAO pode cancelar
  </behavior>

  <action>
PASSO 1 — Escrever os testes primeiro (RED):

Criar `primeaudit/test/services/corrective_action_service_test.dart`:

```dart
// Unit tests para funcoes estaticas de CorrectiveActionService.
// NAO instancia CorrectiveActionService (Supabase.instance.client throws em testes).
// Testa apenas CorrectiveActionService.isNonConforming (static) e
// CorrectiveActionService.canTransitionTo (static).
import 'package:flutter_test/flutter_test.dart';
import 'package:primeaudit/services/corrective_action_service.dart';
import 'package:primeaudit/models/corrective_action.dart';

CorrectiveAction _action({
  String id = 'ca1',
  String responsibleUserId = 'user_resp',
  CorrectiveActionStatus status = CorrectiveActionStatus.aberta,
}) {
  return CorrectiveAction(
    id: id,
    auditId: 'a1',
    templateItemId: 'ti1',
    title: 'Test',
    responsibleUserId: responsibleUserId,
    dueDate: DateTime(2027, 1, 1),
    status: status,
    companyId: 'c1',
    createdBy: 'u2',
    createdAt: DateTime(2026, 4, 25),
    updatedAt: DateTime(2026, 4, 25),
  );
}

void main() {
  group('CorrectiveActionService.isNonConforming — ok_nok', () {
    test("'ok' retorna false", () => expect(CorrectiveActionService.isNonConforming('ok_nok', 'ok'), isFalse));
    test("'nok' retorna true", () => expect(CorrectiveActionService.isNonConforming('ok_nok', 'nok'), isTrue));
    test('null retorna false', () => expect(CorrectiveActionService.isNonConforming('ok_nok', null), isFalse));
    test('vazio retorna false', () => expect(CorrectiveActionService.isNonConforming('ok_nok', ''), isFalse));
  });

  group('CorrectiveActionService.isNonConforming — yes_no', () {
    test("'yes' retorna false", () => expect(CorrectiveActionService.isNonConforming('yes_no', 'yes'), isFalse));
    test("'no' retorna true", () => expect(CorrectiveActionService.isNonConforming('yes_no', 'no'), isTrue));
  });

  group('CorrectiveActionService.isNonConforming — scale_1_5', () {
    test("'1' retorna true (<=2)", () => expect(CorrectiveActionService.isNonConforming('scale_1_5', '1'), isTrue));
    test("'2' retorna true (<=2)", () => expect(CorrectiveActionService.isNonConforming('scale_1_5', '2'), isTrue));
    test("'3' retorna false (>=3)", () => expect(CorrectiveActionService.isNonConforming('scale_1_5', '3'), isFalse));
    test("'5' retorna false", () => expect(CorrectiveActionService.isNonConforming('scale_1_5', '5'), isFalse));
  });

  group('CorrectiveActionService.isNonConforming — percentage', () {
    test("'49' retorna true (<50)", () => expect(CorrectiveActionService.isNonConforming('percentage', '49'), isTrue));
    test("'50' retorna false (>=50)", () => expect(CorrectiveActionService.isNonConforming('percentage', '50'), isFalse));
    test("'100' retorna false", () => expect(CorrectiveActionService.isNonConforming('percentage', '100'), isFalse));
  });

  group('CorrectiveActionService.isNonConforming — text e selection', () {
    test('text com conteudo retorna true', () => expect(CorrectiveActionService.isNonConforming('text', 'qualquer'), isTrue));
    test('selection com opcao retorna true', () => expect(CorrectiveActionService.isNonConforming('selection', 'opcao_a'), isTrue));
    test('text null retorna false', () => expect(CorrectiveActionService.isNonConforming('text', null), isFalse));
  });

  group('CorrectiveActionService.canTransitionTo — admin/superuser/dev', () {
    test('adm pode transicionar para qualquer status', () {
      expect(CorrectiveActionService.canTransitionTo(
        newStatus: 'cancelada', action: _action(), role: 'adm', userId: 'outro',
      ), isTrue);
    });
    test('superuser pode transicionar para qualquer status', () {
      expect(CorrectiveActionService.canTransitionTo(
        newStatus: 'cancelada', action: _action(), role: 'superuser', userId: 'outro',
      ), isTrue);
    });
    test('dev pode transicionar para qualquer status', () {
      expect(CorrectiveActionService.canTransitionTo(
        newStatus: 'aprovada', action: _action(status: CorrectiveActionStatus.emAvaliacao),
        role: 'dev', userId: 'outro',
      ), isTrue);
    });
  });

  group('CorrectiveActionService.canTransitionTo — responsavel', () {
    test('responsavel pode mover aberta -> em_andamento', () {
      expect(CorrectiveActionService.canTransitionTo(
        newStatus: 'em_andamento',
        action: _action(status: CorrectiveActionStatus.aberta, responsibleUserId: 'user_resp'),
        role: 'auditor', userId: 'user_resp',
      ), isTrue);
    });
    test('responsavel pode mover em_andamento -> em_avaliacao', () {
      expect(CorrectiveActionService.canTransitionTo(
        newStatus: 'em_avaliacao',
        action: _action(status: CorrectiveActionStatus.emAndamento, responsibleUserId: 'user_resp'),
        role: 'auditor', userId: 'user_resp',
      ), isTrue);
    });
    test('responsavel pode re-abrir rejeitada -> em_andamento', () {
      expect(CorrectiveActionService.canTransitionTo(
        newStatus: 'em_andamento',
        action: _action(status: CorrectiveActionStatus.rejeitada, responsibleUserId: 'user_resp'),
        role: 'auditor', userId: 'user_resp',
      ), isTrue);
    });
    test('responsavel NAO pode cancelar', () {
      expect(CorrectiveActionService.canTransitionTo(
        newStatus: 'cancelada',
        action: _action(responsibleUserId: 'user_resp'),
        role: 'auditor', userId: 'user_resp',
      ), isFalse);
    });
    test('responsavel NAO pode aprovar (precisa ser auditor/admin)', () {
      expect(CorrectiveActionService.canTransitionTo(
        newStatus: 'aprovada',
        action: _action(status: CorrectiveActionStatus.emAvaliacao, responsibleUserId: 'user_resp'),
        role: 'auditor', userId: 'user_resp',
      ), isFalse);
    });
  });

  group('CorrectiveActionService.canTransitionTo — auditor nao-responsavel', () {
    test('auditor pode aprovar em_avaliacao', () {
      expect(CorrectiveActionService.canTransitionTo(
        newStatus: 'aprovada',
        action: _action(status: CorrectiveActionStatus.emAvaliacao, responsibleUserId: 'outro_user'),
        role: 'auditor', userId: 'auditor_user',
      ), isTrue);
    });
    test('auditor pode rejeitar em_avaliacao', () {
      expect(CorrectiveActionService.canTransitionTo(
        newStatus: 'rejeitada',
        action: _action(status: CorrectiveActionStatus.emAvaliacao, responsibleUserId: 'outro_user'),
        role: 'auditor', userId: 'auditor_user',
      ), isTrue);
    });
    test('auditor NAO pode mover aberta -> em_andamento (somente responsavel/admin)', () {
      expect(CorrectiveActionService.canTransitionTo(
        newStatus: 'em_andamento',
        action: _action(status: CorrectiveActionStatus.aberta, responsibleUserId: 'outro_user'),
        role: 'auditor', userId: 'auditor_user',
      ), isFalse);
    });
    test('auditor NAO pode cancelar', () {
      expect(CorrectiveActionService.canTransitionTo(
        newStatus: 'cancelada',
        action: _action(responsibleUserId: 'outro_user'),
        role: 'auditor', userId: 'auditor_user',
      ), isFalse);
    });
  });
}
```

Confirmar RED: `cd primeaudit && flutter test test/services/corrective_action_service_test.dart -x`

PASSO 2 — Implementar o service (GREEN):

Criar `primeaudit/lib/services/corrective_action_service.dart`:

```dart
import 'package:supabase_flutter/supabase_flutter.dart';
import '../models/corrective_action.dart';
import '../core/app_roles.dart';

// Sem try/catch dentro do service — callers (screens) sao responsaveis pelo tratamento de erros.
class CorrectiveActionService {
  final _client = Supabase.instance.client;

  Future<List<CorrectiveAction>> getActions({
    required String? companyId,
    String? statusFilter,
    String? responsibleFilter,
  }) async {
    // profiles!responsible_user_id(...) desambigua FK — ver RESEARCH.md Pitfall 5
    var query = _client
        .from('corrective_actions')
        .select('*, profiles!responsible_user_id(full_name), audits(title)');
    if (companyId != null) query = query.eq('company_id', companyId);
    if (statusFilter != null) query = query.eq('status', statusFilter);
    if (responsibleFilter != null) {
      query = query.eq('responsible_user_id', responsibleFilter);
    }
    final data = await query.order('created_at', ascending: false);
    return (data as List).map((e) => CorrectiveAction.fromMap(e as Map<String, dynamic>)).toList();
  }

  Future<void> createAction({
    required String auditId,
    required String templateItemId,
    required String title,
    String? description,
    required String responsibleUserId,
    required DateTime dueDate,
    required String companyId,
    required String createdBy,
  }) async {
    await _client.from('corrective_actions').insert({
      'audit_id': auditId,
      'template_item_id': templateItemId,
      'title': title,
      'description': description,
      'responsible_user_id': responsibleUserId,
      'due_date': dueDate.toIso8601String().substring(0, 10), // DATE format YYYY-MM-DD
      'status': 'aberta',
      'company_id': companyId,
      'created_by': createdBy,
    });
  }

  Future<void> updateStatus(String id, String newStatus) async {
    await _client
        .from('corrective_actions')
        .update({
          'status': newStatus,
          'updated_at': DateTime.now().toIso8601String(),
        })
        .eq('id', id);
  }

  Future<int> getOpenActionsCount(String? companyId) async {
    var query = _client
        .from('corrective_actions')
        .select('id')
        .inFilter('status', ['aberta', 'em_andamento', 'em_avaliacao']);
    if (companyId != null) query = query.eq('company_id', companyId);
    final data = await query;
    return (data as List).length;
  }

  /// Determina se uma resposta para um item e nao-conforme (per D-01 do CONTEXT.md).
  /// Funcao estatica para testabilidade sem instancia do Supabase client.
  static bool isNonConforming(String responseType, String? answer) {
    if (answer == null || answer.isEmpty) return false;
    switch (responseType) {
      case 'ok_nok':
        return answer == 'nok';
      case 'yes_no':
        return answer == 'no';
      case 'scale_1_5':
        return (int.tryParse(answer) ?? 99) <= 2;
      case 'percentage':
        return (double.tryParse(answer) ?? 100.0) < 50.0;
      case 'text':
        return answer.isNotEmpty; // icone sempre visivel quando respondido
      case 'selection':
        return answer.isNotEmpty; // sem pesos por opcao na estrutura atual (TemplateItem.options = List<String>)
      default:
        return false;
    }
  }

  /// Determina se o usuario (role + userId) pode transicionar a acao para newStatus.
  /// Funcao estatica para testabilidade (mesma abordagem que AuditAnswerService.calculateConformity).
  /// Ver RBAC matrix em 08-UI-SPEC.md §RBAC Transition Matrix.
  static bool canTransitionTo({
    required String newStatus,
    required CorrectiveAction action,
    required String role,
    required String userId,
  }) {
    final isAdmin = AppRole.canAccessAdmin(role);
    final isSuperDev = AppRole.isSuperOrDev(role);
    if (isAdmin || isSuperDev) return true; // adm/superuser/dev: todas as transicoes

    final isResponsible = action.responsibleUserId == userId;

    switch (newStatus) {
      case 'em_andamento':
        // aberta -> em_andamento: responsavel (inclui re-abertura apos rejeicao)
        return isResponsible &&
            (action.status == CorrectiveActionStatus.aberta ||
                action.status == CorrectiveActionStatus.rejeitada);
      case 'em_avaliacao':
        // em_andamento -> em_avaliacao: responsavel
        return isResponsible && action.status == CorrectiveActionStatus.emAndamento;
      case 'aprovada':
      case 'rejeitada':
        // em_avaliacao -> aprovada/rejeitada: auditor nao-responsavel
        return !isResponsible &&
            role == AppRole.auditor &&
            action.status == CorrectiveActionStatus.emAvaliacao;
      case 'cancelada':
        // somente admin/superDev (ja retornou true acima)
        return false;
      default:
        return false;
    }
  }
}
```

PASSO 3 — Confirmar GREEN: `cd primeaudit && flutter test test/services/corrective_action_service_test.dart -x`

PASSO 4 — Confirmar suite completa: `cd primeaudit && flutter test`
  </action>

  <verify>
    <automated>cd primeaudit && flutter test test/models/corrective_action_test.dart test/services/corrective_action_service_test.dart -x</automated>
  </verify>

  <acceptance_criteria>
    - `flutter test test/models/corrective_action_test.dart test/services/corrective_action_service_test.dart -x` termina com exit code 0
    - `grep "static bool isNonConforming" primeaudit/lib/services/corrective_action_service.dart` retorna match
    - `grep "static bool canTransitionTo" primeaudit/lib/services/corrective_action_service.dart` retorna match
    - `grep "Future<void> createAction" primeaudit/lib/services/corrective_action_service.dart` retorna match
    - `grep "Future<void> updateStatus" primeaudit/lib/services/corrective_action_service.dart` retorna match
    - `grep "Future<int> getOpenActionsCount" primeaudit/lib/services/corrective_action_service.dart` retorna match
    - `grep "inFilter" primeaudit/lib/services/corrective_action_service.dart` retorna match (conta aberta+em_andamento+em_avaliacao)
    - Todos os testes de isNonConforming (15 casos) e canTransitionTo (12 casos) passam
    - Suite completa `flutter test` passa
  </acceptance_criteria>

  <done>CorrectiveActionService completo com CRUD, static isNonConforming e static canTransitionTo — todos os 27+ testes de unit verdes, suite completa verde</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flutter app → Supabase PostgREST | Dados de corrective_actions cruzam esta fronteira — companyId, userId, status |
| Supabase JWT → RLS functions | get_my_role() e get_my_company_id() extraem claims do JWT — ativo por design |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-01 | Elevation of Privilege | RLS policy `auditor_corrective_actions_select` | mitigate | Policy usa `get_my_company_id()` que inclui guard `active=true` (fix SEC-03 ja aplicado em 20260418_fix_active_guard.sql) — auditor de empresa A nao ve acoes de empresa B |
| T-8-02 | Elevation of Privilege | RLS `auditor_corrective_actions_insert` — `created_by = auth.uid()` | mitigate | Auditor somente insere registros onde `created_by` e seu proprio UID; nao pode criar em nome de outro usuario |
| T-8-03 | Elevation of Privilege | RLS `auditor_corrective_actions_update` — sem restricao de coluna | accept | UI (Wave 4) restringe quais colunas mudam (apenas `status`); RLS de UPDATE cobre company scope; status escalation e bloqueado na UI via `canTransitionTo()` — per CONTEXT.md "RLS nao precisa bloquear (UI e suficiente nesta milestone)" |
| T-8-04 | Tampering | Constraint `corrective_actions_status_check` | mitigate | CHECK constraint no banco rejeita qualquer valor de status fora do enum de 6 estados, independente da UI |
| T-8-05 | Denial of Service | get_my_role() retorna NULL para usuario inativo | accept | get_my_role() e get_my_company_id() ja incluem `AND active = true` guard (SEC-03 — 20260418_fix_active_guard.sql) — usuario inativo nao passa nas policies, dados nao retornados |
</threat_model>

<verification>
Wave 1 completa quando:
1. `primeaudit/supabase/migrations/20260425_create_corrective_actions.sql` existe com CREATE TABLE IF NOT EXISTS, 5 FKs idempotentes, CHECK constraint, 5 indexes, 5 RLS policies, NOTIFY pgrst
2. `supabase db push` terminou sem ERROR/FATAL — tabela existe no banco
3. `cd primeaudit && flutter test test/models/corrective_action_test.dart test/services/corrective_action_service_test.dart -x` — exit code 0
4. `cd primeaudit && flutter test` — suite completa verde
</verification>

<success_criteria>
- Migration idempotente aplicada: tabela corrective_actions existe com RLS habilitado
- CorrectiveAction.fromMap() parseia todos os 13 campos incluindo joins de profiles e audits
- CorrectiveActionStatus enum: 6 valores, dbValue, label, chipBackground, chipText, icon, isFinal, fromDb com fallback
- CorrectiveActionService.isNonConforming() cobre ok_nok, yes_no, scale_1_5, percentage, text, selection, null/empty
- CorrectiveActionService.canTransitionTo() aplica RBAC: admin (tudo), responsavel (em_andamento + em_avaliacao + re-abrir), auditor (aprovada + rejeitada), ninguem-fora-admin (cancelada)
- Todos os testes unitarios passam sem Supabase client
- Suite `flutter test` verde
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/08-corrective-actions/08-01-SUMMARY.md` seguindo o template em `@C:\Users\eder3\.claude\get-shit-done\templates\summary.md`
</output>
