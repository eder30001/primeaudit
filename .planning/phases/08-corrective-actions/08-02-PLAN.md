---
phase: 08-corrective-actions
plan: 02
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - primeaudit/lib/screens/create_corrective_action_screen.dart
  - primeaudit/lib/screens/audit_execution_screen.dart
  - primeaudit/lib/services/user_service.dart
  - primeaudit/lib/services/dashboard_service.dart
autonomous: true
requirements:
  - ACT-02

must_haves:
  truths:
    - "Durante execucao de auditoria, o auditor ve icone de acao em itens com resposta nao-conforme"
    - "Tocar no icone abre CreateCorrectiveActionScreen via Navigator.push"
    - "O formulario tem campos titulo (obrigatorio), responsavel (dropdown de usuarios da empresa, obrigatorio), prazo (date picker, obrigatorio), descricao (opcional)"
    - "Responsavel e sempre um usuario do sistema — UUID, nao texto livre"
    - "Submissao bem-sucedida cria registro em corrective_actions e retorna para tela de execucao"
    - "DashboardService.getOpenActionsCount usa inFilter para aberta+em_andamento+em_avaliacao"
  artifacts:
    - path: "primeaudit/lib/screens/create_corrective_action_screen.dart"
      provides: "Formulario de criacao de acao corretiva"
      exports: ["CreateCorrectiveActionScreen"]
    - path: "primeaudit/lib/screens/audit_execution_screen.dart"
      provides: "Icone condicional por item nao-conforme em _ItemCard"
      contains: "onCreateAction"
    - path: "primeaudit/lib/services/user_service.dart"
      provides: "getByCompany() method para dropdown de responsavel"
      contains: "getByCompany"
    - path: "primeaudit/lib/services/dashboard_service.dart"
      provides: "getOpenActionsCount com inFilter corrigido"
      contains: "inFilter"
  key_links:
    - from: "audit_execution_screen.dart _ItemCard"
      to: "create_corrective_action_screen.dart"
      via: "Navigator.push(MaterialPageRoute) quando onCreateAction != null && isNonConforming"
      pattern: "CreateCorrectiveActionScreen"
    - from: "create_corrective_action_screen.dart"
      to: "user_service.dart getByCompany"
      via: "_userService.getByCompany(companyId) em _load()"
      pattern: "getByCompany"
    - from: "create_corrective_action_screen.dart"
      to: "corrective_action_service.dart createAction"
      via: "_service.createAction(...) em _save()"
      pattern: "_service.createAction"
---

<objective>
Entregar o fluxo de criacao de acao corretiva completo: (1) `UserService.getByCompany()` para popular o dropdown de responsavel; (2) `DashboardService.getOpenActionsCount` corrigido para usar inFilter; (3) `CreateCorrectiveActionScreen` com o formulario de 4 campos; (4) modificacoes em `audit_execution_screen.dart` para adicionar o icone condicional por item nao-conforme.

Purpose: ACT-02 — auditor cria acao durante execucao. Este e o ponto de entrada de todo o fluxo CAPA.

Output: create_corrective_action_screen.dart, audit_execution_screen.dart modificado, user_service.dart com getByCompany, dashboard_service.dart corrigido.
</objective>

<execution_context>
@C:\Users\eder3\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\eder3\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-corrective-actions/08-CONTEXT.md
@.planning/phases/08-corrective-actions/08-RESEARCH.md
@.planning/phases/08-corrective-actions/08-PATTERNS.md
@.planning/phases/08-corrective-actions/08-UI-SPEC.md
@.planning/phases/08-corrective-actions/08-01-SUMMARY.md

<interfaces>
<!-- Contratos criados na Wave 1 que esta wave consome. Ler estes antes de implementar. -->

De primeaudit/lib/models/corrective_action.dart (Wave 1):
```dart
class CorrectiveAction { final String id; final String auditId; final String templateItemId; ... }
enum CorrectiveActionStatus { aberta, emAndamento, ... }
```

De primeaudit/lib/services/corrective_action_service.dart (Wave 1):
```dart
class CorrectiveActionService {
  Future<void> createAction({
    required String auditId,
    required String templateItemId,
    required String title,
    String? description,
    required String responsibleUserId,
    required DateTime dueDate,
    required String companyId,
    required String createdBy,
  }) async { ... }

  static bool isNonConforming(String responseType, String? answer) { ... }
}
```

De primeaudit/lib/models/app_user.dart:
```dart
class AppUser { final String id; final String fullName; ... factory AppUser.fromMap(Map<String,dynamic>); }
```

De primeaudit/lib/screens/audit_execution_screen.dart (linhas chave confirmadas em RESEARCH.md):
- _SectionBlock constructor: section, answers, observations, indexMap, readOnly, onAnswer, onObservation, theme (linhas 789-798)
- _ItemCard constructor: item, index, answer, observation, readOnly, onAnswer, onObservation, theme (linhas 869-878)
- _buildBody(): site de chamada do _SectionBlock (linha ~585-594)
- widget.audit: objeto Audit disponivel em _AuditExecutionScreenState

De primeaudit/lib/services/dashboard_service.dart (current getOpenActionsCount — BUG):
```dart
// Atual (buggy): queries apenas status='aberta'
Future<int> getOpenActionsCount(String? companyId) async {
  try {
    var query = _client.from('corrective_actions').select('id').eq('status', 'aberta');
    ...
  } catch (_) { return 0; }  // fallback Phase 7
}
// Corrigir para: .inFilter('status', ['aberta', 'em_andamento', 'em_avaliacao'])
// E remover try/catch (tabela existe apos migration da Wave 1)
```

De primeaudit/lib/services/user_service.dart (getAll() como referencia para getByCompany()):
```dart
// Adicionar APOS getAll():
Future<List<AppUser>> getByCompany(String companyId) async {
  final data = await _client
      .from('profiles')
      .select('*, companies(name)')
      .eq('company_id', companyId)
      .eq('active', true)
      .order('full_name');
  return (data as List).map((e) => AppUser.fromMap(e as Map<String,dynamic>)).toList();
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: UserService.getByCompany + DashboardService.getOpenActionsCount fix</name>
  <files>
    primeaudit/lib/services/user_service.dart
    primeaudit/lib/services/dashboard_service.dart
  </files>

  <read_first>
    - primeaudit/lib/services/user_service.dart — ler o arquivo completo para entender getAll() (linhas 19-33 referenciadas em PATTERNS.md) e posicionar getByCompany() logo apos getAll()
    - primeaudit/lib/services/dashboard_service.dart — ler o arquivo completo para identificar getOpenActionsCount (linhas 13-27 referenciadas em RESEARCH.md) e corrigir in-place
    - .planning/phases/08-corrective-actions/08-RESEARCH.md §Pattern 6 (UserService.getByCompany) e §Pitfall 1 (DashboardService bug)
    - .planning/phases/08-corrective-actions/08-PATTERNS.md §user_service.dart e §dashboard_service.dart
  </read_first>

  <action>
MODIFICACAO 1: user_service.dart — adicionar getByCompany()

Ler primeaudit/lib/services/user_service.dart na integra. Localizar o metodo getAll(). Adicionar o metodo getByCompany IMEDIATAMENTE APOS getAll(), antes do proximo metodo:

```dart
/// Retorna usuarios ativos da empresa pelo companyId.
/// Usado no dropdown de responsavel em CreateCorrectiveActionScreen.
/// NAO chama _getMyProfile() — scoping direto por companyId fornecido pelo caller.
Future<List<AppUser>> getByCompany(String companyId) async {
  final data = await _client
      .from('profiles')
      .select('*, companies(name)')
      .eq('company_id', companyId)
      .eq('active', true)
      .order('full_name');
  return (data as List).map((e) => AppUser.fromMap(e as Map<String, dynamic>)).toList();
}
```

MODIFICACAO 2: dashboard_service.dart — corrigir getOpenActionsCount()

Ler primeaudit/lib/services/dashboard_service.dart na integra. Localizar getOpenActionsCount(). Substituir o metodo completo:

```dart
Future<int> getOpenActionsCount(String? companyId) async {
  // Contagem de todas as acoes nao-finalizadas: aberta + em_andamento + em_avaliacao
  // try/catch removido — tabela corrective_actions existe apos migration da Wave 1 (Phase 8)
  var query = _client
      .from('corrective_actions')
      .select('id')
      .inFilter('status', ['aberta', 'em_andamento', 'em_avaliacao']);
  if (companyId != null) {
    query = query.eq('company_id', companyId);
  }
  final data = await query;
  return (data as List).length;
}
```

ATENCAO: Se o DashboardService ainda tiver um try/catch envolvendo apenas getOpenActionsCount (nao outros metodos), remover apenas o try/catch deste metodo. Nao alterar outros metodos do arquivo.
  </action>

  <verify>
    <automated>cd primeaudit && flutter analyze lib/services/user_service.dart lib/services/dashboard_service.dart --no-fatal-infos 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `grep "Future<List<AppUser>> getByCompany" primeaudit/lib/services/user_service.dart` retorna match
    - `grep "eq('active', true)" primeaudit/lib/services/user_service.dart` retorna match em getByCompany
    - `grep "inFilter" primeaudit/lib/services/dashboard_service.dart` retorna match
    - `grep "em_andamento" primeaudit/lib/services/dashboard_service.dart` retorna match
    - `flutter analyze lib/services/user_service.dart lib/services/dashboard_service.dart` sem erros de compilacao
    - `cd primeaudit && flutter test` passa (nao introduziu regressoes)
  </acceptance_criteria>

  <done>UserService tem getByCompany(); DashboardService.getOpenActionsCount usa inFilter para os 3 estados nao-finais sem try/catch</done>
</task>

<task type="auto">
  <name>Task 2: CreateCorrectiveActionScreen — formulario completo com validacao (D-02, D-03)</name>
  <files>primeaudit/lib/screens/create_corrective_action_screen.dart</files>

  <read_first>
    - primeaudit/lib/screens/audits_screen.dart — padrão de formulario: _NewAuditSheet (linhas 399-553), DropdownButtonFormField para tipo, TextFormField com validator, ElevatedButton para submit, Navigator.pop no sucesso
    - primeaudit/lib/models/audit.dart — Audit class (campos id, title, etc.) para entender o que e passado ao constructor
    - primeaudit/lib/models/audit_template.dart — TemplateItem class (campos id, question, responseType) para entender o que e passado ao constructor
    - primeaudit/lib/models/app_user.dart — AppUser class (id, fullName) para o dropdown
    - .planning/phases/08-corrective-actions/08-PATTERNS.md §create_corrective_action_screen.dart — padroes completos de constructor, _load(), _pickDate(), _save()
    - .planning/phases/08-corrective-actions/08-UI-SPEC.md §Action Creation Screen e §Copywriting Contract §Action Creation Screen
  </read_first>

  <action>
Criar `primeaudit/lib/screens/create_corrective_action_screen.dart` do zero.

O arquivo deve implementar exatamente:
- `CreateCorrectiveActionScreen extends StatefulWidget` com `required Audit audit` e `required TemplateItem item` (per D-02: nova tela, nao bottom sheet)
- `_CreateCorrectiveActionScreenState` com: `_service = CorrectiveActionService()`, `_userService = UserService()`, `_formKey`, `_titleCtrl`, `_descCtrl`, `_users = []`, `_selectedUser`, `_dueDate`, `_isLoading = true`, `_isSaving = false`, `_error`
- `initState()` chama `_load()`
- `_load()`: busca `UserService.getByCompany(companyId)` — se companyId for null, mostra erro "Empresa nao selecionada"
- `_pickDate()`: usa `showDatePicker` com `initialDate: DateTime.now().add(Duration(days: 1))`, `firstDate: DateTime.now()`, `lastDate: DateTime.now().add(Duration(days: 365*2))`
- `_save()`: valida form (`_formKey.currentState!.validate()`), verifica `_dueDate != null`, verifica `_selectedUser != null`, chama `_service.createAction(...)`, em sucesso chama `Navigator.pop(context, true)` e exibe SnackBar "Acao corretiva criada com sucesso" — IMPORTANTE: o snackbar de sucesso deve ser exibido ANTES de Navigator.pop ou usar o contexto do caller via retorno true
- `_snack(String msg)`: `ScaffoldMessenger.of(context).showSnackBar(SnackBar(content: Text(msg), behavior: SnackBarBehavior.floating))`

Strings exatas do Copywriting Contract (08-UI-SPEC.md — copiar verbatim):
- AppBar title: 'Nova Acao Corretiva'
- Label titulo: 'Titulo' — hint: 'Descreva brevemente o problema'
- Label responsavel: 'Responsavel' — hint: 'Selecione o responsavel'
- Label prazo: 'Prazo' — hint: 'Selecione a data limite'
- Label descricao: 'Descricao / Observacao' — hint: 'Detalhes adicionais (opcional)'
- Banner "Pergunta vinculada": label 'Pergunta vinculada' + texto `widget.item.question`
- CTA: 'Criar acao'
- Validator titulo: 'O titulo e obrigatorio'
- Validator prazo passado: 'O prazo deve ser uma data futura'
- Error snackbar: 'Erro ao criar acao. Tente novamente.'

Layout (per 08-UI-SPEC.md §Action Creation Screen):
- Scaffold > AppBar (backgroundColor: AppColors.primary, foregroundColor: Colors.white)
- Body: SingleChildScrollView (keyboardDismissBehavior: ScrollViewKeyboardDismissBehavior.onDrag) > Padding(all: 16) > Form > Column
- Se _isLoading (loading users): Center(CircularProgressIndicator(color: AppColors.primary))
- Se _error != null: Center com texto de erro + TextButton('Tentar novamente', _load)
- Caso contrario: formulario completo
- Order dos campos: banner read-only (pergunta vinculada), SizedBox(16), titulo TextFormField, SizedBox(16), responsavel DropdownButtonFormField, SizedBox(16), prazo TextFormField(readOnly:true) + onTap:_pickDate, SizedBox(16), descricao TextFormField(maxLines:4), SizedBox(24), botao submit ElevatedButton full-width

Banner "Pergunta vinculada":
```dart
Container(
  padding: const EdgeInsets.all(12),
  decoration: BoxDecoration(
    color: t.surface,
    border: Border.all(color: t.divider),
    borderRadius: BorderRadius.circular(8),
  ),
  child: Column(
    crossAxisAlignment: CrossAxisAlignment.start,
    children: [
      Text('Pergunta vinculada',
          style: TextStyle(fontSize: 12, color: t.textSecondary)),
      const SizedBox(height: 4),
      Text(widget.item.question,
          style: TextStyle(fontSize: 14, color: t.textPrimary)),
    ],
  ),
)
```

DropdownButtonFormField para responsavel:
```dart
DropdownButtonFormField<String>(
  value: _selectedUser?.id,
  hint: Text('Selecione o responsavel'),
  items: _users.map((u) => DropdownMenuItem(value: u.id, child: Text(u.fullName))).toList(),
  onChanged: (val) => setState(() {
    _selectedUser = val == null ? null : _users.firstWhere((u) => u.id == val);
  }),
  validator: (val) => val == null ? 'Selecione o responsavel' : null,
  decoration: InputDecoration(
    labelText: 'Responsavel',
    border: OutlineInputBorder(borderRadius: BorderRadius.circular(10)),
    focusedBorder: OutlineInputBorder(
      borderRadius: BorderRadius.circular(10),
      borderSide: BorderSide(color: AppColors.accent, width: 1.5),
    ),
  ),
)
```

Prazo field (readOnly + onTap):
```dart
TextFormField(
  readOnly: true,
  controller: TextEditingController(
    text: _dueDate == null ? '' : '${_dueDate!.day}/${_dueDate!.month}/${_dueDate!.year}',
  ),
  onTap: _pickDate,
  decoration: InputDecoration(
    labelText: 'Prazo',
    hintText: 'Selecione a data limite',
    suffixIcon: const Icon(Icons.calendar_today_rounded),
    border: OutlineInputBorder(borderRadius: BorderRadius.circular(10)),
    focusedBorder: OutlineInputBorder(
      borderRadius: BorderRadius.circular(10),
      borderSide: BorderSide(color: AppColors.accent, width: 1.5),
    ),
  ),
  validator: (_) {
    if (_dueDate == null) return 'Selecione o prazo';
    if (_dueDate!.isBefore(DateTime.now())) return 'O prazo deve ser uma data futura';
    return null;
  },
)
```

Botao submit:
```dart
ElevatedButton(
  onPressed: _isSaving ? null : _save,
  style: ElevatedButton.styleFrom(
    backgroundColor: AppColors.primary,
    foregroundColor: Colors.white,
    minimumSize: const Size(double.infinity, 48),
    shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(10)),
  ),
  child: _isSaving
      ? const SizedBox(height: 20, width: 20, child: CircularProgressIndicator(color: Colors.white, strokeWidth: 2))
      : const Text('Criar acao'),
)
```

Metodo _save() — try/catch no screen, nao no service:
```dart
Future<void> _save() async {
  if (!_formKey.currentState!.validate()) return;
  setState(() => _isSaving = true);
  try {
    final companyId = CompanyContextService.instance.activeCompanyId!;
    final currentUser = Supabase.instance.client.auth.currentUser!;
    await _service.createAction(
      auditId: widget.audit.id,
      templateItemId: widget.item.id,
      title: _titleCtrl.text.trim(),
      description: _descCtrl.text.trim().isEmpty ? null : _descCtrl.text.trim(),
      responsibleUserId: _selectedUser!.id,
      dueDate: _dueDate!,
      companyId: companyId,
      createdBy: currentUser.id,
    );
    if (!mounted) return;
    Navigator.pop(context, true); // true = acao criada com sucesso
  } catch (e) {
    _snack('Erro ao criar acao. Tente novamente.');
  } finally {
    if (mounted) setState(() => _isSaving = false);
  }
}
```

IMPORTANTE: Adicionar `dispose()` fazendo `_titleCtrl.dispose()` e `_descCtrl.dispose()`.

Verificar que o arquivo compila: `cd primeaudit && flutter analyze lib/screens/create_corrective_action_screen.dart`
  </action>

  <verify>
    <automated>cd primeaudit && flutter analyze lib/screens/create_corrective_action_screen.dart --no-fatal-infos 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `grep "class CreateCorrectiveActionScreen" primeaudit/lib/screens/create_corrective_action_screen.dart` retorna match
    - `grep "required this.audit" primeaudit/lib/screens/create_corrective_action_screen.dart` retorna match
    - `grep "required this.item" primeaudit/lib/screens/create_corrective_action_screen.dart` retorna match
    - `grep "getByCompany" primeaudit/lib/screens/create_corrective_action_screen.dart` retorna match (chamada em _load)
    - `grep "showDatePicker" primeaudit/lib/screens/create_corrective_action_screen.dart` retorna match
    - `grep "createAction" primeaudit/lib/screens/create_corrective_action_screen.dart` retorna match
    - `grep "Navigator.pop(context, true)" primeaudit/lib/screens/create_corrective_action_screen.dart` retorna match
    - `grep "Nova Acao Corretiva" primeaudit/lib/screens/create_corrective_action_screen.dart` retorna match (AppBar title)
    - `grep "Criar acao" primeaudit/lib/screens/create_corrective_action_screen.dart` retorna match (CTA label)
    - `flutter analyze lib/screens/create_corrective_action_screen.dart` sem erros de compilacao
  </acceptance_criteria>

  <done>CreateCorrectiveActionScreen completo: 4 campos com validacao, dropdown de usuarios da empresa, date picker, submit com try/catch, Navigator.pop(true) no sucesso</done>
</task>

<task type="auto">
  <name>Task 3: audit_execution_screen.dart — icone condicional em _SectionBlock + _ItemCard (D-01)</name>
  <files>primeaudit/lib/screens/audit_execution_screen.dart</files>

  <read_first>
    - primeaudit/lib/screens/audit_execution_screen.dart — LER NA INTEGRA (1529 linhas). Identificar:
      - _SectionBlock constructor (linhas ~789-798): parametros atuais section, answers, observations, indexMap, readOnly, onAnswer, onObservation, theme
      - _ItemCard constructor (linhas ~869-878): parametros atuais item, index, answer, observation, readOnly, onAnswer, onObservation, theme
      - _ItemCardState.build(): onde fica o bloco "Ver observacao" (linhas ~1004-1030) — injecao vai APOS este bloco
      - _buildBody(): site de chamada do _SectionBlock (linhas ~585-594) — onde adicionar audit e onCreateAction
    - .planning/phases/08-corrective-actions/08-RESEARCH.md §Pattern 4 (_ItemCard integration)
    - .planning/phases/08-corrective-actions/08-PATTERNS.md §audit_execution_screen.dart
    - .planning/phases/08-corrective-actions/08-UI-SPEC.md §Execution Screen Integration
    - primeaudit/lib/services/corrective_action_service.dart (Wave 1) — para import e chamada de CorrectiveActionService.isNonConforming()
  </read_first>

  <action>
Modificar `primeaudit/lib/screens/audit_execution_screen.dart` para adicionar o icone de criacao de acao corretiva em itens nao-conformes.

ESTA E UMA MODIFICACAO CIRURGICA — ler o arquivo completo antes de qualquer alteracao para entender o contexto exato.

MODIFICACAO 1: Adicionar import no topo do arquivo (apos os imports existentes):
```dart
import 'create_corrective_action_screen.dart';
import '../services/corrective_action_service.dart';
```

MODIFICACAO 2: Adicionar 2 campos em `_SectionBlock` (StatefulWidget ou StatelessWidget — verificar na leitura):

Nos campos declarados da classe `_SectionBlock`, adicionar APOS os campos existentes:
```dart
final Audit? audit;                                  // necessario para passar ao CreateCorrectiveActionScreen
final void Function(TemplateItem)? onCreateAction;  // null quando readOnly ou nao aplicavel
```

No constructor `const _SectionBlock({...})`, adicionar APOS os parametros existentes:
```dart
this.audit,
this.onCreateAction,
```

MODIFICACAO 3: Propagar audit e onCreateAction de `_SectionBlock.build()` para cada `_ItemCard` construido internamente.

Na chamada de `_ItemCard(...)` dentro de `_SectionBlock.build()`, adicionar:
```dart
audit: widget.audit,
onCreateAction: widget.onCreateAction,
```

MODIFICACAO 4: Adicionar 2 campos em `_ItemCard`:

Nos campos declarados da classe `_ItemCard`, adicionar APOS os campos existentes:
```dart
final Audit? audit;
final void Function(TemplateItem)? onCreateAction;
```

No constructor `const _ItemCard({...})`, adicionar APOS os parametros existentes:
```dart
this.audit,
this.onCreateAction,
```

MODIFICACAO 5: Adicionar o icone no `_ItemCardState.build()` APOS o bloco "Ver observacao".

Localizar o bloco GestureDetector ou Row que corresponde a "Ver observacao" (aproximadamente linhas 1004-1030). IMEDIATAMENTE APOS este bloco (e ainda dentro do Column.children list), adicionar:

```dart
if (widget.onCreateAction != null &&
    CorrectiveActionService.isNonConforming(widget.item.responseType, widget.answer) &&
    !widget.readOnly) ...[
  const SizedBox(height: 8),
  IconButton(
    icon: const Icon(Icons.assignment_add_rounded),
    onPressed: () => widget.onCreateAction!(widget.item),
    tooltip: 'Criar acao corretiva',
    color: AppColors.accent,
    iconSize: 20,
    constraints: const BoxConstraints(minWidth: 36, minHeight: 36),
    padding: EdgeInsets.zero,
  ),
],
```

Alternativa se o layout nao comportar IconButton isolado: usar GestureDetector + Row conforme PATTERNS.md:
```dart
GestureDetector(
  onTap: () => widget.onCreateAction!(widget.item),
  child: Row(children: [
    Icon(Icons.assignment_add_rounded, size: 16, color: AppColors.accent),
    const SizedBox(width: 6),
    Text('Criar acao corretiva',
        style: TextStyle(fontSize: 12, color: AppColors.accent)),
  ]),
),
```

Escolher o que causar MENOS disrupcao no layout existente — a UI-SPEC diz "executor to choose whichever disrupts existing layout less".

MODIFICACAO 6: No `_buildBody()`, atualizar a chamada de `_SectionBlock` para passar audit e onCreateAction:

Localizar a chamada `_SectionBlock(section: ..., ...)` em `_buildBody()`. Adicionar:
```dart
audit: widget.audit,
onCreateAction: _isReadOnly ? null
    : (item) {
        Navigator.of(context).push(
          MaterialPageRoute(
            builder: (_) => CreateCorrectiveActionScreen(
              audit: widget.audit,
              item: item,
            ),
          ),
        );
      },
```

NOTA: `_isReadOnly` deve ser um campo ou getter existente no state — verificar nome exato ao ler o arquivo. Se for `widget.readOnly` ou `_readOnly`, usar o nome correto.

CONFIRMAR COMPILACAO: `cd primeaudit && flutter analyze lib/screens/audit_execution_screen.dart --no-fatal-infos`
  </action>

  <verify>
    <automated>cd primeaudit && flutter analyze lib/screens/audit_execution_screen.dart --no-fatal-infos 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `grep "onCreateAction" primeaudit/lib/screens/audit_execution_screen.dart` retorna >= 4 matches (declaracao em _SectionBlock, declaracao em _ItemCard, propagacao, uso no if condicional)
    - `grep "audit.*Audit" primeaudit/lib/screens/audit_execution_screen.dart` retorna >= 2 matches em _SectionBlock e _ItemCard
    - `grep "CreateCorrectiveActionScreen" primeaudit/lib/screens/audit_execution_screen.dart` retorna match (chamada em Navigator.push)
    - `grep "CorrectiveActionService.isNonConforming" primeaudit/lib/screens/audit_execution_screen.dart` retorna match
    - `grep "assignment_add_rounded" primeaudit/lib/screens/audit_execution_screen.dart` retorna match (icone do botao)
    - `flutter analyze lib/screens/audit_execution_screen.dart` sem erros de compilacao
    - `cd primeaudit && flutter test` passa (regressao zero)
  </acceptance_criteria>

  <done>audit_execution_screen.dart modificado: _SectionBlock e _ItemCard recebem audit e onCreateAction; icone condicional aparece em itens nao-conformes nao-readonly; Navigator.push abre CreateCorrectiveActionScreen</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CreateCorrectiveActionScreen → CorrectiveActionService.createAction | Dados do formulario (titulo, responsavel UUID, prazo, descricao) cruzam para o banco |
| DropdownButtonFormField → UserService.getByCompany | Lista de usuarios da empresa — somente usuarios ativos com company_id correto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-06 | Tampering | Due date field — past date injection | mitigate | Validator em TextFormField do prazo: `if (_dueDate!.isBefore(DateTime.now())) return 'O prazo deve ser uma data futura'` — aplicado antes de `_service.createAction()` |
| T-8-07 | Tampering | Responsible UUID — free text bypass | mitigate | D-04 enforced: DropdownButtonFormField bound a UUIDs de AppUser retornados por UserService.getByCompany() — nenhum campo de texto livre aceita responsible_user_id |
| T-8-08 | Elevation of Privilege | isNonConforming() — icon visivel em item incorreto | accept | Funcao estatica deterministica sem efeito de seguranca — icone apenas abre formulario; criacao real e validada pelo RLS no banco (auditor_corrective_actions_insert policy) |
| T-8-09 | Spoofing | companyId injetado via CompanyContextService — superuser/dev podem mudar empresa | accept | CompanyContextService e o mecanismo designado para superuser/dev mudar empresa; RLS valida company_id contra get_my_company_id() no banco de qualquer forma |
| T-8-10 | Information Disclosure | UserService.getByCompany lista usuarios da empresa ativa | accept | RLS de profiles ja restringe auditor a ver apenas profiles da propria empresa; getByCompany passa companyId do CompanyContextService que e validado pelo RLS |
</threat_model>

<verification>
Wave 2 completa quando:
1. `grep "getByCompany" primeaudit/lib/services/user_service.dart` retorna match
2. `grep "inFilter" primeaudit/lib/services/dashboard_service.dart` retorna match (sem try/catch)
3. `primeaudit/lib/screens/create_corrective_action_screen.dart` existe e `flutter analyze` sem erros
4. `grep "onCreateAction" primeaudit/lib/screens/audit_execution_screen.dart` retorna >= 4 matches
5. `cd primeaudit && flutter test` — suite completa verde (sem regressoes)
</verification>

<success_criteria>
- UserService.getByCompany(companyId) retorna List<AppUser> filtrado por empresa e active=true
- DashboardService.getOpenActionsCount usa inFilter(['aberta','em_andamento','em_avaliacao']) sem try/catch
- CreateCorrectiveActionScreen: formulario de 4 campos, validacao, dropdown de usuarios, date picker, submit cria acao e pop(true)
- audit_execution_screen.dart: icone aparece em itens nao-conformes (nao-readonly), toca -> CreateCorrectiveActionScreen
- Suite `flutter test` verde
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/08-corrective-actions/08-02-SUMMARY.md` seguindo o template em `@C:\Users\eder3\.claude\get-shit-done\templates\summary.md`
</output>
