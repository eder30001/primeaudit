---
phase: 08-corrective-actions
plan: 03
type: execute
wave: 3
depends_on:
  - "08-01"
  - "08-02"
files_modified:
  - primeaudit/lib/screens/corrective_actions_screen.dart
autonomous: true
requirements:
  - ACT-01

must_haves:
  truths:
    - "Usuario ve lista de acoes corretivas com status, responsavel e prazo"
    - "Filtro por status funciona via FilterChips horizontais com opcao 'Todos'"
    - "Filtro por responsavel funciona via DropdownButton horizontal com opcao 'Todos os responsaveis'"
    - "Card de acao exibe status chip colorido, nome do responsavel, prazo (em vermelho se vencida)"
    - "Pull-to-refresh atualiza a lista"
    - "Estado vazio com mensagem especifica por contexto (sem acoes / filtro sem resultado)"
    - "Estado de erro com botao Tentar novamente"
    - "Tocar em um card navega para CorrectiveActionDetailScreen"
  artifacts:
    - path: "primeaudit/lib/screens/corrective_actions_screen.dart"
      provides: "Tela de listagem ACT-01 com filtros por status e responsavel"
      exports: ["CorrectiveActionsScreen"]
  key_links:
    - from: "corrective_actions_screen.dart"
      to: "corrective_action_service.dart getActions"
      via: "_service.getActions(companyId: companyId, statusFilter: ..., responsibleFilter: ...) em _load()"
      pattern: "_service.getActions"
    - from: "corrective_actions_screen.dart"
      to: "corrective_action_detail_screen.dart"
      via: "Navigator.push em _openDetail(action)"
      pattern: "CorrectiveActionDetailScreen"
---

<objective>
Entregar a tela de listagem de acoes corretivas (ACT-01) com filtros por status (FilterChips) e por responsavel (DropdownButton), card de acao com chip de status colorido, suporte a pull-to-refresh, e estados de loading/error/empty com mensagens de copywriting corretas.

Purpose: ACT-01 — usuario ve o estado atual de todas as acoes corretivas da empresa com filtros. E o hub central de gerenciamento CAPA.

Output: corrective_actions_screen.dart completo e compilando.
</objective>

<execution_context>
@C:\Users\eder3\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\eder3\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-corrective-actions/08-CONTEXT.md
@.planning/phases/08-corrective-actions/08-RESEARCH.md
@.planning/phases/08-corrective-actions/08-PATTERNS.md
@.planning/phases/08-corrective-actions/08-UI-SPEC.md
@.planning/phases/08-corrective-actions/08-02-SUMMARY.md

<interfaces>
<!-- Contratos das waves anteriores. Ler antes de implementar. -->

De corrective_action.dart (Wave 1):
```dart
class CorrectiveAction {
  final String id;
  final String title;
  final String responsibleUserId;
  final String? responsibleName;  // via join profiles!responsible_user_id(full_name)
  final DateTime dueDate;
  final CorrectiveActionStatus status;
  final String? linkedAuditTitle;
  bool get isOverdue;  // dueDate.isBefore(now) && !status.isFinal
}
enum CorrectiveActionStatus {
  aberta, emAndamento, emAvaliacao, aprovada, rejeitada, cancelada;
  String get label;
  Color get chipBackground;
  Color get chipText;
  IconData get icon;
  bool get isFinal;
  static CorrectiveActionStatus fromDb(String? value);
}
```

De corrective_action_service.dart (Wave 1):
```dart
class CorrectiveActionService {
  Future<List<CorrectiveAction>> getActions({
    required String? companyId,
    String? statusFilter,          // DB value string, ex: 'aberta'
    String? responsibleFilter,     // UUID string
  }) async { ... }
}
```

De audits_screen.dart (analog — padroes de filtro chip e lista):
- enum _AuditFilter com extension _AuditFilterLabel on _AuditFilter
- SingleChildScrollView horizontal wrapping Row de FilterChips
- FilterChip: selectedColor: AppColors.primary, checkmarkColor: Colors.white, side: t.divider
- ListView.builder com padding: EdgeInsets.fromLTRB(16, 12, 16, 100)
- RefreshIndicator wrapping ListView
- _snack() helper com SnackBarBehavior.floating
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CorrectiveActionsScreen — lista com filtros, card, estados</name>
  <files>primeaudit/lib/screens/corrective_actions_screen.dart</files>

  <read_first>
    - primeaudit/lib/screens/audits_screen.dart — ANALOG EXATO: ler completo para padroes de enum filter, extension label, FilterChip row, ListView.builder, RefreshIndicator, loading/error/empty states, _snack(), Navigator.push para detalhe
    - primeaudit/lib/core/app_colors.dart — AppColors.primary, AppColors.accent, AppColors.error
    - primeaudit/lib/core/app_theme.dart — AppTheme.of(context) retorna AppTheme com t.background, t.surface, t.textPrimary, t.textSecondary, t.divider
    - .planning/phases/08-corrective-actions/08-PATTERNS.md §corrective_actions_screen.dart — padroes completos de classe, _load(), _filtered getter, _buildBody()
    - .planning/phases/08-corrective-actions/08-UI-SPEC.md §Action List Screen, §Action Card Anatomy, §Copywriting Contract §Action List Screen
  </read_first>

  <action>
Criar `primeaudit/lib/screens/corrective_actions_screen.dart` do zero.

ESTRUTURA COMPLETA:

```dart
import 'package:flutter/material.dart';
import '../core/app_colors.dart';
import '../core/app_theme.dart';
import '../models/corrective_action.dart';
import '../services/corrective_action_service.dart';
import '../services/company_context_service.dart';
import 'corrective_action_detail_screen.dart';

// Enum de filtro de status — "Em aberto" agrupa aberta+emAndamento+emAvaliacao
enum _StatusFilter { todas, abertas, emAndamento, emAvaliacao, finalizadas }

extension _StatusFilterLabel on _StatusFilter {
  String get label {
    switch (this) {
      case _StatusFilter.todas:       return 'Todos';
      case _StatusFilter.abertas:     return 'Em aberto';
      case _StatusFilter.emAndamento: return 'Em andamento';
      case _StatusFilter.emAvaliacao: return 'Em avaliacao';
      case _StatusFilter.finalizadas: return 'Finalizadas';
    }
  }
  // Retorna o DB value para filtrar, ou null para "Todos"/"Finalizadas" (filtro no lado Dart)
  String? get dbValue {
    switch (this) {
      case _StatusFilter.emAndamento: return 'em_andamento';
      case _StatusFilter.emAvaliacao: return 'em_avaliacao';
      default:                        return null;
    }
  }
}

class CorrectiveActionsScreen extends StatefulWidget {
  final String currentUserId;
  final String currentUserRole;

  const CorrectiveActionsScreen({
    super.key,
    required this.currentUserId,
    required this.currentUserRole,
  });

  @override
  State<CorrectiveActionsScreen> createState() => _CorrectiveActionsScreenState();
}

class _CorrectiveActionsScreenState extends State<CorrectiveActionsScreen> {
  final _service = CorrectiveActionService();

  List<CorrectiveAction> _actions = [];
  bool _isLoading = true;
  String? _error;

  _StatusFilter _statusFilter = _StatusFilter.todas;
  String? _responsibleFilter; // UUID do responsavel selecionado, null = todos

  @override
  void initState() {
    super.initState();
    _load();
  }

  Future<void> _load() async {
    setState(() { _isLoading = true; _error = null; });
    try {
      final companyId = CompanyContextService.instance.activeCompanyId;
      // Buscar todas as acoes — filtros de status aplicados no Dart para suportar grupos ('Em aberto', 'Finalizadas')
      // Excecao: em_andamento e em_avaliacao passam o dbValue diretamente para reduzir payload
      String? statusDbValue = _statusFilter.dbValue; // null para todas/abertas/finalizadas
      final data = await _service.getActions(
        companyId: companyId,
        statusFilter: statusDbValue,
        responsibleFilter: _responsibleFilter,
      );
      if (mounted) setState(() => _actions = data);
    } catch (e) {
      if (mounted) setState(() => _error = 'Erro ao carregar acoes.\n$e');
    } finally {
      if (mounted) setState(() => _isLoading = false);
    }
  }

  void _snack(String msg) {
    if (!mounted) return;
    ScaffoldMessenger.of(context).showSnackBar(SnackBar(
      content: Text(msg),
      behavior: SnackBarBehavior.floating,
    ));
  }

  // Filtro Dart-side para grupos que nao tem dbValue direto
  List<CorrectiveAction> get _filtered {
    List<CorrectiveAction> list = _actions;
    switch (_statusFilter) {
      case _StatusFilter.todas:
        break; // sem filtro adicional
      case _StatusFilter.abertas:
        list = list.where((a) =>
          a.status == CorrectiveActionStatus.aberta ||
          a.status == CorrectiveActionStatus.emAndamento ||
          a.status == CorrectiveActionStatus.emAvaliacao).toList();
        break;
      case _StatusFilter.finalizadas:
        list = list.where((a) => a.status.isFinal).toList();
        break;
      case _StatusFilter.emAndamento:
      case _StatusFilter.emAvaliacao:
        // Ja filtrado via dbValue na query
        break;
    }
    if (_responsibleFilter != null) {
      list = list.where((a) => a.responsibleUserId == _responsibleFilter).toList();
    }
    return list;
  }

  // Lista deduplicated de responsaveis para o dropdown
  List<MapEntry<String, String>> get _responsibles {
    final seen = <String>{};
    return _actions
        .where((a) => a.responsibleName != null && seen.add(a.responsibleUserId))
        .map((a) => MapEntry(a.responsibleUserId, a.responsibleName!))
        .toList();
  }

  void _openDetail(CorrectiveAction action) {
    Navigator.of(context).push(
      MaterialPageRoute(
        builder: (_) => CorrectiveActionDetailScreen(
          action: action,
          currentUserId: widget.currentUserId,
          currentUserRole: widget.currentUserRole,
        ),
      ),
    ).then((_) => _load()); // recarregar apos retornar do detalhe
  }

  @override
  Widget build(BuildContext context) {
    final t = AppTheme.of(context);
    final filtered = _filtered;

    return Scaffold(
      backgroundColor: t.background,
      appBar: AppBar(
        backgroundColor: AppColors.primary,
        foregroundColor: Colors.white,
        title: const Text('Acoes Corretivas',
            style: TextStyle(fontWeight: FontWeight.bold)),
        actions: [
          IconButton(
            icon: const Icon(Icons.refresh_rounded),
            tooltip: 'Atualizar',
            onPressed: _load,
          ),
        ],
      ),
      body: Column(
        children: [
          _buildFilters(t),
          Expanded(child: _buildBody(t, filtered)),
        ],
      ),
    );
  }

  Widget _buildFilters(AppTheme t) {
    return Container(
      color: t.surface,
      padding: const EdgeInsets.fromLTRB(16, 8, 16, 8),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          // Filtro de status (FilterChips horizontais)
          SingleChildScrollView(
            scrollDirection: Axis.horizontal,
            child: Row(
              children: _StatusFilter.values.map((f) {
                final selected = _statusFilter == f;
                return Padding(
                  padding: const EdgeInsets.only(right: 8),
                  child: FilterChip(
                    label: Text(f.label,
                        style: TextStyle(
                          fontSize: 12,
                          fontWeight: selected ? FontWeight.w600 : FontWeight.normal,
                          color: selected ? Colors.white : t.textPrimary,
                        )),
                    selected: selected,
                    onSelected: (_) => setState(() {
                      _statusFilter = f;
                      _load(); // recarregar com novo filtro
                    }),
                    selectedColor: AppColors.primary,
                    backgroundColor: t.background,
                    checkmarkColor: Colors.white,
                    side: BorderSide(color: selected ? AppColors.primary : t.divider),
                    padding: const EdgeInsets.symmetric(horizontal: 4),
                    visualDensity: VisualDensity.compact,
                  ),
                );
              }).toList(),
            ),
          ),
          // Filtro de responsavel (DropdownButton)
          if (_responsibles.isNotEmpty) ...[
            const SizedBox(height: 8),
            ConstrainedBox(
              constraints: const BoxConstraints(maxWidth: 220),
              child: DropdownButton<String?>(
                value: _responsibleFilter,
                isExpanded: true,
                hint: Text('Responsavel',
                    style: TextStyle(fontSize: 13, color: t.textSecondary)),
                items: [
                  DropdownMenuItem<String?>(
                    value: null,
                    child: Text('Todos os responsaveis',
                        style: TextStyle(fontSize: 13, color: t.textPrimary)),
                  ),
                  ..._responsibles.map((e) => DropdownMenuItem<String?>(
                    value: e.key,
                    child: Text(e.value,
                        style: TextStyle(fontSize: 13, color: t.textPrimary),
                        overflow: TextOverflow.ellipsis),
                  )),
                ],
                onChanged: (val) => setState(() => _responsibleFilter = val),
                underline: const SizedBox.shrink(),
                style: TextStyle(color: t.textPrimary),
              ),
            ),
          ],
        ],
      ),
    );
  }

  Widget _buildBody(AppTheme t, List<CorrectiveAction> actions) {
    if (_isLoading) {
      return const Center(child: CircularProgressIndicator(color: AppColors.primary));
    }

    if (_error != null) {
      return Center(
        child: Padding(
          padding: const EdgeInsets.all(32),
          child: Column(
            mainAxisSize: MainAxisSize.min,
            children: [
              Icon(Icons.cloud_off_rounded, size: 48, color: t.textSecondary),
              const SizedBox(height: 16),
              Text('Erro ao carregar acoes',
                  style: TextStyle(fontSize: 16, fontWeight: FontWeight.w600, color: t.textPrimary)),
              const SizedBox(height: 8),
              Text('Verifique sua conexao e tente novamente.',
                  style: TextStyle(fontSize: 14, color: t.textSecondary),
                  textAlign: TextAlign.center),
              const SizedBox(height: 16),
              OutlinedButton.icon(
                onPressed: _load,
                icon: const Icon(Icons.refresh_rounded),
                label: const Text('Tentar novamente'),
              ),
            ],
          ),
        ),
      );
    }

    if (actions.isEmpty) {
      // Distinguir: sem acoes nenhuma vs filtro ativo sem resultado
      final hasFilter = _statusFilter != _StatusFilter.todas || _responsibleFilter != null;
      return Center(
        child: Padding(
          padding: const EdgeInsets.all(32),
          child: Column(
            mainAxisSize: MainAxisSize.min,
            children: [
              Icon(Icons.assignment_late_outlined, size: 56, color: t.textSecondary),
              const SizedBox(height: 16),
              Text(
                hasFilter ? 'Nenhuma acao encontrada' : 'Nenhuma acao corretiva',
                style: TextStyle(fontSize: 16, fontWeight: FontWeight.w600, color: t.textPrimary),
              ),
              const SizedBox(height: 8),
              Text(
                hasFilter
                    ? 'Tente ajustar os filtros de status ou responsavel.'
                    : 'Crie uma acao a partir de uma pergunta nao conforme durante a execucao de uma auditoria.',
                style: TextStyle(fontSize: 14, color: t.textSecondary),
                textAlign: TextAlign.center,
              ),
            ],
          ),
        ),
      );
    }

    return RefreshIndicator(
      onRefresh: _load,
      color: AppColors.accent,
      child: ListView.builder(
        padding: const EdgeInsets.fromLTRB(16, 12, 16, 100),
        itemCount: actions.length,
        itemBuilder: (_, i) => _ActionCard(
          action: actions[i],
          theme: t,
          onTap: () => _openDetail(actions[i]),
        ),
      ),
    );
  }
}

// Card privado de acao corretiva
class _ActionCard extends StatelessWidget {
  final CorrectiveAction action;
  final AppTheme theme;
  final VoidCallback onTap;

  const _ActionCard({
    required this.action,
    required this.theme,
    required this.onTap,
  });

  @override
  Widget build(BuildContext context) {
    final t = theme;
    final overdue = action.isOverdue;

    return Padding(
      padding: const EdgeInsets.only(bottom: 8),
      child: Card(
        elevation: 0,
        shape: RoundedRectangleBorder(
          borderRadius: BorderRadius.circular(12),
          side: BorderSide(
            color: overdue
                ? AppColors.error.withValues(alpha: 0.4)
                : t.divider,
            width: overdue ? 1.5 : 1.0,
          ),
        ),
        child: InkWell(
          onTap: onTap,
          borderRadius: BorderRadius.circular(12),
          child: Padding(
            padding: const EdgeInsets.all(16),
            child: Column(
              crossAxisAlignment: CrossAxisAlignment.start,
              children: [
                // Titulo + status chip
                Row(
                  crossAxisAlignment: CrossAxisAlignment.start,
                  children: [
                    Expanded(
                      child: Text(
                        action.title,
                        style: TextStyle(
                          fontSize: 16,
                          fontWeight: FontWeight.w600,
                          color: t.textPrimary,
                        ),
                      ),
                    ),
                    const SizedBox(width: 8),
                    _StatusChip(status: action.status),
                  ],
                ),
                const SizedBox(height: 8),
                // Responsavel
                Row(
                  children: [
                    Icon(Icons.person_outline_rounded, size: 14, color: t.textSecondary),
                    const SizedBox(width: 4),
                    Expanded(
                      child: Text(
                        action.responsibleName ?? action.responsibleUserId,
                        style: TextStyle(fontSize: 12, color: t.textSecondary),
                        overflow: TextOverflow.ellipsis,
                      ),
                    ),
                  ],
                ),
                const SizedBox(height: 4),
                // Prazo
                Row(
                  children: [
                    Icon(
                      Icons.calendar_today_rounded,
                      size: 14,
                      color: overdue ? AppColors.error : t.textSecondary,
                    ),
                    const SizedBox(width: 4),
                    Text(
                      '${action.dueDate.day}/${action.dueDate.month}/${action.dueDate.year}',
                      style: TextStyle(
                        fontSize: 12,
                        color: overdue ? AppColors.error : t.textSecondary,
                      ),
                    ),
                  ],
                ),
                // Auditoria vinculada (se disponivel)
                if (action.linkedAuditTitle != null) ...[
                  const SizedBox(height: 4),
                  Row(
                    children: [
                      Icon(Icons.assignment_outlined, size: 14, color: t.textSecondary),
                      const SizedBox(width: 4),
                      Expanded(
                        child: Text(
                          action.linkedAuditTitle!,
                          style: TextStyle(fontSize: 12, color: t.textSecondary),
                          overflow: TextOverflow.ellipsis,
                        ),
                      ),
                    ],
                  ),
                ],
              ],
            ),
          ),
        ),
      ),
    );
  }
}

// Chip de status CAPA — reutilizavel no detalhe (Wave 4)
class _StatusChip extends StatelessWidget {
  final CorrectiveActionStatus status;

  const _StatusChip({required this.status});

  @override
  Widget build(BuildContext context) {
    return Chip(
      label: Text(
        status.label,
        style: TextStyle(
          fontSize: 12,
          fontWeight: FontWeight.w600,
          color: status.chipText,
        ),
      ),
      backgroundColor: status.chipBackground,
      avatar: Icon(status.icon, size: 14, color: status.chipText),
      visualDensity: VisualDensity.compact,
      padding: EdgeInsets.zero,
      side: BorderSide.none,
    );
  }
}
```

NOTA SOBRE FILTROS: Para "Em aberto" e "Finalizadas", a query busca todas as acoes (statusFilter=null) e filtra no Dart. Para "Em andamento" e "Em avaliacao" individuais, a query passa o dbValue. Isso evita multiplas queries para os grupos, ao custo de carregar mais dados na filtragem client-side — aceitavel dado volumes esperados.

NOTA SOBRE EXPORT DE _StatusChip: O chip e definido com `_` (privado ao arquivo). Wave 4 (CorrectiveActionDetailScreen) vai precisar do mesmo chip — pode reexportar ou redefinir no Wave 4. Preferencia: mover _StatusChip para corrective_action.dart ou criar widget publico `StatusChip` neste arquivo com prefixo sem underscore. Decisao do executor com base no que causar menos disrupcao.

Verificar compilacao: `cd primeaudit && flutter analyze lib/screens/corrective_actions_screen.dart --no-fatal-infos`
  </action>

  <verify>
    <automated>cd primeaudit && flutter analyze lib/screens/corrective_actions_screen.dart --no-fatal-infos 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `grep "class CorrectiveActionsScreen" primeaudit/lib/screens/corrective_actions_screen.dart` retorna match
    - `grep "class _StatusFilter" primeaudit/lib/screens/corrective_actions_screen.dart` retorna match (enum)
    - `grep "FilterChip" primeaudit/lib/screens/corrective_actions_screen.dart` retorna match
    - `grep "DropdownButton" primeaudit/lib/screens/corrective_actions_screen.dart` retorna match (filtro responsavel)
    - `grep "RefreshIndicator" primeaudit/lib/screens/corrective_actions_screen.dart` retorna match
    - `grep "CorrectiveActionDetailScreen" primeaudit/lib/screens/corrective_actions_screen.dart` retorna match (navegacao para detalhe)
    - `grep "Nenhuma acao corretiva" primeaudit/lib/screens/corrective_actions_screen.dart` retorna match (copywriting empty state)
    - `grep "Nenhuma acao encontrada" primeaudit/lib/screens/corrective_actions_screen.dart` retorna match (copywriting filtro vazio)
    - `grep "Tentar novamente" primeaudit/lib/screens/corrective_actions_screen.dart` retorna match (error state)
    - `grep "isOverdue" primeaudit/lib/screens/corrective_actions_screen.dart` retorna match (borda vermelha do card)
    - `flutter analyze lib/screens/corrective_actions_screen.dart` sem erros de compilacao
    - `cd primeaudit && flutter test` passa (suite verde)
  </acceptance_criteria>

  <done>CorrectiveActionsScreen completo: filtros por status (FilterChips) e responsavel (Dropdown), card com status chip colorido, overdue em vermelho, RefreshIndicator, empty/error states com copywriting correto, navegacao para detalhe</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CorrectiveActionsScreen → CorrectiveActionService.getActions | Lista de acoes filtrada por companyId — dados da empresa ativa cruzam para o app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-11 | Information Disclosure | getActions com companyId de CompanyContextService | accept | RLS `auditor_corrective_actions_select` garante que mesmo sem companyId no Dart, a policy no banco restringe ao company_id do JWT; companyId no Dart e camada de performance (evita dados desnecessarios), nao de seguranca |
| T-8-12 | Elevation of Privilege | _filtered Dart-side — usuario ve acoes de outro responsavel | accept | Filtro de responsavel no Dart e apenas UX; dados jah foram restringidos pelo RLS ao company scope; nao h crosscompany leakage — todos os responsaveis no dropdown sao da mesma empresa |
| T-8-13 | Denial of Service | Carregamento de muitas acoes sem paginacao | accept | Volumes esperados por empresa em producao sao baixos (dezenas a centenas de acoes); paginacao e melhoria de performance futura, nao requisito de seguranca desta milestone |
</threat_model>

<verification>
Wave 3 completa quando:
1. `primeaudit/lib/screens/corrective_actions_screen.dart` existe
2. `flutter analyze lib/screens/corrective_actions_screen.dart` sem erros
3. `grep "FilterChip"` retorna match — filtros de status presentes
4. `grep "RefreshIndicator"` retorna match — pull-to-refresh presente
5. `grep "CorrectiveActionDetailScreen"` retorna match — navegacao presente
6. `cd primeaudit && flutter test` verde
</verification>

<success_criteria>
- CorrectiveActionsScreen renderiza lista com status chip, responsavel, prazo e auditoria vinculada
- Filtros por status (5 chips incluindo "Em aberto" agrupado) e por responsavel (dropdown) funcionam
- Overdue: card tem borda e data em AppColors.error quando dueDate passado e status nao-final
- Pull-to-refresh: RefreshIndicator chama _load() ao puxar para baixo
- Estados: loading (spinner), error (mensagem + retry), empty com mensagens distintas para "sem acoes" vs "filtro vazio"
- Suite `flutter test` verde
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/08-corrective-actions/08-03-SUMMARY.md` seguindo o template em `@C:\Users\eder3\.claude\get-shit-done\templates\summary.md`
</output>
