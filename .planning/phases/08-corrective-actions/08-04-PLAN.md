---
phase: 08-corrective-actions
plan: 04
type: execute
wave: 4
depends_on:
  - "08-01"
  - "08-02"
  - "08-03"
files_modified:
  - primeaudit/lib/screens/corrective_action_detail_screen.dart
  - primeaudit/lib/screens/home_screen.dart
autonomous: true
requirements:
  - ACT-03
  - ACT-04

must_haves:
  truths:
    - "Tela de detalhe exibe info completa da acao: titulo, status chip, responsavel, prazo, auditoria vinculada, criada por, data de criacao"
    - "Botoes de transicao de status aparecem APENAS para roles autorizados (per RBAC matrix de 08-UI-SPEC.md)"
    - "Transicoes destrutivas (cancelada, rejeitada) pedem confirmacao via AlertDialog"
    - "Role bloqueado nao ve botao — NUNCA desabilita e mostra erro"
    - "Badge com contagem de acoes abertas (aberta+em_andamento+em_avaliacao) aparece no item do drawer quando count > 0"
    - "Drawer tem item 'Acoes Corretivas' entre Auditorias e posicao futura de Relatorios"
    - "Badge atualiza ao voltar para HomeScreen via _loadDashboard()"
    - "Suite completa flutter test verde"
  artifacts:
    - path: "primeaudit/lib/screens/corrective_action_detail_screen.dart"
      provides: "Tela de detalhe ACT-03 com info card, status chip e botoes RBAC-gated"
      exports: ["CorrectiveActionDetailScreen"]
    - path: "primeaudit/lib/screens/home_screen.dart"
      provides: "_drawerItem com badgeCount + item Acoes Corretivas + badge count real via CorrectiveActionService"
      contains: "badgeCount"
  key_links:
    - from: "corrective_action_detail_screen.dart"
      to: "corrective_action_service.dart updateStatus"
      via: "_service.updateStatus(action.id, newStatus) em _doTransition()"
      pattern: "_service.updateStatus"
    - from: "corrective_action_detail_screen.dart"
      to: "corrective_action_service.dart canTransitionTo (static)"
      via: "CorrectiveActionService.canTransitionTo(newStatus: ..., action: ..., role: ..., userId: ...) em build()"
      pattern: "CorrectiveActionService.canTransitionTo"
    - from: "home_screen.dart _loadDashboard"
      to: "corrective_action_service.dart getOpenActionsCount"
      via: "_correctiveActionService.getOpenActionsCount(companyId) substituindo DashboardService call"
      pattern: "getOpenActionsCount"
    - from: "home_screen.dart drawer"
      to: "corrective_actions_screen.dart"
      via: "_drawerItem('Acoes Corretivas') onTap Navigator.push"
      pattern: "CorrectiveActionsScreen"
---

<objective>
Entregar a tela de detalhe com RBAC de transicao de status (ACT-03) e o badge de contagem na navegacao principal (ACT-04). Apos esta wave, o fluxo CAPA completo esta disponivel e a suite de testes deve estar 100% verde.

Purpose: ACT-03 e ACT-04 — completar o ciclo de vida da acao corretiva com controle de acesso por role e visibilidade na navegacao.

Output: corrective_action_detail_screen.dart, home_screen.dart modificado, suite `flutter test` verde.
</objective>

<execution_context>
@C:\Users\eder3\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\eder3\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-corrective-actions/08-CONTEXT.md
@.planning/phases/08-corrective-actions/08-RESEARCH.md
@.planning/phases/08-corrective-actions/08-PATTERNS.md
@.planning/phases/08-corrective-actions/08-UI-SPEC.md
@.planning/phases/08-corrective-actions/08-03-SUMMARY.md

<interfaces>
<!-- Contratos das waves anteriores. -->

De corrective_action_service.dart (Wave 1):
```dart
class CorrectiveActionService {
  Future<void> updateStatus(String id, String newStatus) async { ... }
  Future<int> getOpenActionsCount(String? companyId) async { ... }

  static bool canTransitionTo({
    required String newStatus,
    required CorrectiveAction action,
    required String role,
    required String userId,
  }) { ... }
}
```

De corrective_action.dart (Wave 1):
```dart
enum CorrectiveActionStatus { aberta, emAndamento, emAvaliacao, aprovada, rejeitada, cancelada;
  String get label; String get dbValue; Color get chipBackground; Color get chipText; bool get isFinal; }
class CorrectiveAction { final String id; final String title; final String? description;
  final String responsibleUserId; final String? responsibleName; final DateTime dueDate;
  final CorrectiveActionStatus status; final String companyId; final String createdBy;
  final DateTime createdAt; final DateTime updatedAt; final String? linkedAuditTitle; }
```

De home_screen.dart (linhas identificadas em RESEARCH.md/PATTERNS.md):
- _drawerItem() atual (linhas ~318-338): icon, title, onTap, color — sem badgeCount
- _openActions field: int ja declarado em _HomeScreenState (Phase 7)
- _loadDashboard(): chama DashboardService.getOpenActionsCount() — substituir por CorrectiveActionService
- Drawer: item "Auditorias" (linhas ~276-283) — inserir item "Acoes Corretivas" logo apos
- _role field: String com o role do usuario logado
- _authService: AuthService instance com currentUser?.id

De home_screen.dart AppRole checks:
```dart
AppRole.canAccessAdmin(_role)  // true para adm, superuser, dev
AppRole.isSuperOrDev(_role)   // true para superuser, dev
```

RBAC Matrix (08-UI-SPEC.md) — botoes exibidos por role:
| De -> Para         | adm | responsavel | auditor(nao-resp) | superuser/dev |
| aberta->em_andamento | show | show | hide | show |
| em_andamento->em_avaliacao | show | show | hide | show |
| em_avaliacao->aprovada | show | hide | show | show |
| em_avaliacao->rejeitada | show | hide | show | show |
| any non-final->cancelada | show | hide | hide | show |
| rejeitada->em_andamento | show | show | hide | show |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CorrectiveActionDetailScreen — info card, status, botoes RBAC-gated, confirmacoes (ACT-03)</name>
  <files>primeaudit/lib/screens/corrective_action_detail_screen.dart</files>

  <read_first>
    - primeaudit/lib/screens/audits_screen.dart — padroes de _confirmEncerrar (AlertDialog com Voltar/Confirmar em AppColors.error), _snack(), Navigator.pop(context, true), _isLoading/bool pattern para loading de operacao
    - primeaudit/lib/screens/corrective_actions_screen.dart (Wave 3) — para importar/reusar o chip de status (_StatusChip ou StatusChip dependendo de como foi definido)
    - primeaudit/lib/core/app_roles.dart — AppRole.canAccessAdmin(), AppRole.isSuperOrDev(), AppRole.auditor
    - .planning/phases/08-corrective-actions/08-PATTERNS.md §corrective_action_detail_screen.dart — padroes de constructor, _confirmTransition, _doTransition, RBAC button visibility
    - .planning/phases/08-corrective-actions/08-UI-SPEC.md §Action Detail / Status Screen, §RBAC Transition Matrix, §Copywriting Contract §Action Detail / Status Screen
  </read_first>

  <action>
Criar `primeaudit/lib/screens/corrective_action_detail_screen.dart` do zero.

ESTRUTURA COMPLETA:

```dart
import 'package:flutter/material.dart';
import 'package:supabase_flutter/supabase_flutter.dart';
import '../core/app_colors.dart';
import '../core/app_roles.dart';
import '../core/app_theme.dart';
import '../models/corrective_action.dart';
import '../services/corrective_action_service.dart';

class CorrectiveActionDetailScreen extends StatefulWidget {
  final CorrectiveAction action;
  final String currentUserId;
  final String currentUserRole;

  const CorrectiveActionDetailScreen({
    super.key,
    required this.action,
    required this.currentUserId,
    required this.currentUserRole,
  });

  @override
  State<CorrectiveActionDetailScreen> createState() =>
      _CorrectiveActionDetailScreenState();
}

class _CorrectiveActionDetailScreenState
    extends State<CorrectiveActionDetailScreen> {
  final _service = CorrectiveActionService();
  bool _isTransitioning = false;
  late CorrectiveAction _action; // mutable — atualizado apos transicao

  @override
  void initState() {
    super.initState();
    _action = widget.action;
  }

  void _snack(String msg) {
    if (!mounted) return;
    ScaffoldMessenger.of(context).showSnackBar(SnackBar(
      content: Text(msg),
      behavior: SnackBarBehavior.floating,
    ));
  }

  Future<bool?> _confirmTransition(String title, String body) {
    return showDialog<bool>(
      context: context,
      builder: (ctx) => AlertDialog(
        title: Text(title),
        content: Text(body),
        actions: [
          TextButton(
            onPressed: () => Navigator.pop(ctx, false),
            child: const Text('Voltar'),
          ),
          TextButton(
            onPressed: () => Navigator.pop(ctx, true),
            style: TextButton.styleFrom(foregroundColor: AppColors.error),
            child: const Text('Confirmar'),
          ),
        ],
      ),
    );
  }

  Future<void> _doTransition(String newStatus) async {
    // Confirmacao para transicoes destrutivas
    if (newStatus == 'cancelada') {
      final confirmed = await _confirmTransition(
        'Cancelar acao corretiva',
        'Esta acao sera marcada como cancelada. Confirma?',
      );
      if (confirmed != true) return;
    } else if (newStatus == 'rejeitada') {
      final confirmed = await _confirmTransition(
        'Rejeitar acao corretiva',
        'Confirma a rejeicao desta acao? O responsavel devera corrigi-la ou abrir uma nova acao.',
      );
      if (confirmed != true) return;
    }

    setState(() => _isTransitioning = true);
    try {
      await _service.updateStatus(_action.id, newStatus);
      if (!mounted) return;
      // Atualizar estado local com novo status (sem nova query ao banco)
      // O status e atualizado localmente — ao voltar, CorrectiveActionsScreen recarrega via .then(_load)
      _snack('Status atualizado para ${CorrectiveActionStatus.fromDb(newStatus).label}');
      Navigator.pop(context, true); // sinaliza ao caller para recarregar lista
    } catch (e) {
      _snack('Erro ao atualizar status. Tente novamente.');
    } finally {
      if (mounted) setState(() => _isTransitioning = false);
    }
  }

  @override
  Widget build(BuildContext context) {
    final t = AppTheme.of(context);
    final a = _action;
    final overdue = a.isOverdue;

    return Scaffold(
      backgroundColor: t.background,
      appBar: AppBar(
        backgroundColor: AppColors.primary,
        foregroundColor: Colors.white,
        title: const Text('Acao Corretiva',
            style: TextStyle(fontWeight: FontWeight.bold)),
      ),
      body: _isTransitioning
          ? const Center(child: CircularProgressIndicator(color: AppColors.primary))
          : SingleChildScrollView(
              padding: const EdgeInsets.all(16),
              child: Column(
                crossAxisAlignment: CrossAxisAlignment.start,
                children: [
                  // Status chip prominente
                  Center(child: _buildStatusChip(a.status)),
                  const SizedBox(height: 16),

                  // Card de informacoes
                  _buildInfoCard(t, a, overdue),
                  const SizedBox(height: 24),

                  // Botoes de transicao (RBAC-gated)
                  ..._buildTransitionButtons(t, a),
                ],
              ),
            ),
    );
  }

  Widget _buildStatusChip(CorrectiveActionStatus status) {
    return Chip(
      label: Text(
        status.label,
        style: TextStyle(
          fontSize: 14,
          fontWeight: FontWeight.w600,
          color: status.chipText,
        ),
      ),
      backgroundColor: status.chipBackground,
      avatar: Icon(status.icon, size: 16, color: status.chipText),
      padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 4),
      side: BorderSide.none,
    );
  }

  Widget _buildInfoCard(AppTheme t, CorrectiveAction a, bool overdue) {
    return Card(
      elevation: 0,
      shape: RoundedRectangleBorder(
        borderRadius: BorderRadius.circular(12),
        side: BorderSide(color: t.divider),
      ),
      child: Padding(
        padding: const EdgeInsets.all(16),
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            Text('Informacoes da acao',
                style: TextStyle(
                  fontSize: 12,
                  fontWeight: FontWeight.w600,
                  color: t.textSecondary,
                  letterSpacing: 0.5,
                )),
            const SizedBox(height: 12),
            _InfoRow(label: 'Titulo', value: a.title, theme: t),
            _divider(t),
            _InfoRow(label: 'Responsavel', value: a.responsibleName ?? a.responsibleUserId, theme: t),
            _divider(t),
            _InfoRow(
              label: 'Prazo',
              value: '${a.dueDate.day}/${a.dueDate.month}/${a.dueDate.year}',
              theme: t,
              valueColor: overdue ? AppColors.error : null,
            ),
            if (a.description != null && a.description!.isNotEmpty) ...[
              _divider(t),
              _InfoRow(label: 'Descricao / Observacao', value: a.description!, theme: t),
            ],
            if (a.linkedAuditTitle != null) ...[
              _divider(t),
              _InfoRow(label: 'Auditoria vinculada', value: a.linkedAuditTitle!, theme: t),
            ],
            _divider(t),
            _InfoRow(
              label: 'Data de criacao',
              value: '${a.createdAt.day}/${a.createdAt.month}/${a.createdAt.year}',
              theme: t,
            ),
          ],
        ),
      ),
    );
  }

  Widget _divider(AppTheme t) => Divider(height: 16, color: t.divider);

  List<Widget> _buildTransitionButtons(AppTheme t, CorrectiveAction a) {
    if (a.status.isFinal) return []; // nenhum botao para status final

    final buttons = <Widget>[];

    void addButton(String newStatus, String label, {bool destructive = false}) {
      if (!CorrectiveActionService.canTransitionTo(
        newStatus: newStatus,
        action: a,
        role: widget.currentUserRole,
        userId: widget.currentUserId,
      )) return;

      buttons.add(
        Padding(
          padding: const EdgeInsets.only(bottom: 12),
          child: destructive
              ? OutlinedButton(
                  onPressed: () => _doTransition(newStatus),
                  style: OutlinedButton.styleFrom(
                    foregroundColor: AppColors.error,
                    side: BorderSide(color: AppColors.error),
                    minimumSize: const Size(double.infinity, 48),
                    shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(10)),
                  ),
                  child: Text(label),
                )
              : ElevatedButton(
                  onPressed: () => _doTransition(newStatus),
                  style: ElevatedButton.styleFrom(
                    backgroundColor: AppColors.primary,
                    foregroundColor: Colors.white,
                    minimumSize: const Size(double.infinity, 48),
                    shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(10)),
                  ),
                  child: Text(label),
                ),
        ),
      );
    }

    // Botoes por estado de origem — per RBAC matrix de 08-UI-SPEC.md
    switch (a.status) {
      case CorrectiveActionStatus.aberta:
        addButton('em_andamento', 'Iniciar acao');
        addButton('cancelada', 'Cancelar acao', destructive: true);
        break;
      case CorrectiveActionStatus.emAndamento:
        addButton('em_avaliacao', 'Enviar para avaliacao');
        addButton('cancelada', 'Cancelar acao', destructive: true);
        break;
      case CorrectiveActionStatus.emAvaliacao:
        addButton('aprovada', 'Aprovar');
        addButton('rejeitada', 'Rejeitar acao', destructive: true);
        addButton('cancelada', 'Cancelar acao', destructive: true);
        break;
      case CorrectiveActionStatus.rejeitada:
        // Re-abertura: responsavel pode voltar para em_andamento
        addButton('em_andamento', 'Iniciar acao');
        addButton('cancelada', 'Cancelar acao', destructive: true);
        break;
      case CorrectiveActionStatus.aprovada:
      case CorrectiveActionStatus.cancelada:
        // Status final — nenhum botao (coberto pelo guard .isFinal acima)
        break;
    }

    if (buttons.isEmpty) {
      // Usuario nao tem permissao para nenhuma transicao
      return [
        Padding(
          padding: const EdgeInsets.only(top: 8),
          child: Text(
            'Voce nao tem permissao para alterar o status desta acao.',
            style: TextStyle(fontSize: 13, color: t.textSecondary),
            textAlign: TextAlign.center,
          ),
        ),
      ];
    }

    return [
      Text('Acoes disponiveis',
          style: TextStyle(
            fontSize: 12,
            fontWeight: FontWeight.w600,
            color: t.textSecondary,
            letterSpacing: 0.5,
          )),
      const SizedBox(height: 12),
      ...buttons,
    ];
  }
}

// Row de informacao label + valor
class _InfoRow extends StatelessWidget {
  final String label;
  final String value;
  final AppTheme theme;
  final Color? valueColor;

  const _InfoRow({
    required this.label,
    required this.value,
    required this.theme,
    this.valueColor,
  });

  @override
  Widget build(BuildContext context) {
    final t = theme;
    return Column(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        Text(label, style: TextStyle(fontSize: 12, color: t.textSecondary)),
        const SizedBox(height: 2),
        Text(value,
            style: TextStyle(
              fontSize: 14,
              color: valueColor ?? t.textPrimary,
            )),
      ],
    );
  }
}
```

NOTA SOBRE STATUS CHIP: O chip de status e reimplementado aqui de forma identica ao _StatusChip de corrective_actions_screen.dart. Se na Wave 3 o executor tornou _StatusChip publico (StatusChip), importar e reusar em vez de redefinir.

Verificar: `cd primeaudit && flutter analyze lib/screens/corrective_action_detail_screen.dart --no-fatal-infos`
  </action>

  <verify>
    <automated>cd primeaudit && flutter analyze lib/screens/corrective_action_detail_screen.dart --no-fatal-infos 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `grep "class CorrectiveActionDetailScreen" primeaudit/lib/screens/corrective_action_detail_screen.dart` retorna match
    - `grep "CorrectiveActionService.canTransitionTo" primeaudit/lib/screens/corrective_action_detail_screen.dart` retorna match
    - `grep "_doTransition" primeaudit/lib/screens/corrective_action_detail_screen.dart` retorna match
    - `grep "_confirmTransition" primeaudit/lib/screens/corrective_action_detail_screen.dart` retorna match
    - `grep "cancelada" primeaudit/lib/screens/corrective_action_detail_screen.dart` retorna match em _confirmTransition (dialog de confirmacao)
    - `grep "rejeitada" primeaudit/lib/screens/corrective_action_detail_screen.dart` retorna match em _confirmTransition (dialog de confirmacao)
    - `grep "Navigator.pop(context, true)" primeaudit/lib/screens/corrective_action_detail_screen.dart` retorna match (sinalizar reload ao caller)
    - `grep "Cancelar acao corretiva" primeaudit/lib/screens/corrective_action_detail_screen.dart` retorna match (dialog title — copywriting)
    - `grep "Iniciar acao" primeaudit/lib/screens/corrective_action_detail_screen.dart` retorna match (botao CTA — copywriting)
    - `grep "Aprovar" primeaudit/lib/screens/corrective_action_detail_screen.dart` retorna match (botao CTA — copywriting)
    - `flutter analyze lib/screens/corrective_action_detail_screen.dart` sem erros
  </acceptance_criteria>

  <done>CorrectiveActionDetailScreen: info card completo, chip de status, botoes condicionais por role usando canTransitionTo(), AlertDialog para cancelada/rejeitada, pop(true) no sucesso para recarregar lista</done>
</task>

<task type="auto">
  <name>Task 2: home_screen.dart — badge _drawerItem + item Acoes Corretivas + count real (ACT-04) + suite final</name>
  <files>primeaudit/lib/screens/home_screen.dart</files>

  <read_first>
    - primeaudit/lib/screens/home_screen.dart — LER NA INTEGRA. Identificar:
      - _drawerItem() assinatura atual (linhas ~318-338): icon, title, onTap, color — SEM badgeCount ainda
      - _openActions field e como e exibido no dashboard card (linhas ~99, ~264)
      - _loadDashboard(): onde chama DashboardService.getOpenActionsCount() — substituir
      - Drawer build(): estrutura de items existentes para inserir "Acoes Corretivas" entre Auditorias e eventual Relatorios
      - _role field e _authService.currentUser?.id para passar a CorrectiveActionsScreen
    - primeaudit/lib/services/corrective_action_service.dart (Wave 1) — assinatura de getOpenActionsCount
    - .planning/phases/08-corrective-actions/08-RESEARCH.md §Pattern 5 (drawer item com badge) e §Pitfall 1 (DashboardService bug)
    - .planning/phases/08-corrective-actions/08-PATTERNS.md §home_screen.dart
    - .planning/phases/08-corrective-actions/08-UI-SPEC.md §Navigation Badge, §Copywriting Contract §Navigation Badge
    - primeaudit/lib/screens/corrective_actions_screen.dart (Wave 3) — assinatura do constructor (currentUserId, currentUserRole)
  </read_first>

  <action>
Modificar `primeaudit/lib/screens/home_screen.dart` em 4 pontos CIRURGICOS. Ler o arquivo completo antes de qualquer alteracao.

MODIFICACAO 1: Adicionar import de CorrectiveActionService e CorrectiveActionsScreen no topo do arquivo:
```dart
import 'services/corrective_action_service.dart';
import 'screens/corrective_actions_screen.dart';
```
(Ajustar o path relativo baseado nos imports existentes no arquivo — podem ser '../services/' ou 'services/' dependendo de onde home_screen.dart esta localizado.)

MODIFICACAO 2: Adicionar field `_correctiveActionService` em _HomeScreenState (junto com outros service fields declarados):
```dart
final _correctiveActionService = CorrectiveActionService();
```

MODIFICACAO 3: Modificar `_drawerItem()` para suportar `badgeCount` opcional:

Localizar o metodo `_drawerItem({required IconData icon, required String title, required VoidCallback onTap, Color? color})`.

Substituir o metodo completo por:
```dart
Widget _drawerItem({
  required IconData icon,
  required String title,
  required VoidCallback onTap,
  Color? color,
  int badgeCount = 0,
}) {
  final itemColor = color ?? AppTheme.of(context).textPrimary;
  Widget iconWidget = Icon(icon, color: itemColor, size: 22);
  if (badgeCount > 0) {
    iconWidget = Badge(
      label: Text('$badgeCount'),
      child: iconWidget,
    );
  }
  return ListTile(
    onTap: onTap,
    leading: iconWidget,
    title: Text(title,
        style: TextStyle(color: itemColor, fontSize: 15, fontWeight: FontWeight.w500)),
    horizontalTitleGap: 8,
    contentPadding: const EdgeInsets.symmetric(horizontal: 20, vertical: 2),
  );
}
```

NOTA: O widget `Badge` e parte do Flutter SDK >= 3.7.0 — projeto usa >= 3.38.4, disponivel sem imports adicionais.

MODIFICACAO 4: Atualizar `_loadDashboard()` para usar `_correctiveActionService.getOpenActionsCount()`:

Localizar no `_loadDashboard()` a linha que chama `_dashboardService.getOpenActionsCount(companyId)`.

Substituir essa linha por:
```dart
final openActions = await _correctiveActionService.getOpenActionsCount(companyId);
```

IMPORTANTE: A chamada deve continuar dentro do mesmo `try/catch` de `_loadDashboard()` se houver um — apenas substituir a origem do dado, nao a estrutura do metodo.

MODIFICACAO 5: Adicionar item "Acoes Corretivas" no Drawer:

Localizar o item do drawer "Auditorias" (onTap que navega para AuditsScreen ou similar). Adicionar o novo item LOGO APOS o item "Auditorias":

```dart
_drawerItem(
  icon: Icons.assignment_late_outlined,
  title: 'Acoes Corretivas',
  badgeCount: _openActions,
  onTap: () {
    Navigator.pop(context); // fechar drawer
    Navigator.of(context).push(
      MaterialPageRoute(
        builder: (_) => CorrectiveActionsScreen(
          currentUserId: Supabase.instance.client.auth.currentUser?.id ?? '',
          currentUserRole: _role,
        ),
      ),
    ).then((_) => _loadDashboard()); // atualizar badge ao voltar
  },
),
```

NOTA: `_role` e o field que contem o role do usuario logado. Verificar o nome exato ao ler o arquivo (pode ser `_userRole`, `_role`, `_profile?.role`). Usar o nome correto.

NOTA: Se o drawer fecha automaticamente ao navegar, nao e necessario `Navigator.pop(context)` antes do push — verificar padrao existente nos outros itens do drawer.

MODIFICACAO 6 (se necessario): Se `_openActions` nao estiver declarado em _HomeScreenState, adicionar:
```dart
int _openActions = 0;
```
(O campo ja deve existir da Phase 7 — confirmar ao ler o arquivo.)

APOS TODAS AS MODIFICACOES:
- `cd primeaudit && flutter analyze lib/screens/home_screen.dart --no-fatal-infos`
- `cd primeaudit && flutter test` — DEVE ESTAR VERDE (suite completa)

Se flutter test falhar, investigar e corrigir ANTES de fechar o plano.
  </action>

  <verify>
    <automated>cd primeaudit && flutter test 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep "badgeCount" primeaudit/lib/screens/home_screen.dart` retorna >= 3 matches (parametro no _drawerItem, chamada com badge: _openActions, declaracao Badge widget)
    - `grep "Badge(" primeaudit/lib/screens/home_screen.dart` retorna match (widget Badge real)
    - `grep "CorrectiveActionsScreen" primeaudit/lib/screens/home_screen.dart` retorna match (drawer item)
    - `grep "Acoes Corretivas" primeaudit/lib/screens/home_screen.dart` retorna match (titulo do drawer item)
    - `grep "assignment_late_outlined" primeaudit/lib/screens/home_screen.dart` retorna match (icone do drawer item)
    - `grep "_correctiveActionService.getOpenActionsCount" primeaudit/lib/screens/home_screen.dart` retorna match
    - `flutter analyze lib/screens/home_screen.dart` sem erros de compilacao
    - `cd primeaudit && flutter test` termina com exit code 0 (SUITE COMPLETA VERDE)
    - Grep confirma que DashboardService.getOpenActionsCount nao e mais chamado em home_screen.dart (ou que a chamada foi substituida): `grep -c "dashboardService.getOpenActionsCount\|_dashboardService.getOpenActionsCount" primeaudit/lib/screens/home_screen.dart` retorna 0
  </acceptance_criteria>

  <done>home_screen.dart: _drawerItem suporta badgeCount com Material 3 Badge, item Acoes Corretivas no drawer com badge real, _loadDashboard usa CorrectiveActionService.getOpenActionsCount para aberta+em_andamento+em_avaliacao, suite completa flutter test verde</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CorrectiveActionDetailScreen → updateStatus | newStatus string cruzando para UPDATE no banco |
| home_screen.dart _loadDashboard → getOpenActionsCount | Count de acoes cruzando para o app — informacao de contagem por empresa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-14 | Elevation of Privilege | Status transition — auditor tentando aprovar status aberta (sem passar por em_avaliacao) | mitigate | CorrectiveActionService.canTransitionTo() verifica TANTO o role QUANTO o status atual da acao — aprovada so aparece para auditor quando `action.status == emAvaliacao`; UI nao renderiza botao em outras condicoes |
| T-8-15 | Elevation of Privilege | Responsavel tentando cancelar | mitigate | canTransitionTo() para 'cancelada' retorna false para qualquer role nao-admin; UI nao renderiza botao "Cancelar acao" para responsavel ou auditor |
| T-8-16 | Tampering | updateStatus chamado com newStatus invalido | accept | CHECK constraint no banco (`corrective_actions_status_check`) rejeita qualquer valor fora do enum de 6 estados — barreira de banco independente da UI |
| T-8-17 | Elevation of Privilege | RLS UPDATE policy — auditor atualizando status para valor nao-autorizado | accept | RLS UPDATE policy cobre company scope (nao colunas especificas); UI e a camada de enforcement de status per CONTEXT.md ("RLS nao precisa bloquear — UI e suficiente nesta milestone"); CHECK constraint bloqueia valores invalidos |
| T-8-18 | Information Disclosure | Badge count expoe numero de acoes abertas a todos os roles | accept | Count e intencional — ACT-04 especifica badge visivel a todos; nao expoe conteudo das acoes, apenas contagem; RLS garante que o count e scoped por empresa do usuario |
</threat_model>

<verification>
Wave 4 completa quando:
1. `primeaudit/lib/screens/corrective_action_detail_screen.dart` existe e `flutter analyze` sem erros
2. `grep "CorrectiveActionService.canTransitionTo"` retorna match em corrective_action_detail_screen.dart
3. `grep "badgeCount"` retorna match em home_screen.dart
4. `grep "CorrectiveActionsScreen"` retorna match em home_screen.dart
5. `cd primeaudit && flutter test` — EXIT CODE 0 (suite completa verde) — GATE BLOQUEANTE
</verification>

<success_criteria>
- CorrectiveActionDetailScreen: info card com todos os campos, chip de status prominente, botoes RBAC-gated usando canTransitionTo(), AlertDialog para cancelada/rejeitada, pop(true) no sucesso
- home_screen.dart: _drawerItem suporta badgeCount, item "Acoes Corretivas" no drawer com badge real, getOpenActionsCount usa CorrectiveActionService (inFilter para aberta+em_andamento+em_avaliacao)
- Badge aparece apenas quando count > 0 (Badge widget do Flutter Material 3)
- Badge atualiza ao voltar de CorrectiveActionsScreen via .then(_loadDashboard)
- SUITE COMPLETA `flutter test` — EXIT CODE 0
- Todos os 4 requisitos ACT-01, ACT-02, ACT-03, ACT-04 cobertos por planos executados
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/08-corrective-actions/08-04-SUMMARY.md` seguindo o template em `@C:\Users\eder3\.claude\get-shit-done\templates\summary.md`
</output>
