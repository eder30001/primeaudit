---
phase: 09-images
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - primeaudit/supabase/migrations/20260427_create_audit_item_images.sql
  - primeaudit/pubspec.yaml
  - primeaudit/android/app/src/main/AndroidManifest.xml
autonomous: false
requirements:
  - IMG-01
  - IMG-02
  - IMG-03

must_haves:
  truths:
    - "A tabela audit_item_images existe no banco com as colunas id, audit_id, template_item_id, company_id, storage_path, created_by, created_at"
    - "RLS da tabela scope queries por company_id (auditor seleciona/insere; adm CRUD completo; superuser/dev acesso total)"
    - "O bucket audit-images existe no Supabase Storage como bucket privado"
    - "O package image_picker ^1.1.2 está declarado em pubspec.yaml"
    - "As três permissões de câmera/storage estão declaradas em AndroidManifest.xml"
  artifacts:
    - path: "primeaudit/supabase/migrations/20260427_create_audit_item_images.sql"
      provides: "DDL idempotente para audit_item_images + bucket audit-images + RLS"
      contains: "CREATE TABLE IF NOT EXISTS audit_item_images"
    - path: "primeaudit/pubspec.yaml"
      provides: "Dependência image_picker"
      contains: "image_picker: ^1.1.2"
    - path: "primeaudit/android/app/src/main/AndroidManifest.xml"
      provides: "Permissões de câmera e storage para image_picker"
      contains: "android.permission.CAMERA"
  key_links:
    - from: "audit_item_images.company_id"
      to: "companies.id"
      via: "FK constraint"
      pattern: "audit_item_images_company_id_fkey"
    - from: "audit_item_images.audit_id"
      to: "audits.id"
      via: "FK constraint com ON DELETE CASCADE"
      pattern: "audit_item_images_audit_id_fkey"
    - from: "audit_item_images.template_item_id"
      to: "template_items.id"
      via: "FK constraint"
      pattern: "audit_item_images_template_item_id_fkey"
---

<objective>
Criar a infraestrutura de backend e dependências de plataforma necessárias para o upload de imagens por pergunta.

Purpose: Sem a tabela `audit_item_images`, o bucket Storage e as permissões de câmera, nenhuma das funcionalidades de imagem pode ser implementada. Este plano entrega toda a fundação antes de qualquer código Dart ser escrito.

Output:
- Migration SQL idempotente criando a tabela `audit_item_images` com RLS e bucket `audit-images`
- Schema aplicado ao banco via `supabase db push`
- `image_picker: ^1.1.2` adicionado ao pubspec.yaml
- Três permissões Android declaradas no AndroidManifest.xml
</objective>

<execution_context>
@C:/Users/eder3/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/eder3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-images/09-UI-SPEC.md

<interfaces>
<!-- Padrão idempotente de migrations existentes — seguir exatamente -->
<!-- De: primeaudit/supabase/migrations/20260425_create_corrective_actions.sql -->

Padrão de migration:
1. CREATE TABLE IF NOT EXISTS {tabela} (id UUID DEFAULT gen_random_uuid() PRIMARY KEY);
2. ALTER TABLE {tabela} ADD COLUMN IF NOT EXISTS {col} {tipo};
3. ALTER TABLE {tabela} DROP CONSTRAINT IF EXISTS {nome}; ALTER TABLE {tabela} ADD CONSTRAINT {nome} FOREIGN KEY ...;
4. CREATE INDEX IF NOT EXISTS idx_{tabela}_{col} ON {tabela} ({col});
5. ALTER TABLE {tabela} ENABLE ROW LEVEL SECURITY;
6. DROP POLICY IF EXISTS "{nome}" ON {tabela}; CREATE POLICY "{nome}" ON {tabela} ...;
7. NOTIFY pgrst, 'reload schema';  -- sempre última linha

Funções RLS existentes (disponíveis no DB):
  get_my_role()       → TEXT ('superuser'|'dev'|'adm'|'auditor'|'anonymous')
  get_my_company_id() → UUID

Padrão pubspec.yaml (flutter pub add não é usado — editar manualmente):
  dependencies:
    image_picker: ^1.1.2   # inserir após fl_chart: ^1.2.0

AndroidManifest.xml atual: zero permissões declaradas (apenas <application> e <queries>).
Permissões devem ser adicionadas ANTES de <application>, dentro de <manifest>.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration SQL — audit_item_images table + Storage bucket policy</name>
  <files>primeaudit/supabase/migrations/20260427_create_audit_item_images.sql</files>

  <read_first>
    - primeaudit/supabase/migrations/20260425_create_corrective_actions.sql (padrão idempotente — seguir exatamente)
    - primeaudit/supabase/migrations/20260427_corrective_actions_delete_policy.sql (arquivo mais recente — confirmar prefixo de data disponível; se 20260427 já existe como prefixo, usar 20260427_create_audit_item_images para este arquivo — nome de arquivo diferente é suficiente)
    - .planning/phases/09-images/09-UI-SPEC.md §Supabase Storage Contract (path pattern, bucket name, access policy)
  </read_first>

  <action>
Criar o arquivo `primeaudit/supabase/migrations/20260427_create_audit_item_images.sql` com o seguinte conteúdo EXATO:

```sql
-- =============================================================================
-- Migracao: tabela audit_item_images + bucket audit-images (Storage)
-- Data: 2026-04-27
-- Idempotente: pode ser executado multiplas vezes sem erro.
-- =============================================================================

-- 1. Tabela base
CREATE TABLE IF NOT EXISTS audit_item_images (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY
);

-- 2. Colunas (idempotentes)
ALTER TABLE audit_item_images ADD COLUMN IF NOT EXISTS audit_id          UUID NOT NULL;
ALTER TABLE audit_item_images ADD COLUMN IF NOT EXISTS template_item_id  UUID NOT NULL;
ALTER TABLE audit_item_images ADD COLUMN IF NOT EXISTS company_id        UUID NOT NULL;
ALTER TABLE audit_item_images ADD COLUMN IF NOT EXISTS storage_path      TEXT NOT NULL;
ALTER TABLE audit_item_images ADD COLUMN IF NOT EXISTS created_by        UUID NOT NULL;
ALTER TABLE audit_item_images ADD COLUMN IF NOT EXISTS created_at        TIMESTAMPTZ NOT NULL DEFAULT NOW();

-- 3. Foreign keys (drop + add para idempotencia)
ALTER TABLE audit_item_images DROP CONSTRAINT IF EXISTS audit_item_images_audit_id_fkey;
ALTER TABLE audit_item_images ADD CONSTRAINT audit_item_images_audit_id_fkey
  FOREIGN KEY (audit_id) REFERENCES audits(id) ON DELETE CASCADE;

ALTER TABLE audit_item_images DROP CONSTRAINT IF EXISTS audit_item_images_template_item_id_fkey;
ALTER TABLE audit_item_images ADD CONSTRAINT audit_item_images_template_item_id_fkey
  FOREIGN KEY (template_item_id) REFERENCES template_items(id) ON DELETE RESTRICT;

ALTER TABLE audit_item_images DROP CONSTRAINT IF EXISTS audit_item_images_company_id_fkey;
ALTER TABLE audit_item_images ADD CONSTRAINT audit_item_images_company_id_fkey
  FOREIGN KEY (company_id) REFERENCES companies(id) ON DELETE RESTRICT;

ALTER TABLE audit_item_images DROP CONSTRAINT IF EXISTS audit_item_images_created_by_fkey;
ALTER TABLE audit_item_images ADD CONSTRAINT audit_item_images_created_by_fkey
  FOREIGN KEY (created_by) REFERENCES profiles(id) ON DELETE RESTRICT;

-- 4. Indexes
CREATE INDEX IF NOT EXISTS idx_audit_item_images_audit_item
  ON audit_item_images (audit_id, template_item_id);
CREATE INDEX IF NOT EXISTS idx_audit_item_images_company_id
  ON audit_item_images (company_id);
CREATE INDEX IF NOT EXISTS idx_audit_item_images_created_at
  ON audit_item_images (created_at DESC);

-- 5. RLS
ALTER TABLE audit_item_images ENABLE ROW LEVEL SECURITY;

-- superuser e dev: acesso total
DROP POLICY IF EXISTS "superuser_dev_audit_item_images_full" ON audit_item_images;
CREATE POLICY "superuser_dev_audit_item_images_full" ON audit_item_images
  USING (get_my_role() IN ('superuser', 'dev'))
  WITH CHECK (get_my_role() IN ('superuser', 'dev'));

-- adm: CRUD na propria empresa
DROP POLICY IF EXISTS "adm_audit_item_images_company" ON audit_item_images;
CREATE POLICY "adm_audit_item_images_company" ON audit_item_images
  USING (get_my_role() = 'adm' AND company_id = get_my_company_id())
  WITH CHECK (get_my_role() = 'adm' AND company_id = get_my_company_id());

-- auditor: SELECT na propria empresa
DROP POLICY IF EXISTS "auditor_audit_item_images_select" ON audit_item_images;
CREATE POLICY "auditor_audit_item_images_select" ON audit_item_images
  FOR SELECT
  USING (get_my_role() = 'auditor' AND company_id = get_my_company_id());

-- auditor: INSERT na propria empresa (apenas proprias imagens)
DROP POLICY IF EXISTS "auditor_audit_item_images_insert" ON audit_item_images;
CREATE POLICY "auditor_audit_item_images_insert" ON audit_item_images
  FOR INSERT
  WITH CHECK (
    get_my_role() = 'auditor'
    AND company_id = get_my_company_id()
    AND created_by = auth.uid()
  );

-- auditor: DELETE na propria empresa (apenas proprias imagens)
DROP POLICY IF EXISTS "auditor_audit_item_images_delete" ON audit_item_images;
CREATE POLICY "auditor_audit_item_images_delete" ON audit_item_images
  FOR DELETE
  USING (
    get_my_role() = 'auditor'
    AND company_id = get_my_company_id()
    AND created_by = auth.uid()
  );

-- 6. Storage bucket audit-images (bucket privado)
-- Insere apenas se ainda nao existir — idempotente
INSERT INTO storage.buckets (id, name, public)
VALUES ('audit-images', 'audit-images', false)
ON CONFLICT (id) DO NOTHING;

-- 7. Storage RLS: upload autenticado restrito ao proprio company_id
-- O path segue o padrao: {companyId}/{auditId}/{itemId}/{uuid}.jpg
-- A policy verifica que o primeiro segmento do path corresponde ao company_id do usuario

-- Politica de INSERT (upload) no Storage
DROP POLICY IF EXISTS "authenticated_upload_audit_images" ON storage.objects;
CREATE POLICY "authenticated_upload_audit_images" ON storage.objects
  FOR INSERT
  TO authenticated
  WITH CHECK (
    bucket_id = 'audit-images'
    AND (storage.foldername(name))[1] = get_my_company_id()::text
  );

-- Politica de SELECT (leitura para signed URL) no Storage
DROP POLICY IF EXISTS "authenticated_read_audit_images" ON storage.objects;
CREATE POLICY "authenticated_read_audit_images" ON storage.objects
  FOR SELECT
  TO authenticated
  USING (
    bucket_id = 'audit-images'
    AND (storage.foldername(name))[1] = get_my_company_id()::text
  );

-- Politica de DELETE no Storage
DROP POLICY IF EXISTS "authenticated_delete_audit_images" ON storage.objects;
CREATE POLICY "authenticated_delete_audit_images" ON storage.objects
  FOR DELETE
  TO authenticated
  USING (
    bucket_id = 'audit-images'
    AND (storage.foldername(name))[1] = get_my_company_id()::text
  );

-- 8. Recarregar schema do PostgREST (sempre ultima linha)
NOTIFY pgrst, 'reload schema';
```
  </action>

  <verify>
    <automated>grep -c "CREATE TABLE IF NOT EXISTS audit_item_images" primeaudit/supabase/migrations/20260427_create_audit_item_images.sql</automated>
  </verify>

  <acceptance_criteria>
    - grep "CREATE TABLE IF NOT EXISTS audit_item_images" no arquivo retorna 1
    - grep "storage_path" no arquivo retorna 1
    - grep "audit-images" no arquivo retorna pelo menos 3 (bucket insert + 3 policies)
    - grep "get_my_company_id" no arquivo retorna pelo menos 5 ocorrências
    - grep "NOTIFY pgrst" no arquivo retorna 1 e é a última instrução SQL do arquivo
    - grep "ON DELETE CASCADE" no arquivo retorna 1 (apenas audit_id — template_item e company_id são RESTRICT)
    - Arquivo termina com newline; não contém tabs (usar espaços)
  </acceptance_criteria>

  <done>Arquivo de migration criado com DDL completo, idempotente, RLS correto e políticas de Storage.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2: [BLOCKING] supabase db push — aplicar schema ao banco</name>
  <files></files>

  <read_first>
    - primeaudit/supabase/migrations/20260427_create_audit_item_images.sql (verificar que foi criado pela Task 1 antes de fazer push)
  </read_first>

  <action>
Executar o push do schema para o banco Supabase remoto.

Comando (rodar a partir de `primeaudit/`):
```bash
cd primeaudit && supabase db push
```

Se o comando solicitar confirmação interativa (y/n prompt), responder `y`.

Se o comando falhar com erro de autenticação: definir `SUPABASE_ACCESS_TOKEN` via `supabase login` antes de repetir.

Se falhar com erro de SQL (e.g., `function storage.foldername does not exist`): Storage RLS pode não ser suportado via migration neste projeto. Nesse caso:
1. Remover as três políticas `storage.objects` do arquivo SQL
2. Adicionar comentário: `-- Storage RLS: configurar manualmente no Supabase Dashboard > Storage > Policies`
3. Re-executar `supabase db push`
4. Sinalizar ao usuário que as políticas de Storage precisam ser criadas manualmente no Dashboard

Verificar que o push completou sem erros antes de prosseguir.
  </action>

  <verify>
    <automated>cd primeaudit && supabase db push 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - Saída do comando não contém "ERROR" ou "error" (case-insensitive)
    - Saída contém "Done" ou "Migrations applied" ou similar indicando sucesso
    - Caso falhe na parte de Storage RLS: arquivo SQL foi atualizado (políticas storage removidas), push re-executado com sucesso, e comentário de ação manual adicionado ao arquivo
  </acceptance_criteria>

  <done>Schema aplicado ao banco remoto. Tabela audit_item_images existe no Supabase com RLS ativo.</done>
</task>

<task type="auto">
  <name>Task 3: pubspec.yaml — image_picker dependency + AndroidManifest permissions</name>
  <files>
    primeaudit/pubspec.yaml
    primeaudit/android/app/src/main/AndroidManifest.xml
  </files>

  <read_first>
    - primeaudit/pubspec.yaml (ler antes de editar — adicionar image_picker após fl_chart na seção dependencies)
    - primeaudit/android/app/src/main/AndroidManifest.xml (ler antes de editar — adicionar permissões antes de &lt;application&gt;)
    - .planning/phases/09-images/09-UI-SPEC.md §Package Safety Gate (confirmar versão ^1.1.2)
  </read_first>

  <action>
**pubspec.yaml:** Adicionar `image_picker: ^1.1.2` na seção `dependencies`, após a linha `fl_chart: ^1.2.0`:

```yaml
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.8.4
  shared_preferences: ^2.3.3
  fl_chart: ^1.2.0
  image_picker: ^1.1.2    # camera + gallery picker (Phase 9)
```

**AndroidManifest.xml:** Adicionar as três permissões ANTES do elemento `<application>`, logo após a tag de abertura `<manifest ...>`:

```xml
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <application
        ...
```

Após editar ambos os arquivos, executar `flutter pub get` a partir de `primeaudit/`:
```bash
cd primeaudit && flutter pub get
```

Verificar que o comando completa sem erros e que `pubspec.lock` foi atualizado com `image_picker`.
  </action>

  <verify>
    <automated>grep -c "image_picker" primeaudit/pubspec.yaml && grep -c "android.permission.CAMERA" "primeaudit/android/app/src/main/AndroidManifest.xml"</automated>
  </verify>

  <acceptance_criteria>
    - grep "image_picker: \^1.1.2" primeaudit/pubspec.yaml retorna 1
    - grep "android.permission.CAMERA" primeaudit/android/app/src/main/AndroidManifest.xml retorna 1
    - grep "android.permission.READ_EXTERNAL_STORAGE" primeaudit/android/app/src/main/AndroidManifest.xml retorna 1 com atributo android:maxSdkVersion="32"
    - grep "android.permission.READ_MEDIA_IMAGES" primeaudit/android/app/src/main/AndroidManifest.xml retorna 1
    - flutter pub get completa sem error (pubspec.lock contém image_picker)
    - grep "image_picker" primeaudit/pubspec.lock retorna pelo menos 1 linha
  </acceptance_criteria>

  <done>image_picker adicionado ao pubspec e pubspec.lock atualizado. Três permissões Android declaradas. flutter pub get limpo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → Supabase Storage | Upload de arquivo binário (imagem JPEG) com JWT do usuário autenticado |
| app → Supabase DB | INSERT em audit_item_images com company_id e created_by do usuário |
| Supabase Storage → app | Signed URL para leitura de imagem privada (1 hora de validade) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Spoofing | audit_item_images INSERT | mitigate | RLS policy `auditor_audit_item_images_insert` força `created_by = auth.uid()` — usuário não pode inserir imagens com created_by de outro usuário |
| T-09-02 | Tampering | storage_path (path injection) | mitigate | Path construído exclusivamente no cliente com UUID v4 gerado localmente; servidor valida apenas que o primeiro segmento = company_id do usuário via `storage.foldername(name)[1]` |
| T-09-03 | Information Disclosure | bucket audit-images | mitigate | Bucket declarado como `public: false`; acesso via signed URL com 1h de expiração; RLS de Storage restringe leitura ao mesmo company_id |
| T-09-04 | Elevation of Privilege | Storage policy bypass | mitigate | Policy de INSERT no Storage verifica `get_my_company_id()` — auditor não pode fazer upload para pasta de outra empresa |
| T-09-05 | Denial of Service | upload de arquivo muito grande | accept | image_picker comprime com maxWidth: 1200 e imageQuality: 85 antes do upload (reduz tamanho ~80%); limite de tamanho de bucket é configurável no Supabase Dashboard — risco residual baixo em auditoria industrial |
</threat_model>

<verification>
Após Task 2 (supabase db push):
- Tabela `audit_item_images` existe no banco (confirmar via Supabase Dashboard ou `supabase db status`)
- Bucket `audit-images` existe em Storage > Buckets no Dashboard

Após Task 3 (flutter pub get):
- `flutter analyze primeaudit/` não reporta erros novos
- `flutter build apk --debug` (opcional — se ambiente disponível) não falha por falta de permissão
</verification>

<success_criteria>
1. Migration SQL idempotente criada seguindo o padrão estabelecido (ADD COLUMN IF NOT EXISTS, DROP CONSTRAINT IF EXISTS / ADD CONSTRAINT)
2. `supabase db push` executado sem erro — schema no banco reflete a migration
3. Tabela `audit_item_images` com 7 colunas, 3 indexes, RLS habilitado e 5 políticas
4. Bucket `audit-images` criado como privado
5. `image_picker: ^1.1.2` em pubspec.yaml com `flutter pub get` limpo
6. 3 permissões Android em AndroidManifest.xml (CAMERA, READ_EXTERNAL_STORAGE maxSdk=32, READ_MEDIA_IMAGES)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/09-images/09-01-SUMMARY.md` com:
- Artifacts created (migration file path, tables/indexes/policies created)
- supabase db push outcome (success / fallback applied for storage RLS)
- pubspec.lock image_picker version resolved
- Any deviations from plan (e.g., Storage RLS manual setup required)
</output>
