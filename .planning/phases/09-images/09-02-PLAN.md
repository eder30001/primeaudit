---
phase: 09-images
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - primeaudit/lib/models/audit_item_image.dart
  - primeaudit/lib/services/image_service.dart
autonomous: true
requirements:
  - IMG-01
  - IMG-02
  - IMG-03

must_haves:
  truths:
    - "AuditItemImage.fromMap() deserializa corretamente todos os campos da tabela audit_item_images"
    - "ImageService.uploadImage() faz upload para Storage no path {companyId}/{auditId}/{itemId}/{uuid}.jpg e insere metadados em audit_item_images"
    - "ImageService.getImages() retorna List<AuditItemImage> para um par (auditId, itemId)"
    - "ImageService.deleteImage() remove o objeto do Storage E exclui o registro da tabela audit_item_images"
    - "Falha de upload NÃO lança exceção propagada — retorna null ou lança para que o caller (_ImageStrip) decida o estado de erro sem bloquear _saveAnswer"
  artifacts:
    - path: "primeaudit/lib/models/audit_item_image.dart"
      provides: "Modelo tipado de linha da tabela audit_item_images"
      exports: ["AuditItemImage"]
      min_lines: 20
    - path: "primeaudit/lib/services/image_service.dart"
      provides: "CRUD de imagens no Supabase Storage + tabela audit_item_images"
      exports: ["ImageService"]
      min_lines: 50
  key_links:
    - from: "ImageService.uploadImage()"
      to: "Supabase.instance.client.storage.from('audit-images').uploadBinary()"
      via: "XFile.readAsBytes()"
      pattern: "storage.from.*audit-images.*uploadBinary"
    - from: "ImageService.uploadImage()"
      to: "Supabase.instance.client.from('audit_item_images').insert()"
      via: "após upload bem-sucedido"
      pattern: "from.*audit_item_images.*insert"
    - from: "ImageService.getImages()"
      to: "audit_item_images table"
      via: "PostgREST .eq('audit_id') + .eq('template_item_id')"
      pattern: "from.*audit_item_images.*eq"
---

<objective>
Criar o modelo `AuditItemImage` e o serviço `ImageService` que encapsulam toda interação com Supabase Storage e a tabela `audit_item_images`.

Purpose: Os widgets de UI (Wave 3) dependem deste contrato de serviço. Implementar aqui, isolado de UI, permite testar a lógica de upload/delete independentemente.

Output:
- `primeaudit/lib/models/audit_item_image.dart` — modelo com `fromMap` factory
- `primeaudit/lib/services/image_service.dart` — 3 métodos públicos: `uploadImage`, `getImages`, `deleteImage`
</objective>

<execution_context>
@C:/Users/eder3/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/eder3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-images/09-UI-SPEC.md
@.planning/phases/09-images/09-01-SUMMARY.md

<interfaces>
<!-- Padrão de model (de primeaudit/lib/models/audit.dart e audit_template.dart): -->
<!-- - const constructor com named parameters (required/optional) -->
<!-- - factory XModel.fromMap(Map<String, dynamic> map) -->
<!-- - Sem toMap() — serialização inline no service -->
<!-- - Sem code generation -->

<!-- Padrão de service (de primeaudit/lib/services/audit_answer_service.dart): -->
<!--   final _client = Supabase.instance.client; -->
<!--   Future<T> getX(...) async { ... } -->
<!--   Sem try/catch interno — caller é responsável -->
<!-- EXCEÇÃO para ImageService: uploadImage pode lançar mas o error state -->
<!-- é gerenciado pelo caller (_ImageStrip) que NUNCA propaga para _saveAnswer -->

<!-- Supabase Storage API (supabase_flutter 2.8.4): -->
<!--   final storageClient = Supabase.instance.client.storage; -->
<!--   await storageClient.from('audit-images').uploadBinary(path, bytes, fileOptions: FileOptions(contentType: 'image/jpeg', upsert: false)); -->
<!--   final signedUrl = await storageClient.from('audit-images').createSignedUrl(path, 3600); -->
<!--   await storageClient.from('audit-images').remove([path]); -->

<!-- UUID gerado no cliente (Dart): usar dart:math para uuid simples -->
<!-- NÃO adicionar package uuid — gerar com: -->
<!--   String _uuid() { -->
<!--     final r = Random.secure(); -->
<!--     final bytes = List<int>.generate(16, (_) => r.nextInt(256)); -->
<!--     bytes[6] = (bytes[6] & 0x0f) | 0x40; -->
<!--     bytes[8] = (bytes[8] & 0x3f) | 0x80; -->
<!--     return [ -->
<!--       bytes.sublist(0,4), bytes.sublist(4,6), bytes.sublist(6,8), -->
<!--       bytes.sublist(8,10), bytes.sublist(10,16) -->
<!--     ].map((b) => b.map((x) => x.toRadixString(16).padLeft(2,'0')).join()).join('-'); -->
<!--   } -->

<!-- XFile (image_picker retorna XFile): -->
<!--   import 'package:image_picker/image_picker.dart'; -->
<!--   final bytes = await xFile.readAsBytes(); -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AuditItemImage model</name>
  <files>primeaudit/lib/models/audit_item_image.dart</files>

  <read_first>
    - primeaudit/lib/models/audit_template.dart (padrão de model com fromMap — seguir exatamente)
    - .planning/phases/09-images/09-UI-SPEC.md §Supabase Storage Contract (confirmar campos da tabela)
  </read_first>

  <action>
Criar `primeaudit/lib/models/audit_item_image.dart` com o seguinte conteúdo:

```dart
class AuditItemImage {
  final String id;
  final String auditId;
  final String templateItemId;
  final String companyId;
  final String storagePath;
  final String createdBy;
  final DateTime createdAt;

  const AuditItemImage({
    required this.id,
    required this.auditId,
    required this.templateItemId,
    required this.companyId,
    required this.storagePath,
    required this.createdBy,
    required this.createdAt,
  });

  factory AuditItemImage.fromMap(Map<String, dynamic> map) {
    return AuditItemImage(
      id: map['id'] as String,
      auditId: map['audit_id'] as String,
      templateItemId: map['template_item_id'] as String,
      companyId: map['company_id'] as String,
      storagePath: map['storage_path'] as String,
      createdBy: map['created_by'] as String,
      createdAt: DateTime.parse(map['created_at'] as String),
    );
  }
}
```

Nota: sem import de flutter/material.dart — este model não usa Color nem IconData. Apenas Dart puro.
  </action>

  <verify>
    <automated>grep -c "factory AuditItemImage.fromMap" primeaudit/lib/models/audit_item_image.dart</automated>
  </verify>

  <acceptance_criteria>
    - grep "class AuditItemImage" retorna 1
    - grep "factory AuditItemImage.fromMap" retorna 1
    - grep "storagePath" retorna 1 (campo camelCase correto — não storage_path)
    - grep "templateItemId" retorna 1
    - Arquivo não importa flutter/material.dart (model é Dart puro)
    - flutter analyze primeaudit/lib/models/audit_item_image.dart não reporta erros
  </acceptance_criteria>

  <done>Modelo AuditItemImage com fromMap factory criado. Todos os campos da tabela audit_item_images representados como propriedades typed.</done>
</task>

<task type="auto">
  <name>Task 2: ImageService — upload, getImages, deleteImage</name>
  <files>primeaudit/lib/services/image_service.dart</files>

  <read_first>
    - primeaudit/lib/services/audit_answer_service.dart (padrão de service — _client, Future métodos, sem try/catch interno)
    - primeaudit/lib/services/corrective_action_service.dart (exemplo de service com insert + fromMap)
    - primeaudit/lib/models/audit_item_image.dart (criado na Task 1 — tipos a retornar)
    - .planning/phases/09-images/09-UI-SPEC.md §Supabase Storage Contract e §Upload State Machine
  </read_first>

  <action>
Criar `primeaudit/lib/services/image_service.dart` com o seguinte conteúdo:

```dart
import 'dart:math';
import 'package:image_picker/image_picker.dart';
import 'package:supabase_flutter/supabase_flutter.dart';
import '../models/audit_item_image.dart';

class ImageService {
  final _client = Supabase.instance.client;
  static const _bucket = 'audit-images';

  // Gera UUID v4 sem dependencia externa
  String _uuid() {
    final r = Random.secure();
    final bytes = List<int>.generate(16, (_) => r.nextInt(256));
    bytes[6] = (bytes[6] & 0x0f) | 0x40;
    bytes[8] = (bytes[8] & 0x3f) | 0x80;
    return [
      bytes.sublist(0, 4),
      bytes.sublist(4, 6),
      bytes.sublist(6, 8),
      bytes.sublist(8, 10),
      bytes.sublist(10, 16),
    ].map((b) => b.map((x) => x.toRadixString(16).padLeft(2, '0')).join()).join('-');
  }

  /// Faz upload de uma imagem para o Storage e registra metadados na tabela.
  /// Path: {companyId}/{auditId}/{itemId}/{uuid}.jpg
  /// Lanca excecao em caso de falha — caller (_ImageStrip) gerencia estado de erro.
  Future<AuditItemImage> uploadImage({
    required String companyId,
    required String auditId,
    required String itemId,
    required XFile file,
  }) async {
    final uuid = _uuid();
    final path = '$companyId/$auditId/$itemId/$uuid.jpg';
    final bytes = await file.readAsBytes();

    await _client.storage.from(_bucket).uploadBinary(
      path,
      bytes,
      fileOptions: const FileOptions(contentType: 'image/jpeg', upsert: false),
    );

    final userId = _client.auth.currentUser!.id;
    final row = await _client.from('audit_item_images').insert({
      'audit_id': auditId,
      'template_item_id': itemId,
      'company_id': companyId,
      'storage_path': path,
      'created_by': userId,
    }).select().single();

    return AuditItemImage.fromMap(row);
  }

  /// Retorna lista de imagens associadas a um item de auditoria.
  Future<List<AuditItemImage>> getImages({
    required String auditId,
    required String itemId,
  }) async {
    final rows = await _client
        .from('audit_item_images')
        .select()
        .eq('audit_id', auditId)
        .eq('template_item_id', itemId)
        .order('created_at');
    return (rows as List).map((r) => AuditItemImage.fromMap(r)).toList();
  }

  /// Gera signed URL com validade de 1 hora para exibicao de imagem privada.
  Future<String> getSignedUrl(String storagePath) async {
    return await _client.storage
        .from(_bucket)
        .createSignedUrl(storagePath, 3600);
  }

  /// Remove imagem do Storage e o registro da tabela.
  /// Remove o registro da tabela mesmo se o Storage falhar (best-effort).
  Future<void> deleteImage({
    required String imageId,
    required String storagePath,
  }) async {
    // Remove do Storage primeiro (best-effort — nao lanca se ja nao existir)
    try {
      await _client.storage.from(_bucket).remove([storagePath]);
    } catch (_) {
      // Storage delete falhou — continua para remover o registro da tabela
    }
    await _client.from('audit_item_images').delete().eq('id', imageId);
  }
}
```

**Notas de implementação:**
- `uploadBinary` vs `upload`: usar `uploadBinary` com `Uint8List` (resultado de `XFile.readAsBytes()`) — mais confiável que `upload(File)` em todos os targets Flutter.
- `getImages` usa `.order('created_at')` sem `ascending` — PostgREST default é ASC, o que é correto (ordem cronológica de upload).
- `deleteImage` tem try/catch interno APENAS no Storage remove — é intencional. O delete do banco deve propagar erro para o caller gerenciar o estado de retry. Não adicionar try/catch no delete da tabela.
  </action>

  <verify>
    <automated>grep -c "class ImageService" primeaudit/lib/services/image_service.dart && grep -c "Future" primeaudit/lib/services/image_service.dart</automated>
  </verify>

  <acceptance_criteria>
    - grep "class ImageService" retorna 1
    - grep "uploadImage" retorna pelo menos 2 (declaration + body)
    - grep "getImages" retorna pelo menos 2
    - grep "deleteImage" retorna pelo menos 2
    - grep "getSignedUrl" retorna pelo menos 2
    - grep "audit-images" retorna pelo menos 1 (_bucket constant)
    - grep "uploadBinary" retorna 1
    - grep "createSignedUrl" retorna 1
    - grep "XFile" retorna pelo menos 1 (import via image_picker)
    - flutter analyze primeaudit/lib/services/image_service.dart não reporta erros
  </acceptance_criteria>

  <done>ImageService implementado com os 4 métodos públicos. Upload persiste no Storage e na tabela. getSignedUrl gera URL temporária de 1 hora. deleteImage remove Storage (best-effort) + registro DB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| XFile → uploadBinary | Dados binários de imagem do dispositivo indo ao Storage |
| ImageService → DB | INSERT/SELECT/DELETE em audit_item_images via PostgREST |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Tampering | storagePath recebido em deleteImage | mitigate | deleteImage recebe storagePath da lista de AuditItemImage carregada do banco — o path é o que foi armazenado pelo uploadImage; nunca aceita path de entrada arbitrária do usuário |
| T-09-07 | Information Disclosure | getSignedUrl expõe URL temporária | accept | Signed URL expira em 1 hora; válida apenas para usuário autenticado da mesma empresa via RLS de Storage; risco residual mínimo |
| T-09-08 | Tampering | upload de arquivo não-JPEG | mitigate | `contentType: 'image/jpeg'` no FileOptions + image_picker retorna apenas imagens — validação client-side suficiente para ASVS L1 neste contexto mobile |
| T-09-09 | Denial of Service | upload de imagem muito grande sem compressão | mitigate | Compressão aplicada no caller (_ImageStrip) via ImagePicker(imageQuality: 85, maxWidth: 1200) antes de passar XFile ao uploadImage — serviço recebe bytes já comprimidos |
</threat_model>

<verification>
Após implementação dos dois arquivos:

```bash
cd primeaudit && flutter analyze lib/models/audit_item_image.dart lib/services/image_service.dart
```

Deve retornar: "No issues found!" ou apenas info/warnings sem erros.

Verificação de cobertura de contrato (sem executar):
- AuditItemImage.fromMap() aceita um map com todas as 7 chaves — verificar manualmente contra colunas da tabela migration
- ImageService.uploadImage retorna AuditItemImage (não void) — o caller usa o objeto retornado para transicionar para estado `uploaded`
- ImageService.deleteImage não tem assinatura que passe storagePath como null
</verification>

<success_criteria>
1. `AuditItemImage.fromMap()` mapeia os 7 campos (id, audit_id, template_item_id, company_id, storage_path, created_by, created_at) — snake_case para camelCase
2. `ImageService.uploadImage()` faz upload binário para path `{companyId}/{auditId}/{itemId}/{uuid}.jpg` e insere registro em `audit_item_images`
3. `ImageService.getImages()` retorna `List<AuditItemImage>` ordenados por created_at ASC
4. `ImageService.getSignedUrl()` retorna signed URL com 3600s de expiração
5. `ImageService.deleteImage()` remove do Storage (best-effort) e da tabela (propagando erro se DB falhar)
6. `flutter analyze` limpo nos dois arquivos novos
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/09-images/09-02-SUMMARY.md` com:
- Artifacts created (model file, service file)
- Method signatures para referência do Wave 3
- Any deviations from plan (e.g., API differences in supabase_flutter version)
</output>
