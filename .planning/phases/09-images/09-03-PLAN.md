---
phase: 09-images
plan: 03
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
files_modified:
  - primeaudit/lib/screens/audit_execution_screen.dart
autonomous: true
requirements:
  - IMG-01
  - IMG-02
  - IMG-03

must_haves:
  truths:
    - "Em qualquer pergunta ativa, o auditor vê um botão de câmera (44dp, cor accent) que abre o picker bottom sheet"
    - "Após selecionar câmera ou galeria, a miniatura aparece imediatamente em estado 'uploading' (spinner overlay)"
    - "Após upload bem-sucedido, a miniatura exibe a imagem final com botão × (20dp visual, 44dp tap) para remover"
    - "Upload com falha mostra ícone de erro na miniatura; o auditor continua e finaliza a auditoria normalmente"
    - "Tocar na miniatura em estado 'uploaded' abre fullscreen viewer (InteractiveViewer, fundo preto)"
    - "Em modo read-only sem imagens, o strip não é renderizado (SizedBox.shrink)"
    - "Em modo read-only com imagens, apenas thumbnails são exibidos (sem câmera, sem botão ×)"
    - "Ao abrir a tela, imagens já salvas são carregadas via ImageService.getImages() para todos os itens"
  artifacts:
    - path: "primeaudit/lib/screens/audit_execution_screen.dart"
      provides: "Widgets _ImageStrip, _ThumbTile, _PickerSheet + fullscreen viewer + propagation chain"
      contains: "class _ImageStrip extends StatefulWidget"
    - path: "primeaudit/lib/screens/audit_execution_screen.dart"
      provides: "Injeção de _ImageStrip no _ItemCard entre observation toggle e criar ação corretiva"
      contains: "_ImageStrip("
  key_links:
    - from: "_ImageStrip._pickImage()"
      to: "ImageService.uploadImage()"
      via: "ImagePicker().pickImage() → XFile → service call"
      pattern: "ImageService.*uploadImage"
    - from: "_AuditExecutionScreenState._load()"
      to: "ImageService.getImages()"
      via: "Future.wait com calls por item, dentro de try/catch resiliente"
      pattern: "ImageService.*getImages"
    - from: "_ItemCard.build()"
      to: "_ImageStrip widget"
      via: "Column children — após observation GestureDetector, antes de criar ação corretiva"
      pattern: "_ImageStrip\\("
---

<objective>
Injetar o sistema de imagens na tela de execução de auditoria: criar os widgets privados `_ImageStrip`, `_ThumbTile`, `_PickerSheet`, implementar o fullscreen viewer, carregar imagens existentes no `_load()`, e propagar dados para `_ItemCard`.

Purpose: Este é o único plano que toca `audit_execution_screen.dart`. Toda a lógica de UI de imagens fica neste arquivo como classes privadas — sem criar novos arquivos de tela. A abordagem self-contained (passar `auditId`, `itemId`, `companyId` diretamente para `_ImageStrip`) evita tocar a assinatura de `_SectionBlock` além do mínimo necessário.

Output:
- `_ImageStrip` StatefulWidget inserido no `_ItemCard` entre observation toggle e "Criar ação corretiva"
- `_ThumbTile` com os 3 estados: uploading / uploaded / error
- `_PickerSheet` bottom sheet com opções "Tirar foto" / "Escolher da galeria"
- Fullscreen viewer via `showDialog` + `InteractiveViewer`
- `_AuditExecutionScreenState._load()` ampliado para buscar imagens existentes por item (em bloco try/catch resiliente)
- `_ItemCard` recebe `images: List<AuditItemImage>` e `companyId` para passar ao `_ImageStrip`
</objective>

<execution_context>
@C:/Users/eder3/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/eder3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-images/09-UI-SPEC.md
@.planning/phases/09-images/09-01-SUMMARY.md
@.planning/phases/09-images/09-02-SUMMARY.md

<interfaces>
<!-- === CONTRATO DE ImageService (criado em 09-02) === -->
<!-- import '../services/image_service.dart'; -->

<!-- Future<AuditItemImage> uploadImage({ -->
<!--   required String companyId, -->
<!--   required String auditId, -->
<!--   required String itemId, -->
<!--   required XFile file, -->
<!-- }) -->
<!-- Lança exceção em caso de falha. -->

<!-- Future<List<AuditItemImage>> getImages({ -->
<!--   required String auditId, -->
<!--   required String itemId, -->
<!-- }) -->

<!-- Future<String> getSignedUrl(String storagePath) -->

<!-- Future<void> deleteImage({ -->
<!--   required String imageId, -->
<!--   required String storagePath, -->
<!-- }) -->

<!-- === CONTRATO DE AuditItemImage (criado em 09-02) === -->
<!-- import '../models/audit_item_image.dart'; -->
<!-- Fields: id, auditId, templateItemId, companyId, storagePath, createdBy, createdAt -->

<!-- === PONTO DE INJEÇÃO EXATO EM _ItemCard (linhas 1055-1076) === -->
<!-- Linha 1055: fecha GestureDetector da observation toggle -->
<!-- Linhas 1056-1076: bloco "Criar ação corretiva" (EXISTENTE — NÃO ALTERAR) -->
<!-- Inserir _ImageStrip ENTRE linha 1055 e linha 1056 -->

<!-- === ESTADO EM _AuditExecutionScreenState === -->
<!-- Campos existentes: _sections, _allItems, _answers, _observations, _failedSaves -->
<!-- Adicionar: Map<String, List<AuditItemImage>> _images = {}; -->
<!-- Chave: templateItemId -->
<!-- _imageService instanciado como final _imageService = ImageService(); -->

<!-- === PROPAGATION CHAIN === -->
<!-- _AuditExecutionScreenState._images[itemId] → _SectionBlock (via parâmetro images) → _ItemCard (via images + companyId) → _ImageStrip -->
<!-- _SectionBlock JÁ TEM parâmetros: section, answers, observations, indexMap, readOnly, onAnswer, onObservation, theme, audit, onCreateAction -->
<!-- Adicionar ao _SectionBlock: images, onImagesChanged, companyId -->
<!-- _ItemCard JÁ TEM: item, index, answer, observation, readOnly, onAnswer, onObservation, theme, audit, onCreateAction -->
<!-- Adicionar ao _ItemCard: images, onImagesChanged, companyId -->

<!-- === COMPANYID SOURCE === -->
<!-- CompanyContextService.instance.activeCompanyId ?? '' -->
<!-- import '../services/company_context_service.dart'; já existe no arquivo? Verificar. -->
<!-- Se não existir: adicionar import. -->

<!-- === image_picker IMPORT === -->
<!-- import 'package:image_picker/image_picker.dart'; -->

<!-- === AppColors TOKENS USADOS === -->
<!-- AppColors.accent (#2196F3) — câmera button, uploading border, _PickerSheet icons -->
<!-- AppColors.error (#E53935) — error badge, remove button -->
<!-- t.surface — bottom sheet background, thumbnail container -->
<!-- t.background — thumbnail loading placeholder -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: _ImageStrip, _ThumbTile, _PickerSheet + fullscreen viewer widgets</name>
  <files>primeaudit/lib/screens/audit_execution_screen.dart</files>

  <read_first>
    - primeaudit/lib/screens/audit_execution_screen.dart (ler INTEIRO antes de editar — confirmar linha exata de fim de arquivo, imports existentes, e posição de _GuidanceTile que vem logo após _ItemCard)
    - .planning/phases/09-images/09-UI-SPEC.md §Component Inventory (_ImageStrip, _ThumbTile, _PickerSheet layouts EXATOS)
    - .planning/phases/09-images/09-UI-SPEC.md §Copywriting Contract (strings pt-BR exatas)
    - .planning/phases/09-images/09-UI-SPEC.md §Interaction Contract (fluxos de upload, remoção, fullscreen)
  </read_first>

  <action>
**Parte 1 — Adicionar imports no topo do arquivo** (após os imports existentes):

```dart
import 'dart:io';
import 'package:image_picker/image_picker.dart';
import '../models/audit_item_image.dart';
import '../services/image_service.dart';
import '../services/company_context_service.dart';
```

Verificar se `company_context_service.dart` já está importado — se sim, não duplicar.

**Parte 2 — Adicionar enum `_UploadState` e classe `_UploadEntry`** logo após os imports, antes da classe `AuditExecutionScreen`:

```dart
enum _UploadState { uploading, uploaded, error }

class _UploadEntry {
  final _UploadState state;
  final XFile? localFile;          // disponível nos estados uploading e error
  final AuditItemImage? image;     // disponível no estado uploaded

  const _UploadEntry({
    required this.state,
    this.localFile,
    this.image,
  });
}
```

**Parte 3 — Adicionar os 3 novos widgets privados NO FINAL do arquivo**, após `_GuidanceTile` e demais classes existentes:

```dart
// ---------------------------------------------------------------------------
// Strip de imagens — inserido no _ItemCard (Phase 9)
// ---------------------------------------------------------------------------
class _ImageStrip extends StatefulWidget {
  final String auditId;
  final String itemId;
  final String companyId;
  final bool readOnly;
  final List<AuditItemImage> initialImages;
  final void Function(List<AuditItemImage>) onImagesChanged;

  const _ImageStrip({
    required this.auditId,
    required this.itemId,
    required this.companyId,
    required this.readOnly,
    required this.initialImages,
    required this.onImagesChanged,
  });

  @override
  State<_ImageStrip> createState() => _ImageStripState();
}

class _ImageStripState extends State<_ImageStrip> {
  // Chave: id local temporário (antes do upload) ou image.id (após upload)
  final Map<String, _UploadEntry> _entries = {};
  final _imageService = ImageService();

  @override
  void initState() {
    super.initState();
    // Popula entries com imagens já carregadas (do _load() da tela pai)
    for (final img in widget.initialImages) {
      _entries[img.id] = _UploadEntry(
        state: _UploadState.uploaded,
        image: img,
      );
    }
  }

  Future<void> _pickImage() async {
    final source = await showModalBottomSheet<ImageSource>(
      context: context,
      backgroundColor: AppTheme.of(context).surface,
      shape: const RoundedRectangleBorder(
        borderRadius: BorderRadius.vertical(top: Radius.circular(16)),
      ),
      builder: (_) => _PickerSheet(),
    );
    if (source == null) return;

    final picker = ImagePicker();
    final xFile = await picker.pickImage(
      source: source,
      imageQuality: 85,
      maxWidth: 1200,
    );
    if (xFile == null) return;

    // Chave temporária para identificar este upload antes do id do banco
    final tempKey = 'temp_${DateTime.now().microsecondsSinceEpoch}';
    setState(() {
      _entries[tempKey] = _UploadEntry(
        state: _UploadState.uploading,
        localFile: xFile,
      );
    });

    try {
      final img = await _imageService.uploadImage(
        companyId: widget.companyId,
        auditId: widget.auditId,
        itemId: widget.itemId,
        file: xFile,
      );
      if (!mounted) return;
      setState(() {
        _entries.remove(tempKey);
        _entries[img.id] = _UploadEntry(
          state: _UploadState.uploaded,
          image: img,
        );
      });
      widget.onImagesChanged(_uploadedImages());
    } catch (_) {
      if (!mounted) return;
      setState(() {
        _entries[tempKey] = _UploadEntry(
          state: _UploadState.error,
          localFile: xFile,
        );
      });
    }
  }

  Future<void> _retryUpload(String tempKey, XFile file) async {
    setState(() {
      _entries[tempKey] = _UploadEntry(
        state: _UploadState.uploading,
        localFile: file,
      );
    });
    try {
      final img = await _imageService.uploadImage(
        companyId: widget.companyId,
        auditId: widget.auditId,
        itemId: widget.itemId,
        file: file,
      );
      if (!mounted) return;
      setState(() {
        _entries.remove(tempKey);
        _entries[img.id] = _UploadEntry(
          state: _UploadState.uploaded,
          image: img,
        );
      });
      widget.onImagesChanged(_uploadedImages());
    } catch (_) {
      if (!mounted) return;
      setState(() {
        _entries[tempKey] = _UploadEntry(
          state: _UploadState.error,
          localFile: file,
        );
      });
    }
  }

  Future<void> _removeImage(String key, AuditItemImage image) async {
    // Remoção otimista
    setState(() => _entries.remove(key));
    widget.onImagesChanged(_uploadedImages());
    try {
      await _imageService.deleteImage(
        imageId: image.id,
        storagePath: image.storagePath,
      );
    } catch (_) {
      // Delete-failure é silencioso sem retry — o auditor continua normalmente
      // Estado de erro fica visível (ícone vermelho) mas sem interação disponível
      if (!mounted) return;
      setState(() {
        _entries[key] = _UploadEntry(
          state: _UploadState.error,
          image: image,
        );
      });
    }
  }

  List<AuditItemImage> _uploadedImages() {
    return _entries.values
        .where((e) => e.state == _UploadState.uploaded && e.image != null)
        .map((e) => e.image!)
        .toList();
  }

  void _openFullscreen(BuildContext context, String signedUrl) {
    showDialog(
      context: context,
      builder: (_) => Dialog.fullscreen(
        backgroundColor: Colors.black87,
        child: Stack(
          children: [
            Center(
              child: InteractiveViewer(
                child: Image.network(signedUrl, fit: BoxFit.contain),
              ),
            ),
            Positioned(
              top: 40,
              right: 16,
              child: IconButton(
                icon: const Icon(Icons.close_rounded, color: Colors.white),
                onPressed: () => Navigator.of(context).pop(),
              ),
            ),
          ],
        ),
      ),
    );
  }

  @override
  Widget build(BuildContext context) {
    // read-only sem imagens: não renderiza nada
    if (widget.readOnly && _entries.isEmpty) return const SizedBox.shrink();

    return Row(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        // Botão câmera (oculto em read-only)
        if (!widget.readOnly)
          GestureDetector(
            onTap: _pickImage,
            child: Tooltip(
              message: 'Adicionar foto',
              child: Container(
                width: 44,
                height: 44,
                decoration: BoxDecoration(
                  color: AppColors.accent.withValues(alpha: 0.08),
                  borderRadius: BorderRadius.circular(10),
                  border: Border.all(
                      color: AppColors.accent.withValues(alpha: 0.3)),
                ),
                child: const Icon(Icons.photo_camera_outlined,
                    size: 22, color: AppColors.accent),
              ),
            ),
          ),
        if (!widget.readOnly && _entries.isNotEmpty)
          const SizedBox(width: 8),
        // Strip de miniaturas
        if (_entries.isNotEmpty)
          Expanded(
            child: SingleChildScrollView(
              scrollDirection: Axis.horizontal,
              child: Row(
                children: _entries.entries.map((entry) {
                  return Padding(
                    padding: const EdgeInsets.only(right: 4),
                    child: _ThumbTile(
                      entryKey: entry.key,
                      entry: entry.value,
                      readOnly: widget.readOnly,
                      onRetry: (key, file) => _retryUpload(key, file),
                      onRemove: (key, img) => _removeImage(key, img),
                      onTapUploaded: (signedUrl) =>
                          _openFullscreen(context, signedUrl),
                    ),
                  );
                }).toList(),
              ),
            ),
          ),
      ],
    );
  }
}

// ---------------------------------------------------------------------------
// Miniatura individual com estados de upload
// ---------------------------------------------------------------------------
class _ThumbTile extends StatefulWidget {
  final String entryKey;
  final _UploadEntry entry;
  final bool readOnly;
  final void Function(String key, XFile file) onRetry;
  final void Function(String key, AuditItemImage img) onRemove;
  final void Function(String signedUrl) onTapUploaded;

  const _ThumbTile({
    required this.entryKey,
    required this.entry,
    required this.readOnly,
    required this.onRetry,
    required this.onRemove,
    required this.onTapUploaded,
  });

  @override
  State<_ThumbTile> createState() => _ThumbTileState();
}

class _ThumbTileState extends State<_ThumbTile> {
  // Instanciado como campo conforme convenção CLAUDE.md (service instances como fields)
  final _imageService = ImageService();
  String? _signedUrl;
  bool _loadingUrl = false;

  @override
  void initState() {
    super.initState();
    if (widget.entry.state == _UploadState.uploaded &&
        widget.entry.image != null) {
      _loadSignedUrl();
    }
  }

  Future<void> _loadSignedUrl() async {
    if (_loadingUrl) return;
    setState(() => _loadingUrl = true);
    try {
      final url = await _imageService
          .getSignedUrl(widget.entry.image!.storagePath);
      if (mounted) setState(() { _signedUrl = url; _loadingUrl = false; });
    } catch (_) {
      if (mounted) setState(() => _loadingUrl = false);
    }
  }

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      width: 72,
      height: 72,
      child: ClipRRect(
        borderRadius: BorderRadius.circular(8),
        child: _buildContent(),
      ),
    );
  }

  Widget _buildContent() {
    final entry = widget.entry;

    if (entry.state == _UploadState.uploading) {
      return Stack(
        fit: StackFit.expand,
        children: [
          if (entry.localFile != null)
            Opacity(
              opacity: 0.5,
              child: Image.file(File(entry.localFile!.path), fit: BoxFit.cover),
            )
          else
            Container(color: AppTheme.of(context).background),
          const Center(
            child: SizedBox(
              width: 24,
              height: 24,
              child: CircularProgressIndicator(
                strokeWidth: 2,
                color: AppColors.accent,
              ),
            ),
          ),
        ],
      );
    }

    if (entry.state == _UploadState.error) {
      return GestureDetector(
        onTap: entry.localFile != null
            ? () => widget.onRetry(widget.entryKey, entry.localFile!)
            : null,
        child: Semantics(
          label: 'Erro ao enviar foto. Toque para tentar novamente.',
          child: Stack(
            fit: StackFit.expand,
            children: [
              if (entry.localFile != null)
                Opacity(
                  opacity: 0.4,
                  child:
                      Image.file(File(entry.localFile!.path), fit: BoxFit.cover),
                )
              else
                Container(color: AppTheme.of(context).background),
              const Center(
                child: Icon(Icons.error_rounded,
                    color: AppColors.error, size: 20),
              ),
            ],
          ),
        ),
      );
    }

    // Estado: uploaded
    if (_signedUrl == null) {
      return Container(
        color: AppTheme.of(context).background,
        child: const Center(
          child: SizedBox(
            width: 20,
            height: 20,
            child: CircularProgressIndicator(strokeWidth: 1.5,
                color: AppColors.accent),
          ),
        ),
      );
    }

    return GestureDetector(
      onTap: () => widget.onTapUploaded(_signedUrl!),
      child: Stack(
        fit: StackFit.expand,
        children: [
          Image.network(_signedUrl!, fit: BoxFit.cover),
          if (!widget.readOnly && entry.image != null)
            Positioned(
              top: 0,
              right: 0,
              child: Semantics(
                label: 'Remover foto',
                child: GestureDetector(
                  onTap: () =>
                      widget.onRemove(widget.entryKey, entry.image!),
                  child: SizedBox(
                    width: 44,
                    height: 44,
                    child: Center(
                      child: Container(
                        width: 20,
                        height: 20,
                        decoration: BoxDecoration(
                          color: AppColors.error,
                          borderRadius: BorderRadius.circular(10),
                        ),
                        child: const Icon(Icons.close_rounded,
                            size: 14, color: Colors.white),
                      ),
                    ),
                  ),
                ),
              ),
            ),
        ],
      ),
    );
  }
}

// ---------------------------------------------------------------------------
// Bottom sheet para escolha de fonte de imagem
// ---------------------------------------------------------------------------
class _PickerSheet extends StatelessWidget {
  const _PickerSheet();

  @override
  Widget build(BuildContext context) {
    return Column(
      mainAxisSize: MainAxisSize.min,
      children: [
        ListTile(
          leading: const Icon(Icons.camera_alt_rounded, color: AppColors.accent),
          title: const Text('Tirar foto', style: TextStyle(fontSize: 14)),
          onTap: () => Navigator.pop(context, ImageSource.camera),
        ),
        ListTile(
          leading:
              const Icon(Icons.photo_library_rounded, color: AppColors.accent),
          title:
              const Text('Escolher da galeria', style: TextStyle(fontSize: 14)),
          onTap: () => Navigator.pop(context, ImageSource.gallery),
        ),
        const SizedBox(height: 8),
      ],
    );
  }
}
```

**Notas críticas:**
- `_PickerSheet` retorna `ImageSource` via `Navigator.pop(context, ImageSource.camera/gallery)` — o `showModalBottomSheet` em `_ImageStrip._pickImage()` recebe esse valor.
- `_ThumbTile` carrega o `signedUrl` via `initState → _loadSignedUrl()` — o tile faz a chamada de rede para signed URL independentemente, usando `_imageService` instanciado como field na state class.
- `Dialog.fullscreen` (Material 3) é preferível a `showDialog` com `Dialog` customizado para garantir o fullscreen correto; se `Dialog.fullscreen` não estiver disponível na versão do Flutter, usar `Dialog(insetPadding: EdgeInsets.zero, ...)` como fallback.
  </action>

  <verify>
    <automated>grep -c "class _ImageStrip" primeaudit/lib/screens/audit_execution_screen.dart && grep -c "class _ThumbTile" primeaudit/lib/screens/audit_execution_screen.dart && grep -c "class _PickerSheet" primeaudit/lib/screens/audit_execution_screen.dart</automated>
  </verify>

  <acceptance_criteria>
    - grep "class _ImageStrip extends StatefulWidget" retorna 1
    - grep "class _ThumbTile extends StatefulWidget" retorna 1
    - grep "class _PickerSheet extends StatelessWidget" retorna 1
    - grep "_UploadState" retorna pelo menos 4 (enum declaration + 3 valores usados)
    - grep "class _UploadEntry" retorna 1
    - grep "Tirar foto" retorna 1 (copywriting exato)
    - grep "Escolher da galeria" retorna 1 (copywriting exato)
    - grep "Adicionar foto" retorna 1 (tooltip do botão câmera)
    - grep "Remover foto" retorna 1 (Semantics label)
    - grep "Erro ao enviar foto. Toque para tentar novamente." retorna 1 (Semantics label)
    - grep "imageQuality: 85" retorna 1
    - grep "maxWidth: 1200" retorna 1
    - grep "InteractiveViewer" retorna 1 (fullscreen viewer)
    - grep "import.*image_picker" retorna 1 (no topo do arquivo)
    - grep "final _imageService = ImageService()" em _ThumbTileState — service instanciado como field, não recebido via construtor
    - grep "imageService:" retorna 0 — nenhum parâmetro imageService no construtor de _ThumbTile
  </acceptance_criteria>

  <done>_ImageStrip, _ThumbTile, _PickerSheet e fullscreen viewer adicionados ao final de audit_execution_screen.dart. Todos os estados de upload implementados conforme UI-SPEC. _ThumbTile instancia _imageService como field conforme convenção CLAUDE.md.</done>
</task>

<task type="auto">
  <name>Task 2: Propagar images para _ItemCard + carregar imagens no _load()</name>
  <files>primeaudit/lib/screens/audit_execution_screen.dart</files>

  <read_first>
    - primeaudit/lib/screens/audit_execution_screen.dart (ler INTEIRO novamente após Task 1 — confirmar as linhas exatas dos locais de modificação abaixo)
    - .planning/phases/09-images/09-UI-SPEC.md §Position in _ItemCard Widget Tree (diagrama de posição exata)
  </read_first>

  <action>
Este task modifica `audit_execution_screen.dart` em 5 locais. Ler o arquivo completo antes de editar para confirmar as linhas exatas (podem ter mudado após Task 1).

**Modificação 1 — `_AuditExecutionScreenState`: adicionar campo e serviço**

Localizar o bloco de campos de estado (próximo de `_sections`, `_allItems`, `_answers`, `_observations`). Adicionar APÓS `_observations`:

```dart
// itemId → lista de imagens (carregadas no _load)
final Map<String, List<AuditItemImage>> _images = {};
```

E no bloco de serviços (próximo de `_templateService`, `_answerService`, `_auditService`). Adicionar:

```dart
final _imageService = ImageService();
```

**Modificação 2 — `_AuditExecutionScreenState._load()`: carregar imagens existentes**

Localizar o método `_load()`. Após o bloco que popula `_answers` e `_observations` (linhas ~95-101) e ANTES do `setState(() { _sections = sections; ... })`, adicionar:

```dart
// Bloco separado e resiliente — falha de rede ao carregar imagens não impede abertura do checklist
final imagesMap = <String, List<AuditItemImage>>{};
try {
  final imagesList = await Future.wait(
    items.map((item) => _imageService.getImages(
      auditId: widget.audit.id,
      itemId: item.id,
    )),
  );
  for (int i = 0; i < items.length; i++) {
    if (imagesList[i].isNotEmpty) {
      imagesMap[items[i].id] = imagesList[i];
    }
  }
} catch (_) {
  // Falha silenciosa — _images fica vazio, auditor continua sem pré-carregamento
}
```

E no `setState()` que define `_sections`, adicionar para atualizar `_images`:

```dart
setState(() {
  _sections = sections;
  _allItems = [...items];
  // ... (código existente de unsectioned) ...
  _images.addAll(imagesMap);  // adicionar esta linha
  _loading = false;
});
```

**Modificação 3 — Onde `_SectionBlock` é construído**: localizar onde `_SectionBlock(...)` é instanciado no `build()` da tela (dentro de `ListView.builder` ou similar). Adicionar os parâmetros `images:`, `onImagesChanged:`, `companyId:`:

```dart
_SectionBlock(
  section: section,
  answers: _answers,
  observations: _observations,
  indexMap: indexMap,
  readOnly: _isReadOnly,
  onAnswer: _onAnswer,
  onObservation: _onObservation,
  theme: t,
  audit: widget.audit,
  onCreateAction: _isReadOnly ? null : _onCreateAction,
  images: _images,                                    // NOVO
  onImagesChanged: (itemId, updated) {                // NOVO
    setState(() => _images[itemId] = updated);
  },
  companyId: CompanyContextService.instance.activeCompanyId ?? '',  // NOVO
),
```

**Modificação 4 — `_SectionBlock`: adicionar parâmetros e passar para `_ItemCard`**

Localizar a classe `_SectionBlock`. Adicionar os 3 novos campos à declaração:

```dart
final Map<String, List<AuditItemImage>> images;
final void Function(String itemId, List<AuditItemImage>) onImagesChanged;
final String companyId;
```

No construtor `const _SectionBlock(...)`, adicionar como `required`:

```dart
required this.images,
required this.onImagesChanged,
required this.companyId,
```

No `...section.items.map((item) => _ItemCard(...))`, adicionar:

```dart
images: images[item.id] ?? [],          // NOVO
onImagesChanged: (updated) =>           // NOVO
    onImagesChanged(item.id, updated),
companyId: companyId,                   // NOVO
```

**Modificação 5 — `_ItemCard`: adicionar parâmetros e inserir `_ImageStrip`**

Localizar a classe `_ItemCard`. Adicionar os 3 novos campos à declaração:

```dart
final List<AuditItemImage> images;
final void Function(List<AuditItemImage>) onImagesChanged;
final String companyId;
```

No construtor `const _ItemCard(...)`, adicionar:

```dart
required this.images,
required this.onImagesChanged,
required this.companyId,
```

No `_ItemCardState.build()`, localizar a linha que fecha o GestureDetector da observation toggle (a linha que termina o `GestureDetector(onTap: widget.readOnly ? null : () => setState(() => _showObs = !_showObs), ...)`) e inserir LOGO APÓS (antes do bloco `if (widget.onCreateAction != null && ...)`):

```dart
// Imagens (Phase 9) — inserido entre observation toggle e criar ação corretiva
const SizedBox(height: 8),
_ImageStrip(
  auditId: widget.audit?.id ?? '',
  itemId: widget.item.id,
  companyId: widget.companyId,
  readOnly: widget.readOnly,
  initialImages: widget.images,
  onImagesChanged: widget.onImagesChanged,
),
```

**Notas de implementação:**
- O `_ImageStrip` deve ser renderizado mesmo quando `widget.images` está vazio (strip renderiza o botão câmera).
- Quando `widget.audit?.id` for null (caso impossível em execução, mas defensivo), o strip renderiza mas o upload falhará — aceitável.
- A propagação `onImagesChanged` atualiza `_images` no estado pai via `setState`, permitindo que `_load()` reflita a lista atualizada em memória.
- O bloco de carregamento de imagens no `_load()` usa try/catch isolado: uma falha de rede ao buscar imagens NÃO lança exceção para fora de `_load()` e NÃO impede a abertura do checklist — valor core "nenhum dado deve ser perdido".
  </action>

  <verify>
    <automated>grep -c "_ImageStrip(" primeaudit/lib/screens/audit_execution_screen.dart</automated>
  </verify>

  <acceptance_criteria>
    - grep "_ImageStrip(" retorna pelo menos 1 (instanciação em _ItemCard.build)
    - grep "final Map<String, List<AuditItemImage>> _images" retorna 1 (campo no state)
    - grep "final _imageService = ImageService()" retorna 1 (no state)
    - grep "ImageService.*getImages" retorna pelo menos 1 (no _load())
    - grep "Future.wait" no contexto de _load() — o arquivo já usava Future.wait (linha ~65), verificar que o segundo Future.wait para imagens foi adicionado dentro de bloco try/catch
    - grep "required this.images" retorna 2 (em _SectionBlock e em _ItemCard)
    - grep "required this.companyId" retorna 2 (em _SectionBlock e em _ItemCard)
    - grep "required this.onImagesChanged" retorna 2 (em _SectionBlock e em _ItemCard)
    - grep "const SizedBox(height: 8)" antes de "_ImageStrip(" no arquivo — verificar posição correta
    - Inspecionar o bloco de carregamento de imagens no _load(): o Future.wait está dentro de um try/catch — falha de rede não lança exceção para fora do bloco (resilência core value)
    - flutter analyze primeaudit/lib/screens/audit_execution_screen.dart não reporta erros
  </acceptance_criteria>

  <done>Cadeia de propagação completa: _AuditExecutionScreenState → _SectionBlock → _ItemCard → _ImageStrip. _load() carrega imagens existentes em bloco try/catch resiliente. _ItemCard renderiza _ImageStrip entre observation toggle e criar ação corretiva.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação humana — fluxo completo de imagens na execução</name>

  <what-built>
    Flutter implementou _ImageStrip com câmera/galeria, upload com estados visuais (uploading spinner, uploaded com botão ×, error com retry), fullscreen viewer e carga de imagens existentes no _load(). Tela de execução modificada com propagação de images por item.
  </what-built>

  <how-to-verify>
    1. Rodar o app: `cd primeaudit && flutter run`
    2. Fazer login e abrir uma auditoria em andamento
    3. Em qualquer pergunta, verificar:
       a. Um botão de câmera (azul, 44dp, arredondado) aparece abaixo da toggle de observação e acima de "Criar ação corretiva" (se houver)
       b. Tocar no botão abre bottom sheet com duas opções: "Tirar foto" e "Escolher da galeria"
       c. Selecionar "Escolher da galeria" — escolher uma foto
       d. A miniatura aparece com spinner de loading (estado uploading)
       e. Após upload: miniatura mostra a foto com botão × no canto superior direito
       f. Tocar na miniatura abre fullscreen (fundo preto, InteractiveViewer, botão × para fechar)
       g. Tocar no botão × da miniatura remove a foto imediatamente
    4. Verificar múltiplas imagens: adicionar 2-3 fotos na mesma pergunta — todas as miniaturas aparecem em linha horizontal com scroll
    5. Fechar a tela de execução e reabrir a mesma auditoria: as fotos devem reaparecer (carregadas do banco)
    6. Abrir uma auditoria concluída (status = concluída): verificar que o botão de câmera NÃO aparece; se houver fotos, apenas miniaturas sem botão ×
    7. Verificar que responder uma pergunta enquanto outra tem upload em andamento funciona normalmente (uploads são independentes de _saveAnswer)
  </how-to-verify>

  <resume-signal>
    Aprovar: "aprovado" ou "ok"
    Reportar problema: descrever o comportamento observado vs esperado
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI (image picker) → ImageService | XFile do dispositivo passado para upload |
| signed URL → Image.network | URL temporária exposta no widget em memória |
| delete tap → ImageService.deleteImage | Ação destrutiva sem confirmação |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10 | Tampering | companyId em _ImageStrip | mitigate | companyId vem de CompanyContextService.instance.activeCompanyId (singleton autorizado), não de input do usuário; RLS no banco valida company_id mesmo assim |
| T-09-11 | Denial of Service | signed URL expirada durante sessão longa | accept | Signed URL tem 1h de validade; se expirar durante sessão, Image.network mostrará erro de carregamento — aceitável, usuário pode fechar e reabrir a tela; solução de refresh de URL é v2 |
| T-09-12 | Information Disclosure | signedUrl em memória/logs | accept | URL efêmera (1h), escopo restrito à empresa do usuário; não persistida em disco; risco de info disclosure mínimo em contexto de auditoria industrial interna |
| T-09-13 | Tampering | _load() carrega imagens de outros itens | mitigate | getImages() filtra por auditId E templateItemId — cada _ImageStrip recebe apenas as imagens do seu item |
</threat_model>

<verification>
```bash
cd primeaudit && flutter analyze lib/screens/audit_execution_screen.dart
```
Deve retornar: "No issues found!" ou apenas avisos sem erros.

Verificação manual adicional (não automatizável):
- Reproduzir o fluxo de "upload falha → indicador de erro inline → auditor finaliza auditoria normalmente" — confirmar que _saveAnswer NÃO é bloqueado
- Confirmar que "Tirar foto" e "Escolher da galeria" são as strings exatas no bottom sheet (copywriting contract)
</verification>

<success_criteria>
1. Botão de câmera visível em cada card de pergunta durante auditoria ativa
2. Bottom sheet "Tirar foto" / "Escolher da galeria" aparece ao tocar no botão
3. Miniatura com spinner imediato após seleção (antes do upload completar)
4. Miniatura com imagem real e botão × após upload bem-sucedido
5. Miniatura com ícone de erro (vermelho) após falha de upload; auditor pode continuar respondendo
6. Toque na miniatura uploaded abre fullscreen InteractiveViewer
7. Múltiplas imagens por item exibidas em row com scroll horizontal
8. Reabrir auditoria carrega imagens existentes (persistência via _load())
9. Modo read-only: câmera oculta; thumbnails sem botão ×; item sem imagens = SizedBox.shrink
10. `flutter analyze` limpo em audit_execution_screen.dart
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/09-images/09-03-SUMMARY.md` com:
- Widgets added: _ImageStrip, _ThumbTile, _PickerSheet (line ranges in audit_execution_screen.dart)
- Propagation chain implemented (_AuditExecutionScreenState → _SectionBlock → _ItemCard → _ImageStrip)
- _load() change: images loaded in parallel after existing answers/observations load, wrapped in try/catch (resilient — network failure does not block checklist opening)
- Human verify result (approved/issues found)
- Any deviations from plan
</output>
