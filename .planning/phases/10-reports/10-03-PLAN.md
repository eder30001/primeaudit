---
phase: 10-reports
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
  - 10-02
files_modified: []
autonomous: false
requirements:
  - REP-03
  - REP-04

# NOTE: REP-03/REP-04 listed for traceability per planning_context instructions.
# Effective deliverable: human checkpoint verifying CAL-01 (calendar renders correctly
# in dashboard) and CAL-02 (tap-to-filter navigation works end-to-end).
# This plan has no automated tasks — it is a pure human verification gate.

must_haves:
  truths:
    - "Calendar section is visible below KPI cards in the dashboard"
    - "Month navigation arrows change the displayed month correctly"
    - "Dot indicators appear on days that have audits"
    - "Tapping a day with audits opens AuditsScreen with the correct filter chip"
    - "Filter chip shows the date and clearing it restores all audits"
    - "Drawer does not contain a 'Relatórios' item"
  artifacts: []
  key_links: []
---

<objective>
Human visual and interaction verification of the Phase 10 calendar dashboard. Confirms that the calendar widget renders correctly, month navigation works, dot indicators are visible, day-tap navigation passes the correct filter, and the drawer is clean.

Purpose: Automated tests cover pure-function logic. This checkpoint covers the visual rendering, navigation integration, and live-data behavior that unit tests cannot validate.

Output: Human confirmation that all CAL-01/02/03 behaviors work correctly on device/emulator.
</objective>

<execution_context>
@C:/Users/eder3/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/eder3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-reports/10-01-SUMMARY.md
@.planning/phases/10-reports/10-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual and integration verification of calendar dashboard</name>
  <read_first>
    - .planning/phases/10-reports/10-01-SUMMARY.md (confirm Plan 01 completed successfully)
    - .planning/phases/10-reports/10-02-SUMMARY.md (confirm Plan 02 completed successfully)
  </read_first>
  <what-built>
    Plans 01 and 02 delivered:
    - Monthly calendar widget embedded in HomeScreen dashboard below the 4 KPI cards
    - Colored dot indicators per day: blue (Novas), red (Atrasadas), green (Concluídas)
    - Month navigation via left/right arrows (re-buckets from cached list, no extra network call)
    - Today's date shown with accent-colored filled circle
    - Tap on a day with audits navigates to AuditsScreen with filterDate parameter
    - AuditsScreen shows dismissible "Auditorias de DD/MM/YYYY" chip when filterDate is set
    - Chip X clears the date filter (stays on screen, shows all audits)
    - Drawer "Relatórios" item removed (D-08)
  </what-built>
  <how-to-verify>
    Run on Android emulator or device (flutter run from primeaudit/):

    Before starting, run the full test suite to confirm green:
    `cd primeaudit && flutter test`

    STEP 1 — Drawer check (CAL-03 / D-08):
    1. Open app, log in, reach HomeScreen
    2. Open the drawer (swipe right or tap hamburger)
    3. Verify: "Relatórios" item is NOT present in the menu
    4. Verify: Perfil and Configurações are still present
    Expected: No "Relatórios" entry visible anywhere in the drawer

    STEP 2 — Calendar section visible (CAL-01 / D-01):
    1. On HomeScreen dashboard, scroll down below the 4 KPI cards
    2. Verify: "Calendário de Auditorias" section title is visible
    3. Verify: Month header row shows current month name and year (e.g., "Maio 2026")
    4. Verify: Left arrow (‹) and right arrow (›) are visible flanking the month label
    5. Verify: Weekday headers "Dom Seg Ter Qua Qui Sex Sáb" are visible above the grid
    6. Verify: Calendar grid shows numbered day cells
    Expected: Calendar section renders below KPI cards with grid, month header, nav arrows

    STEP 3 — Month navigation (CAL-01 / D-02):
    1. Tap the right arrow (›) once
    2. Verify: Month label advances by 1 month (e.g., "Maio 2026" → "Junho 2026")
    3. Tap the right arrow again
    4. Verify: Month label advances again ("Junho 2026" → "Julho 2026")
    5. Tap the left arrow (‹) twice
    6. Verify: Month returns to original month ("Maio 2026")
    Expected: Month navigation is smooth, label updates correctly, no crash at month boundaries

    STEP 4 — Dot indicators (CAL-01 / D-04):
    1. Return to current month via navigation
    2. Look for day cells that have colored dot(s) below the day number
    3. Verify: If audits exist for any day this month, at least one dot is visible on that day
    4. Verify dots use correct colors: blue dot = Novas (rascunho/emAndamento), red = Atrasadas, green = Concluídas
    5. Verify: Days with no audits show only a plain day number (no dots, no circle border)
    Note: If no audits exist for the current month, navigate to a month that has audits
    Expected: Dot indicators correctly reflect audit status groups

    STEP 5 — Today's date highlight (CAL-01):
    1. Find today's date cell in the current month view
    2. Verify: Today's date shows a filled blue (AppColors.accent) circle around the number
    3. Verify: The day number inside the circle is white and bold
    4. Verify: If today has audits, dots still appear below the circle
    Expected: Today's date is visually distinct from other days

    STEP 6 — Day tap navigation to AuditsScreen (CAL-02 / D-05):
    1. Find a day cell that has at least one dot indicator (has audits)
    2. Tap on that day cell
    3. Verify: AuditsScreen opens (screen title "Auditorias" visible)
    4. Verify: A chip "Auditorias de DD/MM/YYYY" is visible above the audit list (where DD/MM/YYYY = the tapped day's date)
    5. Verify: The list shows only audits for that specific day (not all audits)
    Expected: Navigation succeeds with correct filter chip showing the tapped date

    STEP 7 — Filter chip dismissal (CAL-02):
    1. While on the filtered AuditsScreen (chip visible from Step 6)
    2. Tap the X (close) icon on the filter chip
    3. Verify: Chip disappears
    4. Verify: Full audit list is restored (shows all audits, not just that day's)
    5. Verify: The screen does NOT navigate back to HomeScreen — user stays on AuditsScreen
    Expected: Chip clears filter in-place, list expands to show all audits

    STEP 8 — Empty day behavior (D-06):
    1. On HomeScreen calendar, find a day cell without any dot indicators (no audits)
    2. Tap on that cell
    3. Verify: Nothing happens — no navigation, no error
    Expected: Empty day cells are not tappable

    STEP 9 — Pull-to-refresh preserves calendar (CAL-01):
    1. On HomeScreen, navigate the calendar to a non-current month (e.g., next month)
    2. Pull down to trigger RefreshIndicator
    3. Verify: Calendar reloads and shows the SAME month (not reset to current)
    4. Verify: KPI cards also refresh
    Expected: Pull-to-refresh reloads data without resetting month navigation position
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 9 steps pass.
    Or describe any issues found — be specific about which step failed and what was observed instead of expected.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human verifier → running app | Verifier confirms visual and interactive behaviors on emulator/device |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08 | Information Disclosure | Role scoping — auditor sees only own audits in calendar | mitigate | During Step 4/6: if testing with an auditor role, verify that only audits assigned to that auditor appear. If testing with adm/superuser, all company audits should appear. Visual check confirms client-side auditor filter works correctly alongside backend RLS. |
</threat_model>

<verification>
Pre-checkpoint (automated):
- `cd primeaudit && flutter test` — full suite must be green before human verification begins

Post-checkpoint:
- Human approval confirms all 9 steps passed
- If issues found: create gap-closure plan targeting specific failures
</verification>

<success_criteria>
- Full test suite green before checkpoint begins
- Human confirms: calendar visible below KPI cards
- Human confirms: month navigation works (prev/next arrows)
- Human confirms: today's date has accent circle
- Human confirms: dot indicators appear on days with audits
- Human confirms: tapping a day with audits opens AuditsScreen with correct filter chip
- Human confirms: chip X clears filter, stays on AuditsScreen
- Human confirms: empty days are not tappable
- Human confirms: drawer has no "Relatórios" item
- Human confirms: pull-to-refresh preserves month navigation position
</success_criteria>

<output>
After human approval, create `.planning/phases/10-reports/10-03-SUMMARY.md` using the summary template.
</output>
