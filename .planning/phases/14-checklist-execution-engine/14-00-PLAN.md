---
phase: 14-checklist-execution-engine
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - primeaudit/test/checklist_conformity_test.dart
  - primeaudit/test/checklist_execution_service_test.dart
  - primeaudit/test/checklist_answer_service_test.dart
  - primeaudit/test/checklist_pending_save_test.dart
autonomous: true
requirements:
  - EXEC-01
  - EXEC-02
  - EXEC-03
  - EXEC-05

must_haves:
  truths:
    - "4 arquivos de test stub existem antes das waves de implementação"
    - "Cada arquivo tem pelo menos 1 test() com TODO que nomeia o comportamento a testar"
    - "flutter test primeaudit/test/checklist_conformity_test.dart executa sem erros de parse"
  artifacts:
    - path: "primeaudit/test/checklist_conformity_test.dart"
      provides: "Stub de testes de conformidade (calculateConformity)"
      contains: "checklist_conformity_test"
    - path: "primeaudit/test/checklist_execution_service_test.dart"
      provides: "Stub de testes de createExecution"
      contains: "checklist_execution_service_test"
    - path: "primeaudit/test/checklist_answer_service_test.dart"
      provides: "Stub de testes de upsertAnswer e observações"
      contains: "checklist_answer_service_test"
    - path: "primeaudit/test/checklist_pending_save_test.dart"
      provides: "Stub de testes do padrão PendingSave"
      contains: "checklist_pending_save_test"
---

<objective>
Criar 4 arquivos de test stub ANTES das waves de implementação. Estes stubs estabelecem o mapa de validação da VALIDATION.md e garantem que os comportamentos críticos (calculateConformity, upsertAnswer, PendingSave) têm um ponto de ancoragem de teste antes do código de produção ser escrito.

Os stubs têm `test()` com `// TODO: implementar` — passam no runner mas documentam o comportamento esperado. Serão preenchidos durante ou após a execução das waves de implementação.
</objective>

<execution_context>
@C:/Users/eder3/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/eder3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-checklist-execution-engine/14-VALIDATION.md
@.planning/phases/14-checklist-execution-engine/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar 4 test stubs para Phase 14</name>

  <read_first>
    - .planning/phases/14-checklist-execution-engine/14-VALIDATION.md (Mapa de testes e comportamentos a cobrir)
    - .planning/phases/14-checklist-execution-engine/14-RESEARCH.md Seção "Validation Architecture" (Wave 0 Gaps)
    - primeaudit/test/ (verificar se diretório existe e padrão de imports existente)
  </read_first>

  <files>
    primeaudit/test/checklist_conformity_test.dart,
    primeaudit/test/checklist_execution_service_test.dart,
    primeaudit/test/checklist_answer_service_test.dart,
    primeaudit/test/checklist_pending_save_test.dart
  </files>

  <action>
Criar os 4 arquivos de test stub abaixo. Cada arquivo usa apenas `package:flutter_test/flutter_test.dart` — sem imports dos services de produção ainda (eles não existem no Wave 0). Os testes têm `// TODO` e chamam `expect(true, isTrue)` como placeholder para não falhar.

**checklist_conformity_test.dart:**
```dart
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('ChecklistAnswerService.calculateConformity', () {
    test('TODO: yes_no=yes conta como conforme', () {
      // TODO: instanciar ChecklistAnswerService e verificar que yes_no='yes' = conforme
      expect(true, isTrue);
    });

    test('TODO: yes_no=no conta como não conforme', () {
      // TODO: yes_no='no' deve reduzir conformidade
      expect(true, isTrue);
    });

    test('TODO: number excluído do denominador', () {
      // TODO: item com item_type=number não deve entrar no denominador
      expect(true, isTrue);
    });

    test('TODO: date excluído do denominador', () {
      // TODO: item com item_type=date não deve entrar no denominador
      expect(true, isTrue);
    });

    test('TODO: text não vazio = conforme', () {
      // TODO: item_type=text com resposta não vazia = conforme
      expect(true, isTrue);
    });

    test('TODO: sem respostas = conformidade 0.0', () {
      // TODO: nenhuma resposta registrada = 0.0%
      expect(true, isTrue);
    });
  });
}
```

**checklist_execution_service_test.dart:**
```dart
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('ChecklistExecutionService', () {
    test('TODO: createExecution retorna objeto com status rascunho', () {
      // TODO: mockar Supabase e verificar que createExecution insere status='rascunho'
      // e retorna ChecklistExecution com os campos preenchidos corretamente
      expect(true, isTrue);
    });

    test('TODO: finalizeExecution atualiza status para concluido', () {
      // TODO: mockar Supabase e verificar UPDATE status='concluido' + conformity_percent
      expect(true, isTrue);
    });
  });
}
```

**checklist_answer_service_test.dart:**
```dart
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('ChecklistAnswerService', () {
    test('TODO: upsertAnswer salva resposta yes_no', () {
      // TODO: mockar Supabase e verificar upsert com onConflict='execution_id,item_id'
      expect(true, isTrue);
    });

    test('TODO: upsertAnswer salva texto', () {
      // TODO: upsert com item_type=text e response não vazio
      expect(true, isTrue);
    });

    test('TODO: upsertAnswer salva number como string', () {
      // TODO: upsert com item_type=number e response='42'
      expect(true, isTrue);
    });

    test('TODO: upsertAnswer salva data como yyyy-MM-dd', () {
      // TODO: upsert com item_type=date e response='2026-05-06'
      expect(true, isTrue);
    });

    test('TODO: upsertAnswer salva observação junto com resposta (EXEC-03)', () {
      // TODO: upsert com observation não nulo e verificar que observation é persistida
      expect(true, isTrue);
    });
  });
}
```

**checklist_pending_save_test.dart:**
```dart
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('ChecklistPendingSave', () {
    test('TODO: copyWithAttempt incrementa attemptCount', () {
      // TODO: ChecklistPendingSave(itemId:'x', response:'y', attemptCount: 0).copyWithAttempt().attemptCount == 1
      expect(true, isTrue);
    });

    test('TODO: falha no upsert não propaga exceção para a tela (EXEC-05)', () {
      // TODO: simular throw no upsert e verificar que a screen captura silenciosamente
      expect(true, isTrue);
    });
  });
}
```
  </action>

  <verify>
    <automated>ls primeaudit/test/checklist_conformity_test.dart primeaudit/test/checklist_execution_service_test.dart primeaudit/test/checklist_answer_service_test.dart primeaudit/test/checklist_pending_save_test.dart</automated>
    Todos os 4 arquivos devem existir.
    <automated>cd primeaudit && flutter test test/checklist_conformity_test.dart --no-pub 2>&1 | tail -5</automated>
    Deve reportar "All tests passed" ou similar (testes com expect(true, isTrue) sempre passam).
  </verify>

  <done>
    - 4 arquivos de test stub criados em primeaudit/test/
    - Cada arquivo tem group() com testes TODO que passam no runner
    - Nenhum import de código de produção (services ainda não existem no Wave 0)
    - flutter test test/checklist_conformity_test.dart executa sem erros de parse
  </done>
</task>

</tasks>

<verification>
1. `ls primeaudit/test/checklist_conformity_test.dart` → arquivo existe
2. `ls primeaudit/test/checklist_execution_service_test.dart` → arquivo existe
3. `ls primeaudit/test/checklist_answer_service_test.dart` → arquivo existe
4. `ls primeaudit/test/checklist_pending_save_test.dart` → arquivo existe
5. `cd primeaudit && flutter test test/checklist_conformity_test.dart` → testes passam (placeholders)
</verification>

<success_criteria>
- 4 test stubs existem antes das waves de implementação
- Stubs documentam os comportamentos a testar por req ID
- Runner não falha — `expect(true, isTrue)` sempre verde
- Wave 0 completo antes de Wave 1 (migrations + models) iniciar
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/14-checklist-execution-engine/14-00-SUMMARY.md`
</output>
