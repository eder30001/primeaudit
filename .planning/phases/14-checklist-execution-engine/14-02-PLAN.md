---
phase: 14-checklist-execution-engine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - primeaudit/lib/models/checklist_template.dart
  - primeaudit/lib/models/checklist_execution.dart
autonomous: true
requirements:
  - EXEC-01
  - EXEC-02
  - EXEC-03

must_haves:
  truths:
    - "ChecklistTemplateItem tem campo options List<String> com parse correto de TEXT[] do Supabase"
    - "ChecklistExecution desserializa data_execucao como DATE sem conversão de timezone"
    - "ChecklistExecution.isConcluido retorna true quando status == 'concluido'"
    - "ChecklistAnswer tem campos executionId, itemId, response, observation para mapear linhas de checklist_answers"
  artifacts:
    - path: "primeaudit/lib/models/checklist_template.dart"
      provides: "ChecklistTemplateItem com campo options List<String>"
      contains: "List<String> options"
    - path: "primeaudit/lib/models/checklist_execution.dart"
      provides: "ChecklistExecution + ChecklistAnswer models"
      exports: ["ChecklistExecution", "ChecklistAnswer"]
  key_links:
    - from: "ChecklistTemplateItem.fromMap"
      to: "map['options']"
      via: "(map['options'] as List?)?.cast<String>() ?? []"
      pattern: "cast<String>"
    - from: "ChecklistExecution.fromMap"
      to: "map['data_execucao']"
      via: "DateTime.parse(map['data_execucao']) — sem .toLocal()"
      pattern: "data_execucao"
---

<objective>
Criar o modelo `ChecklistExecution` (com `ChecklistAnswer` inline no mesmo arquivo) e atualizar `ChecklistTemplateItem` adicionando o campo `options List<String>` com parse de `TEXT[]` do Supabase.

Purpose: Os serviços (Plan 14-03) e a tela (Plan 14-04) dependem desses modelos para tipagem correta. O campo `options` é obrigatório para widgets de `multiple_choice`. A desserialização correta de `data_execucao` como DATE evita bug de timezone (pitfall documentado em 14-RESEARCH.md).

Output: `checklist_template.dart` atualizado + `checklist_execution.dart` novo.
</objective>

<execution_context>
@C:/Users/eder3/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/eder3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/14-checklist-execution-engine/14-RESEARCH.md
@.planning/phases/14-checklist-execution-engine/14-PATTERNS.md

<interfaces>
<!-- Arquivo existente a ser modificado: primeaudit/lib/models/checklist_template.dart -->
<!-- Leia este arquivo antes de editar — ele é o destino das modificações -->

Classe atual ChecklistTemplateItem (linhas 49-73):
```dart
class ChecklistTemplateItem {
  final String id;
  final String templateId;
  final String description;
  final String itemType; // 'yes_no' | 'text' | 'number' | 'date' | 'multiple_choice' | 'photo'
  final int orderIndex;

  ChecklistTemplateItem({
    required this.id,
    required this.templateId,
    required this.description,
    required this.itemType,
    required this.orderIndex,
  });

  factory ChecklistTemplateItem.fromMap(Map<String, dynamic> map) {
    return ChecklistTemplateItem(
      id: map['id'],
      templateId: map['template_id'],
      description: map['description'] ?? '',
      itemType: map['item_type'] ?? 'yes_no',
      orderIndex: map['order_index'] ?? 0,
    );
  }
}
```

Padrão de modelo do projeto — analog lib/models/audit.dart:
```dart
// Constructor pattern (campo opcional nullable com default):
this.options = const [],  // List<String> com default const []

// fromMap pattern para TEXT[] do Supabase:
options: (map['options'] as List?)?.cast<String>() ?? [],

// DATE column pattern (sem .toLocal() — pitfall documentado):
dataExecucao: DateTime.parse(map['data_execucao']),
// NÃO usar: DateTime.parse(map['data_execucao']).toLocal()

// TIMESTAMPTZ pattern (com .toLocal()):
createdAt: DateTime.parse(map['created_at']).toLocal(),
completedAt: map['completed_at'] != null
    ? DateTime.parse(map['completed_at']).toLocal()
    : null,

// Joined table pattern:
templateName: map['checklist_templates']?['name'] ?? '',
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar campo options em ChecklistTemplateItem</name>

  <read_first>
    - primeaudit/lib/models/checklist_template.dart (arquivo completo — ler ANTES de editar)
    - .planning/phases/14-checklist-execution-engine/14-PATTERNS.md Seção "checklist_execution.dart" → subseção "ChecklistTemplateItem modification"
    - .planning/phases/14-checklist-execution-engine/14-RESEARCH.md Seção 1.3 (decisão TEXT[])
  </read_first>

  <files>primeaudit/lib/models/checklist_template.dart</files>

  <action>
Editar `primeaudit/lib/models/checklist_template.dart` para adicionar o campo `options` em `ChecklistTemplateItem`. Apenas `ChecklistTemplateItem` é modificado — `ChecklistTemplate` não muda.

Mudanças em `ChecklistTemplateItem`:

1. **Adicionar campo `options` ao corpo da classe:**
   ```dart
   final List<String> options; // Opções para item_type == 'multiple_choice'; [] para outros tipos
   ```

2. **Adicionar parâmetro `options` ao construtor** (opcional com default `const []`):
   ```dart
   ChecklistTemplateItem({
     required this.id,
     required this.templateId,
     required this.description,
     required this.itemType,
     required this.orderIndex,
     this.options = const [],  // ADICIONAR esta linha
   });
   ```

3. **Adicionar linha `options:` no `fromMap` factory:**
   ```dart
   factory ChecklistTemplateItem.fromMap(Map<String, dynamic> map) {
     return ChecklistTemplateItem(
       id: map['id'],
       templateId: map['template_id'],
       description: map['description'] ?? '',
       itemType: map['item_type'] ?? 'yes_no',
       orderIndex: map['order_index'] ?? 0,
       options: (map['options'] as List?)?.cast<String>() ?? [],  // ADICIONAR esta linha
     );
   }
   ```

Nenhum import novo necessário — `List<String>` é Dart puro. Não modificar `ChecklistTemplate`.
  </action>

  <verify>
    <automated>grep -c "List\<String\> options" primeaudit/lib/models/checklist_template.dart</automated>
    Deve retornar `1`.
    Também verificar parse: `grep "cast\<String\>" primeaudit/lib/models/checklist_template.dart` deve retornar a linha do fromMap.
  </verify>

  <done>
    - `ChecklistTemplateItem` tem campo `final List<String> options`
    - Construtor tem `this.options = const []` (parâmetro opcional com default)
    - `fromMap` tem `options: (map['options'] as List?)?.cast<String>() ?? []`
    - `ChecklistTemplate` não foi alterada
    - Sem novos imports adicionados ao arquivo
  </done>
</task>

<task type="auto">
  <name>Task 2: Criar checklist_execution.dart com ChecklistExecution e ChecklistAnswer</name>

  <read_first>
    - primeaudit/lib/models/audit.dart (padrão completo de modelo — fromMap, enum, computed getters, _parseDateOnly)
    - .planning/phases/14-checklist-execution-engine/14-RESEARCH.md Seção "Modelo ChecklistExecution" (código completo)
    - .planning/phases/14-checklist-execution-engine/14-PATTERNS.md Seção "checklist_execution.dart" (todas as adaptações de fromMap)
  </read_first>

  <files>primeaudit/lib/models/checklist_execution.dart</files>

  <action>
Criar `primeaudit/lib/models/checklist_execution.dart` com dois modelos no mesmo arquivo: `ChecklistExecution` e `ChecklistAnswer`.

**Estrutura completa do arquivo:**

```dart
// Representa uma execução de checklist.
//
// Mapeado da tabela `checklist_executions`.
// Módulo independente do módulo de Auditoria — zero acoplamento com [Audit].
class ChecklistExecution {
  final String id;
  final String templateId;
  final String templateName;   // join via checklist_templates(name)
  final String? companyId;
  final String createdBy;
  final String responsavel;
  final String local;
  final String? numero;
  final DateTime dataExecucao; // DATE — sem timezone (não usar .toLocal())
  final String status;         // 'rascunho' | 'concluido'
  final double? conformityPercent;
  final DateTime createdAt;
  final DateTime? completedAt;

  ChecklistExecution({
    required this.id,
    required this.templateId,
    required this.templateName,
    this.companyId,
    required this.createdBy,
    required this.responsavel,
    required this.local,
    this.numero,
    required this.dataExecucao,
    required this.status,
    this.conformityPercent,
    required this.createdAt,
    this.completedAt,
  });

  factory ChecklistExecution.fromMap(Map<String, dynamic> map) {
    return ChecklistExecution(
      id: map['id'],
      templateId: map['template_id'],
      templateName: map['checklist_templates']?['name'] ?? '',
      companyId: map['company_id'],
      createdBy: map['created_by'] ?? '',
      responsavel: map['responsavel'] ?? '',
      local: map['local'] ?? '',
      numero: map['numero'],
      // DATE column — NÃO usar .toLocal() (pitfall de timezone UTC-3)
      dataExecucao: DateTime.parse(map['data_execucao']),
      status: map['status'] ?? 'rascunho',
      conformityPercent: (map['conformity_percent'] as num?)?.toDouble(),
      // TIMESTAMPTZ — usar .toLocal()
      createdAt: DateTime.parse(map['created_at']).toLocal(),
      completedAt: map['completed_at'] != null
          ? DateTime.parse(map['completed_at']).toLocal()
          : null,
    );
  }

  bool get isConcluido => status == 'concluido';
  bool get isRascunho => status == 'rascunho';
}

// Representa uma resposta a um item de checklist.
//
// Mapeado da tabela `checklist_answers`.
// Usado pelos services para popular Map<String, String> _answers na screen.
class ChecklistAnswer {
  final String id;
  final String executionId;
  final String itemId;
  final String response;
  final String? observation;
  final DateTime answeredAt;

  ChecklistAnswer({
    required this.id,
    required this.executionId,
    required this.itemId,
    required this.response,
    this.observation,
    required this.answeredAt,
  });

  factory ChecklistAnswer.fromMap(Map<String, dynamic> map) {
    return ChecklistAnswer(
      id: map['id'],
      executionId: map['execution_id'],
      itemId: map['item_id'],
      response: map['response'] ?? '',
      observation: map['observation'],
      answeredAt: DateTime.parse(map['answered_at']).toLocal(),
    );
  }
}
```

Sem imports necessários — nenhum Color, IconData ou material.dart usado neste modelo. Dart puro.
  </action>

  <verify>
    <automated>grep -c "class ChecklistExecution" primeaudit/lib/models/checklist_execution.dart</automated>
    Deve retornar `1`.
    Verificar ambas as classes: `grep "class Checklist" primeaudit/lib/models/checklist_execution.dart` deve retornar 2 linhas (`ChecklistExecution` e `ChecklistAnswer`).
    Verificar pitfall de timezone: `grep "data_execucao" primeaudit/lib/models/checklist_execution.dart` deve NÃO conter `.toLocal()` na mesma linha.
  </verify>

  <done>
    - Arquivo `primeaudit/lib/models/checklist_execution.dart` criado
    - Contém `class ChecklistExecution` e `class ChecklistAnswer`
    - `ChecklistExecution.fromMap` usa `DateTime.parse(map['data_execucao'])` sem `.toLocal()`
    - `ChecklistExecution.fromMap` usa `map['checklist_templates']?['name'] ?? ''` para join
    - `ChecklistExecution.isConcluido` getter retorna `status == 'concluido'`
    - `ChecklistAnswer.fromMap` desserializa todos os 6 campos
    - Sem imports (Dart puro)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase response→Dart model | fromMap recebe Map<String, dynamic> com tipos não garantidos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Tampering | ChecklistExecution.fromMap | mitigate | Null-coalescing em todos os campos: `?? ''`, `?? 0`, `?? 'rascunho'` — sem crash em dados parciais |
| T-14-06 | Tampering | ChecklistTemplateItem.options cast | accept | `(map['options'] as List?)?.cast<String>() ?? []` — retorna [] se null ou tipo inesperado; impacto baixo (multiple_choice sem opções mostra mensagem) |
</threat_model>

<verification>
1. `grep -c "List<String> options" primeaudit/lib/models/checklist_template.dart` → `1`
2. `grep "cast<String>" primeaudit/lib/models/checklist_template.dart` → linha do fromMap presente
3. `grep -c "class ChecklistExecution" primeaudit/lib/models/checklist_execution.dart` → `1`
4. `grep -c "class ChecklistAnswer" primeaudit/lib/models/checklist_execution.dart` → `1`
5. Verificar pitfall: `grep "data_execucao.*toLocal\|toLocal.*data_execucao" primeaudit/lib/models/checklist_execution.dart` → SEM resultado (não deve usar toLocal em DATE)
</verification>

<success_criteria>
- `ChecklistTemplateItem.options` aceita `List<String>` com default `const []`; `fromMap` usa `(map['options'] as List?)?.cast<String>() ?? []`
- `ChecklistExecution` tem todos os campos do schema (11 campos, incluindo `conformityPercent` nullable, `completedAt` nullable)
- `dataExecucao` é parseado sem `.toLocal()` (DATE sem timezone)
- `templateName` é populado via `map['checklist_templates']?['name'] ?? ''`
- `ChecklistAnswer` tem `executionId`, `itemId`, `response`, `observation` (nullable), `answeredAt`
- Nenhum import externo adicionado (ambos os arquivos são Dart puro)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/14-checklist-execution-engine/14-02-SUMMARY.md`
</output>
