---
phase: 14-checklist-execution-engine
plan: 03
type: execute
wave: 2
depends_on:
  - "14-01"
  - "14-02"
files_modified:
  - primeaudit/lib/services/checklist_execution_service.dart
  - primeaudit/lib/services/checklist_answer_service.dart
autonomous: true
requirements:
  - EXEC-01
  - EXEC-02
  - EXEC-03
  - EXEC-05

must_haves:
  truths:
    - "ChecklistExecutionService.createExecution() faz INSERT em checklist_executions e retorna ChecklistExecution com templateName populado via join"
    - "ChecklistExecutionService.finalizeExecution() faz UPDATE com status='concluido', conformity_percent e completed_at"
    - "ChecklistAnswerService.upsertAnswer() usa onConflict: 'execution_id,item_id' — sem esse parâmetro o upsert gera 409"
    - "ChecklistAnswerService.calculateConformity() exclui number e date do denominador"
    - "ChecklistAnswerService.getAnswers() retorna List<Map<String, dynamic>> para o screen mapear para _answers"
  artifacts:
    - path: "primeaudit/lib/services/checklist_execution_service.dart"
      provides: "CRUD de execuções: createExecution, getExecution, finalizeExecution"
      exports: ["ChecklistExecutionService"]
    - path: "primeaudit/lib/services/checklist_answer_service.dart"
      provides: "Upsert de respostas e cálculo de conformidade"
      exports: ["ChecklistAnswerService"]
  key_links:
    - from: "ChecklistAnswerService.upsertAnswer"
      to: "checklist_answers"
      via: ".upsert({...}, onConflict: 'execution_id,item_id')"
      pattern: "onConflict"
    - from: "ChecklistAnswerService.calculateConformity"
      to: "conformityTypes"
      via: "const {'yes_no', 'text', 'multiple_choice'}"
      pattern: "conformityTypes"
    - from: "ChecklistExecutionService.createExecution"
      to: "checklist_executions"
      via: ".insert({...}).select('*, checklist_templates(name)').single()"
      pattern: "checklist_templates\\(name\\)"
---

<objective>
Criar `ChecklistExecutionService` (CRUD de execuções) e `ChecklistAnswerService` (upsert de respostas + cálculo de conformidade). Ambos seguem o padrão de serviço já estabelecido no projeto.

Purpose: A tela `ChecklistExecutionScreen` (Plan 14-04) e o wiring (Plan 14-05) dependem desses serviços. `upsertAnswer` com `onConflict` correto é o mecanismo central do auto-save silencioso (EXEC-05). `calculateConformity` é puro Dart (sem round-trip de rede) e exclui `number`/`date` conforme decisão STATE.md.

Output: Dois novos arquivos de serviço em `primeaudit/lib/services/`.
</objective>

<execution_context>
@C:/Users/eder3/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/eder3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/14-checklist-execution-engine/14-RESEARCH.md
@.planning/phases/14-checklist-execution-engine/14-PATTERNS.md
@.planning/phases/14-checklist-execution-engine/14-02-SUMMARY.md

<interfaces>
<!-- Padrão de serviço do projeto. Extraído de: -->
<!-- primeaudit/lib/services/audit_answer_service.dart -->
<!-- primeaudit/lib/services/audit_service.dart -->

Estrutura de serviço:
```dart
import 'package:supabase_flutter/supabase_flutter.dart';
import '../models/checklist_execution.dart';
import '../models/checklist_template.dart';  // para ChecklistTemplateItem

class XService {
  final _client = Supabase.instance.client;
  
  // Métodos: Future<T> — sem try/catch interno (callers são responsáveis)
}
```

Padrão createX (de audit_service.dart createAudit):
```dart
final result = await _client
    .from('checklist_executions')
    .insert({ ... })
    .select('*, checklist_templates(name)')
    .single();
return ChecklistExecution.fromMap(result);
```

Padrão upsert (de audit_answer_service.dart upsertAnswer):
```dart
await _client.from('checklist_answers').upsert(
  { 'execution_id': executionId, 'item_id': itemId, ... },
  onConflict: 'execution_id,item_id',  // CRÍTICO — sem isto: 409
);
```

Padrão getAnswers retornando raw maps (sem modelo intermediário):
```dart
final data = await _client
    .from('checklist_answers')
    .select()
    .eq('execution_id', executionId)
    .order('answered_at');
return List<Map<String, dynamic>>.from(data as List);
```

Serialização de data_execucao sem intl (pitfall A4 de RESEARCH.md):
```dart
// Usar toIso8601String().substring(0, 10) em vez de DateFormat
final dateStr = dataExecucao.toIso8601String().substring(0, 10); // 'yyyy-MM-dd'
```

UserId do usuário logado:
```dart
final userId = _client.auth.currentUser!.id;
```

CompanyId do contexto ativo (importar CompanyContextService):
```dart
import '../services/company_context_service.dart';
// ...
final companyId = CompanyContextService.instance.activeCompanyId;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar ChecklistExecutionService</name>

  <read_first>
    - primeaudit/lib/services/audit_service.dart (padrão completo: _select const, createAudit, finalizeAudit, getAudit, deleteAudit)
    - primeaudit/lib/models/checklist_execution.dart (tipos retornados — criado em 14-02)
    - .planning/phases/14-checklist-execution-engine/14-RESEARCH.md Seção "ChecklistExecutionService.createExecution()" e "finalizeExecution()" (código completo)
    - .planning/phases/14-checklist-execution-engine/14-PATTERNS.md Seção "checklist_execution_service.dart" (todos os snippets)
  </read_first>

  <files>primeaudit/lib/services/checklist_execution_service.dart</files>

  <action>
Criar `primeaudit/lib/services/checklist_execution_service.dart` com os métodos: `createExecution`, `getExecution`, `finalizeExecution`, `deleteExecution`.

**Estrutura completa:**

```dart
import 'package:supabase_flutter/supabase_flutter.dart';
import '../models/checklist_execution.dart';

class ChecklistExecutionService {
  final _client = Supabase.instance.client;

  static const _select = '''
    *,
    checklist_templates(name)
  ''';

  /// Cria execução com status 'rascunho'. Retorna o objeto com templateName populado.
  /// [dataExecucao] é serializado como 'yyyy-MM-dd' (sem timezone).
  Future<ChecklistExecution> createExecution({
    required String templateId,
    required String? companyId,
    required String responsavel,
    required String local,
    String? numero,
    required DateTime dataExecucao,
  }) async {
    final userId = _client.auth.currentUser!.id;
    final result = await _client
        .from('checklist_executions')
        .insert({
          'template_id': templateId,
          'company_id': companyId,
          'created_by': userId,
          'responsavel': responsavel,
          'local': local,
          'numero': numero,
          // DATE sem timezone: toIso8601String().substring(0,10) = 'yyyy-MM-dd'
          'data_execucao': dataExecucao.toIso8601String().substring(0, 10),
          'status': 'rascunho',
        })
        .select(_select)
        .single();
    return ChecklistExecution.fromMap(result);
  }

  /// Busca execução por id com join do nome do template.
  Future<ChecklistExecution> getExecution(String id) async {
    final result = await _client
        .from('checklist_executions')
        .select(_select)
        .eq('id', id)
        .single();
    return ChecklistExecution.fromMap(result);
  }

  /// Finaliza execução: seta status='concluido', conformity_percent e completed_at.
  Future<void> finalizeExecution({
    required String id,
    required double conformityPercent,
  }) async {
    await _client.from('checklist_executions').update({
      'status': 'concluido',
      'conformity_percent': conformityPercent,
      'completed_at': DateTime.now().toIso8601String(),
    }).eq('id', id);
  }

  /// Remove execução e suas respostas (cascade definido no banco).
  Future<void> deleteExecution(String id) async {
    await _client.from('checklist_executions').delete().eq('id', id);
  }
}
```

Sem try/catch interno — callers (screens) são responsáveis por capturar exceções (padrão do projeto).
  </action>

  <verify>
    <automated>grep -c "class ChecklistExecutionService" primeaudit/lib/services/checklist_execution_service.dart</automated>
    Deve retornar `1`.
    Verificar join: `grep "checklist_templates(name)" primeaudit/lib/services/checklist_execution_service.dart` deve retornar a linha do _select.
    Verificar data: `grep "toIso8601String.*substring" primeaudit/lib/services/checklist_execution_service.dart` deve retornar a serialização de data_execucao.
  </verify>

  <done>
    - Arquivo `primeaudit/lib/services/checklist_execution_service.dart` criado
    - `createExecution` insere na tabela `checklist_executions` com todos os campos e retorna `ChecklistExecution`
    - `_select` inclui `checklist_templates(name)` para popular `templateName`
    - `data_execucao` serializado via `.toIso8601String().substring(0, 10)` (sem intl)
    - `finalizeExecution` faz UPDATE com `status='concluido'`, `conformity_percent` e `completed_at`
    - Sem try/catch interno
    - Sem import de CompanyContextService (o companyId é recebido como parâmetro pelo caller)
  </done>
</task>

<task type="auto">
  <name>Task 2: Criar ChecklistAnswerService com calculateConformity</name>

  <read_first>
    - primeaudit/lib/services/audit_answer_service.dart (padrão completo: getAnswers, upsertAnswer, calculateConformity)
    - primeaudit/lib/models/checklist_template.dart (ChecklistTemplateItem com campo options — criado em 14-02)
    - .planning/phases/14-checklist-execution-engine/14-RESEARCH.md Seção 4 (cálculo de conformidade, regras exatas)
    - .planning/phases/14-checklist-execution-engine/14-PATTERNS.md Seção "checklist_answer_service.dart" (todos os snippets, incluindo calculateConformity simplificado)
  </read_first>

  <files>primeaudit/lib/services/checklist_answer_service.dart</files>

  <action>
Criar `primeaudit/lib/services/checklist_answer_service.dart` com os métodos: `getAnswers`, `upsertAnswer`, e o método estático `calculateConformity`.

**Estrutura completa:**

```dart
import 'package:supabase_flutter/supabase_flutter.dart';
import '../models/checklist_template.dart';

class ChecklistAnswerService {
  final _client = Supabase.instance.client;

  /// Retorna todas as respostas de uma execução ordenadas por answered_at.
  /// Retorna raw maps para o screen mapear:
  ///   _answers[row['item_id']] = row['response']
  ///   _observations[row['item_id']] = row['observation']
  Future<List<Map<String, dynamic>>> getAnswers(String executionId) async {
    final data = await _client
        .from('checklist_answers')
        .select()
        .eq('execution_id', executionId)
        .order('answered_at');
    return List<Map<String, dynamic>>.from(data as List);
  }

  /// Upsert idempotente de resposta por (execution_id, item_id).
  /// onConflict é obrigatório — sem ele o banco retorna 409 quando o item
  /// já tem resposta (violação da constraint UNIQUE).
  Future<void> upsertAnswer({
    required String executionId,
    required String itemId,
    required String response,
    String? observation,
  }) async {
    await _client.from('checklist_answers').upsert(
      {
        'execution_id': executionId,
        'item_id': itemId,
        'response': response,
        'observation': observation,
        'answered_at': DateTime.now().toIso8601String(),
      },
      onConflict: 'execution_id,item_id',
    );
  }

  /// Calcula conformidade excluindo tipos informativos (number, date, photo).
  ///
  /// Tipos elegíveis: yes_no, text, multiple_choice.
  /// Regras:
  ///   yes_no: 'yes' = conforme, 'no' = não conforme
  ///   text: qualquer string não vazia = conforme
  ///   multiple_choice: qualquer opção selecionada = conforme
  ///   Itens sem resposta não entram no numerador (mas entram no denominador).
  ///
  /// Decisão STATE.md v1.2: "Tipos number e date excluídos do denominador".
  static double calculateConformity(
    List<ChecklistTemplateItem> items,
    Map<String, String> answers,
  ) {
    const conformityTypes = {'yes_no', 'text', 'multiple_choice'};
    final eligible =
        items.where((i) => conformityTypes.contains(i.itemType)).toList();
    if (eligible.isEmpty) return 100.0;

    final int total = eligible.length;
    int conforming = 0;

    for (final item in eligible) {
      final ans = answers[item.id];
      if (ans == null || ans.isEmpty) continue;
      switch (item.itemType) {
        case 'yes_no':
          if (ans == 'yes') conforming++;
        case 'text':
          if (ans.isNotEmpty) conforming++;
        case 'multiple_choice':
          if (ans.isNotEmpty) conforming++;
      }
    }

    return (conforming / total * 100).clamp(0.0, 100.0);
  }
}
```

Sem try/catch interno — callers são responsáveis.
  </action>

  <verify>
    <automated>grep -c "class ChecklistAnswerService" primeaudit/lib/services/checklist_answer_service.dart</automated>
    Deve retornar `1`.
    Verificar onConflict: `grep "onConflict" primeaudit/lib/services/checklist_answer_service.dart` deve retornar linha com `'execution_id,item_id'`.
    Verificar exclusão de number/date: `grep "conformityTypes" primeaudit/lib/services/checklist_answer_service.dart` deve retornar a linha com o Set.
  </verify>

  <done>
    - Arquivo `primeaudit/lib/services/checklist_answer_service.dart` criado
    - `getAnswers` retorna `List<Map<String, dynamic>>` ordenado por `answered_at`
    - `upsertAnswer` usa `onConflict: 'execution_id,item_id'` — sem este parâmetro o auto-save falha
    - `calculateConformity` é `static double` — sem I/O, puro Dart
    - `conformityTypes` é `const {'yes_no', 'text', 'multiple_choice'}` — `number` e `date` ausentes (excluídos do denominador)
    - Itens `yes_no` com resposta `'no'` entram no denominador mas não somam conformidade
    - Itens sem resposta (`ans == null || ans.isEmpty`) não penalizam: pulam com `continue`
    - Sem try/catch interno
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Screen→Service | Screen passa parâmetros para insert/upsert; service não valida conteúdo |
| Service→Supabase | PostgREST recebe dados; RLS valida autoria; constraints validam integridade |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07 | Tampering | upsertAnswer response field | accept | `response TEXT NOT NULL DEFAULT ''` no banco; qualquer string incluindo vazia é aceita; validação de "não vazio antes de upsert" é responsabilidade da screen |
| T-14-08 | Tampering | calculateConformity — input não validado | accept | Método estático puro; recebe apenas dados já em memória da screen; conformidade é calculada no cliente e salva via finalizeExecution; banco não valida o valor |
| T-14-09 | Spoofing | createExecution created_by | mitigate | userId obtido de `_client.auth.currentUser!.id` — não recebido como parâmetro; RLS WITH CHECK adiciona segunda camada |
</threat_model>

<verification>
1. `grep -c "class ChecklistExecutionService" primeaudit/lib/services/checklist_execution_service.dart` → `1`
2. `grep "checklist_templates(name)" primeaudit/lib/services/checklist_execution_service.dart` → linha do _select presente
3. `grep "toIso8601String.*substring(0, 10)" primeaudit/lib/services/checklist_execution_service.dart` → serialização de data presente
4. `grep -c "class ChecklistAnswerService" primeaudit/lib/services/checklist_answer_service.dart` → `1`
5. `grep "onConflict.*execution_id,item_id" primeaudit/lib/services/checklist_answer_service.dart` → linha presente
6. `grep "conformityTypes" primeaudit/lib/services/checklist_answer_service.dart` → linha com Set{yes_no, text, multiple_choice} presente
</verification>

<success_criteria>
- `ChecklistExecutionService.createExecution` insere com `created_by = auth.uid()`, serializa data como `'yyyy-MM-dd'`, retorna `ChecklistExecution` com join
- `ChecklistExecutionService.finalizeExecution` faz UPDATE com `status='concluido'`, `conformity_percent` e `completed_at`
- `ChecklistAnswerService.upsertAnswer` usa `onConflict: 'execution_id,item_id'` — idempotente
- `ChecklistAnswerService.calculateConformity` é estático, retorna `100.0` quando eligible vazio, exclui `number`/`date`/`photo` do denominador
- `ChecklistAnswerService.getAnswers` retorna `List<Map<String, dynamic>>` (sem modelo intermediário)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/14-checklist-execution-engine/14-03-SUMMARY.md`
</output>
