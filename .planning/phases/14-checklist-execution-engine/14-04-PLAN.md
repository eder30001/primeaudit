---
phase: 14-checklist-execution-engine
plan: 04
type: execute
wave: 3
depends_on:
  - "14-03"
files_modified:
  - primeaudit/lib/screens/checklist/checklist_execution_screen.dart
  - primeaudit/lib/screens/checklist/checklist_pending_save.dart
autonomous: true
requirements:
  - EXEC-02
  - EXEC-03
  - EXEC-05

must_haves:
  truths:
    - "Tela exibe lista plana de itens do template com widget de resposta correto para cada item_type: yes_no, text, number, date, multiple_choice, photo"
    - "Cada resposta chama _saveAnswer sem await — fire-and-forget — sem bloquear a UI"
    - "Falha no upsert adiciona item a _failedSaves; snackbar 'Não foi possível salvar' com action 'Tentar novamente' aparece sem modal"
    - "_scheduleRetry faz até 4 tentativas com backoff exponencial (1s, 2s, 4s, 8s) usando pow(2, attemptCount)"
    - "Botão Finalizar bloqueia se _failedSaves.isNotEmpty — mostra dialog e não prossegue"
    - "BottomBar mostra conformidade ao vivo excluindo number/date do cálculo"
    - "AppBar mostra progresso respondidos/total"
    - "Observação por item é salva junto com a resposta via _onObservation → _saveAnswer"
  artifacts:
    - path: "primeaudit/lib/screens/checklist/checklist_pending_save.dart"
      provides: "ChecklistPendingSave data class com copyWithAttempt"
      contains: "class ChecklistPendingSave"
    - path: "primeaudit/lib/screens/checklist/checklist_execution_screen.dart"
      provides: "Tela completa de execução de checklist"
      contains: "class ChecklistExecutionScreen"
  key_links:
    - from: "_onAnswer"
      to: "_saveAnswer"
      via: "chamada sem await (fire-and-forget)"
      pattern: "_saveAnswer\\(itemId"
    - from: "_saveAnswer catch"
      to: "_failedSaves"
      via: "setState(() => _failedSaves[itemId] = _PendingSave(...))"
      pattern: "_failedSaves\\[itemId\\]"
    - from: "_scheduleRetry"
      to: "Future.delayed"
      via: "pow(2, pending.attemptCount) segundos"
      pattern: "pow.*attemptCount"
    - from: "_finalize"
      to: "_failedSaves.isNotEmpty"
      via: "guard bloqueante antes do UPDATE"
      pattern: "_failedSaves.isNotEmpty"
---

<objective>
Criar a tela `ChecklistExecutionScreen` — a tela central da fase — com auto-save silencioso, todos os tipos de widget de resposta, observações por item, retry exponencial, e botão de finalização com cálculo de conformidade ao vivo.

Purpose: Esta é a entrega de valor central da Phase 14. O auditor preenche o checklist aqui. O padrão `_PendingSave` + `_scheduleRetry` garante que falhas de rede não bloqueiam o preenchimento (EXEC-05 — core value do produto). A lista plana de itens, os 5 tipos de resposta e o campo de observação entregam EXEC-02 e EXEC-03.

Output: `checklist_pending_save.dart` (data class de retry) e `checklist_execution_screen.dart` (tela completa).
</objective>

<execution_context>
@C:/Users/eder3/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/eder3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/14-checklist-execution-engine/14-RESEARCH.md
@.planning/phases/14-checklist-execution-engine/14-PATTERNS.md
@.planning/phases/14-checklist-execution-engine/14-02-SUMMARY.md
@.planning/phases/14-checklist-execution-engine/14-03-SUMMARY.md

<interfaces>
<!-- Padrão de screen de execução. Extraído de: -->
<!-- primeaudit/lib/screens/audit_execution_screen.dart -->
<!-- primeaudit/lib/screens/pending_save.dart -->

Estrutura de PendingSave (para copiar com rename):
```dart
class ChecklistPendingSave {
  final String itemId;
  final String response;
  final String? observation;
  final int attemptCount;

  const ChecklistPendingSave({
    required this.itemId,
    required this.response,
    this.observation,
    this.attemptCount = 0,
  });

  ChecklistPendingSave copyWithAttempt() => ChecklistPendingSave(
        itemId: itemId,
        response: response,
        observation: observation,
        attemptCount: attemptCount + 1,
      );
}
```

Imports obrigatórios para checklist_execution_screen.dart:
```dart
import 'dart:math' show pow;
import 'package:flutter/material.dart';
import 'package:flutter/services.dart' show FilteringTextInputFormatter;
import '../../core/app_colors.dart';
import '../../core/app_theme.dart';
import '../../models/checklist_execution.dart';
import '../../models/checklist_template.dart';
import '../../services/checklist_answer_service.dart';
import '../../services/checklist_execution_service.dart';
import '../../services/checklist_template_service.dart';
import 'checklist_pending_save.dart';

typedef _PendingSave = ChecklistPendingSave;
```

State fields da screen:
```dart
static const int _maxAutoRetryAttempts = 4;

final _templateService = ChecklistTemplateService();
final _answerService = ChecklistAnswerService();
final _executionService = ChecklistExecutionService();

List<ChecklistTemplateItem> _allItems = [];
final Map<String, String> _answers = {};
final Map<String, String> _observations = {};
final Map<String, _PendingSave> _failedSaves = {};
final Set<String> _retrying = {};

bool _loading = true;
bool _finalizing = false;
String? _error;
```

Padrão _load com Future.wait:
```dart
Future<void> _load() async {
  setState(() { _loading = true; _error = null; });
  try {
    final templateId = widget.execution.templateId;
    final results = await Future.wait([
      _templateService.getItems(templateId),
      _answerService.getAnswers(widget.execution.id),
    ]);
    final items = results[0] as List<ChecklistTemplateItem>;
    final answerRows = results[1] as List<Map<String, dynamic>>;
    final merged = <String, String>{};
    final mergedObs = <String, String>{};
    for (final row in answerRows) {
      merged[row['item_id'] as String] = row['response'] as String? ?? '';
      if (row['observation'] != null) {
        mergedObs[row['item_id'] as String] = row['observation'] as String;
      }
    }
    // Preservar respostas pendentes (não salvas) sobre dados do banco
    merged.addAll(_failedSaves.map((k, v) => MapEntry(k, v.response)));
    if (mounted) setState(() {
      _allItems = items;
      _answers.addAll(merged);
      _observations.addAll(mergedObs);
      _loading = false;
    });
  } catch (e) {
    if (mounted) setState(() { _error = '$e'; _loading = false; });
  }
}
```

Getter de progresso (para AppBar):
```dart
int get _answeredCount => _allItems
    .where((i) => _answers.containsKey(i.id) && _answers[i.id]!.isNotEmpty)
    .length;
int get _totalCount => _allItems.length;
```

Getter de conformidade ao vivo:
```dart
double get _conformity =>
    ChecklistAnswerService.calculateConformity(_allItems, _answers);
```

Cor de conformidade:
```dart
static Color _conformityColor(double pct) {
  if (pct >= 80) return const Color(0xFF43A047);
  if (pct >= 60) return const Color(0xFFFFA000);
  return AppColors.error;
}
```

Padrão _showSaveError (capturar messenger ANTES de await):
```dart
void _showSaveError(String itemId, String response, String? observation) {
  if (!mounted) return;
  final messenger = ScaffoldMessenger.of(context); // ANTES de qualquer await
  messenger.clearSnackBars();
  messenger.showSnackBar(SnackBar(
    content: const Text('Não foi possível salvar'),
    backgroundColor: AppColors.error,
    behavior: SnackBarBehavior.floating,
    duration: const Duration(seconds: 6),
    action: SnackBarAction(
      label: 'Tentar novamente',
      textColor: Colors.white,
      onPressed: () => _saveAnswer(itemId, response, observation: observation),
    ),
  ));
}
```

Checklist_template_service.getItems existe — verificar assinatura:
```dart
// primeaudit/lib/services/checklist_template_service.dart
Future<List<ChecklistTemplateItem>> getItems(String templateId)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1a: Criar ChecklistPendingSave e ChecklistExecutionScreen (scaffold + state + _load)</name>

  <read_first>
    - primeaudit/lib/screens/pending_save.dart (arquivo completo — copiar com rename de classe)
    - primeaudit/lib/screens/audit_execution_screen.dart linhas 1-250 (imports, state fields, initState, _load)
    - primeaudit/lib/services/checklist_template_service.dart (confirmar assinatura de getItems)
    - .planning/phases/14-checklist-execution-engine/14-PATTERNS.md Seção "checklist_execution_screen.dart" (snippets de state e _load)
    - .planning/phases/14-checklist-execution-engine/14-RESEARCH.md Seção 2 (auto-save) e Pitfall 3 (preservar pendentes)
  </read_first>

  <files>
    primeaudit/lib/screens/checklist/checklist_pending_save.dart,
    primeaudit/lib/screens/checklist/checklist_execution_screen.dart
  </files>

  <action>
**Parte A — Criar checklist_pending_save.dart:**

Criar `primeaudit/lib/screens/checklist/checklist_pending_save.dart` copiando `pending_save.dart` verbatim com único rename `PendingSave` → `ChecklistPendingSave`. Sem outras alterações.

**Parte B — Criar checklist_execution_screen.dart (scaffold + state + _load):**

Criar `primeaudit/lib/screens/checklist/checklist_execution_screen.dart` com a estrutura da screen até o ponto em que _load está funcional. A screen recebe `ChecklistExecution execution` como parâmetro obrigatório.

**Widget raiz:**
```dart
class ChecklistExecutionScreen extends StatefulWidget {
  final ChecklistExecution execution;
  const ChecklistExecutionScreen({super.key, required this.execution});
  @override
  State<ChecklistExecutionScreen> createState() => _ChecklistExecutionScreenState();
}
```

**State class — todos os campos e inicialização:**
- `typedef _PendingSave = ChecklistPendingSave;`
- `static const int _maxAutoRetryAttempts = 4;`
- Services: `_templateService`, `_answerService`, `_executionService`
- `List<ChecklistTemplateItem> _allItems = []`
- `final Map<String, String> _answers = {}`
- `final Map<String, String> _observations = {}`
- `final Map<String, _PendingSave> _failedSaves = {}`
- `final Set<String> _retrying = {}`
- Booleans: `_loading = true`, `_finalizing = false`
- `String? _error`
- `@override void initState()` — chama `_load()` após `super.initState()`

**`_load()`** — usa `Future.wait([getItems, getAnswers])`, popula `_answers` e `_observations` dos rows do banco, depois mescla respostas de `_failedSaves` sobrescrevendo as do banco (Pitfall 3 — preservar pendentes). Padrão completo está em `<interfaces>` acima.

**Getters (definir agora para uso no build):**
- `int get _answeredCount` — itens com resposta não vazia em `_answers`
- `int get _totalCount` — `_allItems.length`
- `double get _conformity` — `ChecklistAnswerService.calculateConformity(_allItems, _answers)`
- `static Color _conformityColor(double pct)` — verde >= 80, amarelo >= 60, vermelho < 60

**`build`:** Scaffold com `_buildAppBar()`, body condicional (loading/error/body com `_buildBody`), `bottomNavigationBar: _buildBottomBar()`. Os métodos `_buildAppBar`, `_buildBody`, `_buildBottomBar` e `_buildError` devem ser criados como stubs que retornam `AppBar()`, `const SizedBox.shrink()`, `const SizedBox.shrink()` e `const SizedBox.shrink()` respectivamente — serão preenchidos na Task 1b.

**`_buildError(AppTheme t)`:** `Center` com ícone, texto de erro e `OutlinedButton.icon` 'Tentar novamente' que chama `_load()`. (Implementar completo agora — é simples e independente.)
  </action>

  <verify>
    <automated>grep -c "class ChecklistPendingSave" primeaudit/lib/screens/checklist/checklist_pending_save.dart</automated>
    Deve retornar `1`.
    <automated>grep -c "class ChecklistExecutionScreen" primeaudit/lib/screens/checklist/checklist_execution_screen.dart</automated>
    Deve retornar `1`.
    <automated>grep -c "Future.wait" primeaudit/lib/screens/checklist/checklist_execution_screen.dart</automated>
    Deve retornar `1` (confirmando que _load usa Future.wait).
  </verify>

  <done>
    - `checklist_pending_save.dart` existe com classe `ChecklistPendingSave` e método `copyWithAttempt()`
    - `checklist_execution_screen.dart` existe com `class ChecklistExecutionScreen`, todos os state fields, `_load()` completo com Future.wait e merge de _failedSaves
    - Getters `_answeredCount`, `_totalCount`, `_conformity`, `_conformityColor` presentes
    - `build()` delega para `_buildAppBar`, `_buildBody`, `_buildBottomBar` (stubs OK neste ponto)
    - `_buildError` implementado com botão de retry
    - `initState` chama `_load()`
  </done>
</task>

<task type="auto">
  <name>Task 1b: Completar auto-save + build methods AppBar e BottomBar</name>

  <read_first>
    - primeaudit/lib/screens/checklist/checklist_execution_screen.dart (arquivo criado na Task 1a — ler completo)
    - primeaudit/lib/screens/audit_execution_screen.dart linhas 250-450 (_onAnswer, _onObservation, _saveAnswer, _showSaveError, _scheduleRetry, _finalize)
    - .planning/phases/14-checklist-execution-engine/14-PATTERNS.md Seção "_saveAnswer, _scheduleRetry, _finalize"
    - .planning/phases/14-checklist-execution-engine/14-RESEARCH.md Pitfall 1 (messenger antes de await), Pitfall 4 (_retrying guard), Pitfall 5 (dispose)
  </read_first>

  <files>primeaudit/lib/screens/checklist/checklist_execution_screen.dart</files>

  <action>
Adicionar ao arquivo criado na Task 1a os métodos de auto-save e os build methods de AppBar e BottomBar. Substituir os stubs pelos métodos reais.

**`_onAnswer(String itemId, String response)`:**
- `if (_finalizing) return;`
- `setState(() => _answers[itemId] = response);`
- `_saveAnswer(itemId, response);` — SEM await

**`_onObservation(String itemId, String obs)`:**
- `_observations[itemId] = obs;`
- `final resp = _answers[itemId]; if (resp != null) _saveAnswer(itemId, resp, observation: obs);` — SEM await

**`_saveAnswer(String itemId, String response, {String? observation})`:**
- `final obs = observation ?? _observations[itemId];`
- try: `await _answerService.upsertAnswer(executionId: widget.execution.id, itemId: itemId, response: response, observation: obs);`
  - se `_failedSaves.containsKey(itemId) && mounted`: `setState(() => _failedSaves.remove(itemId));`
- catch: `debugPrint('[_saveAnswer] $e');`
  - `if (!mounted) return;`
  - `setState(() { _failedSaves[itemId] = _PendingSave(itemId: itemId, response: response, observation: obs); });`
  - `_showSaveError(itemId, response, obs);`
  - `_scheduleRetry(itemId);`

**`_showSaveError`** — captura `ScaffoldMessenger.of(context)` ANTES de qualquer await, exibe SnackBar floating com action 'Tentar novamente'. Padrão completo em `<interfaces>`.

**`_scheduleRetry(String itemId)`:**
- Guard: `if (_retrying.contains(itemId)) return; _retrying.add(itemId);`
- try/finally com while loop:
  - `pending.attemptCount >= _maxAutoRetryAttempts` → break
  - `await Future.delayed(Duration(seconds: pow(2, pending.attemptCount).toInt()));`
  - `if (!mounted || !_failedSaves.containsKey(itemId)) break;`
  - try upsert → sucesso: `setState(() => _failedSaves.remove(itemId)); break;`
  - catch: `setState(() { _failedSaves[itemId] = pending.copyWithAttempt(); });`
- finally: `_retrying.remove(itemId);`

**`_finalize()`:**
1. Guard: `if (_failedSaves.isNotEmpty)` → dialog AlertDialog "Respostas não salvas" com count, ação 'Entendido', `return;`
2. Dialog de confirmação com `_answeredCount / _totalCount` e `_conformity.toStringAsFixed(1)%`
3. Se confirmado: `setState(() => _finalizing = true);`
4. `await _executionService.finalizeExecution(id: widget.execution.id, conformityPercent: _conformity);`
5. `if (mounted) Navigator.of(context).pop(true);`
6. Catch: snackbar de erro, `setState(() => _finalizing = false);`

**`_buildAppBar()`:** AppBar primário com título `widget.execution.templateName`. No `bottom:` adicionar `PreferredSize` com `LinearProgressIndicator` mostrando `_answeredCount / max(_totalCount, 1)` e Text '$_answeredCount/$_totalCount itens'.

**`_buildBody(AppTheme t)`:** `RefreshIndicator` envolvendo `ListView.builder` sobre `_allItems`, cada item renderizado por `_ChecklistItemCard` (stub a ser implementado na Task 2 — usar `const Placeholder()` temporariamente para que o arquivo compile).

**`_buildBottomBar(AppTheme t)`:** Container com Row: coluna de conformidade (label + `${_conformity.toStringAsFixed(1)}%` com `_conformityColor`) + `Expanded(ElevatedButton('Finalizar checklist', onPressed: !_finalizing ? _finalize : null))`.

**Observação sobre `use_build_context_synchronously`:** Todo uso de `context` após `await` deve ter `if (!mounted) return;` antes. O messenger em `_showSaveError` deve ser capturado ANTES de qualquer await (Pitfall 1).
  </action>

  <verify>
    <automated>grep "_saveAnswer(itemId" primeaudit/lib/screens/checklist/checklist_execution_screen.dart</automated>
    Linha sem `await` na mesma linha (confirma fire-and-forget).
    <automated>grep "_failedSaves.isNotEmpty" primeaudit/lib/screens/checklist/checklist_execution_screen.dart</automated>
    Linha presente (guard de finalização).
    <automated>grep "pow(2" primeaudit/lib/screens/checklist/checklist_execution_screen.dart</automated>
    Linha do backoff presente.
  </verify>

  <done>
    - `_saveAnswer` é chamado sem `await` em `_onAnswer` e `_onObservation`
    - `_scheduleRetry` usa `pow(2, pending.attemptCount)` para backoff com `_maxAutoRetryAttempts = 4`
    - `_finalize` tem guard `_failedSaves.isNotEmpty` que bloqueia com dialog antes de finalizar
    - `if (!mounted) return;` presente em todos os métodos async após await
    - `_buildAppBar` com LinearProgressIndicator e contagem de itens respondidos
    - `_buildBottomBar` com conformidade ao vivo e botão Finalizar
    - Arquivo compila sem erros (Placeholder usado onde _ChecklistItemCard ainda não existe)
  </done>
</task>

<task type="auto">
  <name>Task 2: Widgets de resposta (_ChecklistItemCard e tipos yes_no, text, number, date, multiple_choice)</name>

  <read_first>
    - primeaudit/lib/screens/audit_execution_screen.dart linhas 1007-1723 (_ItemCard, _AnswerWidget, _TwoOptionButtons, _TextAnswer, _SelectionAnswer, _Badge)
    - .planning/phases/14-checklist-execution-engine/14-RESEARCH.md Seção 3 (todos os 5 tipos de widget com código)
    - .planning/phases/14-checklist-execution-engine/14-PATTERNS.md Seção "_AnswerWidget switch pattern", "_TwoOptionButtons", "_TextAnswer", "_SelectionAnswer reuse as _MultipleChoiceAnswer", "_ItemCard pattern", "_NumberAnswer e _DateAnswer sem analog"
  </read_first>

  <files>primeaudit/lib/screens/checklist/checklist_execution_screen.dart</files>

  <action>
Adicionar ao final de `checklist_execution_screen.dart` as classes privadas de widgets. Todas são definidas no mesmo arquivo (padrão do projeto — `_PascalCase` privadas no mesmo arquivo que a screen). Substituir o `Placeholder()` temporário em `_buildBody` pela chamada correta a `_ChecklistItemCard`.

**`_ChecklistItemCard` (StatelessWidget):**
Parâmetros: `item` (ChecklistTemplateItem), `itemNumber` (int, base-1), `answer` (String?), `observation` (String?), `readOnly` (bool), `onAnswer` (void Function(String)), `onObservation` (void Function(String)), `theme` (AppTheme).

Estrutura: `Card` com `margin: EdgeInsets.only(bottom: 12)`, elevation 0, border `t.divider`. Corpo: `Padding(padding: EdgeInsets.all(16))` com `Column`:
1. Número badge + texto da pergunta (Row)
2. `_AnswerWidget(item, answer, readOnly, onAnswer, theme)`
3. Campo de observação colapsável: `TextFormField` para observação com label 'Observação (opcional)', `onChanged: onObservation`, `initialValue: observation`. Inicialmente visível (sem colapse complexo — simples como no audit_execution_screen).

Sem campos `required`, `guidance`, nem seção de ações corretivas (diferenças do módulo de Auditoria).

**`_AnswerWidget` (StatelessWidget):**
Switch em `item.itemType`:
- `'yes_no'` → `_TwoOptionButtons` com opções Sim (Icons.thumb_up_rounded, Color(0xFF43A047)) e Não (Icons.thumb_down_rounded, AppColors.error)
- `'text'` → `_TextAnswer`
- `'number'` → `_NumberAnswer`
- `'date'` → `_DateAnswer`
- `'multiple_choice'` → `_MultipleChoiceAnswer(options: item.options, ...)`
- `'photo'` → `_PhotoPlaceholder` (Phase 15)
- default → `const SizedBox.shrink()`

**`_TwoOptionButtons` (StatelessWidget):**
Copiar verbatim de `audit_execution_screen.dart` linhas 1406-1463. Usa `_OptionDef` record ou classe para (value, label, icon, color). Dois botões em Row: `AnimatedContainer` estilizado com border e background quando selecionado.

**`_TextAnswer` (StatefulWidget):**
Copiar verbatim de `audit_execution_screen.dart` linhas 1600-1651: `late final TextEditingController _ctrl`, `initState` com `_ctrl = TextEditingController(text: widget.initial)`, `dispose()` com `_ctrl.dispose()`, `TextField` multiline com `onChanged: widget.onChanged`.

**`_NumberAnswer` (StatefulWidget — NOVO, sem analog no codebase):**
Mesma estrutura que `_TextAnswer` (StatefulWidget com controller + dispose), mas:
- `keyboardType: TextInputType.numberWithOptions(decimal: true)`
- `inputFormatters: [FilteringTextInputFormatter.allow(RegExp(r'[0-9.]'))]`
- `maxLines: 1`
- `onChanged: widget.readOnly ? null : widget.onChanged`

**`_DateAnswer` (StatefulWidget — NOVO, sem analog no codebase):**
Parâmetros: `value` (String? — ISO 'yyyy-MM-dd'), `readOnly` (bool), `onAnswer` (void Function(String)), `theme` (AppTheme).

`_parsedDate` getter: `value != null && value!.isNotEmpty ? DateTime.tryParse(value!) : null`
`_displayDate` getter: se parsedDate != null → `'${d.day.toString().padLeft(2,'0')}/${d.month.toString().padLeft(2,'0')}/${d.year}'` (dd/MM/yyyy para display), senão `'Selecionar data'`.

Renderiza `OutlinedButton.icon(icon: Icon(Icons.calendar_today_outlined, size: 16), label: Text(_displayDate))`.

`onPressed` quando não readOnly: `showDatePicker(context: context, initialDate: _parsedDate ?? DateTime.now(), firstDate: DateTime(2020), lastDate: DateTime(2030))` → se picked != null: `widget.onAnswer(picked.toIso8601String().substring(0, 10))`.

**`_MultipleChoiceAnswer` (StatelessWidget):**
Copiar `_SelectionAnswer` de `audit_execution_screen.dart` linhas 1654-1702 com rename. Se `options.isEmpty`: retornar `Text('Nenhuma opção configurada.', style: TextStyle(color: t.textSecondary, fontSize: 12))`. Senão: `Wrap` de `AnimatedContainer` chips com seleção.

**`_PhotoPlaceholder` (StatelessWidget):**
Simples: `Container` com ícone `Icons.photo_camera_outlined` e texto 'Foto disponível na próxima versão', cor `t.textSecondary`. Não interativo.

**`_Badge` (StatelessWidget):**
Copiar verbatim de `audit_execution_screen.dart` linhas 1704-1723. Usado para eventual uso futuro de badges nos cards.

**Verificação de Pitfall 5 — dispose de controllers:**
Toda classe que cria `TextEditingController` (`_TextAnswer`, `_NumberAnswer`) DEVE ter `@override void dispose() { _ctrl.dispose(); super.dispose(); }`.
  </action>

  <verify>
    <automated>grep -c "class _ChecklistItemCard" primeaudit/lib/screens/checklist/checklist_execution_screen.dart</automated>
    Deve retornar `1`.
    <automated>grep "case 'number'" primeaudit/lib/screens/checklist/checklist_execution_screen.dart</automated>
    Deve retornar linha do switch.
    <automated>grep "ctrl.dispose" primeaudit/lib/screens/checklist/checklist_execution_screen.dart</automated>
    Deve retornar pelo menos 2 linhas (TextAnswer + NumberAnswer).
    <automated>cd primeaudit && flutter analyze lib/screens/checklist/checklist_execution_screen.dart</automated>
    Deve retornar zero erros e zero warnings.
  </verify>

  <done>
    - `_ChecklistItemCard` existe e renderiza badge numérico, texto da pergunta, widget de resposta e campo de observação
    - `_AnswerWidget` tem switch completo com todos os 6 casos: yes_no, text, number, date, multiple_choice, photo
    - `_TwoOptionButtons` renderiza Sim/Não com AnimatedContainer e ícones
    - `_TextAnswer` tem TextEditingController com dispose()
    - `_NumberAnswer` tem keyboardType numérico, FilteringTextInputFormatter, dispose()
    - `_DateAnswer` abre showDatePicker, serializa como 'yyyy-MM-dd', exibe como 'dd/MM/yyyy'
    - `_MultipleChoiceAnswer` exibe "Nenhuma opção configurada" quando options vazio
    - `_PhotoPlaceholder` exibe placeholder não interativo
    - Sem import de `intl` — datas formatadas manualmente com `.toIso8601String().substring(0, 10)` e `padLeft`
    - `flutter analyze` retorna zero erros
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| widget state→service | Respostas em _answers saem da UI para upsert; nenhum dado externo entra |
| Rede→retry queue | Falha de rede mantém dados em memória; retry os resubmete |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-10 | Denial of Service | _scheduleRetry — loop infinito | mitigate | `_maxAutoRetryAttempts = 4` — break após 4 falhas; `_retrying` Set previne retry duplo por item |
| T-14-11 | Tampering | context uso pós-dispose | mitigate | `if (!mounted) return;` antes de todo setState e context após await; messenger capturado antes de await |
| T-14-12 | Denial of Service | _finalize com pendentes | mitigate | Guard `_failedSaves.isNotEmpty` bloqueia finalização e informa usuário — dados não são perdidos |
</threat_model>

<verification>
1. `grep -c "class ChecklistPendingSave" primeaudit/lib/screens/checklist/checklist_pending_save.dart` → `1`
2. `grep -c "copyWithAttempt" primeaudit/lib/screens/checklist/checklist_pending_save.dart` → `1`
3. `grep -c "class ChecklistExecutionScreen" primeaudit/lib/screens/checklist/checklist_execution_screen.dart` → `1`
4. `grep "_saveAnswer(itemId" primeaudit/lib/screens/checklist/checklist_execution_screen.dart` — linha sem `await` na mesma linha
5. `grep "pow(2" primeaudit/lib/screens/checklist/checklist_execution_screen.dart` → linha do backoff presente
6. `grep "_failedSaves.isNotEmpty" primeaudit/lib/screens/checklist/checklist_execution_screen.dart` → linha presente
7. `grep "case 'number'" primeaudit/lib/screens/checklist/checklist_execution_screen.dart` → linha presente
8. `grep "case 'date'" primeaudit/lib/screens/checklist/checklist_execution_screen.dart` → linha presente
9. `grep "ctrl.dispose" primeaudit/lib/screens/checklist/checklist_execution_screen.dart` → ao menos 2 linhas
10. `cd primeaudit && flutter analyze lib/screens/checklist/checklist_execution_screen.dart` → sem erros de lint
</verification>

<success_criteria>
- Screen aceita `ChecklistExecution execution` e carrega itens + respostas existentes em paralelo via Future.wait
- `_load()` mescla respostas do banco com `_failedSaves` (pendentes não são sobrescritas)
- `_onAnswer` e `_onObservation` chamam `_saveAnswer` sem await — UI nunca bloqueia
- `_saveAnswer` em falha: adiciona a `_failedSaves`, exibe snackbar, inicia `_scheduleRetry`
- `_scheduleRetry` faz até 4 tentativas com atraso pow(2, n) segundos
- `_finalize` bloqueia se `_failedSaves` não vazio, confirma, calcula conformidade, finaliza via service, navega de volta
- AppBar mostra progresso `_answeredCount/_totalCount`
- BottomBar mostra conformidade ao vivo com cor semântica e botão Finalizar
- Todos os 5 widgets de resposta funcionais (yes_no, text, number, date, multiple_choice)
- `_PhotoPlaceholder` renderiza badge informativo sem interação
- Todos os TextEditingController têm `dispose()` — sem memory leak
- `flutter analyze` zero erros no arquivo completo
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/14-checklist-execution-engine/14-04-SUMMARY.md`
</output>
