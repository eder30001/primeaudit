---
phase: 14-checklist-execution-engine
plan: 05
type: execute
wave: 3
depends_on:
  - "14-03"
files_modified:
  - primeaudit/lib/screens/checklist/checklist_template_list_screen.dart
autonomous: true
requirements:
  - EXEC-01

must_haves:
  truths:
    - "Cada card de template tem botão 'Executar' que abre _StartChecklistSheet"
    - "_StartChecklistSheet coleta responsável, local, data e número/código com validação de campos obrigatórios"
    - "Ao confirmar, ChecklistExecutionService.createExecution() é chamado e a navegação para ChecklistExecutionScreen ocorre"
    - "Ao retornar de ChecklistExecutionScreen, a lista de templates recarrega (_load)"
    - "Ao retornar de ChecklistExecutionScreen com resultado true (finalizado), snackbar 'Checklist finalizado com sucesso' aparece"
  artifacts:
    - path: "primeaudit/lib/screens/checklist/checklist_template_list_screen.dart"
      provides: "Botão Executar em _ChecklistTemplateCard + _StartChecklistSheet modal"
      contains: "_StartChecklistSheet"
  key_links:
    - from: "_ChecklistTemplateCard"
      to: "_showStartSheet"
      via: "ElevatedButton 'Executar' no rodapé do card"
      pattern: "_showStartSheet"
    - from: "_StartChecklistSheet"
      to: "ChecklistExecutionService.createExecution"
      via: "onPressed do botão Confirmar"
      pattern: "createExecution"
    - from: "Navigator.push"
      to: "_load"
      via: ".then((_) => _load())"
      pattern: "\\.then.*_load"
---

<objective>
Conectar o ponto de entrada de execução: adicionar botão "Executar" aos cards de template na `ChecklistTemplateListScreen`, criar o modal `_StartChecklistSheet` com o formulário de identificação (responsável, local, data, número), e navegar para `ChecklistExecutionScreen` após o insert no banco.

Purpose: Sem este wiring, o auditor não tem como iniciar uma execução. O botão "Executar" é o trigger que une o módulo de templates (Phase 13) com o engine de execução (Phase 14). O modal coleta os metadados obrigatórios de EXEC-01 antes de criar o registro no banco.

Output: `checklist_template_list_screen.dart` modificado com botão "Executar" nos cards e `_StartChecklistSheet` como nova classe privada no mesmo arquivo.
</objective>

<execution_context>
@C:/Users/eder3/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/eder3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/14-checklist-execution-engine/14-RESEARCH.md
@.planning/phases/14-checklist-execution-engine/14-PATTERNS.md
@.planning/phases/14-checklist-execution-engine/14-03-SUMMARY.md
@.planning/phases/14-checklist-execution-engine/14-04-SUMMARY.md

<interfaces>
<!-- Arquivo existente a ser modificado: -->
<!-- primeaudit/lib/screens/checklist/checklist_template_list_screen.dart -->
<!-- LER O ARQUIVO COMPLETO ANTES DE EDITAR -->

Estado atual do _ChecklistTemplateCard (linhas 271-448):
- É StatelessWidget com parâmetros: template, currentUserId, onDelete, onEdit, onClone
- trailing: IconButton copy (seed) ou PopupMenuButton (isOwn) ou chevron (outros)
- onTap: seed → onClone, isOwn → onEdit, outros → nada
- Não tem botão "Executar" ainda

Estado atual da _ChecklistTemplateListScreenState:
- Tem _service (ChecklistTemplateService), _templates, _isLoading, _error, _currentUserId
- Tem métodos: _load, _openCreate, _openEdit, _confirmDelete, _showCloneSheet
- Não tem _executionService nem _showStartSheet ainda

Padrão de showModalBottomSheet existente (linhas 126-140 — _showCloneSheet):
```dart
Future<void> _showCloneSheet(ChecklistTemplate t) async {
  await showModalBottomSheet(
    context: context,
    isScrollControlled: true,
    shape: const RoundedRectangleBorder(
      borderRadius: BorderRadius.vertical(top: Radius.circular(16)),
    ),
    builder: (ctx) => _CloneBottomSheet(
      template: t,
      service: _service,
      parentContext: context,
      onAfterClone: _load,
    ),
  );
}
```

Padrão de captura de messenger ANTES de async (linhas 473-486 de _CloneBottomSheetState._clone):
```dart
final messenger = ScaffoldMessenger.of(widget.parentContext);
// ... await ...
if (mounted) Navigator.pop(context);
messenger.showSnackBar(...);
```

Padrão de Navigator.push com .then para refresh (padrão do projeto):
```dart
Navigator.of(context)
    .push(MaterialPageRoute(builder: (_) => ChecklistExecutionScreen(execution: created)))
    .then((result) {
      _load();
      if (result == true && mounted) {
        ScaffoldMessenger.of(context).showSnackBar(const SnackBar(
          content: Text('Checklist finalizado com sucesso.'),
          behavior: SnackBarBehavior.floating,
        ));
      }
    });
```

CompanyContextService disponível:
```dart
import '../../services/company_context_service.dart';
// uso:
final companyId = CompanyContextService.instance.activeCompanyId;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar botão Executar ao _ChecklistTemplateCard e método _showStartSheet na screen</name>

  <read_first>
    - primeaudit/lib/screens/checklist/checklist_template_list_screen.dart (arquivo COMPLETO — ler antes de qualquer edição)
    - primeaudit/lib/screens/checklist/checklist_execution_screen.dart (confirmar assinatura do construtor: ChecklistExecutionScreen({required this.execution}))
    - primeaudit/lib/models/checklist_execution.dart (ChecklistExecution — tipo retornado por createExecution)
    - primeaudit/lib/services/checklist_execution_service.dart (assinatura de createExecution)
    - .planning/phases/14-checklist-execution-engine/14-RESEARCH.md Seção 5 "Ponto de entrada" e "_showStartSheet" (código completo)
    - .planning/phases/14-checklist-execution-engine/14-PATTERNS.md Seção "showModalBottomSheet for _StartChecklistSheet" e "mounted + messenger capture"
  </read_first>

  <files>primeaudit/lib/screens/checklist/checklist_template_list_screen.dart</files>

  <action>
Editar `primeaudit/lib/screens/checklist/checklist_template_list_screen.dart` com as seguintes mudanças:

**1. Adicionar imports (topo do arquivo, após imports existentes):**
```dart
import '../../models/checklist_execution.dart';
import '../../services/checklist_execution_service.dart';
import '../../services/company_context_service.dart';
import 'checklist_execution_screen.dart';
```

**2. Adicionar `_executionService` à state class:**
```dart
final _executionService = ChecklistExecutionService();
```

**3. Adicionar método `_showStartSheet` à `_ChecklistTemplateListScreenState`:**
```dart
Future<void> _showStartSheet(ChecklistTemplate t) async {
  final result = await showModalBottomSheet<ChecklistExecution>(
    context: context,
    isScrollControlled: true,
    shape: const RoundedRectangleBorder(
      borderRadius: BorderRadius.vertical(top: Radius.circular(16)),
    ),
    builder: (ctx) => _StartChecklistSheet(
      template: t,
      service: _executionService,
      parentContext: context,
    ),
  );
  if (result != null && mounted) {
    Navigator.of(context)
        .push(MaterialPageRoute(
            builder: (_) => ChecklistExecutionScreen(execution: result)))
        .then((finished) {
      _load();
      if (finished == true && mounted) {
        ScaffoldMessenger.of(context).showSnackBar(const SnackBar(
          content: Text('Checklist finalizado com sucesso.'),
          behavior: SnackBarBehavior.floating,
        ));
      }
    });
  }
}
```

**4. Modificar `_ChecklistTemplateCard` para aceitar callback `onExecute` e adicionar botão Executar:**

Adicionar parâmetro ao construtor:
```dart
final VoidCallback onExecute;  // novo parâmetro
```

Modificar o `build` do card: adicionar `ElevatedButton` "Executar" como rodapé do card (dentro do `Card`, abaixo do `ListTile`).

Usar `Column` como `child` do `Card` em vez de apenas `ListTile`:
```dart
child: Column(
  children: [
    ListTile(
      // ... conteúdo existente inalterado ...
    ),
    Padding(
      padding: const EdgeInsets.fromLTRB(16, 0, 16, 12),
      child: SizedBox(
        width: double.infinity,
        child: ElevatedButton.icon(
          onPressed: onExecute,
          icon: const Icon(Icons.play_arrow_rounded, size: 18),
          label: const Text('Executar',
              style: TextStyle(fontWeight: FontWeight.w600, fontSize: 13)),
          style: ElevatedButton.styleFrom(
            backgroundColor: AppColors.primary,
            foregroundColor: Colors.white,
            padding: const EdgeInsets.symmetric(vertical: 10),
            shape: RoundedRectangleBorder(
                borderRadius: BorderRadius.circular(8)),
            elevation: 0,
          ),
        ),
      ),
    ),
  ],
),
```

**5. Atualizar instanciação de `_ChecklistTemplateCard` no `ListView.builder`:**
```dart
itemBuilder: (_, i) => _ChecklistTemplateCard(
  template: _templates[i],
  currentUserId: _currentUserId,
  onDelete: () => _confirmDelete(_templates[i]),
  onEdit: () => _openEdit(_templates[i]),
  onClone: () => _showCloneSheet(_templates[i]),
  onExecute: () => _showStartSheet(_templates[i]),  // NOVO
),
```

**IMPORTANTE:** Manter o `onTap` do `ListTile` exatamente como está (seed → clone, isOwn → edit, outros → nada). O botão "Executar" é ADICIONAL ao comportamento de toque existente.
  </action>

  <verify>
    <automated>grep -c "_showStartSheet" primeaudit/lib/screens/checklist/checklist_template_list_screen.dart</automated>
    Deve retornar ao menos `3` (declaração + chamada no card + chamada no builder).
    Verificar import: `grep "checklist_execution_screen" primeaudit/lib/screens/checklist/checklist_template_list_screen.dart` deve retornar a linha de import.
    Verificar botão: `grep "Executar" primeaudit/lib/screens/checklist/checklist_template_list_screen.dart` deve retornar linha do ElevatedButton.
  </verify>

  <done>
    - Import de `checklist_execution_screen.dart` adicionado ao arquivo
    - Import de `ChecklistExecution`, `ChecklistExecutionService`, `CompanyContextService` adicionados
    - `_executionService = ChecklistExecutionService()` adicionado ao state
    - `_showStartSheet(ChecklistTemplate t)` adicionado à state class
    - `_ChecklistTemplateCard` tem parâmetro `onExecute` e botão "Executar" no rodapé
    - `ListView.builder` passa `onExecute: () => _showStartSheet(_templates[i])`
    - Comportamento de `onTap` do `ListTile` inalterado
  </done>
</task>

<task type="auto">
  <name>Task 2: Criar _StartChecklistSheet — modal de início de execução</name>

  <read_first>
    - primeaudit/lib/screens/checklist/checklist_template_list_screen.dart (estado após Task 1 — arquivo modificado)
    - primeaudit/lib/screens/home_screen.dart (padrão _NewAuditSheet — BottomSheet com Form, TextFormFields, DatePicker, validação)
    - primeaudit/lib/screens/checklist/checklist_template_list_screen.dart linhas 452-570 (_CloneBottomSheet — padrão de parentContext para messenger)
    - .planning/phases/14-checklist-execution-engine/14-RESEARCH.md Seção 5 "_StartChecklistSheet" (campos e comportamento)
    - .planning/phases/14-checklist-execution-engine/14-PATTERNS.md Seção "mounted + messenger capture before async"
  </read_first>

  <files>primeaudit/lib/screens/checklist/checklist_template_list_screen.dart</files>

  <action>
Adicionar a classe `_StartChecklistSheet` ao final de `checklist_template_list_screen.dart` (após `_CloneBottomSheet`).

**Estrutura:**
```dart
class _StartChecklistSheet extends StatefulWidget {
  final ChecklistTemplate template;
  final ChecklistExecutionService service;
  final BuildContext parentContext;

  const _StartChecklistSheet({
    required this.template,
    required this.service,
    required this.parentContext,
  });

  @override
  State<_StartChecklistSheet> createState() => _StartChecklistSheetState();
}

class _StartChecklistSheetState extends State<_StartChecklistSheet> {
  final _formKey = GlobalKey<FormState>();
  final _responsavelCtrl = TextEditingController();
  final _localCtrl = TextEditingController();
  final _numeroCtrl = TextEditingController();
  DateTime _dataExecucao = DateTime.now();
  bool _isLoading = false;

  @override
  void dispose() {
    _responsavelCtrl.dispose();
    _localCtrl.dispose();
    _numeroCtrl.dispose();
    super.dispose();
  }

  String get _displayDate {
    final d = _dataExecucao;
    return '${d.day.toString().padLeft(2, '0')}/${d.month.toString().padLeft(2, '0')}/${d.year}';
  }

  Future<void> _pickDate() async {
    final picked = await showDatePicker(
      context: context,
      initialDate: _dataExecucao,
      firstDate: DateTime(2020),
      lastDate: DateTime(2030),
    );
    if (picked != null && mounted) {
      setState(() => _dataExecucao = picked);
    }
  }

  Future<void> _confirm() async {
    if (!_formKey.currentState!.validate()) return;
    setState(() => _isLoading = true);
    // Capturar messenger ANTES do await (use_build_context_synchronously)
    final messenger = ScaffoldMessenger.of(widget.parentContext);
    try {
      final companyId = CompanyContextService.instance.activeCompanyId;
      final execution = await widget.service.createExecution(
        templateId: widget.template.id,
        companyId: companyId,
        responsavel: _responsavelCtrl.text.trim(),
        local: _localCtrl.text.trim(),
        numero: _numeroCtrl.text.trim().isEmpty ? null : _numeroCtrl.text.trim(),
        dataExecucao: _dataExecucao,
      );
      if (mounted) Navigator.pop(context, execution);
    } catch (e) {
      if (mounted) setState(() => _isLoading = false);
      messenger.showSnackBar(SnackBar(
        content: const Text('Erro ao iniciar checklist. Tente novamente.'),
        backgroundColor: AppColors.error,
        behavior: SnackBarBehavior.floating,
      ));
    }
  }
}
```

**`build` do sheet:**
- `Padding` com `EdgeInsets.fromLTRB(24, 24, 24, MediaQuery.of(context).viewInsets.bottom + 24)`
- `Column(mainAxisSize: MainAxisSize.min)` com:
  - Header: Text "Iniciar checklist" (bold, 16) + SizedBox(height: 4) + Text template.name (cor textSecondary, 13)
  - Divider
  - `Form(key: _formKey)` com `Column`:
    - `TextFormField` Responsável (obrigatório): `controller: _responsavelCtrl`, `decoration: InputDecoration(labelText: 'Responsável *')`, `validator: (v) => v!.trim().isEmpty ? 'Campo obrigatório' : null`
    - `SizedBox(height: 12)`
    - `TextFormField` Local (obrigatório): `controller: _localCtrl`, `decoration: InputDecoration(labelText: 'Local *')`, `validator: (v) => v!.trim().isEmpty ? 'Campo obrigatório' : null`
    - `SizedBox(height: 12)`
    - Data de execução: `InkWell(onTap: _pickDate, child: InputDecorator(decoration: InputDecoration(labelText: 'Data de execução *'), child: Text(_displayDate)))` — sem TextFormField (não é editável por teclado)
    - `SizedBox(height: 12)`
    - `TextFormField` Número/código (opcional): `controller: _numeroCtrl`, `decoration: InputDecoration(labelText: 'Número/código (opcional)')`, sem validator
  - `SizedBox(height: 24)`
  - Row com botões:
    - `TextButton('Cancelar', onPressed: () => Navigator.pop(context))`
    - `ElevatedButton('Confirmar', onPressed: _isLoading ? null : _confirm)` com `AppColors.primary` — se `_isLoading`: `CircularProgressIndicator(strokeWidth: 2, color: Colors.white)` em vez do texto

**Regra de validação de campos obrigatórios:**
- Responsável: não pode ser vazio após trim
- Local: não pode ser vazio após trim
- Data: sempre tem valor (default = DateTime.now())
- Número/código: opcional — `null` quando vazio
  </action>

  <verify>
    <automated>grep -c "class _StartChecklistSheet" primeaudit/lib/screens/checklist/checklist_template_list_screen.dart</automated>
    Deve retornar `1`.
    Verificar messenger capture: `grep "ScaffoldMessenger.of(widget.parentContext)" primeaudit/lib/screens/checklist/checklist_template_list_screen.dart` deve retornar linha em `_confirm` (antes do await).
    Verificar Navigator.pop com execução: `grep "Navigator.pop(context, execution)" primeaudit/lib/screens/checklist/checklist_template_list_screen.dart` deve retornar linha presente.
    Verificar dispose dos controllers: `grep "responsavelCtrl.dispose" primeaudit/lib/screens/checklist/checklist_template_list_screen.dart` deve retornar linha.
  </verify>

  <done>
    - `_StartChecklistSheet` e `_StartChecklistSheetState` existem no arquivo
    - Form tem 4 campos: responsável (obrigatório), local (obrigatório), data (DatePicker, obrigatório), número (opcional)
    - `_confirm()` valida o Form antes de chamar `createExecution`
    - `ScaffoldMessenger.of(widget.parentContext)` capturado ANTES do `await createExecution` (use_build_context_synchronously)
    - `Navigator.pop(context, execution)` retorna o `ChecklistExecution` criado ao caller `_showStartSheet`
    - Em erro: snackbar de erro via `messenger`, sem Navigator.pop (sheet permanece aberto para retry)
    - Todos os 3 controllers têm `dispose()` correto
    - `_isLoading` desativa botão Confirmar durante o insert
    - `CompanyContextService.instance.activeCompanyId` usado para companyId
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI form→service | Campos do formulário são trimados antes de insert; campos obrigatórios têm validator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-13 | Tampering | _StartChecklistSheet — campos sem validação | mitigate | `validator` em campos obrigatórios; trim em todos os valores antes de chamar createExecution |
| T-14-14 | Spoofing | companyId enviado pelo cliente | mitigate | `CompanyContextService.instance.activeCompanyId` — derivado da sessão do usuário logado; RLS do banco valida como segunda linha |
| T-14-15 | Denial of Service | double-tap em Confirmar | mitigate | `_isLoading` flag desativa o botão durante o await — sem insert duplicado |
</threat_model>

<verification>
1. `grep -c "_showStartSheet" primeaudit/lib/screens/checklist/checklist_template_list_screen.dart` → ao menos 3
2. `grep "checklist_execution_screen" primeaudit/lib/screens/checklist/checklist_template_list_screen.dart` → import presente
3. `grep -c "class _StartChecklistSheet" primeaudit/lib/screens/checklist/checklist_template_list_screen.dart` → `1`
4. `grep "Navigator.pop(context, execution)" primeaudit/lib/screens/checklist/checklist_template_list_screen.dart` → linha presente
5. `grep "ScaffoldMessenger.of(widget.parentContext)" primeaudit/lib/screens/checklist/checklist_template_list_screen.dart` → linha em _confirm antes do await
6. `grep "Executar" primeaudit/lib/screens/checklist/checklist_template_list_screen.dart` → linha do ElevatedButton presente
7. `flutter analyze primeaudit/lib/screens/checklist/checklist_template_list_screen.dart` → sem erros de lint
</verification>

<success_criteria>
- Todo card de template (seed e próprio) tem botão "Executar" no rodapé do card
- Toque no botão "Executar" abre `_StartChecklistSheet` como BottomSheet modal
- Sheet coleta responsável, local, data, número/código; responsável e local são obrigatórios (validator)
- "Confirmar" chama `ChecklistExecutionService.createExecution` e fecha o sheet retornando o objeto criado
- Após criar a execução, `ChecklistExecutionScreen` é aberta via `Navigator.push`
- Ao retornar da execução com `result == true`, snackbar "Checklist finalizado com sucesso." aparece
- `_load()` é chamado ao retornar (`.then((_) => _load())`), atualizando a lista de templates
- Comportamento de toque original nos cards (seed → clone, isOwn → edit) inalterado
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/14-checklist-execution-engine/14-05-SUMMARY.md`
</output>
