---
phase: 15-photos-per-item
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified: []
autonomous: true
requirements:
  - EXEC-04

must_haves:
  truths:
    - "Tabela checklist_item_images existe no banco de dados remoto"
    - "Bucket checklist-images existe no Supabase Storage com RLS ativa"
    - "RLS Pattern 3 (auditor via EXISTS subquery) está ativa no banco remoto"
    - "API PostgREST recarregou o schema (NOTIFY aplicado)"
  artifacts:
    - path: "primeaudit/supabase/migrations/20260510_create_checklist_item_images.sql"
      provides: "Migration aplicada ao banco — deve constar em supabase_migrations"
  key_links:
    - from: "supabase db push"
      to: "checklist_item_images (remote DB)"
      via: "supabase CLI applying migration file"
      pattern: "20260510_create_checklist_item_images"
---

<objective>
[BLOCKING] Aplicar a migration 20260510_create_checklist_item_images.sql ao banco de dados Supabase remoto via `supabase db push`.

Purpose: Sem este passo, a tabela `checklist_item_images` e o bucket `checklist-images` não existem no banco remoto. Qualquer chamada Dart de upload ou query de imagens falhará com erro 42P01 (relation does not exist). Este plano DEVE ser concluído antes de Plan 15-03.

Output: Tabela, bucket e todas as políticas RLS da migration aplicados ao banco remoto. `supabase db push` deve retornar exit 0.
</objective>

<execution_context>
@C:\Users\eder3\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\eder3\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-photos-per-item/15-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] supabase db push</name>
  <files></files>

  <read_first>
    - primeaudit/supabase/migrations/20260510_create_checklist_item_images.sql (confirmar que o arquivo existe e está correto ANTES de fazer push)
    - .planning/phases/15-photos-per-item/15-01-SUMMARY.md (confirmar que Plan 15-01 foi concluído)
  </read_first>

  <action>
Executar o push da migration do diretório `primeaudit/`:

```bash
cd primeaudit && supabase db push
```

Este comando:
1. Conecta ao projeto Supabase remoto (usando as credenciais em `supabase/.temp/` ou variáveis de ambiente configuradas)
2. Identifica migrations não aplicadas (20260510_create_checklist_item_images.sql)
3. Aplica a migration ao banco remoto
4. Registra a migration em `supabase_migrations`

**Se o comando falhar com erro de autenticação:**
- Verificar se `supabase link` foi executado no projeto: `cd primeaudit && supabase status`
- Se necessário: `supabase link --project-ref <PROJECT_REF>`

**Se o comando falhar com erro de SQL (constraint, FK, tipo):**
- Ler o erro completo do output
- Corrigir o arquivo `20260510_create_checklist_item_images.sql`
- Reexecutar `supabase db push`

**Não prosseguir para Plan 15-03 se este comando não retornar exit 0.**
  </action>

  <verify>
    <automated>cd primeaudit && supabase db push 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `supabase db push` exits 0
    - Output contains no ERROR lines
    - Migration `20260510_create_checklist_item_images` appears in applied migrations output (or no "pending migrations" error is shown)
  </acceptance_criteria>

  <done>`supabase db push` concluído com exit 0. Tabela `checklist_item_images`, bucket `checklist-images` e todas as políticas RLS estão ativas no banco remoto. Plan 15-03 pode prosseguir.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → Supabase remoto | supabase db push usa token de serviço local — não expõe credenciais no código |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06 | Tampering | Migration aplicada com SQL incorreto | mitigate | Migration revisada em Plan 15-01; arquivo lido antes do push nesta task |
| T-15-07 | Denial of Service | Push falha e bloqueia Plan 15-03 | accept | Este plano é explicitamente BLOCKING — Plan 15-03 não executa se este falhar |
</threat_model>

<verification>
```bash
# Confirmar que a migration foi aplicada
cd primeaudit && supabase db push

# Saída esperada: "Applying migration 20260510_create_checklist_item_images.sql" ou "No migrations to apply"
# Exit code: 0
```
</verification>

<success_criteria>
- `supabase db push` retorna exit 0
- Sem erros de SQL no output
- Tabela `checklist_item_images` e bucket `checklist-images` acessíveis pelo SDK Dart
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/15-photos-per-item/15-02-SUMMARY.md`
</output>
