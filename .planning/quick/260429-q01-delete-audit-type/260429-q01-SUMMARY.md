---
quick_id: 260429-q01
status: complete
date: 2026-04-29
---

# Summary: Excluir Tipo de Auditoria

## What was done

Adicionada opção de exclusão de tipos de auditoria em `AuditTypesScreen`:

1. **Novo `PopupMenuItem`** com valor `'delete'` e texto vermelho no menu de contexto de cada tipo não-global
2. **Método `_confirmDelete`** com `AlertDialog` de confirmação antes da exclusão
3. **Tratamento de erro FK**: mensagem amigável quando o tipo está vinculado a auditorias existentes (`foreign key` / `violates` no texto do erro)
4. **Recarregamento**: `_load()` chamado após exclusão bem-sucedida

O método `deleteType(String id)` já existia em `AuditTemplateService` — nenhuma alteração no service foi necessária.

## Files changed

- `primeaudit/lib/screens/templates/audit_types_screen.dart` — adicionado `_confirmDelete()` e item "Excluir" no `PopupMenuButton`

## Verification

- `dart analyze lib/screens/templates/audit_types_screen.dart` → No issues found
- Lógica de guard: apenas tipos com `!type.isGlobal && _canManage` exibem o menu com delete
