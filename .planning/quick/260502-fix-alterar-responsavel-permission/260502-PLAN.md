---
phase: quick
plan: 260502
type: execute
wave: 1
depends_on: []
files_modified:
  - primeaudit/lib/screens/corrective_action_detail_screen.dart
autonomous: true
requirements:
  - CORR-RBAC-01
must_haves:
  truths:
    - "Usuário com role auditor pode ver e usar o botão 'Alterar responsável' em ações não-finais"
    - "O responsável atual pela ação pode ver e usar o botão 'Alterar responsável' em ações não-finais"
    - "ADM, superuser e dev continuam podendo alterar o responsável (comportamento existente preservado)"
    - "Criador sem role auditor/admin/responsável não perde acesso (comportamento existente preservado)"
  artifacts:
    - path: "primeaudit/lib/screens/corrective_action_detail_screen.dart"
      provides: "Condição do botão 'Alterar responsável' expandida para 3 perfis"
      contains: "_isAuditor || _isResponsible || _isAdmin || _isCreator"
  key_links:
    - from: "_isAuditor getter"
      to: "AppRole.auditor constant"
      via: "widget.currentUserRole == AppRole.auditor"
      pattern: "AppRole\\.auditor"
---

<objective>
Corrigir a condição de visibilidade do botão "Alterar responsável" em
`corrective_action_detail_screen.dart` para incluir dois perfis que estavam
excluídos indevidamente: auditores (qualquer usuário com role `auditor`) e o
responsável atual pela ação (`_isResponsible`).

Purpose: O auditor que criou a auditoria e designou a ação corretiva, e o
próprio responsável pela ação, devem poder reatribuir a responsabilidade sem
depender de um ADM — alinha o fluxo real de campo com as permissões do app.

Output: Uma linha alterada na condição do `if` na linha ~320 e um novo getter
`_isAuditor` adicionado ao bloco de getters da state class.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expandir permissão do botão "Alterar responsável"</name>
  <files>primeaudit/lib/screens/corrective_action_detail_screen.dart</files>
  <action>
No bloco de getters da state class (linhas 45-51), adicionar imediatamente após
`_isCreator`:

```dart
bool get _isAuditor => widget.currentUserRole == AppRole.auditor;
```

Na linha ~320, alterar a condição do `if` que controla a visibilidade do botão
"Alterar responsável" de:

```dart
if ((_isCreator || _isAdmin) && !a.status.isFinal) ...[
```

para:

```dart
if ((_isAdmin || _isAuditor || _isResponsible || _isCreator) && !a.status.isFinal) ...[
```

Ordem dos termos: admin primeiro (caso mais privilegiado), depois auditor
(role-based), depois responsável (identity-based), depois criador. Não alterar
nenhuma outra linha — a lógica de transição de status (`_buildTransitionButtons`
e `canTransitionTo`) já é tratada separadamente via `CorrectiveActionService`
e não é afetada por esta mudança.
  </action>
  <verify>
    <automated>cd primeaudit && flutter analyze lib/screens/corrective_action_detail_screen.dart --no-fatal-infos 2>&1 | tail -5</automated>
  </verify>
  <done>
`flutter analyze` não reporta erros ou warnings no arquivo. O getter `_isAuditor`
existe na state class. A condição do botão inclui os quatro termos
`_isAdmin || _isAuditor || _isResponsible || _isCreator`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → Service | Permissão checada na UI; RLS no banco é a barreira real |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260502-01 | Elevation of Privilege | Botão "Alterar responsável" | accept | Alteração apenas expande acesso para perfis legítimos (auditor, responsável); o dado de `currentUserRole` vem da sessão Supabase autenticada — não é input do usuário. RLS na tabela `corrective_actions` é a barreira de segurança real para UPDATE. |
</threat_model>

<verification>
Após aplicar a mudança:

1. `flutter analyze lib/screens/corrective_action_detail_screen.dart` — zero erros.
2. Revisão manual: o getter `_isAuditor` está presente no bloco de getters.
3. Revisão manual: a condição na linha do botão "Alterar responsável" contém
   `_isAdmin || _isAuditor || _isResponsible || _isCreator`.
4. O botão de exclusão (AppBar action, linha ~286) continua com a condição
   original `_isCreator || _isAdmin` — não deve ser alterada.
</verification>

<success_criteria>
- getter `_isAuditor` definido como `widget.currentUserRole == AppRole.auditor`
- Condição do botão "Alterar responsável": `(_isAdmin || _isAuditor || _isResponsible || _isCreator) && !a.status.isFinal`
- `flutter analyze` sem erros no arquivo modificado
- Nenhuma outra linha do arquivo foi alterada
</success_criteria>

<output>
Após conclusão, criar `.planning/quick/260502-fix-alterar-responsavel-permission/260502-SUMMARY.md`
</output>
