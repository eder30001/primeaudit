---
quick_id: 260505-001
slug: checklist-category-picker
description: Redesenhar ChecklistTemplatesScreen — substituir TabBar por tela de seleção de categoria com cards centralizados; lista de templates em tela separada
date: 2026-05-05
status: planned
---

# Quick Task 260505-001: Checklist Category Picker

## Goal

Substituir a `ChecklistTemplatesScreen` atual (TabBar com 3 abas) por uma tela de seleção de categoria com 3 cards grandes centralizados. Ao tocar num card, navega para `ChecklistTemplateListScreen`, nova tela que exibe a lista de templates filtrada por categoria.

## Tasks

### TASK-01: Criar ChecklistTemplateListScreen

**Files:**
- `primeaudit/lib/screens/checklist/checklist_template_list_screen.dart` (criar)

**Action:**
Extrair a lógica de lista de templates da `ChecklistTemplatesScreen` atual para uma nova tela. A tela recebe `category` (String: `'industrial'`, `'transportadora'`, `'meus'`) e `title` (String para o AppBar). Mantém toda a lógica de carga, CRUD, clone, delete e FAB "Novo checklist".

**Verify:** Arquivo criado com `ChecklistTemplateListScreen` completa.

**Done:** `checklist_template_list_screen.dart` existe com widget completo.

---

### TASK-02: Redesenhar ChecklistTemplatesScreen como category picker

**Files:**
- `primeaudit/lib/screens/checklist/checklist_templates_screen.dart` (modificar)

**Action:**
Substituir toda a implementação por uma tela simples de seleção de categoria:
- AppBar: "Checklist"
- Body: coluna centralizada com 3 `_CategoryCard` widgets (Industrial, Transportadora, Meus checklists)
- Cada card navega para `ChecklistTemplateListScreen` com a categoria correspondente
- Sem TabController, sem listas, sem FAB na tela principal

**Verify:** `ChecklistTemplatesScreen` não tem mais TabBar; tem 3 cards com navegação.

**Done:** Tela simplificada compilando sem erros.
