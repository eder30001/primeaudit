---
quick_id: 260505-001
slug: checklist-category-picker
status: complete
date: 2026-05-05
---

# Summary: Checklist Category Picker

## What changed

- **`checklist_templates_screen.dart`** — Reescrita completa. Removidos: TabController, 3 listas de templates, `_isLoading`, `_load()`, FAB, `_ChecklistTemplateCard`, `_CloneBottomSheet`. A tela agora é um `StatelessWidget` com 3 `_CategoryCard` centralizados que navegam para `ChecklistTemplateListScreen`.

- **`checklist_template_list_screen.dart`** — Novo arquivo. Contém toda a lógica de lista extraída da tela anterior: carregamento por categoria (`getByCategory` ou `getOwned`), CRUD (editar, clonar, deletar), FAB "Novo checklist", pull-to-refresh, empty states, `_ChecklistTemplateCard` e `_CloneBottomSheet`.

## Result

Fluxo: Drawer → ChecklistTemplatesScreen (3 cards) → ChecklistTemplateListScreen (lista filtrada)
`flutter analyze` — No issues found.
