import 'package:flutter_test/flutter_test.dart';

void main() {
  group('ChecklistPendingSave', () {
    test('TODO: copyWithAttempt incrementa attemptCount', () {
      // TODO: ChecklistPendingSave(itemId:'x', response:'y', attemptCount: 0).copyWithAttempt().attemptCount == 1
      expect(true, isTrue);
    });

    test('TODO: falha no upsert não propaga exceção para a tela (EXEC-05)', () {
      // TODO: simular throw no upsert e verificar que a screen captura silenciosamente
      expect(true, isTrue);
    });
  });
}
